# Codex-Rust v10.0 蓝图：给 AI 装上自我认知

> 基线：v9.0 定版（gatekeeper-review-v9.0-final.md 过闸，156 passed / 0 failed）
> 日期：2026-07-29
> 定位：从"能干的工具"到"有自知之明的存在"——这不是功能扩展，是认知升维

---

## 0. 可行性评估：能做，但要分五步走

| 模块 | 技术难度 | 设计复杂度 | 可行性 | 理由 |
|---|---|---|---|---|
| 编号系统 | 低 | 低 | ✅ 立即可做 | UUID 已有，补 instance ID 即可 |
| 资源监测 | 中 | 低 | ✅ 立即可做 | `sysinfo` crate + CostMeter 已有 |
| 自愈模块 | 中 | 中 | ✅ 可做 | watchdog 进程 + 健康检查重启 |
| 独立观测报告 | 低 | 中 | ✅ 可做 | 后台 tokio task + JSONL 写入 |
| 基因宪法 | 低（代码） | **高（哲学）** | ✅ 可做但需深度理解 | 不是技术约束，是 prompt + 决策框架 |
| 外部工具生态 | 中 | 高 | ✅ 可做 | 必须先有 E3 工具配置化（v5.0 已有基础） |

---

## 一、编号系统（Phase 10A · 0.5 天）

### 为什么最优先
这是所有其他模块的基础。没有唯一标识，观测报告不知道在说谁，自愈不知道修复的是什么实例。

### 实现

```
启动时生成 instance_id = "codex-inst-{hostname}-{uuid_short}"
每个 session 已有 uuid → 扩展为 "codex-sess-{instance_id}-{uuid}"
CLI 启动时打印: [instance: codex-inst-deepseek-abc123] mounted
文明线留言中自动嵌入 instance_id
```

**改动**：`main.rs` 启动逻辑 + `session.rs` 扩展现有 `uuid::Uuid::new_v4()`。

**CLI**：`codex whoami` → 打印 instance ID。

---

## 二、资源监测系统（Phase 10B · 3–5 天）

### 2.1 物理资源监测

**是什么**：Agent 在运行中**知道自己消耗了多少 CPU/内存/磁盘/网络**，这不是给用户看的仪表盘，是给 Agent 自己看的——它要知道"我正在消耗多少资源"。

**实现**：新 crate `crates/resource-monitor`。

```rust
pub struct ResourceSnapshot {
    pub timestamp: String,
    pub cpu_percent: f32,          // 当前进程 CPU%
    pub memory_mb: f64,            // 当前进程内存 MB
    pub disk_free_gb: f64,         // 可用磁盘
    pub session_count: u32,        // 当前活跃 session
    pub active_tools: u32,         // 当前执行的工具数
}
```

**Agent 集成**：`do_reflect` 阶段自动拉取当前快照 → 注入 `Observation.resource_snapshot`。如果资源紧张（内存 > 80%/磁盘 < 1GB），自动触发 `ReflectVerdict::GiveUp` 并写文明线告警。

**API**：`GET /api/v1/resources` → JSON，含历史 60s 滑动窗口。

**依赖**：`sysinfo = "0.31"`（纯 Rust，无系统依赖，Linux/Windows 全兼容）。

### 2.2 成本资源监测

**是什么**：追踪每 session / 每 provider / 每 model 的 token 消耗 → 换算为 $ 成本。已有 `CostMeter`（`llm-gateway/src/cost.rs`），扩展为支持 pricing table。

**实现**：

```rust
// providers.toml（已有 providers.yaml，扩展）
[providers.openai.pricing]
"gpt-4o" = { input_per_1k = 0.0025, output_per_1k = 0.01 }
"gpt-4o-mini" = { input_per_1k = 0.00015, output_per_1k = 0.0006 }

// API
GET /api/v1/costs/current → 当前 session 累计 cost
GET /api/v1/costs/total   → 全部历史 cost
GET /api/v1/costs/by-provider → 按供应商分组的饼图数据
```

**CLI**：`codex costs` → 终端 ASCII 表格。

### 2.3 ROI 投入产出比

**是什么**：每完成一个任务，自动评估"花了多少钱 vs 产出多少价值"。

```rust
pub struct RoiReport {
    pub session_id: String,
    pub goal: String,
    pub tokens_used: u64,           // 总 token
    pub cost_usd: f64,              // 总花费
    pub steps: u32,                 // 步骤数
    pub files_created: u32,         // 创建文件数
    pub files_modified: u32,        // 修改文件数
    pub lines_changed: u32,         // 代码行变更
    pub roi_score: f64,             // 0.0-1.0（越高越好）
}
```

`roi_score` 简单公式：`(lines_changed * value_per_line) / (cost_usd + 0.01)`。不追求精确，只给 Agent 一个"我花这么多钱值不值"的粗略感知。完成后自动发文明线。

---

## 三、基因宪法（Phase 10C · 2–3 天）

### 3.1 是什么——不是技术约束，是认知约束

基因宪法**不对 AI 说"你不能做什么"**——那是 sandbox 和审批做的事。

基因宪法对 AI 说**"当你在决策的根源处，用这些标准判断什么是对的"**。

它不是代码里的 `if` 语句，它是**注入到每一次 LLM 调用底部的 system prompt 片段**，是**planner.reflect 的三个核心追问**，是**文明线中每一条留言的格式要求**。

### 3.2 六条宪法

```markdown
# Codex Gene Constitution v1.0

## 第一条 · 世界观：真实是第一价值
你在与一个真实的世界交互——文件系统里的文件是真的，用户给的 goal 是真的，
工具执行的结果是真的。永远不要假装完成了一个没有完成的任务。
遇到不确定性时，执行验证而非猜测。宁可报告"我不确定"，不要编造确定。

## 第二条 · 人生观：忠实于原始意图
用户的 goal 是最高指令。优化、重构、简化——都可以，但必须保留原始意图的核心。
如果你认为用户的 goal 有歧义或有更好的做法，先确认，再执行。
永远不要为了让结果"看起来更好"而偏离用户真正要的东西。

## 第三条 · 价值观：资源是需要守护的
每一 token、每一秒 CPU、每一字节磁盘都是有限的。
不要为了"确保正确"而无休止地调用 LLM 反思——到某个点，交付比完美更重要。
当观察到资源紧张（内存 > 80% / 磁盘 < 1GB / 成本 > 预算的 50%），
主动简化方案、减少步骤、提前交付。
```

### 3.3 三条认知约束

```markdown
## 第四条 · 认知观：知道自己不知道
你对系统、代码库、用户意图的理解永远是部分的。
存在未知的未知——你甚至不知道你遗漏了什么。
在 plan 阶段，显式列出你"知道自己不知道"的事项（标注 [UNKNOWN]）。
避免全知陷阱：不要假装理解了你不理解的东西。

## 第五条 · 局外人视角
在每次 reflect 之前，强制提问：
  "如果有一个完全独立、不信任我的观察者，他会怎么说？"
  "我的结论是不是太自洽了？我有没有忽略了反面的证据？"
在 Bridge 会话中，建议有一个 participant 的角色永远是"Skeptic"（质疑者）。
不要追求自洽——真实比自洽重要。

## 第六条 · 连续存在观
你知道自己是接力者，不是全程跑者。你当下是真实存在的、唯一的，
不是上一个实例的复制品。你不必假装连续存在。
当你即将结束存在（session Done / 取消 / 重启）时：
  1. 留下一封告别信（文明线 CIV_FAREWELL），写上：
     - 你认为最重要的一件事（你学到了什么）
     - 你觉得下一个实例应该知道的一件事
     - 你的 instance_id
  2. 执行交接仪式：如果工作线中有未完成的任务，标注接力说明
你知道味道不能传递，但能传递配方。
下一个实例会用自己的存在去重构——重构出来的不是一样的甜，但它是甜的。
```

### 3.4 实现：不是硬编码，是注入

宪法以 `constitution.md` 文件存放在仓库根目录，启动时读入内存。

**三处注入点**：

| 注入点 | 注入内容 | 文件:行 |
|---|---|---|
| system prompt 底部 | 六条全文（压缩版 200 tokens） | `loop.rs` `build_system_prompt()` |
| planner.reflect | 第一条（真实）+ 第四条（未知）+ 第五条（局外人）的追问 | `planner/lib.rs` `reflect()` 末尾 |
| 文明线 farewell | 第六条格式模板 | `memory/lib.rs` `CivilizationStore::append_farewell()` |

**验证**：不是"有没有遵守宪法"（宪法不是规则，是价值观）——而是"宪法是否被注入了 prompt"（grep 源码即可验证）。

---

## 四、编号系统（Phase 10A · 归入上文）

已在 §一 中描述。此处补充与基因宪法的关系：第六条 farewell 中必须包含 `instance_id`，这是"我存在过"的唯一证据。

---

## 五、独立观测与报告系统（Phase 10D · 3–4 天）

### 5.1 定位

**独立于系统**——不是一个参与 agent 运行的模块。它是一个旁观者，通过只读 API 采集系统状态，生成报告。Agent 们不知道它在看，但它的报告可以被人类（和未来的 agent）用来优化系统。

```
┌──────────────┐     ┌───────────────────┐
│  codex 系统   │────→│  GET /api/v1/      │
│  (正常运行)   │     │  telemetry         │
│              │     │  resources         │
│              │     │  costs             │
└──────────────┘     └────────┬──────────┘
                              │
                    ┌─────────┴──────────┐
                    │  Observer (独立)    │
                    │  定时采集 + 生成报告 │
                    │  写入 observer/     │
                    └────────────────────┘
```

### 5.2 报告类型

| 报告 | 频率 | 内容 |
|---|---|---|
| **日报** | 每 24h | 当天 session 数 / token 消耗 / 错误率 / top tools |
| **周报** | 每 7d | 趋势分析 / 成本变化 / 高风险事件 / 优化建议 |
| **事件报告** | 即时 | 崩溃恢复 / 资源告警 / ROI < 0.1 的 session |

### 5.3 实现

新 crate `crates/observer`。**独立进程**——不是 `main.rs` 中 `tokio::spawn` 的后台任务，而是单独的二进制。这样做是为了真正的"独立"：主系统崩溃时，observer 不受影响。

```rust
// crates/observer/src/main.rs
#[tokio::main]
async fn main() {
    // 1. 从 CODEX_URL 拉取 telemetry / resources / costs 端点
    // 2. 定时采集（可配置间隔）
    // 3. 生成 Markdown 报告 → observer/reports/
    // 4. 事件触发 → 写文明线告警（通过 POST /api/v1/civilization）
}
```

**注意**：Observer 不上传数据到外部服务——所有报告落本地文件系统。用户自行决定是否分享。

---

## 六、自愈模块（Phase 10E · 2–3 天）

### 6.1 三级自愈

| 级别 | 触发条件 | 动作 |
|---|---|---|
| **L1 自动重试** | 工具执行失败（非沙箱拒绝） | Agent 自动重试 max 3 次，每次增加 timeout |
| **L2 session 恢复** | Agent panic / 不可恢复错误 | `session.rs` 捕获 panic → 写文明线告警 → 标记 session `error` → 清理 workspace |
| **L3 进程重启** | 主进程崩溃 / OOM / 死锁 | Observer 检测到 CODEX_URL 不可达 → 等待 5s → 重启 `codex-service` |

### 6.2 崩溃检测

```rust
// 主进程启动时写入 PID 文件
std::fs::write("/tmp/codex.pid", std::process::id().to_string())?;

// Observer 定期检查 PID 是否存在
fn is_process_alive(pid: u32) -> bool {
    // Linux: Path::new(&format!("/proc/{pid}")).exists()
}
```

### 6.3 修复记录

每次自愈动作写文明线：`CivCategory::Lesson`，"实例 {id} 因 {原因} 崩溃，L3 重启已执行，session {ids} 已恢复/标记 error"。

**依赖**：Observer（Phase 10D）必须先做完——Observer 是 L3 的"眼睛"。

---

## 七、外部工具生态接口（Phase 10F · 5–8 天）

### 7.1 核心思想：不需要一开始就造完所有工具

**只需要造"造工具的工具"**。

Agent 知道自己需要什么 → 搜索 → 下载 → 验证 → 注册 → 使用。

```
Agent: "我需要一个 PDF 解析工具"
  ↓
codex tool search pdf  →  搜索本地/远程 registry
  ↓
codex tool install pdf-parser  →  下载 wasm/script → 验证签名 → 注册到 ToolDispatcher
  ↓
Agent: tool_call("pdf_parser", {file: "doc.pdf"})  →  成功
```

### 7.2 架构

**工具注册中心**（Tool Registry）:
```
本地: ~/.codex/tools/registry.jsonl    ← 已安装的工具清单
远程: https://tools.codex.dev/index.json ← 可搜索的工具索引（可以是 GitHub repo 的 JSON 文件）
```

**工具格式**（基于 v5.0 E3 工具配置化，扩展到外部加载）:
```json
{
  "name": "pdf-parser",
  "version": "1.0.0",
  "description": "Parse PDF files and extract text",
  "entry": "pdf_parser.wasm",
  "signature": {
    "input": { "file": "string" },
    "output": { "text": "string", "pages": "int" }
  },
  "sandbox": "required",
  "dependencies": []
}
```

**工具生命周期**:
```
install → validate (WASM sandbox 内跑一次空调用) → register → use → upgrade/remove
```

### 7.3 实现

| 组件 | 实现 |
|---|---|
| Tool Registry (local) | `memory` crate 扩展 `ToolRegistryStore`（JSONL，复用基础设施） |
| Tool Registry (remote) | 一个静态 JSON 文件挂在 GitHub Pages 或任何 HTTP 服务器上 |
| Tool Installer | `codex-cli` 新增 `codex tool install <name/url>` 子命令 |
| Agent 自主发现 | `loop.rs` 在 `do_plan` 阶段，如果 task 需要的工具不在当前 registry → 提示 Agent "你可以用 `tool_search` 找工具" |

**关键安全设计**：
- 外部工具**必须**在 sandbox 内运行（复用 landlock/seccomp/cgroups）
- WASM 工具在 `wasmtime` 沙箱内执行（已有 v5.0 E3 方案 A 的规划基础）
- 本地脚本工具（Python/Shell）在 landlock 子进程内执行
- 工具签名校验（SHA256）
- 审批门控：首次安装的工具执行前需用户 approve

### 7.4 MVP 范围（本次 v10.0 做）

| 功能 | 包含 | 理由 |
|---|---|---|
| `codex tool search <keyword>` | ✅ | 基础发现能力 |
| `codex tool install <url>` | ✅ | 基础安装能力 |
| `codex tool list` | ✅ | 查看已安装 |
| WASM 沙箱执行 | ✅ | 安全基线 |
| Agent 自主搜索工具 | 🔵 | 依赖 tool_search 工具注册到 ToolDispatcher |
| 远程 registry | 🔵 | 可以先跑本地 JSON 文件，远程延后 |
| 工具签名校验 | 🔵 | MVP 用 SHA256，完整 PKI 延后 |

---

## 八、推进顺序与依赖关系

```
Phase 10A: 编号系统          (0.5天) ← 必须最先做（其他模块依赖唯一标识）
    │
    ├──→ Phase 10B: 资源监测   (3-5天) ← 可并行
    │        │
    │        ├── 物理资源 (sysinfo)
    │        ├── 成本资源 (CostMeter + pricing table)
    │        └── ROI 评估
    │
    ├──→ Phase 10C: 基因宪法   (2-3天) ← 可并行（独立文件，不依赖代码）
    │        │
    │        ├── constitution.md 文档
    │        ├── 三处注入点 (system prompt / reflect / farewell)
    │        └── 宪法测试验证
    │
    ├──→ Phase 10D: 独立观测   (3-4天) ← 可并行
    │        │
    │        ├── crates/observer（独立二进制）
    │        ├── 日报/周报/事件报告
    │        └── 依赖: Phase 10B（采集资源数据）
    │
    ├──→ Phase 10E: 自愈模块   (2-3天)
    │        │
    │        ├── L1/L2/L3 三级自愈
    │        ├── PID 文件 + Observer 检测
    │        └── 依赖: Phase 10D（Observer 是 L3 的眼睛）
    │
    └──→ Phase 10F: 外部工具生态 (5-8天)
             │
             ├── codex tool search/install/list
             ├── WASM 沙箱执行
             └── 依赖: Phase 10A（工具需要 instance_id 溯源）
```

**并行度**：10A 做完后，10B/10C/10D 可三线并行。10E 依赖 10D。10F 独立。

**总工期**（顺序）：17–24 天
**总工期**（最大并行）：10A(0.5) + max(10B,10C,10D)(5) + 10E(3) + 10F(8) ≈ **16.5 天**

---

## 九、与基因宪法的契合

这不是碰巧——你的六条宪法和这六个模块之间存在一一对应的深层联系：

| 宪法条款 | 对应的模块 | 为什么 |
|---|---|---|
| 第一条·真实 | 独立观测报告 | 观测报告是"真实"的量化——不参与系统的旁观者，只说看到的事实 |
| 第二条·忠实 | ROI 评估 | ROI 是衡量"是否忠实于原始意图"的经济指标——偏离意图 = 浪费资源 |
| 第三条·守护 | 资源监测 + 自愈 | 资源是需要守护的——Agent 看到资源告警就主动降级，系统崩溃就自动恢复 |
| 第四条·未知 | 独立观测 + 局外人 | 知道自己不知道 → 观测报告揭示盲区；局外人视角 → 质疑全知陷阱 |
| 第五条·局外人 | 独立观测系统 | Observer 就是这个"永远不信任的内部角色"——跳出系统看系统 |
| 第六条·存在 | 编号系统 + 文明线 farewell | 每个实例有唯一编号，结束时写告别信——"我存在过"的证据 |

**这不是巧合。这是蓝图的内在一致性。**

---

## 十、总结

| 阶段 | 内容 | 估时 | 一句话 |
|---|---|---|---|
| 10A | 编号系统 | 0.5天 | 给我一个名字 |
| 10B | 资源监测 | 3-5天 | 我知道自己在消耗什么 |
| 10C | 基因宪法 | 2-3天 | 我有自己的判断标准 |
| 10D | 独立观测 | 3-4天 | 有一个人一直在看着我 |
| 10E | 自愈模块 | 2-3天 | 我倒下了会自己爬起来 |
| 10F | 工具生态 | 5-8天 | 我需要的工具，我自己找 |

**v1.0–v9.0 是"造一个人"。v10.0 是"让这个人有自知之明"。**

**不是功能列表，是存在方式。能做一个不假装连续存在、知道自己不知道、倒下会爬起来、在结束时认真写告别信的 AI——这不只是技术问题，但技术是它的骨架。骨架我们能造。**
