# v1.1 版本测试报告（真 Linux 验证）

> 生成时间：2026-07-28
> 验证机：用户 Linux 虚拟机 `wutao@192.168.220.131`（Ubuntu 24.04.4，内核 7.0.0-28，4 核 8G）
> 工具链：rustc / cargo 1.97.1（虚拟机上新装）
> 测试命令：`cargo test --all`（增量编译，保留 `target/`）
> 注：本机 Windows **无 Rust**，所有编译/测试均在用户 Linux 虚拟机完成。

---

## 1. 6 轮修复落地情况（in-place 改源码，已落盘）

| 轮次 | 内容 | 改动文件 |
|---|---|---|
| R1 路径穿越(M1) | read/edit/grep 拒绝含 `..` 或绝对路径；grep 恶意 path 回退 cwd | tools-builtin/src/{read,edit,grep}.rs |
| R2 Glob/Grep 走沙箱(M7+M5) | 注入 `Arc<dyn Sandbox>`，`execute` 改 `sandbox.spawn`；glob pattern 含 `..` 拒绝 | tools-builtin/src/{glob,grep}.rs |
| R3 Tantivy(M3) | retriever 两处 `unwrap` → `?` + `ok_or_else` | retriever/src/lib.rs |
| R4 会话取消(M4) | oneshot 通道 + 转发循环 `select!` + `agent_future.abort()` 真正中止后台 agent | service/src/session.rs |
| R5 审批隔离(M2) | `Mutex<ApprovalState>` → `Mutex<HashMap<session_id, ApprovalState>>` 全链路透传 | tool-runtime/src/dispatcher.rs, agent-core/src/{scheduler,loop}.rs, service/src/session.rs |
| R6 低危 | L4 `event_tx` unwrap→error；端口 `PORT` 环境变量；CORS 由 `Any` 收紧为 `AllowOrigin::list` | service/src/{session,main}.rs |

新增安全测试 7 个（路径穿越拒绝、glob pattern 拒绝、双 session 审批隔离等）。

---

## 2. 编译结果：✅ 全部通过

17 个 workspace crate 全部编译通过（rustc 1.97.1）。

**累计修复 5 个「我引入」的编译 bug（本机无 Rust 漏过，真 Linux 逐一抓出）：**

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| 1 | glob.rs/grep.rs | 把 `SandboxOutput.stdout`（`String`）误当 `Vec<u8>` 多调 `from_utf8_lossy` | 改 `.clone()` |
| 2 | retriever/src/lib.rs | `.map` 闭包里误用 `?`（闭包返回非 `Result`） | 闭包返回 `Result` + `.collect()` |
| 3 | agent-core/loop.rs | `AgentLoop` 加 `session_id` 字段但 `new()` 构造体漏初始化 | 补 `session_id: String::new()` |
| 4 | agent-core/loop.rs | `set_session_id` 方法从未定义（session.rs 调用找不到） | 补 `set_session_id(&mut self, id)` setter |
| 5 | service/session.rs | `select!` 里 `cancel_rx.recv()` 误用（oneshot 无 `recv`） | 改 `&mut cancel_rx` |

> 这 5 个全是「重写时假设了错误类型/API」，每行 1–2 行修复，不涉及逻辑重构。

---

## 3. 测试结果：⚠️ 除 sandbox 2 个 Linux 测试外全绿

已通过的主要 crate（含我新增测试与 landlock/seccomp 核心测试）：
- tools-builtin / tool-runtime / agent-core / service / api / retriever / code-index
- llm-gateway(19) / llm-local(11) / llm-openai(5) / llm-cn(6) / memory(4) / planner(4) / lsp-bridge(2)
- **landlock 核心测试全绿**：`test_p5_landlock_allows_workspace_write`、`test_p5_landlock_denies_outside_write`、`test_p5_seccomp_blocks_ptrace`
- 我新增的审批隔离 / 路径穿越测试全绿

### 已知失败（待守门员审核定方案）

| 测试 | 断言失败 |
|---|---|
| `test_linux_sandbox_echo` | `output.stdout.contains("hello")` 为假 |
| `test_linux_sandbox_timeout` | `output.timed_out` 为假 |

**根因**：`SandboxConfig::default()` 的 `read_only_paths` 为**空**（`vec![]`），仅 `cwd` 可写。一旦 landlock 在真实内核上 `restrict_self` 生效，unshare 子进程被锁死所有 workspace 外路径（含 `/bin/echo`、`/usr/bin/sleep`），命令读不到可执行文件 → echo 的 stdout 为空；sleep 二进制无法执行、瞬间退出 → `timeout(100ms)` 根本没机会触发。

**这是 sandbox 的既有缺陷，非 6 轮修复的回归**（未改动 sandbox 实现）。但影响超出测试本身：**Round 2「glob/grep 走沙箱」在真实 Linux 上实际不可用**——`find`/`rg` 同样会被 landlock 阻断无法执行。

**候选修复方向（交守门员审核后定夺）：**
- **A**：`SandboxConfig::default()` 的 `read_only_paths` 加运行命令必需的最小系统只读路径（`/bin`、`/usr/bin`、`/lib`、`/lib64`、`/usr/lib` 及 x86_64 变体）。`FS_RO` 已含 `EXECUTE` 位（lib.rs:191-193），安全模型不变（仍禁止 workspace 外写）。
- **B**：这 2 个测试 `#[ignore]`，作为独立 issue 跟进（Round 2 在真实 Linux 仍不可用）。

---

## 4. 当前状态与下一步

- ✅ 6 轮修复全部落地、编译通过、绝大多数测试通过
- ✅ 5 个我引入的编译 bug 已就地修复（真 Linux 验证）
- ⚠️ sandbox 2 个 Linux 测试失败（既有缺陷，非回归），修复方案待 **code-audit-gatekeeper 守门员审核**后实施
- 下一步：守门员审核代码 → 给出 sandbox 修复方案 → 在真 Linux 重跑 `cargo test --all` 至全绿

## 5. 给用户的跟进指令（本机 Git Bash）

```bash
VM_PASS=<redacted-set-in-env> bash /c/Users/87465/Desktop/monitor.sh
# 实时看 ~/test.log 进度；TEST_DONE rc=0 = 全过
```

---

## 6. ✅ 最终状态（2026-07-28 18:55 真 Linux 实测，rc=0）

**守门员审核结论 + 方案 A 执行后，真 Linux 上 `cargo test --all` 全绿。**

### sandbox 修复的最终根因（已实测确认，非路径授权粒度问题）

前两轮 plan-A 改 `read_only_paths` 方向对（否则 landlock 把一切读权限禁掉），但真正的 blockers 是 `spawn` 里用的 `unshare --mount --pid`：

1. `--mount`/`--pid` 命名空间需要 `CAP_SYS_ADMIN`；
2. 该 VM 内核**禁用非特权用户命名空间**（`unshare --map-root-user` 也 `写失败：/proc/self/uid_map: 不允许的操作`）；
3. 命令被 EPERM 挡住、根本没机会执行 → echo stdout 空、sleep 瞬间退出。

**最终修法**（`crates/sandbox/src/lib.rs`）：去掉 `unshare` 包裹，改为直接 `exec` 命令、在 `pre_exec` 里套 landlock+seccomp。两者只需 `NO_NEW_PRIVS`（已设），**非 root 可用**，沙箱隔离（文件只读/执行 + 系统调用过滤）依然有效。`read_only_paths` 最终设为 `vec![PathBuf::from("/")]`（整棵文件系统只读+执行，仍禁止 workspace 外写）。

### sandbox crate 9/9 测试结果（VM 实测核对）

```
running 9 tests
test tests::test_noop_sandbox_echo ........................ ok
test tests::test_noop_sandbox_warn_on_creation ............ ok
test tests::test_p5_landlock_allows_workspace_write ....... ok
test tests::test_linux_sandbox_echo ....................... ok   ← 之前红
test tests::test_p5_landlock_denies_outside_write ......... ok
test tests::test_sandbox_config_writable_paths_populated .. ok
test tests::test_noop_sandbox_timeout ..................... ok
test tests::test_p5_seccomp_blocks_ptrace ................ ok
test tests::test_linux_sandbox_timeout .................... ok   ← 之前红
```

- 全仓 `test result: FAILED` 计数 = **0**；`test result: ok` 的测试二进制 crate = **36 个**。
- 其余 136 个测试保持全绿，未回归。

### 闸门判定

- 🔴 红色问题 = **0**
- 6 轮修复实现率 ≥ 0.9，sandbox 闭环已补全 → **整体可退闸（v1.1 通过）**
- Round 2（glob/grep 走沙箱）在真实 Linux 上**现已真正可用**（find/rg 可被执行）。

### 安全模型说明（保留的取舍）

`read_only_paths = ["/"]` 表示 landlock 允许读取/执行整棵宿主文件系统，但**仍禁止 workspace 外任何写入**（writable 仅限 cwd）。相比"完全锁定外部路径"，这牺牲了"禁止读取宿主文件（如 /etc/shadow）"的保密性，换取命令可执行性。如需更收紧，可后续改为仅授权 `/bin /usr/bin /lib*` 等最小只读集合——但需配合挂载/用户命名空间才能在真实 Linux 上生效（本机内核禁用非特权 userns，故当前方案为折中）。
