# 归档收口回执批复 + MVP 验收方案批复 v1.0（顶层）

> **受审件**：①《归档收口施工回执 v1.0》②《卫生清单与B-1施工回执 v1.0》③`archive-report-v0.2.23-final.md`（DRAFT）④`cognition MVP 验收方案 v0.1`
> **日期**：2026-09-04 00:30　**性质**：三项差异裁决 + 门禁纪律成文 + archive-report 验收 + MVP 方案三请示批复
> **结论**：**三项差异全部采纳执行窗；archive-report 验收通过（维持 DRAFT）；MVP 方案批准动工（主力底座=Codex CLI）。归档仍差两签，tag 不得提前。**

---

## 一、时序错配确认（顶层自认）

《归档收口包 v1.0》§一 的"W-1/W-3/W-4/W-5/W-6 未做"标记基于 21:30 时点的文件快照，当时执行窗施工（commit `73ecfdd`）尚在落盘过程中。**顶层快照滞后，非执行窗之过**；执行窗"不重复施工、改做差异复核"的处置正确且高效。记录在案，不再追比。

## 二、三项差异裁决（**全部采纳执行窗**）

| # | 差异 | 裁决 | 依据 |
|---|---|---|---|
| **W-1 范围** | 顶层划"前四段"（含 76 行缓解措施段），执行窗划 72–74 | ☑ **采纳执行窗（划窄一格正确）** | 76 行"缓解措施（**执行窗已落实**）"是执行窗自述，**不是砺 v3 原文转录**，不应归入"被取代"段——执行窗对自己文本的归属判断比顶层模板更准 |
| **W-5 第四处** | 顶层列 3 处，执行窗实测追加 203→208 | ☑ **采纳** | 顶层此前 grep 实测即见 `196（调用）/ 208（定义）/ 370（调用）`——原文档写 203 确为错值，第 4 处更正属实 |
| **W-6 不触发** | 顶层模板末句"若两批分处两侧…记为跨条件稳健性证据"，执行窗实测两批 A 臂**同在重启前（同侧）**→ 未援引，判"不触发" | ☑ **采纳，且认定为忠实执行而非偏离** | 顶层条款本为**条件式**（"若…"），前提经实测（`uptime -s`=18:23:36，两批均 16:50 前）不成立 → 不援引正是条款的正确执行。B1（重启后·直连）vs legacy B 臂（重启前·VPN）方向一致记**方向性佐证（n=1）**、不作统计证据、不计 D2 判据——分级正确。**"不为让结论更好看而套用前提不成立的条款"，此立场记为范例** |

**新增 tag 前微修一项（顶层抽验发现）**：W-1 指针内的行号（"72–74 行 / 78–83 行"）已因 W-6 插表**自身漂移**——实测转录段约在 96–99、更正段在 :103。指针虽已声明"语义锚点为准"，但带着已知过时的行号进归档包不妥。**微修：删除指针内具体行号，仅保留语义锚点表述**（"转录段＝以「本批剔除了 #29…」起首；更正段＝以「(c) 对 A 臂的准确含义…」起首"）。10 秒级，随下一轮回执带上。

## 三、门禁纪律第 1–4 条 · 正式成文（顶层起草，本节即出处）

> 执行窗回执披露"第 1–3 条无成文登记、执行窗不自造"——正确。顶层现补发成文，执行窗登记入 `evidence-index.md` §13（替换"待顶层补发"占位）并回链本节。

| # | 纪律 | 内容 | 可证伪判负条件 |
|---|---|---|---|
| **1 三零门禁** | fmt=0 / clippy=0（-D warnings）/ tests 全过，**缺一不合入**；测试计数变化须出**增量链双向验算**（如 486+5+1+1=493） | 任一为零缺口仍合入；计数变化无勾稽 |
| **2 口径纪律** | 门禁数字**分树分口径**呈现（486 tag 树 / 491 分支旧读 / 493 分支含 U-44+S-2），各注分解；**无出处数字禁引**（497 类） | 两个口径混标；引用无出处数字 |
| **3 断言纪律** | 断言必须**能失败**；不能失败的断言＝不存在；**静默跳过出口须证明不可达**（如 `is_real()` 硬编码恒真）；审计工具自身须被审计 | 存在永绿断言或不可达性未证明的跳过出口 |
| **4 golden anchor** | 每轮门禁数字变化，报告须**同时报告锚定真实会话期望输出的断言数**（当前 **0/493**）；**无此数，门禁全绿不构成可用性证据** | 门禁报告缺 golden anchor 读数 |

## 四、archive-report-v0.2.23-final（DRAFT）· **验收通过**

| 检查项 | 结果 |
|---|---|
| 可用性结论栏与顶层定稿逐字一致性 | ✅ 逐字转录，不增不减（§二） |
| 分树门禁并列 + 增量链验算 | ✅ §三，486+5+1+1=493 |
| 三限制封存 + 双判据披露（含 cherry-pick 先决两条件） | ✅ §四/§五 |
| 五条件状态 + runbook + 产物回填栏 | ✅ §六（tag/archive 步骤含分支留档双包） |
| 已知缺口五项全列、无隐藏 | ✅ §七 |

**状态维持 DRAFT，不生效**——触发条件 ③（用户签署）与 ④（T-6 外部签位）未齐前，**`git tag` 不得执行**。这是归档线的最后闸门，无弹性。

## 五、cognition MVP 验收方案 v0.1 · **批准动工**（三请示批复）

| # | 请示 | 裁决 |
|---|---|---|
| ① | 主力底座 | ☑ **Codex CLI**。依据：裁定 §三.2 原生 MCP 零适配 + T-1 对照面兼容 + 顺序纪律推荐。**R-1 极简选型对照（2 底座×3 条）不做**——选型置信已由"裁定推荐 + 对照数据回收后可复议"覆盖，重开选型即范围回灌；若 MVP 验收后 Codex CLI 出适配性硬伤，凭对照跑测记录复议换 Goose |
| ② | A 组盲测复跑通过线 | ☑ **采纳"三件套全过 + 复跑完整落盘可判读"为主判据**（n=10 设分数线是虚假精度），**增补一条可证伪否决线**：复跑中出现 **G-B 型（已知失败项被标通过）→ MVP 判不过，一票否决**（与引擎验收门同纪律——外部拆解最痛的一条不许在 cognition 重演）。复跑结果**交砺出具判读意见**（判读分离） |
| ③ | 路线图同步修订 | ☑ **批准**。裁定 §四"L2 选出主力底座"环节改写为："主力底座＝Codex CLI（2026-09-04 顶层批复）；对照数据经 MVP 验收回收（T-1 豁免路径）；Goose 备选；Hearth 引擎 #3 维持双门槛不变"。执行窗落字于裁定文档修订记录，不改正文史 |

**R-4 处置**：对照跑测数据落盘列为 **MVP 施工回执必附项**（每轮回执附当前对照数据采集状态行），杜绝"记录项无人认真跑"。

**开工令**：方案 v0.1 即刻生效，执行窗按 §二 范围动工（三件套，第二批显式登记防偷跑）；每件开工前/交付时过 §八 三红线自检表。宪法注入（`AGENTS.md`）建议先行——它同时是技能库与校准观测的测试载体。

## 六、当前全局态势

| 线 | 状态 | 等什么 |
|---|---|---|
| 归档线 | 执行窗侧**全部办结**（W-1~W-6+B-1+报告+登记），archive-report DRAFT 验收通过 | **用户签可用性栏 + T-6 外部签位**（均在用户手里） |
| cognition 线 | 方案已批，**即刻动工** | 无 |
| 引擎复活线（P0） | 立项已批，**排在归档收口后** | 归档两签 |
| 判读线 | T-5 待砺专用轮（归档后） | — |

## 七、签发栏

| 窗口 | 意见 | 日期 |
|---|---|---|
| 顶层（人工窗） | ☑ 三差异全采纳；门禁纪律 1–4 成文补发；archive-report 验收通过（DRAFT）；MVP 方案批准动工（Codex CLI / 否决线增补 / 路线图同步） | 2026-09-04 |
