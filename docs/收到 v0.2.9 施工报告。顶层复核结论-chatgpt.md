收到 v0.2.9 施工报告。顶层复核结论：

**ContextBuilder 主体施工通过，但本轮暂不宣布最终验收闭合。进入 Closure Window，不回退、不重构、不扩大 ContextBuilder scope。**

当前裁决：

* ContextBuilder L1/L2/L4 架构：PASS
* 6/6 核心回归单测：PASS
* topology_sig / last_graph_sig 分立：PASS
* Experience 采用方案 X：PASS
* FAILED 事实保全：PASS
* D 类零触碰：PASS
* A System Stability：PASS
* B Message Prefix：OPEN
* C Provider Outcome：OPEN（口径断裂，不判失败）
* T2：根因已定位并修复，复跑实质完成
* T3：OPEN，**禁止以“Agnes 波动”作为未经验证的结论**
* 第二窗口 rubric：OPEN
* tool_schema_hash：OPEN

### Closure Window 只做以下事项

#### 1. 先闭合 B 层 Stability

补齐逐消息 hash chain telemetry。

要求：

* 对同一执行 session 的 Message 序列按顺序计算 hash chain；
* 至少能够区分：

  * system message
  * stable prefix
  * dynamic tail
* 不改变 ContextBuilder 语义；
* 不做 tool schema phase pruning；
* 不触碰 D 类。

补完后用一个最小重复调用测试证明：

> system 内容相同 → system hash 相同 → stable prefix chain 相同；动态消息变化只能发生在允许的 L4 区域。

#### 2. 补齐 tool_schema_hash telemetry

这是已明确记录的偏差，现在关闭它。

要求只增加观测，不改变 schema 内容和工具注册生命周期。

`ChatRequest.tools` 保持全量稳定。

#### 3. 调查 T3 failed

这是当前唯一未解释的行为回归。

**不要先假设是 Agnes 波动。**

从失败 run 的完整日志开始，建立最小证据链：

```text
T3 输入
→ ContextBuilder 最终消息结构
→ planner/reflect/replan 决策
→ tool call
→ tool result
→ failure 位置
→ failure classification
→ 最终 failed 原因
```

判断它属于：

A. ContextBuilder 回归
B. Tier3 逻辑问题
C. 工具执行问题
D. provider/model 波动
E. 其他

只有拿到证据后才能归类。

如果确认与 ContextBuilder 无关，保留 OPEN/UNKNOWN 证据，不要为了让数字好看而强行闭合。

#### 4. 做 EC-01 DeepSeek 同口径 cache 对照

严格复用：

* EC-01 原 4 个任务
* 同 Agnes 以外的原 DeepSeek 通道
* 同 TelemetryProvider v2
* `HEARTH_ALLOW_NO_CGROUP=1`
* 同任务顺序与采集方式

对比：

1. token-level hit rate
2. request-level hit ratio
3. system_hash unique count
4. Message prefix hash chain

特别关注：

> system_hash 是否达到每个主任务 session = 1。

**不要预写“cache 提升”。**

如果 A/B 达成而 C 没改善，结论写：

> Hearth 侧 stability 已达成，provider outcome 未改善；provider segment/granularity 仍 UNKNOWN。

不要因为 C 不涨而回退 ContextBuilder。

#### 5. 完成第二窗口 rubric 复核

从 EC-03 中抽至少 2 个任务，按原 0-2 rubric 重新盲评。

至少覆盖：

* 一个多步任务（优先 T2）
* 一个隐含约束/换向任务（T3 或 T4）

与 EC-03 基线逐项比较，不得只比较最终 rc。

### Closure Window 禁止事项

本轮禁止：

* 修改 ContextBuilder 架构
* 改 Experience X/Y 策略
* 改 TaskGraph 事实模型
* 修改 Goal Revision
* 修改 bash stdout
* 修改 filesystem accounting
* 修改 resource 控制流
* tool schema phase pruning
* bridge
* subagent
* TUI

### 关于 T2 修复

允许保留已经发现并完成验证的：

> v20 all_done gate 正常 plan 重入不应累计 graph stall count。

这个修复属于已有 Tier3 问题的正确修复，不视为 ContextBuilder scope 扩张。

但请补一个回归测试，锁住：

```text
all_done gate 正常重入
→ graph_stall_count reset
→ 不触发误判 stall / give_up
```

同时确保真正的 Reflect → Replan 路径仍然累计 stall count。

### 最终交付格式

Closure Window 结束后只报告：

1. commit/diff
2. gate 实测
3. B-layer hash-chain evidence
4. tool_schema_hash evidence
5. T3 根因归类及证据
6. DeepSeek EC-01 cache 对照
7. EC-03 rubric 复核
8. T2 stall regression test
9. OPEN / UNKNOWN / DEFER
10. 是否满足最终验收

**现在进入 Closure Window。**

不要再写新的架构设计稿；把剩余证据闭合即可。

---

# 守门员批注（2026-08-29 · v0.2.9 独立复核后补充，与正文同效力）

> 复核实证：VM `~/t_gate_r2c.log` **FMT/CLIPPY/RT4_SOLO/TEST 全 RC=0，TOTAL_PASSED=381** ✅；关键接线逐点查实——`last_topology_sig`（:578，id/description/deps 无 status）、L2 Topology 块（:1449-1452）、Continuity 失败节点行（:978-995）、T4 误杀修复（`graph_stall_count` gate 重入重置 :1818 + commit `7116278`）、EC-05 勘误回填（`32fa346`，我上轮补充 1 落地）。Closure Window 五事项照办，以下 6 条为执行细则。

## 补充 1：telemetry schema 三项合并一次动刀

Closure 第 1 项（逐消息 hash chain）与第 2 项（tool_schema_hash）都要改采集器 schema，而施工报告自曝的 A 层口径偏差根源正是"**telemetry 缺 phase/caller 维度**"——三项合并为**一次 schema 修订**：per-message hash chain + tool_schema_hash + phase/caller 字段，一次落、一次过门禁。hash 链格式建议：`chain_k = H(chain_{k-1} ‖ role ‖ content_hash)` 逐消息一条，离线算相邻请求公共前缀长度。别补 B 层又漏 phase，再来一轮返工。

## 补充 2：T3 排查有机械捷径——先读结构化终态再深挖

v0.2.9 的 run report（`.hearth/reports/<sid>/`）已带结构化 **terminal_state/reason**，Continuity 块含失败节点摘要。T3 复跑 failed 的排查第一步：读 report 的 terminal_state——若 `budget_exhausted` 且 13 步，直接归入 Tier3 T5 已解的"cgroup env + 预算落点"类（ContextBuilder 回归嫌疑 A 先被结构性排除）；只有 terminal 异常（stalled/verify_failed 等）才需要深挖 ContextBuilder 路径。另：T3 复跑排查时开 `RUST_LOG=trace`——顺手把 B04/B06 一直缺的 trace 级样本补上（一鱼两吃）。

## 补充 3：DeepSeek 对照的通道前提先确认

EC-01 基线的 deepseek 通道：先确认现在可用（key/额度/端点活着），**并写清当前 deepseek 与 Agnes relay 的关系**（R2 时代 deepseek 曾走 relay 单点——若现在仍经 relay，"同口径"的定义要包含 relay 路径）。避免跑到一半通道死（T2 的教训重演）。deepseek 免费额度撞 429 按 H4 语义停。

## 补充 4：A 层口径修正追认 + 分析口径沉淀

"planner decompose/reflect 独立调用混入 session 统计"的口径修正**追认**（诚实且正确——v2 全出口采集后分析必须分段）。把它沉淀成固定分析口径：**session 内按 msgs 数/phase 分段统计，主链 = msgs≥3 的正式组装请求**——EC-03 复测/T7 遥测都用这个口径，防止下次又混。

## 补充 5：rubric 复核的执行者与盲评方式

第二窗口盲评建议由 **Claude 评审窗口**做（未参与施工，无利益相关）；材料 = EC-03 的 5 任务 transcript + 产物文件，**盲评时不告知变体标签（A/B/C/E）**防锚定；覆盖 T2（多步）+ T3/T4（约束/换向）至少 2 任务，与 EC-03 基线逐项对比。

## 补充 6：T2 stall 回归测试零预算口径

gate 重入重置 vs Reflect→Replan 保留计数两条断言，用 **MockLlm 构造**（同图重入场景 + 真 replan 场景），不烧预算——与 T1-T7 测试同口径。

## 追认

T2 误杀修复（`7116278`）判定正确："v20 all_done gate 正常 plan 重入产同图 ≠ 语义停滞"语义清晰，且报告如实把它列为行为回归并定位修复——诚实披露纪律持续在线。Closure Window 产出后照例停等顶层。
