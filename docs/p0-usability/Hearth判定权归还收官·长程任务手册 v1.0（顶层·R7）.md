# Hearth 判定权归还收官 · 长程任务手册 v1.0（顶层·R7）

> **签发**：顶层　**日期**：2026-09-06
> **输入**：①《外部窗口-六相拆除与判定权归还·汇总报告 v1.0》（J-1~J-10 + 拆除清单 D-1~D-10）②《六相拆除汇总报告·执行窗评审 v1.0》（8.7 填报：可执行·需调整时序）③《阶段2判别实验战报 v1.0》④《阶段2判别实验·执行窗验收报告 v1.0》（VERIFIED@ce71f7e）
> **性质**：判别实验出数后的**收官手册**——一次签发剩余全部工程动作，含顶层五项裁决、三条作战线、三重闸门与总验收预注册。
> **铁律**：本手册全部行号基于 `1429b58` / `96f4161`，**引用前重新 grep**；门禁纪律 1-6 条全文沿用（含第 6 条行数判据）。

---

## 〇、顶层判读：判别实验五项裁决（本节为最终口径，禁再争）

### 裁决 1｜结果采信，分歧按题型分解收口

判别实验 VERIFIED@ce71f7e 采信入账。**预注册字面裁决**：严口径 G-A 两臂均 <7/10（A 臂 3-5、B 臂 2-3）→ 按预注册字面 = 模型层占比大（**砺方字面胜**，其认输条件未触发）；外部的框架层全称主张同样无需全盘认输。

**按题型分解（信息量更大的真实图景）**：

| 题型 | 证据 | 归因 |
|---|---|---|
| 无上下文体感/状态题（A1/A10） | **双臂同错**（均追问澄清） | **模型层**——框架改造不解决 |
| 多步执行题（A4/A6/A9/A7） | A 臂全面占优：A4 9→2 calls、A6 10→2、A9 15→5、A7 6→1；A 臂 15 条**零 Task failed 自擒、零假完成** | **框架编排病已由 R6-1~9 实证消解** |

**两方各对一半。** 此口径为终审口径，此后任何窗口不得再引"谁赢了"的简化表述。

### 裁决 2｜调用降幅 25.3% 未达 50% 判据——如实记账

结构性判据（每步 1 次模型调用 vs B 臂 ≥3 次）成立；端到端降幅被 A 臂模型自发探索（A5：3→20 calls）抵消，未达 ≥50%。**不粉饰、不改判据**。A 臂探索行为校准列入 R7-5；若校准后仍不达标，判据修订案须呈顶层，**不得私改**。

### 裁决 3｜混杂因子成立——A 臂翻默认**暂缓**

B 臂构建 = a1485c2f（**pre-96f4161**）+ cwd 未隔离 + DEBUG 探针泄漏 → 多步题 B 臂失败幅度被污染**部分夸大** → "判定权归还使多步题改善"的幅度**存疑，待干净对照复核**。A1/A10 模型层结论不受影响（双臂同构建下同错）。

**处置**：A 臂翻默认裁决暂缓，前置 = R7-1 干净 B 臂复跑（分钟级，同构建同协议）。**翻或不翻，均须引用 R7-1 数据编号**。

### 裁决 4｜执行窗时序修正采纳：E-A 收尾 ∥ E-B 并行

外部 J-4（E-B 先于 E-A）调整为**并行**——E-A 剩余动作（R7-1/R7-4）小时级，E-B（R7-6）天级，无资源冲突。**"E-B 出结果前不删 B 臂"维持为铁闸**（外部原判，执行窗亦同意）——避免 2,000-4,000 行删除后发现更优基座，删除动作的最后闸门交给 E-B。

### 裁决 5｜P1 僵尸收割修复：冻结区有条件授权（签署位见 §四）

P1（bash/cargo 子进程 `<defunct>` 未收割 → 进程不退出，A 臂 B5 实证 1054s 挂起）涉 sandbox 冻结区 FZ-RFC-1。**有条件授权**：

- 范围**仅限** bash 工具 wait/reap 路径的子进程收割逻辑；
- Landlock / seccomp / cgroups **安全语义零改动**，diff 越界审查照常；
- 修复后必须复跑 B5 场景（`HEARTH_SECCOMP_MODE=errno cargo build`）验证批次不挂起、无僵尸。

---

## 一、总路线图：三条线、三重闸门、一个终点

```
线A 引擎收口（.133 执行窗）      线B 底座对照 E-B（独立窗口）
  R7-1 干净B臂复跑                R7-6 装Codex CLI+Goose跑盲测包
  R7-2 P1僵尸收割（授权已附）      R7-7 决策矩阵拍板件
  R7-3 版本串staleness                 │
  R7-4 A臂翻默认（视R7-1裁决）         │
  R7-5 A臂探索校准                     │
        └──────────┬───────────────────┘
                   ▼
        【三重闸门】①R7-1干净对照支持A臂 ②E-B决策=留自研 ③用户体感通过
                   ▼
线C 手术（阶段C）
  R7-8 删B臂 D-1~D-10（逐项grep/逐项提交/同步删测试红线）
  R7-9 wc -l 行数验收（12,523 → ~9,000）
```

**终点定义**：A 臂成唯一路径（或裁定换基座后 cognition 层整体迁移），loop.rs 回到 <10,000 行量级，G-W1 首份真尺在手。**行数从 12,523 掉下来，是"范式真的换了"的唯一不可辩解的证据。**

---

## 二、任务包明细（一次签发）

### 线 A · 引擎收口（执行窗/traecode，.133）

| # | 任务 | 精确要求 | 可证伪判据 |
|---|---|---|---|
| **R7-1** | **干净 B 臂复跑**（最高优先） | HEAD（≥96f4161）构建 B 臂；cwd 隔离已生效；DEBUG 已删；与 armA 同语料同协议同机 | 对照表落盘（md5 两臂留档）；多步题 B 臂数据与首轮差异逐题标注 |
| **R7-2** | **P1 僵尸收割修复**（授权见 §裁决5） | 仅 bash 工具 wait/reap 路径；安全语义零改动；.131+.133 双机验证 | B5 场景复跑：批次不挂起、`ps` 无 defunct、门禁绿 |
| **R7-3** | **P2 版本串 staleness** | build.rs 加 `cargo:rerun-if-changed=.git/HEAD` 类指令 | git pull 后重建，`--version` = HEAD commit |
| **R7-4** | **A 臂翻默认**（视 R7-1 裁决） | `single_loop` 构造默认 true；`HEARTH_SINGLE_LOOP=0` 逃生门保留一个版本周期；A 臂测试转主路径断言 | 翻转 commit 引用 R7-1 数据编号；门禁绿；逃生门可回退实测一次 |
| **R7-5** | **A 臂探索行为校准**（A5 3→20 异常） | 排查 A 臂无 T4 停滞后的探索引导缺口；budget 交接提示是否被模型忽略 | 重跑 A5：calls 回落且结论质量不降；端到端降幅重测；**不达标则判据修订案呈顶层，禁私改** |

### 线 B · 底座对照 E-B（独立窗口，与线 A 并行）

| # | 任务 | 精确要求 | 可证伪判据 |
|---|---|---|---|
| **R7-6** | **E-B 底座对照实验** | 装 Codex CLI（首选）+ Goose（备选）；接 agnes（`api.agnes-ai.cn/v1`，config 字段以官方文档为准）；跑盲测包 15 条**逐字、全新 session**；Q1-Q5 逐条 YES/NO + 每题调用数；**每份日志必含引擎/底座版本标记**（R4.2 最贵教训） | 对照表落盘：Codex CLI / Goose × Hearth-A 臂，15 条 × (成功/Q1-Q5/calls/耗时) |
| **R7-7** | **决策矩阵拍板件** | 按汇总报告 §一·五 三行矩阵填实测数：A 胜+底座明显强→换基座；A 胜+不明显→A 臂转正+删 B 臂；A 败→止损评审 | 拍板件含两线数据引用编号，呈顶层签署 |

**E-B 环境注意**：.131 国际出口默认被拦，开工前探活 api.agnes-ai.cn（期望 401）；VPN 由用户负责挂载。

### 线 C · 手术（阶段 C，三重闸门全绿后启动）

**三重闸门**（全部满足才许动第一刀）：
1. R7-1 干净对照数据支持 A 臂（多步题优势经混杂因子清除后依然成立，或至少不劣于 B 臂）；
2. E-B 决策矩阵 = 留自研（R7-7 顶层签署）；
3. 用户体感测试通过（0.2.25 alias 已就绪，用户实操确认改善明显）。

| # | 任务 | 精确要求 | 可证伪判据 |
|---|---|---|---|
| **R7-8** | **删 B 臂 D-1~D-10** | 外部汇总报告 §3.4 清单照执行（D-1 140 词表 → D-9 计数器体系）；**逐项全仓 grep 确认无隐藏依赖 → 逐项单独 commit → 每项后跑全量门禁 + 真机回归集**；`Event::Phase` 先改 2 处投影（session.rs:1002 / api/src/lib.rs:265）再删枚举；**B 臂测试同步删除，严禁改断言使其绿（纪律红线）**；风险过高项可 `#[deprecated]` 冻结而非硬删 | 每项 commit diff 越界审查干净；删除清单 D-1~D-10 逐项留痕 |
| **R7-9** | **行数验收** | `wc -l crates/agent-core/src/loop.rs` 一行命令 | 12,523 → **~9,000**（硬线 <10,500）；净减达标 = 范式更换唯一不可辩解证据 |

**保留清单（不得删）**：预算+优雅降级 / 沙箱全家（Landlock+seccomp+cgroups，全项目唯一达标资产 4.5/5）/ observer 事件流 / checkpoint / 诊断 JSONL / 大输出落盘 / 真机回归集。

---

## 三、挂账登记（不在本手册施工，防丢失）

| 项 | 说明 | 归口 |
|---|---|---|
| D2 历史 40 条硬切 | A 臂长任务上下文策略，与删 B 臂解耦，另立小包 | 线 C 之后 |
| F4 MAX_DEPTH=2 子 agent | 非阻塞 | 观察项 |
| 追问类回归子集（#11/#12/#21/#29/#39） | 模型层澄清行为，框架无关 | cognition MVP 线 |
| G-W1 首份真尺 | 前置 = C 语料补齐 11 句（**仅语料所有者可做**）+ 独立评审判分（执行窗自判不计） | 用户拍板后 |
| 归档线两签 | 用户签可用性结论栏 + T-6 外部签位 → tag+双 tar 仪式 | 不受本手册影响 |
| cognition MVP 线 | Codex CLI 主力底座方案已批；与 R7-6 E-B 数据天然合流 | 独立线 |

---

## 四、用户拍板项（签署位）

| # | 事项 | 选项 | 签署 |
|---|---|---|---|
| **签 1** | **P1 冻结区有条件授权** | ☑ 同意（范围限 bash wait/reap 收割，安全语义零改动）　☐ 驳回 | **☑ 用户已签（2026-09-06 21:03）——R7-2 授权生效，执行窗可开工** |
| **签 2** | **C 语料 11 句补齐** | 由语料所有者补齐后，G-W1 真尺流程启动 | 进行中（补齐方式：顶层从手工测试日志抽取用户原话→语料所有者确认） |
| **签 3** | **E-B 决策矩阵拍板**（数据到齐后触发） | ☐ 换基座（cognition 走 MCP/hooks 挂载）　☐ A 臂转正+删 B 臂　☐ 其他 | ________ |
| 签 4 | 三重闸门第 3 闸（用户体感 0.2.25） | 实操后填写 | ________ |

---

## 五、总验收预注册（8 条，验收时逐条对号）

1. **干净 A/B 对照表落盘**：同构建同协议同机，两臂 md5 留档，逐题差异标注；
2. **A 臂翻默认裁决有据**：翻或不翻均引用 R7-1 数据编号，无数据不裁决；
3. **P1 修复实证**：B5 场景复跑不挂起、无 defunct、门禁绿、diff 越界审查干净；
4. **版本可验证**：git pull 后重建 `--version` = HEAD；
5. **G-W1 首份真尺**：C 语料 14/14 + 独立评审逐条判分，数字入档；
6. **E-B 对照表**：Codex CLI/Goose × Hearth-A 臂，15 条 × Q1-Q5/calls，日志含版本标记；
7. **若启动线 C**：删后门禁绿（B 臂测试同步删除清单在案，零改断言）+ Q1-Q5 不回退 + `wc -l` 硬线 <10,500；
8. **全程纪律**：口径分树禁混标（486/493/500/524/判别实验新口径各自独立）；引擎身份可验证；行数判据（第 6 条）适用本手册每个包。

---

## 六、施工秩序与并发纪律

- **判读分离**：战报逐件 → 验收窗独立核验（只采信可独立复现证据）→ 里程碑宣告权归砺 → 顶层只裁例外与闸门；
- **并发红线**：.133 跑版本冻结测试期间严禁同机并发 LLM 任务；E-B 安装实验与线 A 跑测如需同机，错峰执行；
- **冻结区**：仅 R7-2 在授权范围内动 sandbox，其余任何窗口不得触碰；
- **诚实边界**：判别实验执行窗自判的 10 题评分、A5 20 calls 异常未剔除、B4 占位语料作废——三条已随战报在案，后续引用时须带限定语。

## 七、一句话

> **框架编排病已被数据坐实消解，模型层残差与"要不要自研"是最后两个未决问题。本手册把两个问题各配一条数据线（R7-1 干净对照、R7-6 底座对照），数据到齐、三闸全绿，然后动那 2,000-4,000 行的手术——拆完，loop.rs 回到 9,000 上下，这才是这场范式手术的收尾证明。**

---

> 顶层签发 · 2026-09-06 · 依据：判别实验 VERIFIED@ce71f7e + 外部汇总报告 J-1~J-10 + 执行窗 8.7 填报
