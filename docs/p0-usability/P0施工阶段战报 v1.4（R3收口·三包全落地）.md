# P0 引擎复活线 · R3 包收口战报 v1.4（执行窗 → 顶层/砺）

> **承接**：顶层《R2包收口顶层批复 v1.0》（R3 四件批准开工、可并作一轮）
> **分支**：`p0-usability-01`　**累计门禁**：**500 passed / 0 failed**（FMT=0 / CLIPPY=0）
> **施工窗口**：2026-09-04 00:58 – 05:10　**性质**：**R3 包收口 + 三包全落地的总战报**

---

## 一、一句话

**R1 诚实包 + R2 状态包 + R3 沟通包——任务书三包全部落地**，全量门禁 **500 passed / 0 failed**，增量链 `486+14=500` 双向验算成立，golden anchor **15/500**。R4 总验收（G/W 门 + 双会话重放）就绪待启。

## 二、R3 包四件（并作一轮，每件照"病理→语义→判据"）

### R3-1 收尾三行（W-E 进度可问的终态面）

- **病理**：0.9-0.3 会话 472 条横幅噪声淹没正文；用户疑问句得到的是文件路径而非结论。
- **语义**：每轮终态必附三行——**改了什么**（本轮产物清单）/ **还剩什么**（SessionLedger 未完成栏开放条目）/ **依据**（完成决策 + 验证状态）。**全部由 Observe/账本采集的事实生成，非模型自报**。failed/cancelled 轮也渲染（"还剩什么"对失败轮更重要——用户据此下继续指令）。
- **判据**：`test_r31_ledger_pending_texts`——开放条目返回、已关闭条目不计入"还剩什么"。

### R3-2 goal_drift 升级投影（G-F 知情必拦截）

- **病理**：goal_drift 只发 ThinkSummary（淹没在噪声里），用户在终态看不到"任务可能被替换"的警示。
- **语义**：终态处**明确警示行**（"⚠ G-F goal_drift 疑似：终局产物与原始目标语义相关度存疑——请人工核对"）。任务书允许"改终态**或**明确提示"，取后者——LLM 判定可能误报，改 failed 会冤枉；**真正改终态留待 R4 以双会话重放的误报率数据裁定**（这个克制与 R1-4 报告层拦截同一逻辑）。
- **判据**：summary.goal_drift 字段（RC31 既有）→ 投影读取；字段链已有，真机检验归 R4。

### R3-3 视觉层级反转 + 降噪三档（W-F）

- **病理**：E15 倒挂——正文 `.dimmed()` 暗灰、工具行 `.yellow()` 亮黄（正文比噪声还暗）；472 条横幅无静音手段。
- **语义**：
  - **反转**：正文（token 流）恢复正常色；工具行/工具结果降 dimmed——正文才配亮色；
  - **三档**：`--quiet`（0 横幅 0 工具行 0 流式增量，只留用户指令/审批/产物/终态/错误）／ normal（默认，反转后层级）／ `--verbose`（全量等价旧版）；CLI 全局 flag 位置无关接线（hearth_main）。
- **判据**：`test_r33_verbosity_levels`——三档往返、上限钳制、quiet 档过程函数安全调用；终端级静默效果由 R4 e2e 快照终验（println 不可进程内捕获，与 RC34 同口径）。

### R3-4 完成度口径治理（G-E 统计不自污染）

- **病理**：0.9-0.3 会话报"完成度 103%"——行数分母含 agent 自写文档，写文档即可刷高完成度（自污染）。
- **语义**：completed 终态 summary 携带 `completion_metrics`——**代码产物 / 文档产物分列**（文档不计入代码口径）+ 政策明示："行数/字节数不作完成度分母；完成与否由 acceptance/verify 事实裁决"。
- **判据**：metrics 字段链（completed 分支内联计算，json! 宏外提取）；真机口径验证归 R4。

## 三、三包全景（任务书 §三 施工总包 · 全部落地）

| 包 | 项 | commit | 病理终结 |
|---|---|---|---|
| **R1 诚实** | R1-1 完成语义重造 | `bc89678` | 7/28 假完成改判通道（存活重验 + UNVERIFIED 不计成功率） |
| R1 | R1-3 Observe 实职化 | `af60c4a` | Observe 全 0ms 空转（结构化判定每轮落盘） |
| R1 | R1-2 守门恒真修复 | `113b869` | 中文目标守门恒真（字符级扫描双语） |
| **R2 状态** | R2-1 SessionLedger | `ae9c0d8` | 十项清单蒸发（只 close 不 remove，结构级防蒸发） |
| R2 | R2-2 切片归档 | `52509d2` | 假事实跨会话存活/真事实不能（E18 反向保护） |
| R2 | R2-3 语义摘要 | `1804e32` | 压缩后失忆（动作-结果语义行，零 LLM） |
| R2 | R2-4 否定检测 | `ac568f2` | 18 次同警告误报（否定前缀检测） |
| **R3 沟通** | R3-1 收尾三行 | `b13a42c` | 横幅淹没/疑问句答路径（事实三行必附） |
| R3 | R3-2 goal_drift 投影 | `b13a42c` | 知情不拦截（明确警示行） |
| R3 | R3-3 视觉+降噪 | `b13a42c` | E15 倒挂 + 472 横幅（反转 + 三档） |
| R3 | R3-4 口径治理 | `b13a42c` | 103% 自污染（分列口径 + 政策明示） |
| **R1×R2** | R1-4 known-failing | `6e905de` | 0/16 标 ✅100%（G-B 报告层拦截） |

## 四、门禁纪律（第 1-4 条）

| 纪律 | 读数 |
|---|---|
| 三零门禁 | `FMT=0` / `CLIPPY=0` / `TEST=0`（全量 workspace 05:06，`.131:~/fa/p0_gate_r1full.log`） |
| 口径纪律 | tag 树 486/0（未动）｜p0 分支 **500/0**——分列禁混标 |
| 断言纪律 | 全线 +14 测试全部能失败（每条判据都在旧语义下必红——病理对照见 §三） |
| golden anchor | **15/500**——每件判据均锚定两份真实会话（0.9-0.3/0.9-0.4）拆解铁证，零凭空断言 |

**增量链**：`486 +14 = 500`（14 = r11×2 + r13×2 + r12×2 + r22×1 + r21×3 + serde×1 + r23×1 + r31×1 + r33×1）✅

## 五、下一步：R4 总验收（最后一关）

1. **砺里程碑判读**（R1"不再说谎"+ R2"不再失忆"两件合并判词——顶层已建议合并出判）；
2. **G-A~G-H 八门正式跑测**（G-B 一票否决——机制已就位）+ **W-A~W-F 六判据**；
3. **双会话场景重放方案**：执行窗可即起草（0.9-0.3/0.9-0.4 用户轮次重放新引擎，FileBrowser"已知失败不得标 ✅"与"103% 假事实不得被引用"专项复测），交砺+外部定稿后执行。

> **诚实边界（任务书 §五）**：三包治的是 harness 的"说谎/失忆/不验/淹没"；模型能力上限（agnes-2.5-flash 生成质量）由校准观测量化、模型渠道独立解决——分开记账。
> 施工层自证到此为止；**真机检验全部归 R4，不作数的地方一律不预支结论**。
