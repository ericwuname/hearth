# Hearth R9 收官手术·执行委托书 v1.0（执行窗→traecode）

> **拟**：执行窗（WorkBuddy）　**日期**：2026-09-08
> **依据**：顶层签发件《Hearth闸门评估与收官手术·长程任务书顶层签发件 v1.0》（dfc6bf4，四签落笔+三增补）+ 拟稿 8fe1251 全文条款（照常执行）
> **你的定位**：**包E（线 C 手术）施工主责**。当前阶段=预研就绪（P0），手术本体=**触发式**（签 2），触发未齐前**禁动 loop.rs**。
> **不归你**：包B 四组对照（独立新窗口 .131）、包C G-W1 判分（砺主评）、用户体感（用户）。你只对手术负责。

---

## 一、大局一句话

闸 1 已收官（A 臂墙钟中位 −73.6% 坐实，顶层批准入账）。剩两闸：包B 四组对照数据 + R7-7 拍板件签署（闸 2）、用户体感 0.2.25（闸 3）。**两条齐，签 2 自动生效，无需新文书**——你发一条"开刀申报"即可动刀。刀已磨好（十项施工卡见拟稿 §四），但两点必须先认清：

1. **D-9 被实测砍了半条**（签 3）：`a_arm_act_tally`（convergence 收口 + strategy-forced 换法）是 −73.6% 的功臣机制，**保留**；D-9 收窄为"删 B 臂侧计数残留"。
2. **行数起点比外部预估高了 543 行**：13,055 → 硬线 <10,500，净减 ≥2,558；顶层算术诚实条款（增 1）实测：收窄后十项预估合计 ≈1,850–1,900，**不能默认达标**——落在 (10,500, 11,043] 走顶层重基线评审，>11,043 = 手术未完成，禁自宣达标。

## 二、即刻任务 P0 · 重锚定与预研（纯只读分析，零代码改动）

**为什么是现在**：外部 §3.4 删除清单锚点为 1429b58 时代坐标，R8 之后 loop.rs 整体漂移（96f4161=12,523 → 61dea7c=13,055，+532~543 不均匀偏移）。"动刀前全仓 grep"本就是每项施工闸第 1 条——现在做完，触发时直接开刀，不浪费闸 2/闸 3 的等待窗口。预研成本为零风险（纯静态 grep + wc -l），若包B 裁决 ≠ 留自研则手术取消、预研件归档，损失可忽略。

**基线自证（开工第一件事，证据入报告）**：

```bash
git rev-parse HEAD                      # 应 ≥ 61dea7c（手术分支基线）
wc -l crates/agent-core/src/loop.rs     # 必须 = 13,055
```

若行数 ≠ 13,055：**停下上报**，禁在未知基线上预研（陈旧工作树三次历史事故的教训）。

**十项重锚表（产出物核心，顺序=施工建议顺序）**：

| 序 | D 项 | 对象 | 原锚点（已失效，仅索引） | 重锚定要求 |
|---|---|---|---|---|
| 1 | D-10 | emit_think_summary 相位套话 | 各相位尾部 | grep 全部相位尾套话分支；**标出 A 臂投影保留项**（ThinkSummary 主体、R8 预算注入 [budget-warn]/[budget-critical] **勿误删**） |
| 2 | D-8 | B 臂机关块 | :3541-3648（注释自述块） | 重定位该块；确认写盘逼迫 / forced-replan / MUST tool_calls 三件 A 臂旁路属实（逐段 grep） |
| 3 | D-1 | goal_requires_product() + 98 词表 | :230-369 | 全仓 grep 调用面（含测试/宏/文档）→ 引用清单 |
| 4 | D-2 | UserInputKind 三分类 + 42 词 | :404-473 | 同上（C2 误判 29% 病灶源头） |
| 5 | D-7 | ReflectVerdict 三值决策 | agent-types/src/lib.rs:110-117 | 跨 crate 引用先清点（agent-types 独立 crate） |
| 6 | D-5 | LoopPhase 枚举 + step() match | :29-37 / :5071-5160 | **最后删**；标注 LoopPhase::Error 路径（R8-A1 仍用）与 `Event::Phase` 消费端 2 处新坐标（原 session.rs:1002 / api/src/lib.rs:265，96f4161 坐标） |
| 7 | D-3 | planner.decompose() 调用链 | :2939 + planner crate | planner crate（1,282 行）外部未深审，影响面可能低估——外科范围自查后如实上报 |
| 8 | D-4 | task_graph / TaskGraph / plan_state | 全文件多处 | **checkpoint 挂钩处先解绑再删**（R6-8 保留项），挂钩点新坐标必须标出 |
| 9 | D-6 | do_observe() / do_reflect() | :4087 / :4439 | B 臂相位本体；B4 同名异构随之消解；error_kind 归类缺口随删接受（R8-A2 遗留已登记） |
| 10 | D-9 | 计数器残留（**签 3 收窄版**） | consecutive_errors / steps_without_progress / same_tool_repeat / write_attempted 等 | **逐计数器判定 A 臂路径触达**：`a_arm_act_tally` = 现役**保留**；R8 已实证 steps_without_progress 与 R5-3 相关计数自 R6-9 起 A 臂死代码（=可删）；其余逐个沿 HEARTH_SINGLE_LOOP 路径 grep 出判定 |

**每项产出六元组**：①当前锚点区间（grep 实测行号）②全仓引用清单 ③实测行数 ④修正预估减量（对照增 1 的 1,850–1,900 总估算逐项更新）⑤A 臂触达判定 ⑥风险与隐藏依赖预判（签 4：疑似需 `#[deprecated]` 冻结的项，预置申报材料——引用清单+理由）。

**达标性预判三态**（对照增 1）：修正合计 ≥2,558 → 预判达标；预估落 (10,500, 11,043] → 预判评审带；预估 >11,043 → 预判不足，附"继续深挖候选"清单。

**冲突申报（增 3 元原则）**：预研发现任一 D 项对象实为 A 臂现役机制 → 同款 D-9 处理：**保留现役、收窄清单、写进报告申报**——不得为凑清单执行率而自毁机制。

## 三、触发链路与开刀申报（签 2 原文执行）

触发生效条件（两条**同时**齐）：
1. 包B 四组对照数据落盘 + R7-7 拍板件顶层签署（裁决=留自研）；
2. 用户体感确认报顶层。

生效即自动动工授权，无需新文书。你的动作序列：
1. 发**开刀申报**一条，含三件：闸 2/闸 3 状态确认 + tag `pre-linec-baseline` 打点证明 + 源码 tar 日期戳存 Desktop 完成证明（用户既定纪律）；
2. 按拟稿 §四施工卡逐项动刀（顺序：D-10→D-8→D-1→D-2→D-7→D-5→D-3→D-4→D-6→D-9，低风险→高风险，每项独立 commit）；
3. 若包B 裁决 ≠ 留自研：手术取消，预研件归档，等顶层新指令。

## 四、动刀施工闸（每项不可跳，拟稿 §4.2 原文）

1. 动刀前全仓 grep 该对象全部引用（含测试/文档/宏）→ 引用清单入战报；
2. 单独 commit，diff 越界审查（禁夹带）；
3. 全量门禁（**537 基线**，B 臂测试**同步删除**，禁改断言使其绿——纪律红线）；
4. 盲测包 15 条冒烟（分钟级，**冒烟红即停**，连续两项红 → 手术暂停评审）；
5. 每 3 项 `wc -l` 里程碑报数一次。

**保留清单（违反=手术失败，手册原文）**：预算+优雅降级 / 沙箱全家（Landlock+seccomp+cgroups，4.5/5，**手术全程零触碰**）/ observer 事件流 / checkpoint / 诊断 JSONL / 大输出落盘 / 真机回归集 / **R8 校准机制（a_arm_act_tally + 预算双闸，签 3 裁定保留）**。

**增 1 算术诚实三裁定（术后验收适用）**：
- <10,500 维持目标硬线，禁止把 R8 校准保留项（+543）当删除对象；
- 全 D 项完成（含冻结项）且术后验收 2-7 条全过后，`wc -l` 实测落 **(10,500, 11,043]** → 触发顶层重基线评审（按"R8 校准机制为授权保留项"裁定手术成功）；
- 实测 **>11,043 → 手术未完成**：继续深挖 B 臂残留或另立删减小包，**禁止自行宣布达标**；预估与实测差值逐项如实记账。

**签 4 冻结纪律**：隐藏依赖不可安全拆除的 D 项允许 `#[deprecated]` 冻结而非硬删——**逐项申报（含引用清单与理由），禁批量**；冻结项计入术后验收清单，**冻结不豁免 wc -l**（冻结代码仍在行数内，倒逼真删）。

**术后验收 7 条**（拟稿 §4.4，全过才算完）：①`wc -l` < 10,500（硬线）②门禁绿（B 臂测试删除清单在案，零改断言）③Q1-Q5 不回退（术后盲测包+C 语料双臂各 1 轮，C YES ≥13 不降、G-A ≥3）④A 臂墙钟中位不劣化（单轮 >2× 中位即警）⑤`HEARTH_SINGLE_LOOP=0` 逃生门实测回退一次后按手册保留一个版本周期 ⑥版本 bump **0.2.26** + `--version` 跟踪验证（R7-3 机制）⑦D-1~D-10 逐项 commit 链 + 隐藏依赖处置清单（含冻结项）。

## 五、禁区与红线

| # | 红线 | 依据 |
|---|---|---|
| 1 | 触发未齐前禁改 loop.rs B 臂本体（预研=只读分析） | 签 2 触发式 |
| 2 | sandbox 全家不碰（Landlock+seccomp+cgroups） | 冻结区 FZ-RFC-1 |
| 3 | A 臂现役机制不碰（a_arm_act_tally / convergence / strategy-forced / 预算双闸） | 签 3 |
| 4 | 包B/包C 不归你：不跑四组对照、不做 G-W1 判分 | 签发件 §四执行顺序 |
| 5 | 删代码致测试失效 = 同步删测试，禁改断言使其绿 | 手册纪律红线 |
| 6 | 包B 跑批期间不在 .131 并发跑 LLM 任务（预研纯静态无冲突，跑构建/真机去 .133 错峰） | 并发纪律 |
| 7 | 引擎身份纪律：手术 commit 链逐个留档，0.2.26 后 `--version`=HEAD 验证 | R7-3 机制 |
| 8 | 一次性脚本不落仓库根目录；门禁 FMT 用 RC 直取（防管道吃码假绿） | 既有纪律 + 仪器缺陷登记 |

## 六、产出与战报格式

**预研件（现在交）**：`docs/p0-usability/线C手术·重锚定与预研报告 v1.0（traecode→执行窗）.md`，必含七件：
1. 基线自证（HEAD hash + `wc -l`=13,055 证据）；
2. 十项重锚六元组表；
3. 修正减量合计 + 达标三态预判；
4. B 臂测试删除清单预编（哪些测试随哪个 D 项死——预期内删除，禁改断言）；
5. 疑似冻结项预置申报（签 4 材料格式）；
6. 冲突申报（如有，增 3 口径）；
7. `Event::Phase` 消费端 2 处新坐标 + checkpoint 挂钩点新坐标。

**手术战报（动刀后）**：沿用五要素证据包——commit 链+改动清单 / 判据原文 / 门禁原文（**RC 直取**）/ 日志路径 / 每 3 项 wc -l 里程碑数。交执行窗独立复算验收（不信转录，逐格重算）。

## 七、一句话

> 现在的活是磨刀不误砍柴工：把十项清单在 13,055 行的新坐标上重新钉牢、把每个计数器的 A 臂触达判清楚；两条闸一齐，开刀申报一条消息直接按卡动刀——**a_arm_act_tally 是战果不是累赘，谁删谁负责**；行数按诚实算术验收，10,500 达不到就走重基线评审，谁也不许凑数。

---
*执行窗拟 · 2026-09-08 · 依据：dfc6bf4 签发件 + 8fe1251 拟稿 + 汇总报告 §3.4 原文；loop.rs 13,055 行经本窗 wc -l 复核*
