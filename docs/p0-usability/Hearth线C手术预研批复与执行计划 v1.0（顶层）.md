# Hearth 线C 手术预研批复与执行计划 v1.0（顶层）

> **签发**：顶层　**日期**：2026-09-08
> **输入**：《R9包E预研·执行窗验收报告 v1.0》（e38051，VERIFIED）+ trae 预研（4c055a6）+ R9 委托书（9eb250d）
> **锚点自证**：顶层独立抽验三处——`:4001`（`next: LoopPhase::Reflect` 审批旁路，无 single_loop 门控，实测命中）、`:1512-1524`（restore_task_graph 函数体，`:1518 self.task_graph` 与 `:1519 plan_state.task_graph` **双写点**实测）、`:3347-3351`（经验门 `consecutive_errors >= 3`，实测在位）——与预研/验收报告一致。

---

## 一、C-1~C-5 一笔批复（预研五项裁定请求）

| # | 事项 | 顶层批复 |
|---|---|---|
| **C-1** | stuck 族保留 | **批准**——维护/注入/硬停（:4194-4225/:2787/:4228）全 A 臂可达有行为，元原则第二案：实测优先于清单。 |
| **C-2** | UserInputKind 改写删除 | **批准（顶层明示）**——判定权归还的一致性要求；C2 误判 29% 的病灶正是它；test_r69/r75 回归保障在案。**附回滚触发器**：术后 C-control 类语料（C-control-endtest 等）若误判回潮，单条判负即回滚本项并呈顶层——A 臂行为语义变化纳入术后验收观察面，此点已作为知会用户事项记录。 |
| **C-3** | checkpoint 载荷缩编 | **批准**——R6-8 本意=checkpoint 机制不死，载荷随数据源消失属自然收窄。**施工注记**：解绑点为 :1512-1524 函数体，`self.task_graph`（:1518）与 `plan_state.task_graph`（:1519）**双写点**须一并处理，动刀战报如实记录。 |
| **C-4** | consecutive_errors 经验门死分支删除 | **批准**——零行为变化（A 臂恒 0 恒 false 实测）；经验注入需求挂账 cognition 线，如实入账。 |
| **C-5** | 算术口径确认 | **确认**——B 臂测试同步删除计入 <10,500 硬线口径（施工闸 3 本意，非放水）；loop.rs 的 wc -l 口径含测试区（wc 口径测试区 :6287-13,055=6,769 行）。据此预研达标预判 ≈8,970 成立、下界 10,220 仍达标；**增 1 评审带 (10,500, 11,043] 保留不动**（保险条款，预期触达概率趋零）。 |

## 二、登记项确认（2 笔，即刻生效）

- **R-1 口径统一令**：手术里程碑报数与验收清单一律 **wc -l 坐标**（13,055 基线口径），禁再混用 Measure-Object 非空行口径（空行实测 522，两口径差 522 行）。
- **R-2 FA01 坐标修正**：:5294 在 do_reflect 尾（非 do_act）；GiveUp 测试预编按"经 do_reflect/MockPlanner-GiveUp 驱动者死"重判，偏差方向保守（安全侧），采信。

## 三、执行计划（关键路径定案）

手术本体维持签 2 触发挂起——**剩余阻塞只有两件，都在用户手里**：

| 序 | 动作 | 窗口 | 依赖 |
|---|---|---|---|
| 1 | **挂 VPN**（探活 api.agnes-ai.cn 期望 401） | 用户 | 包B 唯一前置 |
| 2 | **包B 四组对照开工**（裸跑/Codex CLI/Goose/Hearth-A，15 条逐字+Q1-Q5+失败呈现样本采集；判分外包砺/外部） | 独立新窗口（.131） | 序 1 |
| 3 | **0.2.25 体感实测**（双机 alias 已就绪，结果报顶层即闸 3） | 用户 | 无 |
| 4 | **包C 脱敏版产出**（r8-all.tar.gz md5 7d4453b8，抹臂标/引擎行/耗时）→ 移交砺 | 执行窗 | 无，即刻 |
| 5 | **包C 判分**（秒答条重点复核；B 臂判负 ±1 与 exit_code 弱仪器两笔登记收口）→ G-W1 首份真尺入档 | 砺 | 序 4 |
| 6 | **开刀申报**（= pre-linec-baseline tag + Desktop tar + 双闸确认证明，一条消息） | trae | 序 2 数据落盘+拍板件顶层签署 ∧ 序 3 完成 |
| 7 | **线 C 手术**（十项已毫米级重锚，按施工卡低→高风险逐项：每项 grep 清单/单独 commit/门禁 537/盲测冒烟红即停/每 3 项 wc -l 报数） | trae | 序 6 |
| 8 | **术后验收 7 条**（wc -l <10,500 硬线+评审带裁定 / Q1-Q5 不回退 / 逃生门实测 / 0.2.26 bump / D 项留痕链） | 验收窗→砺→顶层 | 序 7 |
| 9 | **归档仪式**（tag+双 tar）+ cognition MVP 线开工（包B 数据合流） | 全体 | 序 8 |

**并行说明**：序 2/3/4/5 相互无依赖，即刻并行；序 6 的触发条件 = 闸 2（包B 数据+拍板件签署）∧ 闸 3（体感），包C 不是手术闸门，不阻塞动刀。包C 若在动刀前出数，G-W1 真尺可作为术后 Q1-Q5 对照的额外基线；未出数则按施工卡原计划对照 R8 post 基线。

## 四、纪律与红线（沿用不变）

手术零改动红线已由预研验证（loop.rs 零触碰、包B/包C 未越界、冻结区未碰、无 LLM 调用）；动刀后按施工卡五道闸执行；B 臂测试同步删除禁改断言（红线原文）；D-1~D-10 坐标以 4c055a6 重锚版为准（外部 1429b58 坐标已漂移 +230~540，**禁盲抄旧锚点**）。

## 五、一句话

> **刀已磨到毫米级、五笔冲突一笔批清、达标算术（≈8,970）算术透明——执行计划里没有工程阻塞，只有两把用户手里的钥匙：挂 VPN 开包B、抽一小时做体感。钥匙一到，开刀申报一条消息，十项手术按卡推进，术后 8,970 见。**

---

*顶层签发 · 2026-09-08 · 依据：e38051 验收 + 4c055a6 预研；三处关键锚点顶层独立实测*
