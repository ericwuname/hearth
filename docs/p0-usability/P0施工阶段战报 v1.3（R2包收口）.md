# P0 引擎复活线 · R2 包收口战报 v1.3（执行窗 → 顶层/砺）

> **承接**：顶层《R2-1与R1-4顶层验收批复 v1.0》（验收通过 + R2-3 为下一刀 + "勿因进度顺而压缩判据设计"提醒——本篇照"病理→语义→判据"全文格式）
> **分支**：`p0-usability-01`　**累计门禁**：**498 passed / 0 failed**（FMT=0 / CLIPPY=0）
> **施工窗口**：2026-09-04 00:58 – 04:40　**性质**：**R2 包收口报告**

---

## 一、一句话

**R2 状态包四件全部落地**（R2-1 账本 / R2-2 切片归档 / R2-3 语义摘要 / R2-4 否定检测），"不再失忆"的全部机制就位；累计门禁 498/0，增量链 `486+12=498` 双向验算成立，golden anchor **13/498**。

## 二、R2-3 语义摘要·内存侧（commit `1804e32`）

| 项 | 内容 |
|---|---|
| **病理** | `summarize_turn` 只有元数据（"轮次目标 60 字 \| 工具名 \| 写盘路径"）——名词清单没有动作语义，压缩后问"之前做了什么、为什么失败"答不出（W-D 前置缺口） |
| **新语义** | **动作-结果语义行**（确定性提取，**零 LLM 不烧 token**）：按 call_id 配对 ToolCalls↔ToolResults，产出"写了 \`plan.html\`；执行 \`cargo build --release\`（失败）；读取/其他 N 次"的可读动作流 + 成败统计；失败动作带（失败）标记——"之前为什么失败"压缩后仍可答 |
| **互补关系** | R2-2 归档管**原文找回**（grep），R2-3 摘要管**快速理解**——两层合起来压缩才不等于失忆 |
| **判据** | 测试构造三态 turn（写入成功 / bash 失败 / 读取）→ 断言动作语义行、失败标记、成败统计、**旧格式移除**（"\| 工具: "不再出现）全部锁定 |

## 三、R2 包四件总览（本包全部已验收或待判读）

| 项 | 病理（真实会话实证） | 机制 | 状态 |
|---|---|---|---|
| R2-1 SessionLedger | 十项未完成清单全蒸发 | 五栏账本，只 close 不 remove（结构级防蒸发），切片后注入，跨重启 serde | ✅ 已验收 |
| R2-2 切片归档 | 假事实跨会话存活、真事实不能（保护反向） | 被切早轮 Turn 落盘 archive/<sid>.jsonl + grep 找回提示 | ✅ 已验收 |
| **R2-3 语义摘要** | 压缩后"做了什么/为什么"答不出 | 动作-结果语义行（零 LLM） | ✅ **本篇** |
| R2-4 否定检测 | "不应该是"误报 18 次 | 否定前缀检测 + 单轮去重 | ✅ 已验收 |

**里程碑"R2 绿=不再失忆"**：机制三件（账本注入/切片归档/跨重启 serde）+ 摘要升级全部就位——**正式宣告权在砺**（判读分离）。

## 四、门禁纪律（第 1-4 条）

| 纪律 | 读数 |
|---|---|
| 三零门禁 | `FMT=0` / `CLIPPY=0` / `TEST=0`（全量 workspace 04:35，`.131:~/fa/p0_gate_r1full.log`） |
| 口径纪律 | tag 树 486/0（未动）｜p0 分支 **498/0**——分列禁混标 |
| 断言纪律 | 本篇 +1 测试（r23）能失败：旧代码下"写了 `plan.html`"动作行不存在必红 |
| golden anchor | **13/498**——r23 锚定"压缩后失忆"病理（两份真实会话长程轮次的核心痛感） |

**增量链**：`486 + 2 + 2 + 2 + 1 + 0 + 3 + 1 + 1 = 498` ✅（R1-1/R1-3/R1-2/R2-2/R2-4/R2-1/R1-4/R2-3）

## 五、下一步：R3 沟通包四件

| 项 | 内容 | 坐标（施工前重 grep） |
|---|---|---|
| R3-1 | **收尾三行**：改了什么/还剩什么/依据——由 Observe 采集的事实生成，每轮终态必附 | render.rs（重 grep） |
| R3-2 | goal_drift 升级拦截（改终态或明确提示，不再 observe-only） | goal_drift 输出处 |
| R3-3 | 视觉层级反转 + 降噪三档（`--quiet`/默认/`--verbose`） | render.rs:60/64-66/70 |
| R3-4 | 完成度口径治理：行数不作唯一分母、不含自写文档 | agent 侧统计 |

R3 收口后 → **R4 总验收**：G-A~G-H 八门（G-B 机制已就位）+ W-A~W-F 六判据 + 双会话场景重放 + 砺判读 + 外部交叉。

> 判读分离：本报告为施工证据；"不再失忆"里程碑宣告归砺。
