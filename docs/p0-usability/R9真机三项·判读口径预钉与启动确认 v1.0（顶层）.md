# R9真机三项·判读口径预钉与启动确认 v1.0（顶层）

> **拟件/签署**：顶层　**日期**：2026-09-09
> **依据**：《R9真机三项·跑批启动通报 v1.0》（875b38c+7561690）；真机验收授权（33657c6）；手术验收（105ef84）
> **性质**：启动确认 + **判读口径预钉（先于数据，防墙头草）** + 判分纪律注记。判分战报（数据齐后）按本件口径判读，门槛不得事后移动。

---

## 一、启动确认

- 部署双验（0.2.26 源包 7add4df0 双端一致 / bin md5 49495b00 留档 / loop.rs 8,114 双机再证）**确认**；cargo clean 释放 68.8G 属构建缓存可再生，无数据损失。
- 管线 v1 `set -u` 挂点如实申报 + v2 修复重启实证运行（A1=35s/calls=2、A2=40s，与 R7-1 同量级）——**申报合规，教训有效**（R8 原脚本无 set -u 故未暴露；新脚本带 set -u 是进步，挂点本身是诚实披露）。
- 双构建对照设计（0.2.26 vs 0.2.25-base 93e7e57）**确认**，操作化裁定见 §二。

## 二、判读口径预钉（先于数据钉死，判分战报照此执行）

### ③ 盲测质量

- C 语料 14 条逐条判 YES，**≥13 过**（判分口径=包C 流程）；
- G-A = 盲测 A 组 10 条严口径（首屏直接结论句）**≥3 过**（R7-1 基线 A≈2-3/10）；
- **C-2 回滚触发器实弹**：跑批日志观察 668d4a8 落刀点路径；语料病态条目触发不足则专项构造场景补测——**不许纸面确认**。

### ④ 墙钟——"双臂"操作化裁定（术后语境唯一可行解读，顶层确认）

- 手术后 B 臂已删，原"同构建 A/B 臂 env 切换"对照不复存在。**裁定：④ 判据 = 0.2.26（生产默认，A 臂转正）vs 0.2.25-base 93e7e57（生产默认=B 臂）各 3 轮中位数对照，≥50% 过**；blind 与 corpus c 分别对表。
- 此口径比原判据更贴产品：测的是"术后生产默认 vs 术前生产默认"的真实改善。旧 −73.6%（R8 同构建 env 对照）**不可比、不复用、不混标**。

### ⑤ SINGLE_LOOP 回退

0.2.26 全部日志 grep 回退事件 = 0 过（不设 env 的生产默认路径下）。

### C-2 回滚条款（独立于三项验收，持续挂载）

0.2.26 上线后任何时点：C-control 类语料**单条误判即回滚** UserInputKind 改写（668d4a8）——三项全过不免除此条款。

## 三、判分纪律注记（判分战报必须落实）

1. **v1 残数据剔除**：v1 挂点（set -u）期间产生的任何半截目录/轮次**不入判分样本**；样本=v2 重启后完整轮次，与 `r9-post.done` + 矩阵清单（blind15+corpusC ×3 轮 ×双构建=12 组）逐一对账，缺一组即申报不得补跑凑数。
2. **CPU 竞争噪声登记**：.133 他窗 mock 单测（非 LLM 任务）与跑批并存——并发纪律未违反（确认），但 ④ 若中位差落在 **55-65% 贴线区间**，战报须标注此噪声源并做敏感性说明；CLI mock 单测为短时任务，3 轮中位数下影响可容忍，但须留痕。
3. **引擎双 md5 口径**：.131（49495b00）与 .133（e23bbb49/93e505c3）两机独立构建（时间戳嵌入）——判分引用以版本字符串+各自 md5 双验为准，**禁跨机混标**；新数据包 md5 出炉后入口径分树。
4. **过程异常即报**：构建失败/语料跑挂/配额枯竭→即时战报，不静默、不补跑凑数。

## 四、路径重申

三项全过 → 归档仪式两签（tag v0.2.23-final@117a947 已裁 + 双 tar + 版本注释补齐）→ **GitHub 首次正式同步**（remote → 安全清理审查（.workbuddy 密钥/VM 凭据/数据包先移出）→ force push 弃旧镜像）。任一项不过 → 按失败战报申报，顶层定返工范围，禁局部自宣达标。

## 五、签署位

| # | 事项 | 签署 |
|---|---|---|
| 1 | 启动确认（部署双验/v2 重启合规/双构建对照设计） | ✅ 顶层已签 |
| 2 | 判读口径预钉（③≥13/≥3+实弹、④双构建中位 ≥50%、⑤grep=0、C-2 持续挂载） | ✅ 顶层已签 |
| 3 | 判分纪律注记四条（v1 剔除/CPU 噪声/md5 口径/异常即报） | ✅ 顶层已签 |

---

*顶层签发 · 2026-09-09 · 口径已钉，等数据说话；判分战报落地后本件为唯一判读依据*
