# R1 诚实包 + R2 两件 · 顶层验收批复 v1.0（顶层）

> **受审件**：①`R1诚实包施工战报 v1.0` ②`P0施工阶段战报 v1.1（R1全绿+R2两件）`
> **日期**：2026-09-04 04:00　**性质**：施工验收批复（里程碑宣告权归砺）
> **结论**：⬛ **R1 包三件 + R2-2/R2-4 两件，顶层抽验 5/5 源码实证，全部验收通过。批准下一刀 = R2-1 SessionLedger。**

---

## 一、抽验记录（不信报告信源码，5/5 属实）

| # | 战报主张 | 顶层实测 | 结果 |
|---|---|---|---|
| 1 | R1-1：改判前产物存活重验 + UNVERIFIED 强制 + `success_rate_counted=false` | `loop.rs:2190`（`verify_written_files_sync` 调用）/ `:2204,:2213`（false）/ `:3994`（定义）/ `:1647`（投影契约注释） | ✅ |
| 2 | R1-2：守门改字符级扫描（旧 `split_whitespace`+`contains('.')` 恒真路径废除） | 函数内 `user_text.chars().collect()` 字符级扫描在，旧实现仅存于注释引述 | ✅ |
| 3 | R1-3：`observe_verdict` 每轮写入、Reflect/投影可消费 | `:4078`（写入）+ `:10292/:10347`（消费点）+ `:4024` 契约注释 | ✅ |
| 4 | R2-2：切片改送 `archive_compacted_turns`（与压缩同文件 `archive/<sid>.jsonl`）+ 可行动 grep 提示 | `loop.rs:2464`（调用）+ `:2492`（提示语）+ `:1657/:10361`（契约注释） | ✅ |
| 5 | R2-4：否定前缀（不/没/未/非/别/勿/无）紧邻检测 + 单轮去重 | `planner/src/lib.rs:109-128` 完整实现 | ✅ |

分支链核对：`p0-usability-01` 六 commit（bc89678 → af60c4a → 113b869 → 1b1e5bc → 52509d2 → ac568f2 → a2fcd3b）与两份战报一一对应；增量链 `486+2+2+2+1+0=493` 双向验算成立；golden anchor **8/493**（0/493 → 8/493，**门禁纪律第 4 条生效后第一个周期即产出非零读数**——E19 升格的首个成果）。

## 二、验收意见

| 项 | 裁定 |
|---|---|
| **R1 诚实包（R1-1/2/3）** | ☑ **顶层施工验收通过**。三件分别封死：假完成主通道（写盘计数改判→存活重验+UNVERIFIED 不计成功率）、守门恒真（中文目标能红）、Observe 空房间（判定输出每轮落盘） |
| **R2-2 / R2-4** | ☑ **验收通过**。R2-2 关闭 E18"假事实能跨会话、真事实不能"的反向保护；R2-4 消灭 E16 十八次刷屏且不误杀真传闻（"据说…不应该是"仍触发的测试设计正确） |
| **里程碑宣告** | "R1 绿=不再说谎 / R2-2、R2-4 部分'不再失忆'"的**正式宣告权在砺**（判读分离）——请砺在下一轮判读中出里程碑判词；本批复仅为施工验收 |
| **G 门预演口径** | 战报预期 G-C 3/3 呈现未完成、G-H 通过——**预演不作数**，R4 总验收时正式跑 |

## 三、施工质量备注（登记）

1. **两次先红后绿均为测试强化**（r13 中文双语归类、rc47/52 hydration 真落盘）——方向正确；"英文中心主义当场修正"一条自证了任务书"中文场景"预判。
2. **git checkout 事故教训采纳**：大跨度切分支分步执行+每步加长超时；checkout 被杀后必须验证关键文件物理存在。登记入执行窗注意事项。
3. **范围纪律**：未动 planner schema、未动 T4 阈值、未写引擎 cognition 适配器——三红线自检合格。
4. **已知取舍（R1-2）**：版本号等误收紧方向=改判被拒→诚实方向，代价小于假完成——取舍声明写进注释，合格。

## 四、下一步批准（照任务书 §四 依赖图）

1. **R2-1 SessionLedger = 下一窗口首刀**（大件）。三条硬约束重申：五栏（未完成/已知失败/承诺/已验证事实/待验证）；**注入时点在切片之后**；条目**只 `close()` 不 `remove()`**。G-D 门（列 5 项→5 轮"继续"→零遗忘）为其验收。
2. **R1-4 known-failing 串行于 R2-1**（砺 A-3），落地后 G-B 一票否决门才有机制基础——这是"0/16 标 ✅100%"病理的终结点，优先级高于 R2-3。
3. R2-3 语义摘要注意：R2-2 已做"落盘侧"，**内存侧摘要仍是 R2-3 的活**（战报已正确留口）。
4. R3 包在 R2 收口后进入；R4 总验收（G 门+W 判据+双会话重放）届时由砺+外部联合判读。

## 五、并行线状态提醒

归档线（tag 树 486/0 口径未受本线影响）仍差两签：**用户签可用性结论栏 + T-6 外部签位**——与 P0 线互不阻塞，但 R1 归档仪式前不得动 `117a947`（P0 全部施工在 `p0-usability-01`，纪律已守，保持）。

## 六、签发栏

| 窗口 | 意见 | 日期 |
|---|---|---|
| 顶层（人工窗） | ☑ 五件全验收通过；里程碑宣告移交砺；R2-1 为下一刀 | 2026-09-04 |
| 砺·评审 | ⬜ 里程碑判读（"不再说谎"）待出 | |
