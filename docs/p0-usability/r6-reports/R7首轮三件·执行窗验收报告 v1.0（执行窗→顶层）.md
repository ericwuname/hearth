# R7 收官·首轮三件验收报告 v1.0（执行窗→顶层）

> **验收窗**：WorkBuddy 执行窗　**日期**：2026-09-07 05:13–05:50
> **对象**：traecode 首轮交付（R7-2 / R7-3 / R7-1，commits f09d3d8 / 3a9cd77 / 5a4f2e7 / ebd2f52 / 025e102）
> **方法**：附录 A 协议——不采信转写；.133 原始日志独立重算 + diff 逐行审查 + .131 双机交叉核验 + **验收窗独立复跑全量门禁**

## 一、总裁决：**VERIFIED（三件全部属实，附 3 条登记 + 4 项顶层决策）**

委托书首轮范围三件全部落地，全部数据经独立重算无一虚构；traecode 三项主动申报（战报合并 commit / fmt 残留 / 未越界）均查实属实。判据逐条对号：**预注册 #1（同构建对照）达成、#2（翻默认有据）达成、#3（P1 实证）达成、#4（版本可验证）达成**。

## 二、R7-2 · P1 僵尸收割（f09d3d8）✅

- **diff 越界审查**：仅 `sandbox/src/lib.rs` +106/−5。setsid 在 `sandbox_pre_exec` **之前**执行，安全钩子本体零改动；landlock/seccomp/cgroups 四个安全函数零触碰；timeout 路径组杀带 `getpgid` 校验防误杀。**授权边界严格遵守**。
- 修法三件套与委托书假设 1（孙进程管道继承）完全吻合：setsid 组长不变式（fail-closed）→ 读线程并行排空 → `wait()` 即刻收尸 → 组杀滞留孙 → join。
- **新测试可红**：`test_linux_sandbox_grandchild_pipe_holder_does_not_hang`——旧码下 `elapsed<3s` 断言必挂（阻塞 ≥5s），符合"测试必须能失败"铁律。
- 双机 B5 复跑：.133/.131 各一次干净退出（预算护栏 25 步正常交还，`~/b5-verify.log` 两机在档）；.133 当前 `defunct` 计数 **0**。
- **行为注记（如实）**：组杀语义 = 直接子退出即杀全组，孙进程在父死后的残余输出被丢弃——防挂起优先的正确取舍，但属工具输出语义的可见变化，登记在案。
- **运维教训入账**：修复期间实锤"僵尸持 `.cargo/.package-cache` flock 楔死整条工具链 10 小时"——僵尸危害从内存槽位升级为全局锁杠杆。

## 三、R7-3 · 版本串 staleness（3a9cd77）✅

build.rs +13：`rerun-if-changed` 补 `.git/HEAD` + `.git/refs/heads`（目录）+ `.git/index`；`HEARTH_GIT_SHA` env 优先级保留。双机验证 `--version = ebd2f52` 与 HEAD 一致（.131 仓库构建版实测复核通过）。判据 #4 达成。

## 四、R7-1 · 干净双臂复跑 + C 语料 14/14 首跑（025e102）✅

### 4.1 同构建生命线（预注册 #1）——达成
四个有效目录 + 废弃偏差目录（153035Z，`--approve-within` 漏带证据，如实保留）的 md5 **全部 = 9ec07d9d**（ebd2f52 构建），arm.txt 门控逐目录核验正确。首轮"两臂不同构建"混杂因子**根除**。

### 4.2 独立重算（逐格吻合战报）

| 数据 | 战报 | 独立重算 |
|---|---|---|
| 盲测 calls | B 66 / A 71（A **+7.6%**，判据未达） | **66 / 71** ✓ |
| 盲测耗时 | B 1540s / A 713s（A **−53.7%**） | **1540 / 713** ✓ |
| C 语料 calls | B 59 / A 35（**−40.7%**） | **59 / 35** ✓ |
| C 语料耗时 | B 6554s / A 1609s（**−75.5%**） | ✓ |
| C 语料判分 | B：YES4/NO6/部分4；A：**YES8/NO0**/部分6 | 抽验 8 条终端投影全命中 ✓ |

抽验锚点：A6-A"明确的状态不一致问题"（2 calls/7s）；A1 双臂追问（**跨构建跨轮次同错**——模型层残差铁证）；C-retest-fail-A 命中 BUG-003 + `src/app.rs:52` 锚点；B 臂 5 条 deadline（900–930s）+ 1 条 Task failed 实证；fixture（PROGRESS.md）真实进入上下文。

### 4.3 裁决数据判读（呈顶层定夺）
1. **R7-4 翻默认：数据不支持，建议批准"暂缓维持"**——calls 判据盲测 +7.6%、C 语料 −40.7%，双双未达 50%；且首轮"多步题 A 臂全面占优"经干净对照证实**部分系 cwd 污染夸大**（B 臂 A4 本轮 27 步"空目录无任务"，无漂移对象可漂移）。顶层裁决 3 的谨慎被数据证实正确。
2. **但 A 臂的真实优势清晰可见**：耗时两口径全降（−53.7% / −75.5%）、C 语料失败 6→0、零 Task failed 自擒维持。**A 臂是"快且不作死"，B 臂是"慢且易漫游"**——翻默认的最终钥匙在 R7-6 底座对照与 R7-5 校准。
3. **判据噪声警示（诚实边界⑤实证）**：A 臂轮次间 calls 波动巨大（A3 本轮 3→11 vs 首轮 11；A4 8→20 vs 首轮 2；A9 6→12 vs 5）——agnes-2.5-flash 探索行为随机性大，**单轮 50% 判据读数不可作为终审依据**。建议 R7-5 校准时改为"多轮中位数"口径，或呈判据修订案。

## 五、验收窗独立门禁复跑 ✅

战报仅报 sandbox 子树 27/27，**全量门禁由验收窗补跑**（.131 @ ebd2f52，正确环境变量，单实例干净日志）：

> **531 passed / 0 failed / 1 ignored，61 套件，RC=0**（524 基线 + R6 各包新测试 + R7-2 新测试；增量编译 1.4s，全程 71s）。

日志：`.131:~/gate-r7-clean.log`（首次启动曾产生双实例交叉污染日志，已弃用并单实例重跑——过程如实记录）。

## 六、traecode 三项申报的裁决

| 申报 | 核实 | 裁决 |
|---|---|---|
| R7-2/R7-3 战报合并在 5a4f2e7 | 属实（代码修复本身逐件独立 commit 合规） | **接受不拆**——重写历史成本 > 收益，格式瑕疵登记 |
| 工作树 context.rs/render.rs fmt 残留 | 属实，纯 `cargo fmt` 重排零语义 | 处置权交顶层：单独 fmt commit 收编或还原均可 |
| 未越界（R7-4/5/线B/线C 未动） | 属实 | 合规确认 |

**战报笔误一处（登记不阻塞）**：R7-1 战报 §1.3 "A3 22 calls 后 No response requested."——实为 22 **steps**（calls=11，§1.1 表正确）；"No response requested." 模型层新病理实证在档，转 R7-5 排查清单。

## 七、登记项（不阻塞，防丢失）

1. **.131 PATH 卫生地雷**：`/usr/local/bin/hearth` 仍为 0.2.23 (unknown)（md5 34f6f807），且 `.bashrc` alias `hearth→codex-r4 旧构建`（9月5日）——裸跑 `hearth` 必命中错引擎。R4 三次引擎身份事故同族隐患，建议下批授权清理（低风险、非冻结区）。
2. R7-1 原始数据 tar 已入仓库（`docs/data/r7-20260907/r7-all.tar.gz`）——首轮"只存 .133"缺口已按委托书修正。
3. C 语料 fixture 为重构示例项目（非用户原始 TUI 仓库），上下文保真度有限——G-W1 真尺仍需独立评审判分（预注册 #5 未完）。

## 八、呈顶层决策项

1. **R7-4 裁决**：按数据批准"翻默认暂缓维持"（引用 R7-1 数据：盲测 calls +7.6%、C 语料 −40.7%，均未达 50%）；A 臂耗时/质量优势转 R7-5/R7-6 消化。
2. **R7-5 立项**：A 臂探索校准（A3/A4/A9 探索轮 + "No response requested." 病理）+ calls 判据改多轮中位数的修订案（或按手册呈判据修订）。
3. **线 B 启动令**：R7-6/7 底座对照（Codex CLI + Goose）已无前置阻塞，与 .133 跑测错峰即可；"E-B 前不删 B 臂"铁闸维持。
4. **.131 PATH/alias 清理授权**（非冻结区，5 分钟级）+ fmt 两文件处置。
5. **G-W1 真尺**：C 语料已跑通但系执行窗自判——按预注册 #5 需独立评审逐条判分后数字方可入档。

---
*验收脚本留档：.workbuddy/verify_r7_p1b/p2/p3/p4/gate_*.py；原始证据：.133 四结果目录 + .131 gate-r7-clean.log @ ebd2f52*
