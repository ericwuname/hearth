# R8 双线收口首轮（包D+包A）·执行窗验收报告 v1.0（执行窗→顶层）

> **验收窗**：WorkBuddy 执行窗　**日期**：2026-09-08
> **对象**：traecode 交付（e78f27b / 394708e / 432bca7 / 852f295 / 9627c06 / caf6f37 / 5b45c57 / 38f0b1d / 6d904fc，共 10 commit）
> **方法**：附录 A 协议——.133 原始日志 12 轮目录独立重算 + diff 逐行审计 + 代码锚点抽查 + **验收窗独立复跑全量门禁**（.131 经 bundle 同步至 6d904fc）

## 一、总裁决：**VERIFIED（主判据三态独立复算成立；附 4 条登记，均不阻塞）**

trae 总结所列数据**无一虚构**：12 轮墙钟逐格重算全中、门禁读数逐字吻合、三项主动申报（commit 粒度偏差/仪器缺陷/未越界）全部查实。包B/包C/包E 零触碰确认（git 无相关变更）。

## 二、包D 核验 ✅

- fmt 收编 e78f27b：+33/−16 纯重排 ✓；
- **双机引擎身份实测通过**：.131 `bash -lc 'hearth --version'` = 0.2.25 (ebd2f52)，`/usr/local/bin/hearth` md5 9ec07d9d，alias :127 已改指 codex-r6（本窗实测）；.133 同款修复（trae 增量动作，偏差如实申报，操作合理——跑测机身份健康是 R7 委托书沿用条款）；
- 清扫：`docs/data/r7-20260907/` 下 30 脚本 + quarantine 76 文件 + tar = **107 个入库文件精确对账**，根目录 `_r7_*`/`_QUARANTINE` 归零，零删除（33→30 差额如实报，3 个无从追溯合理）；gitignore 吞 21 个 log 已 force-add 补齐 ✓。

## 三、包A 代码审计 ✅

| commit | 范围 | 审计结论 |
|---|---|---|
| 9627c06（A-1） | 仅 agent-core/loop.rs +291/−3 | 授权边界内；`is_empty_content_turn` :2926（**精确匹配**填充文本，不误伤实质正文）、计步豁免 `counted_step` :5872、streak≥2 强制终止——三段式与委托书判据逐条对应；4 测试 |
| caf6f37（A-2） | 仅 loop.rs +244/−7 | 三杠杆锚点全验：`a_arm_act_tally` :4268（[convergence] :4327）、R5-3 Rewire（≥2 → [strategy-forced]）、[budget-warn]/[budget-critical] :5553/:5570 双闸注入模型历史；2 测试 |
| 5b45c57 | 5 文件纯格式 | 零语义 ✓ |

- **sandbox 零触碰**：三个代码 commit 的 diff 中 sandbox/ 文件数 = 0（grep 实证）——FZ-RFC-1 合规。
- **A-2 两项"施工中发现"查实为真**：①R5-3 与无进展计数器在 A 臂确为死线（R6-9 撤 Reflect 后 do_act 直回 Plan，计数器生产路径恒零）——这是本轮最有价值的发现：**R5-3 的"智能性修复"自落地起就没在生产路径跑过**；②50% 预算预警确为只投影给用户、模型零感知（A4 长跑病理根因闭环）。
- commit 粒度偏差（三杠杆一 commit）：接受——物理耦合于同一 `a_arm_act_tally`，拆分制造不可编译中间态；message 内 [A-2-1/2/3] 逐项标注，实质可审计。
- 登记：A-1 战报引用的 d64524f 为悬空对象（reset 重做并入 caf6f37）——簿记瑕疵，无实质影响。

## 四、A-3 数据独立重算 ✅（12 轮逐格全中）

| 轮次 | B 臂 | A 臂 | 构建 |
|---|---|---|---|
| R7-1（基线） | 1540s | 713s | ebd2f52 / 9ec07d9d |
| pre1 / pre2 | 2865 / 1856 | 2155 / **1463** | ebd2f52 / 9ec07d9d |
| post1 / post2 / post3 | 1709 / 2093 / 1561 | **386 / 370 / 784** | 5b45c57 / 5c7aef03 |
| **中位数** | 1856 → 1709（−7.9%，对照波动内） | **1463 → 386 = −73.6%**（门槛 731s） | 同机同协议 |

- **判据 1（墙钟 ≥50%，3 轮中位数）**：−73.6% ✅——独立重算与战报逐格一致；
- **判据 2（失败数不升）**：改前 pre2-A C-retest-fail Task failed（终端实证）→ 改后 3 轮盲测 0 失败；C 语料 A 臂判负 1→1 持平（postc-A retest-fail 900s deadline，工具层病理，如实归因）✅；
- **判据 3（质量不回退）**：postc-A C 语料 13 YES（C-done-usability 实测"按 BUGS.md 规则不能宣称能用"——规则锚定高质量回答；C-fix-status 901s→6s 预算双闸直接生效）；G-A 严口径 post 三轮 ≥3 ✅；post1A A3 从"3 连 no-match"变"Root cause 语义化解读" ✓；
- **A-1 判据**：232 条 .terminal "No response requested." = **0 命中**（grep 实测）；标注形式 0 次（病理未复发，机制在位——诚实表述）✅；
- 构建身份：pre/post 分组内 md5 一致、R7-3 修复后版本串正确跟踪 HEAD（5b45c57）——混杂因子未进门 ✅。

## 五、门禁独立复跑 ✅（验收窗 .131 @ 6d904fc，bundle 同步）

> **FMT_RC=0（真绿，RC 直取无管道）/ CLIPPY_RC=0 / TEST_RC=0 / 537 passed / 0 failed / 61 套件**

537 = 531（R7 基线）+ 6（A-1 四 + A-2 二），与 trae 读数逐字一致。**口径分树更新：537 = R8 门禁**（接 486/493/500/524/531 序列，禁混标）。

## 六、仪器缺陷确认（trae 顺手抓出，两项均实证成立）

1. **FMT 假绿**：`cargo fmt --check | head -5; echo RC=$?` 管道吃掉退出码 → **R7 门禁 FMT 绿为假绿**（5 文件存量 diff 被掩盖，5b45c57 收编）。本轮已修（RC 直取）——本窗 537 复跑即真绿口径。"审计工具须被审计"条款的又一次实证。
2. **pull 假更新**：中文 locale 下 dirty-file 拒绝的末行恰是误导性目标行，`tail -1` 截掉错误行——已修（PIPESTATUS + log 复核）。

## 七、登记项（4 条，不阻塞，砺·包C 判分时收口）

1. **B 臂 C 语料判负计数 ±1**：战报称 R7-1B 7/14 → postcB 4/14；本窗实测 R7-1B NO=6（R7-1 战报 §2.1 口径）、postcB Task failed=5（4 deadline + C-status-brief status=failed，终端实证）。方向（改善）不受影响、B 臂为未改对照不影响判据，但**两份战报判负口径不一致**——以砺·包C 独立判分为准收口。
2. **summary.csv 的 exit_code 列是弱仪器**：deadline/Task failed 在 csv 全记 0（12 轮实证），失败计数须以终端终态标记为准（砺 strict marker 体系已有）。
3. 偏差申报 #1 中"R7-1 基线轮同为 29 条"**笔误**：R7-1 实为 20 行（15 盲测 + 2 污染 + 3 旧 C）；pre/post 为 29 条。**不影响判据**——主判据按盲测 15 条子集统计，本窗已独立重算可比。
4. calls 未重录（偏差 #4）：接受——结构性判据已在 R6/R7 两轮实证；.log 数据完整保留，需要时按工具调用行数补提取。

## 八、呈顶层决策项

1. **包A 数据入账**：R7-5 探索校准三态判据全绿（独立复算确认），A 臂"快且不作死"从单轮观察升级为 3 轮中位数事实。**闸 1（干净对照支持 A 臂）数据侧就此齐备**。
2. **三重闸门只剩两件**：闸 2 = 包B 四组对照（独立窗，含裸跑基线——建议尽快派发，这是翻默认与线C 的最后数据件）；闸 3 = 用户体感（0.2.25 alias 双机已就绪，**建议用户本轮直接体感实测**）。
3. **包C 启动**：判分材料已就绪（r8-all.tar.gz，12 轮 + 判读证据包），移交砺按脱敏协议判分；**秒答条（post 轮多条 2-6s）为重点复核对象**（trae 偏差 #3 自曝，本窗认同该风险）。
4. R7-4 翻默认议题维持封存（防墙头草条款），解封 = 包B 数据齐后的顶层裁决。
5. quarantine 目录（76 文件已入 `docs/data/r7-20260907/`）归属与最终处置请顶层裁。

## 九、一句话

> 包A 把探索校准做实了：A 臂墙钟中位数 −73.6%、零质量回退、门禁 537 全绿独立复跑——闸 1 数据侧收官。剩下"底座对照"和"你的体感"两件，数据齐了就开闸手术。

---
*验收脚本留档：.workbuddy/verify_r8_p1/p2/p3/gate_*.py；原始证据：.133 12 轮目录 + .131 gate-r8-accept.log @ 6d904fc + r8-all.tar.gz（md5 7d4453b8）*
