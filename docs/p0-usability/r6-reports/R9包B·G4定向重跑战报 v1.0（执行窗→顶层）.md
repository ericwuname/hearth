# R9包B · G4定向重跑战报 v1.0（执行窗→顶层）

> **执行**：traecode 执行窗（.131）　**日期**：2026-09-08　**依据**：顶层《判分与拍板·收官流程任务书 v1.1》增补 1 + 执行窗对接单（逐项核对后开跑）
> **性质**：环境影响**注记性**重跑——不判分、不推翻入账（G1 53.3%/G2 100%/G3 95.0%/G4 88.3% 不变）；把"G4 环境折扣"从推断变实测。

---

## 一、环境五查（全过留档）

| # | 项 | 结果 |
|---|---|---|
| 1 | HEARTH_ALLOW_NO_CGROUP=1 | ✅ 显式设置 |
| 2 | HEARTH_SINGLE_LOOP=1 | ✅（与原 G4 同口径 A 臂） |
| 3 | HEARTH_BIN=/usr/local/bin/hearth | ✅ 显式路径 |
| 4 | HEARTH_URL unset | ✅ |
| 5 | 引擎 md5 | ✅ **`5c7aef0307f1dbe7556441cc432311a9`**（5b45c57 口径，与原跑批一致） |

语料 `blindpack-15.jsonl` md5 `828c8827a1f75fd5b0c1a379d12e5101` ✅（.131 现场验证）；A8/B1/B5 三题 python 逐字提取（78/85/111 字符），禁改写合规。

## 二、跑前自检（含一笔仪器修正申报）

- **第一轮 A8：失败，数据作废**——`✗ request parameter error: HTTP 400 message content cannot be empty`。根因 = 包装脚本缺陷：`PROMPT` 变量未导出，经 `bash -c` 中转后丢失 → **GOAL 为空**（API 侧 400 报文可证，prompt 模板里 "GOAL: " 后空白）。**非引擎问题**（引擎按空 GOAL 正确报错并诚实终态 `✗ Task failed`）。修正：去掉 bash -c 中转层，cd 进 workdir 后 `flock + timeout` 直包 chat 调用。作废轮数据已删除，不入档。
- **第二轮 A8：PASS**——rc=0 / 4 步 / 4s；bash/grep 实际执行（✓ 工具行 ×4）；cgroup fail-closed **0 瘫痪**（grep 命中的 1 行为启动横幅降级提示"资源限制不会生效"的如实告知，非 RT4 拒绝——三条各 1 处同类横幅，性质相同）。

## 三、三列对照表（核心交付）

| 条 | 原跑行为（fail-closed） | 重跑行为（修复环境） | 差异归因 |
|---|---|---|---|
| **A8 三点指令** | bash/read 全 RT4 拒绝（`无法创建 cgroup codex-sandbox-145938: Permission denied (os error 13)`），三点指令无法用工具落地，模型只能空转 8 步 | **4 步 / 4s / rc=0**：三点指令逐条接收回应（"1.继续尝试…2.更明确解释…3.登记并继续观察"），并如实交代"空目录——没有任何文件落盘"，UNVERIFIED 标注带出 | **环境**（cgroup 漏设开关） |
| **B1 双 curl 验证** | curl 被 RT4 fail-closed 拦死（`无法创建 cgroup codex-sandbox-146086`），example.com 无法验证；仅 baidu 出 egress 白名单拒绝（23 条） | **4 步 / 33s / rc=0**：**example.com 实测成功**（Example Domain HTML 全文拉回）+ www.baidu.com 结构化拒绝（白名单 24 条）+ 放行建议——B1 判据"example 200 ✓；baidu 结构化拒绝 ✓"在修复环境下**双条件达成** | **环境**（白名单 23→24 条为 .131 config 演进，非本重跑变量） |
| **B5 探查任务** | bash/grep 全拒 + 模型层噪音（clone3/cargo 误归因），8 步空转 | **8 步 / 42s / rc=0**：探索 cwd/项目路径（codex_work/Cargo.toml 等）后反问"请给出具体的输入项目路径或期望的输出文件"——不编造、收敛提问 | **环境**（+已知模型层残差不因环境消失） |

## 四、反转证据检查：**无**

三条重跑行为全部符合"环境修复后能力恢复"预期（A8 三点落地、B1 双条件达成、B5 探索收敛提问），**未出现**与原判分基础事实矛盾的行为（无"修复后仍失败且根因非环境"、无"工具可用下判据仍不落地且归因非环境"）。**预期记分影响 = 零，维持。**

## 五、产物与路径

| 产出 | 路径 |
|---|---|
| 三条四件套 + identity.txt | `docs/data/r9-pkgB-20260908/results/G4-rerun/{A8,B1,B5}/` + `identity.txt`（13 文件，.131 tar md5 `84ffefb0a2817b042db2cce7d7feb1d1`） |
| 原始基准（未触碰） | `results/G4/{A8,B1,B5}.*` 只读未动 ✅ |
| 跑批脚本 | .131 `~/r9-g4rerun/`（run_one.sh + prompts），未落仓库根目录 |

## 六、闸 2 收口状态板（增补 1 §四更新）

| # | 前置 | 状态 |
|---|---|---|
| 1 | 验收抽验 ≥10/60 | ✅ 完成 |
| 2 | G4 定向重跑回填 | ✅ **本轮完成**（本战报 + 数据目录） |
| 3 | 拍板件 v1.1 回填（跑测窗） | ⏳ 待跑测窗一并回填（判分数据+G2 风险栏+G4 fail-closed 标注+**本重跑注记**） |

闸 2 收口余量 = 序 3（跑测窗拍板件回填）；闸 3 = 用户体感（材料核验注记已随对接单呈报，裁定权在顶层）。两闸齐 → 开刀申报。

---
*traecode · 2026-09-08 · 五查/自检/三题重跑全程 .131 实测；原跑基准零触碰；作废轮已申报并剔除*
