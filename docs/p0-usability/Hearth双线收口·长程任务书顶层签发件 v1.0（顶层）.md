# Hearth 双线收口 · 长程任务书顶层签发件 v1.0（顶层）

> **签发**：顶层　**日期**：2026-09-07
> **对象**：执行窗拟稿《Hearth双线收口·长程任务书 v1.0》（commit 1be195e）——**四签落笔后即刻生效**，拟稿全文条款照常执行，本签发件只载批复与顶层增补。
> **输入**：《R7首轮三件·执行窗验收报告》（5cd63e9，VERIFIED）+ 拟稿全文 + 外部 v3.0 合并版 §〇·六 增补（工作树实查）。
> **锚点自证**：commit 链 7e807a3→0600206→f09d3d8(R7-2)→3a9cd77(R7-3)→ebd2f52(fixture)→5a4f2e7→025e102(R7-1 数据)→5cd63e9(验收) 逐一对上，`git log` 实测；`_r7_*.sh` 33 个脚本 `git ls-files` = 0（未入库，仅工作树散落）。

---

## 一、验收报告五项决策批复

| # | 事项 | 顶层批复 |
|---|---|---|
| 1 | R7-4 裁决 | **批准"翻默认暂缓维持"**——引用 R7-1 数据编号：盲测 calls +7.6%、C 语料 −40.7%，均未达 50%（旧口径）；且首轮"多步题 A 臂全面占优"经干净对照证实部分系 cwd 污染夸大。手册裁决 3 关闭。A 臂优势（耗时 −53.7%/−75.5%、C 语料 6 败→0、零 Task failed）如实入账，转 R7-5/R7-6 消化。**翻默认议题在包A+包B 数据齐前不再重议**（防墙头草条款）。 |
| 2 | calls 判据修订 | **批准（见签 1 条件）**——单轮 calls 波动实证（A4：2↔20）使 50% calls 判据丧失终审效力，墙钟中位数是更稳的端到端读数。 |
| 3 | R7-5 立项 | **批准**——按拟稿包A 施工，含 "No response requested." 裸透传修复（根因=模型自吐填充文本、crates/ 零命中，验收窗已查实）。 |
| 4 | 线 B 启动令 | **批准**——前置阻塞已清零，"E-B 前不删 B 臂"铁闸维持。窗口归属见签 4。 |
| 5 | fmt 两文件 | **裁：收编**——单独 commit `style: cargo fmt` 收编，不还原（还原徒增 churn）。 |

## 二、拟稿四签（顶层落笔）

| # | 事项 | 签署 |
|---|---|---|
| **签 1** | calls 判据修订案 | ☑ **批准，附三条件**：①calls 降为**必录观察指标**（每份战报仍须报数，不得消失）；②结构性判据（A 臂每步 1 次调用 vs B 臂 ≥3 次）独立成立，不受本次修订影响；③修订仅适用 A 臂端到端验收，手册其余预注册条款零改动。**主判据自此为：墙钟耗时降 ≥50%（≥3 轮中位数）+ 失败数不升 + 质量不回退。** |
| **签 2** | R7-4 批复 | ☑ 批准"翻默认暂缓维持"（批复 #1 原文） |
| **签 3** | 包D 卫生清理授权 | ☑ 批准，范围按拟稿包D + **顶层增补第 3 条**（工作树根目录清扫） |
| **签 4** | 包B 窗口归属 | ☑ **独立新窗口**（手册原判维持）——E-B 是利益相关实验（结论决定 A 臂建设方的工作是否保留），跑测窗与被测建设方分离。**弹性条款**：若窗口资源不济，允许 traecode 兼任**跑测**，但 Q1-Q5 判分**必须外包**（砺或外部窗），包B 战报由判分窗独立复核后方可呈顶层。 |

## 三、顶层增补（拟稿之上新增三件）

### 增 1 · F-4 裸跑基线并入包B（外部 v3.0 §〇·六 提案，顶层采纳）

同一 agnes-2.5-flash、**绕开 Hearth 框架**直连 API 跑盲测包 15 条（逐字、fresh session、与包B 同协议）：

> **框架能力损耗 = 裸跑成功率 − Hearth 成功率**

- 裸跑高而 Hearth 低 → 纯框架工程问题，可施工可改善；裸跑也低 → 任务集或调用方式需重设计。
- 包B 对照表自此为**四组**：裸跑 / Codex CLI / Goose / Hearth-A 臂，同一把 Q1-Q5 尺。
- 成本一次性（15 条），与 E-B 同窗口同批执行。

### 增 2 · 外部 v3.0 合并版 §〇·六 增补——追认 + 程序提醒

内容**追认**（目标重定义与 R7 手册终审口径一致，Q1-Q5 升为交付基准正确；引用顶层 09-06 指令属实）。程序性登记一条：**定稿件增补须在下一份战报中向顶层申报**，防"定稿件静默变更"成为先例——本次内容无害，程序上记一笔。

### 增 3 · 工作树根目录清扫（并入包D）

- 根目录 33 个 `_r7_*.sh` 一次性脚本 + `_QUARANTINE_undesired_209d/`（76 文件）均**未入库**、仅散落工作树：脚本移入 `docs/data/r7-20260907/scripts/`（或删除，执行窗自选），quarantine 目录确认归属后处置。**处置前先确认零引用**（`git status` 之外不被任何 runbook 引用）。
- 防再犯：一次性脚本今后直接落 `.133`/`.131` 或 `.workbuddy/tmp/`，**不落仓库根目录**。

## 四、包C 评审归属（拟稿"砺/外部"落定）

**砺·评审主评**（脱敏判分，材料由验收窗脱敏后移交），外部窗备份复核；判分差异 >2 条逐条复议留痕（拟稿原文）。产出=G-W1 首份真尺数字入档，执行窗自判版本留档对照。

## 五、三重闸门现状（顶层登记）

| 闸 | 现状 |
|---|---|
| 闸 1 干净对照支持 A 臂 | 验收窗初判"倾向满足"——**顶层确认初判成立**（C 语料 0 败 vs 6 败、耗时双口径全降、calls 噪声已按签 1 修订口径处理），终判待包A/包B 数据齐后随闸门评估件一并裁 |
| 闸 2 E-B 决策=留自研 | 待包B（含增 1 四组对照） |
| 闸 3 用户体感 0.2.25 | 待用户实操（不阻塞包A/包B/包C 开工） |

**包E（线 C 手术）在闸门全绿前维持禁动刀**，拟稿预写细则（tag pre-linec-baseline + Desktop 备份 tar + 每项删除后冒烟红即停 + wc -l <10,500 硬线）照抄生效。

## 六、口径分树更新（禁混标，接手册 #8）

| 口径 | 含义 |
|---|---|
| 486 / 493 / 500 | 归档线历史读数（含义见总账） |
| 524 | R5 基线门禁 |
| **531** | **R7 门禁**（5cd63e9 验收窗 .131 独立复跑，含 R7-2 新测试） |
| **9ec07d9d** | **R7 同构建 md5 口径**（ebd2f52 构建，四结果目录一致） |
| 5cd63e9 / 1be195e | R7 首轮验收 / 本任务书拟稿 commit |

## 七、生效与派发顺序

1. **即刻可发**：包D（签 3 已落）→ 包A（traecode，.133）；
2. **并行启动**：包B（独立新窗口，.131，含增 1 F-4）；
3. **材料已就绪即启**：包C（砺判分，原始 tar 已入仓 `docs/data/r7-20260907/r7-all.tar.gz`）；
4. **条件触发**：包E（三闸全绿）。
5. 纪律全文沿用拟稿 §八（判读分离/并发红线/冻结区零触碰/诚实边界三条/引擎身份留档）。

## 八、一句话

> **首轮验收把"翻默认"按住了，也把 A 臂"快且不作死"坐实了。四包并行：校准探索、四组对照（含裸跑基线）、独立判分立真尺、扫地清根目录——数据到齐、三闸齐绿，然后才动那 2,000-4,000 行的手术。**

---

*顶层签发 · 2026-09-07 · 依据：5cd63e9 验收报告 + 1be195e 拟稿 + 外部 v3.0 §〇·六；commit 链与入库状态经 `git log`/`git ls-files` 实测*
