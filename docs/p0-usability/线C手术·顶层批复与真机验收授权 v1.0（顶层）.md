# 线C手术·顶层批复与真机验收授权 v1.0（顶层）

> **拟件/签署**：顶层　**日期**：2026-09-08
> **依据**：《线C手术·执行窗验收报告 v1.0》（VERIFIED，附录 A 口径）；手术战报（9fc3bd8）；施工三卡（4c055a6+9eb250d+a6ed0a9）；两签件（6abd609）
> **性质**：手术本体确认 + 呈报三件批复 + 真机验收授权。
> **顶层锚点自证（签发前独立实测）**：①wc -l loop.rs = **8,114**（R-1 口径，与验收报告一致，硬线 <10,500 达标）；②git 链 6abd609..9fc3bd8 = **15 节点账目闭合**：5279915（开工令转达）+ 93e7e57（基线清理，即 pre-linec-baseline tag 实指与双跑 diff 基线）+ 13 手术施工节点（434efd8→9fc3bd8）——与验收报告 §2.1 申报自洽；③`a_arm_act_tally` grep 实测 **9 处原位未动**（R8 −73.6% 战果资产完好）；④ReflectVerdict/decompose 残留实测仅为手术痕迹注释 + prompt 文案（loop.rs :2840/:3044/:4980/:5672/:7843/:2031/:2429/:2464-2468），零生产代码残留；⑤0.2.26 bump 在位（根 Cargo.toml :34）。
> **行数诚实条款结算**：终值 8,114 < 10,500 硬线，评审带 (10,500, 11,043] 未触发，">11,043 禁自宣"条款履行完毕。实测低于预研预判（≈8,970）约 856 行——多删合规性不以"删得多"自证，而以四证自证：十项清单落刀逐项对账 ✓ / 残留 grep 零 ✓ / 测试双跑零新增回归 ✓ / R8 资产原位 ✓。条款归档。

---

## 一、手术本体确认

**确认执行窗验收结论：线C手术 VERIFIED。** 十项 D 项（D-9 收窄版）落刀、commit 链 13 节点合规、wc 轨迹 13,055→8,114（−37.9%）、零生产残留、R8 资产完好、四 crate 测试双跑 diff 零新增回归（HEAD 失败集 ⊆ 基线失败集 ∪ 已定性闪烁，r22 基线挂→HEAD 过=连带改善）。验收 7 条 ①②⑥⑦ 四条顶层确认通过；③④⑤ 真机域移交跑测窗（本件 §三授权）。

事故披露认可：D-1 红 gate（cf1e88c）→ 283cec3 自愈回升 12,296（+156 实证修复真实性）；gcc 丢 PATH 工具链事故——**均已在战报申报，无隐瞒，符合诚实披露纪律**。D-3+D-4 合并落刀（单独落 D-3 会激活 B 臂空图短路）裁定为合规工程判断。

## 二、呈报三件批复（自战报转达，逐件落笔）

### 批 1：R6-8 载荷缩编确认——**追认**

TurnCheckpoint→turns+taskgoal 缩编即 C-3"checkpoint 机制不死、载荷缩编"条款（5e3d61f）的落地实现；测试双跑零新增回归实证行为保持，a_arm_act_tally 路径未受牵连。**追认生效**。后续如需扩载字段（如经验注入预留），一律走 D2 小包申报，禁手术外零散扩。

### 批 2：xray 锁裁定——**随功能删除追认，附重建条件**

xray 锁定断言锁的是被删 B 臂机械路径——**测试随功能走**（线 5 规则），且已在对账科目显式申报（25 红对账之 xray+1），非静默删测掩盖。**附条件条款**：若 xray 断言中含 A 臂现役行为锁定成分，随 D2 第一批补测重建（与批 3 同包）；重建时断言只许锁现役路径，禁止复活已删门。

### 批 3：service 集成覆盖重建——**批准入 D2，排第一批**

394fd7b 删 5 个 service 集成测试，A 臂现役路径集成覆盖存在缺口。裁定：**D2 小包第一批 = service 集成覆盖重建**（针对 A 臂现役路径写新集成测试，先红后绿留证据），优先级高于 D2 历史 40 条一般项。时点：0.2.26 真机三项全过后、归档仪式前完成（归档不等它的话须顶层另批）。

## 三、真机验收授权（③④⑤ → 跑测窗）

1. **部署 0.2.26 至 .131**：构建 md5 双验登记（沿用引擎身份纪律：which+md5 必验）。
2. **③ 盲测**：C YES≥13 / G-A≥3；**C-2 回滚触发器挂载验证**（668d4a8 落刀点实证）——C-control 类语料单条误判即回滚，触发器必须实弹验证而非纸面确认。
3. **④ 墙钟**：双臂 ≥3 轮中位数降 ≥50%（主判据）+ 失败数不升 + 质量不回退；calls=必录观察指标。**A 臂 −73.6% 为 0.2.25 旧值不可复用**，一切以 0.2.26 新跑为准。
4. **⑤ SINGLE_LOOP=0 回退实测**（`HEARTH_ALLOW_NO_CGROUP=1` 口径——**闸 3 与 G4 事故前车之鉴：跑前必查降级开关设置**）。
5. 三项全过 → 归档仪式两签（tag v0.2.23-final 已裁 + 双 tar）→ **GitHub 首次正式同步**（remote → 安全清理审查（.workbuddy 密钥/VM 凭据/数据包先移出）→ force push 弃旧镜像）。

## 四、登记事项认可

1. **计器 +1 口径**：trae 申报恒偏 +1 同向（13,056/13,055、8,115/8,114）——R-1 以 wc -l 实测为准，登记备查。
2. **版本注释瑕疵**：0.2.26 行未追加本版注释（仍挂 0.2.25 描述）——文档性遗漏，**随归档仪式补齐**，不阻塞真机验收。
3. **checkout 事故教训入库（采纳为窗口纪律）**：共享主 repo 上 checkout 往返后必须立即 `git status` 全量复核工作树完整性；后续窗口避免在共享主 repo 做 checkout 往返，**一律改用 git worktree**（与既有多窗口 worktree 纪律合流）。
4. **测试对账闭合**：删 64 增 5 净删 54，与四 crate 实测分毫不差——"测试随被删功能走，无静默删测掩盖"纪律经受了实测检验。

## 五、签署位

| # | 事项 | 签署 |
|---|---|---|
| 1 | 手术本体 VERIFIED 确认 + 验收 7 条 ①②⑥⑦ 通过 | ✅ 顶层已签 |
| 2 | 批 1 R6-8 载荷缩编追认 | ✅ 顶层已签 |
| 3 | 批 2 xray 锁随功能删除追认（附 D2 重建条件） | ✅ 顶层已签 |
| 4 | 批 3 service 集成重建入 D2 第一批 | ✅ 顶层已签 |
| 5 | 真机验收授权（③④⑤ → 跑测窗，0.2.26 新跑口径） | ✅ 顶层已签 |

---

*顶层签发 · 2026-09-08 · 手术刀已落地，真机三验收见分晓；全过即归档，归档即 GitHub 首次同步*
