# 外部窗口「Hearth 可用性根因分析」验证报告（呈顶层）

> **受验件**：`docs/Hearth可用性根因分析-对照WorkBuddy（外部窗口）.md`（五轮分析、十三条根因、修复优先级表）
> **验证者**：执行窗｜**验证方式**：砺铁律——不信报告信源码，全部锚点重新 grep 实测
> **日期**：2026-09-06

---

## 一、总体判定：**分析成立，质量高，建议顶层采纳其修复路线**

**抽验 10/10 锚点全部命中**（源码级实测，非转述采信）：

| # | 外部结论 | 我的实测 | 判定 |
|---|---|---|---|
| 根因一 | 规划=独立 LLM 调用（loop.rs:3359） | `:3359 provider.chat` 在位 | ✅ |
| 根因二 | system prompt 零环境注入 | grep cwd/环境信息=0 命中 | ✅ |
| 根因三 | 死流程 prompt + 作者自认注释 | `:2327` 注释原文在、`ONLY uses tool_calls`/`follow strictly` 原文在 | ✅ |
| 根因四 | read 工具描述 5 词 | `read.rs:28` = "Read the contents of a file" | ✅ |
| 根因八 | truncate 6000/4000/1500 + read 无 offset/limit | `:5659-5660` 在；read.rs offset/limit grep=**0** | ✅ |
| 根因九 | verification 只看命令名不看退出码 | `:2716 is_verification_command(c)`——R4.1 收紧了分类（bash -c 解包+cd）**但确实仍不看退出码** | ✅（部分收窄） |
| 根因十一 | 主循环 temperature 0.5 / goal-drift 0.0 | `:3362=0.5`、`:1381=0.0`——**同仓两处，主循环用高温** | ✅ |
| 根因十三 | observe_verdict 消费端仅测试、不进 LLM 上下文 | 全仓 grep：生产端 loop.rs，消费端仅测试 | ✅ |
| §九 | archive 只写不读 | `context.rs:366` 写入；读回仅测试（:841） | ✅ |
| 根因七 | MAX_HISTORY_MSGS 40 | `:2481`（外部标 :2464，行号漂移 +17，内容一致） | ✅ |

**方法论加分项**：外部窗口自己做了反向验证（§十：两条自我修正 + 一条推测被推翻）、前提修正（§13.0 承认 A/B 非修复对照）、**且在 §十四 主动修正了自己"Observe 0ms=空转"的过时判据**（承认 R1-3 是高质量真修复）——这种自我纠错纪律与砺同款，可信度高。

## 二、与当前修复状态的对照（哪些已修、哪些仍开放）

### 已被 R1–R3 / R4.1 覆盖的（外部部分修正其紧迫性）

| 项 | 状态 |
|---|---|
| Observe 空转（根因六生产端） | ✅ R1-3 真修复（外部 §十四 自我修正确认"质量很高"） |
| bash 非零退出码假绿 | ✅ W4/RC20 已修 |
| sticky VERIFIED | ✅ 9076f7f 已修（run 起始重置，门禁 501/0） |
| verification 分类函数过松 | 🔶 R4.1 半修（bash -c 解包+cd 入只读表）——**"不看退出码"仍开放** |

### 仍开放的高价值项（外部 P0 级，全部未经修复）

| 项 | 锚点 | 成本 | 与用户体感的关联 |
|---|---|---|---|
| **P0-0 temperature 分档**（Act 0.0-0.2 / Plan 0.2-0.3） | `:3362` 两行 | **极低** | 85 次重新规划的燃料；A/B 立见 |
| **P0-1 环境上下文注入**（cwd/项目根/技术栈/文件树） | `:2337` 附近 | 极低 | "项目不存在"假完成、"搜 .go 文件"瞎猜的直接解 |
| **P0-2 prompt 死流程→原则** | `:2334-2383` | 低 | 解除假完成的机制压力（MUST write_file） |
| **P0-4 verification 看退出码** | `:910/:2716` | 极低 | 失败命令拿 VERIFIED 的合法性来源 |
| **P0-5 read 加 offset/limit+行号** | `read.rs` | 低 | 82k 字符截断（引擎亲口承认）的死结 |
| **根因十三：事实注入决策**（observe_verdict/失败策略→LLM 上下文） | `build_messages()` 附近有注入先例 | 低 | **同错重复 3 次的机制级解**（"失败不教学"） |
| P1-1 工具描述重写 | tools-builtin | 低 | 弱模型表现的直接杠杆 |
| P1-3 错误回喂三段式 | `:2684` | 低 | 同上 |

## 三、外部分析与 R4 跑测证据的交叉印证

**外部从静态代码推出的根因，与 R4 动态跑测的病理一一对应**：

| 外部根因（静态） | R4 跑测病理（动态） | 互证 |
|---|---|---|
| 根因五/九（假完成机制） | A 段 49 个未验证终态、B 段鼠标 bug 14 轮未修好 | ✅ |
| 根因十三（失败不教学） | C 语料同错重复 3 次、回执含答案仍一字不改 | ✅ |
| 根因二（环境盲） | G-A"项目不存在"假完成、glob no matches 后无自救 | ✅ |
| 根因一（Plan 空转） | C 语料"一句问答烧 5 相位、工具 41ms vs LLM 26286ms" | ✅ |

**两条证据链独立得出同一结论——根因分析的可信度因此加强。**

## 四、给顶层的建议

1. **采纳外部修复路线**，但按当前语境重排：**宣告路径 (b)（引擎行为层达标）已经由 R4 证据支撑**；用户体感"不好用"的瓶颈在外部指出的**智能性层**（temperature/环境注入/失败教学/信息完备性），**与已宣告的诚实性层是两个独立维度**——修这批不推翻 (b)。
2. **第一批施工建议（成本极低、可单次判负验收）**：**P0-0（temperature 两行）+ P0-1（环境注入）+ P0-4（退出码）+ 根因十三 P0-A（失败策略注入）**——四条全部是低风险文本/单行改动，且每条都有外部预注册的可证伪判据（重跑 C 语料看规划次数/同错重复次数/首轮是否还搜 *.go）。
3. **验收方式照搬外部建议**：用用户原话场景复测（"我们的 hearth TUI 项目做的进度如何了"——首轮不得搜 `*.go`），**单次可判负**。
4. 外部窗口的诚实边界声明（§六/§十四）已自我标注局限（未跑真机、未实测温度），**其判断与我方动态跑测互证后局限已被覆盖大半**。

## 五、一句话给顶层

> 外部窗口用五轮静态分析把"为什么不好用"拆到了源码行号级，我方动态跑测从用户侧独立印证了同一批根因；**R1–R4 修好了"诚实性"（不说谎不失忆），用户体感差的剩余瓶颈在"智能性"（temperature/环境盲/失败不教学/信息残缺）——外部给出的 P0 清单成本极低且每条带可证伪判据，建议批准第一批施工（P0-0/1/4 + 根因十三注入），用用户原话场景做单次判负验收。**
