# R9真机三项·判分战报 v1.0（执行窗→顶层）

- **出品**：执行窗/验收窗　**日期**：2026-09-09 18:30
- **判据**：顶层预钉口径（c070411）：④双构建对照各 3 轮中位 ≥50%（blind/corpus 分表）；判分纪律四条（v1 残数据排除/12 组对账/CPU 竞争登记/双 md5 分机禁混标）
- **原始数据**：`docs/data/r9-real-20260909/`（12 组日志束+results 全量，已入库）
- **引擎身份**：0.2.26=e23bbb49（.133）/ 0.2.25-base 93e7e57=93e505c3；语料=blindpack-15+corpus-c（tui-sample fixture）

---

## 一、12 组矩阵对账

| 构建 | blind | corpus | 对账 |
|---|---|---|---|
| 0.2.26 | r1（all 模式前 15 条）+ r2 + r3 | r1（all 后 14 条）+ r2 + r3 | 6 组全 ✓（r3-corpus 含 1 条外部事故挂死，见 §五） |
| 0.2.25-base | r1 + r2 + r3（各 15 条） | r1 + r2 + r3（各 14 条） | 6 组全 ✓ |

v1 半截残数据（20260908T162948Z 之前的目录）不存在；165317Z 半截（5/14 条）按预钉口径作废不入样本。

## 二、判据逐项判定

### ③ 盲测质量

**G-A（A 组 10 条，terminal 终态口径†）：**

| 轮 | 0.2.26 | 0.2.25-base |
|---|---|---|
| r1 | 7/10 | 8/10 |
| r2 | 9/10 | 8/10 |
| r3 | **10/10** | 4/10 |
| **中位** | **9** | **8** |

**判定：✅ 大幅过（≥3）**。26-r3 全满（A1/A10 历史残差题在 r3 双双 ✓）。

**C YES（语料 14 条）：**

| 轮 | 0.2.26 | 0.2.25-base |
|---|---|---|
| r1 | **14/14** | 11/14 |
| r2 | 10/14 | 8/14 |
| r3 | 12/14（剔挂死条后同值） | 8/14 |
| **中位** | **12** | **8** |

**判定：✗ 差 1（≥13 未达）**——但质量侧全面占优（8→12，+4 条）。r1 曾 14/14 满分，r2 的 4 条 fail 全部是 900s 预算超时条。

**C-2 回滚触发器**：跑批全程 rollback/回滚 **零触发**（无误触发=符合预期）；"实弹构造验证"未在跑批中自然发生，**待补专项**（15 分钟量级）。

### ④ 墙钟双构建中位（剔除事故条后）

| 语料 | 0.2.26 三轮 | 中位 | 0.2.25 三轮 | 中位 | 降幅 | 判定 |
|---|---|---|---|---|---|---|
| blind | 1054 / 2032 / 318 | **1054s** | 1968 / 1075 / 2298 | **1968s** | **46.4%** | ✗ 差 3.6pp |
| corpus | 355 / 5191 / 2454‡ | **2454s** | 3264 / 3759 / 4274 | **3759s** | **34.7%** | ✗ |

**判定：✗ 未达 ≥50%**。两处均未达标线，blind 贴线（46.4%）。

### ⑤ SINGLE_LOOP=0

全量日志 grep `single_loop` **零命中**——✅（且 0.2.26 已无 B 臂回退路径，机制性达成）。

### 质量不回退 + 失败数不升（判据组合附带项）

- 失败数：blind 26 侧 3/1/0 vs 25 侧 3/3/6；corpus 26 侧 0/4/2 vs 25 侧 3/6/6——**大幅下降 ✅**
- 质量：G-A 中位 8→9、C YES 中位 8→12——**全面占优 ✅**

## 三、敏感性说明（预钉纪律要求）

1. **轮间方差巨大**：26 blind 三轮 318~2032s（6 倍差）、26 corpus C YES 14/10/12。3 轮中位数在小样本+高方差下不稳健，46.4% 与 12/13 都在判据线附近抖动区间内；
2. **900s 超时机制两侧同在**（26 corpus r2 有 4 条、25 侧 r1-r3 均有多条）——属预算耗尽的既有机制，计入性能不剔除；
3. **r1 口径例外**：26-r1 为 all 模式（runner 缺省）一次连跑 29 条，与 r2/r3 的分表调用在数据上等价（fresh session 无共享状态），已按条目拆分归组。

## 四、膨胀病定量实证（本战报新增发现）

| 账单口径 | 0.2.26 | 0.2.25-base |
|---|---|---|
| blind 3 轮条均累计 prompt tokens | **48,409** | 34,433 |
| 单次调用基底（sum tokens / sum calls） | **9,725** | 7,866（**+23.6%**） |
| 单条最大 | 451,914 | 210,069 |

**手术删了 5,000 行编排，但单条注意力税不降反升 23.6%**——坐实"膨胀病主犯是常驻治理注入（宪法/talent/账本投影），不是相位机"。**prompt 瘦身的优先级由本数据钉死：新路线第一批。**

## 五、事故登记

- **C-fix-status 挂死**（26-r3-corpus）：模型跑 cargo check → cargo 等全局锁 5.5h（他窗 cargo test 持锁）→ hearth 工具无超时 → 会话无限挂起。人工杀链+watchdog 部署。该条 elapsed（21029s）**已剔除**（外部事故，非 hearth 性能表现；独立证据链：watchdog 日志+kill 记录）。**该条在两侧同题均 fail**（25-r3 也挂 C-fix-status），题本身的环境依赖问题另立问题卡。
- 剔除后 corpus r3=2454s；若不剔除 r3=23483s，26 corpus 中位将升至 5191s（降幅 -38%）——两种口径下 ④ 均未达标，结论不变。

## 六、呈顶层裁决

**字面结果：三项未全过**——⑤✅、③ G-A✅/C YES 差 1/C-2 待补实弹、④ 两项未达（46.4%/34.7%）。

**执行窗如实呈报 + 供裁决的两条路**：

- **路线 A（按字面）**：④ 未达 → 真机三项不通过 → 不归档，继续修 hearth 至达标。风险：墙钟未达的主因（膨胀病）的修复=新路线的 prompt 瘦身——旧路线上继续修，与已拍板的新路线重叠冲突。
- **路线 B（有条件收口，执行窗建议）**：质量侧全面占优（G-A 8→9、C YES 8→12、失败数大降）+ ⑤ 达成 + 墙钟未达差值已定量归因于膨胀病（§四）且其修复（prompt 瘦身）本就是新路线第一批任务 → 旧战役**有条件收口**（墙钟达标义务转入新路线第一批验收：瘦身后 L1-01 同题重考+本语料复测须兑现 ≥50%），归档仪式照走，EMBER 火种即刻点火。
- 无论 A/B：C-2 实弹构造验证（15 分钟）建议先行补做，不阻塞裁决。

## 七、执行窗附注

† terminal 终态口径（✓/completed=过；Task failed/budget=不过）为宽口径初判；G-A 严口径（首屏直接结论句）的终判需逐条 log 抽验，**若顶层要求严口径终判，执行窗补做**（预计 1 小时）。原始束全量在库，任何数字可独立重算。
