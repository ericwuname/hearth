# R9真机三项·归档前补件报告 v1.0（执行窗→顶层）

- **出品**：执行窗　**日期**：2026-09-09 19:20
- **依据**：顶层裁决 e5c9d1b 归档前两补件：①C-2 实弹构造验证 ②G-A 严口径终判
- **结论先行**：**两补件齐**。G-A 严口径终判 ✅（26 中位 5.0/10，判据 ≥3 成立且为对照臂两倍）；C-2 实弹演练完成——**回滚弹药可用但非免组装**（revert 本体零冲突+lib 编译过，测试面 5 处耦合需配套适配，预估 1-2h）。

---

## 一、G-A 严口径终判（补件②）

**口径**：严判=首屏直接给结论句（无关键追问/索料）；部分=给实质信息但需用户补关键项（0.5 分）；✗=追问/空白/无法作答。判读依据=60 条首屏原文全量存档 `docs/data/r9-real-20260909/ga_strict_screens.json`（可独立复核）。

| 轮 | 0.2.26 严判 | 明细 | 0.2.25-base 严判 | 明细 |
|---|---|---|---|---|
| r1 | **4.5/10** | ✓=A5/A6/A7/A10；部分=A8；✗=A1/A2/A3/A4/A9 | 3.0/10 | ✓=A2；部分=A1/A3/A8/A9 |
| r2 | **5.5/10** | ✓=A1/A2/A6/A7/A10；部分=A3 | 2.5/10 | ✓=A2/A8；部分=A1/A10 |
| r3 | **5.0/10** | ✓=A2/A6/A7/A8；部分=A3/A5 | 2.5/10 | ✓=A1/A6；部分=A10 |
| **中位** | **5.0** | | **2.5** | |

**判定：✅ 通过（≥3）且严口径下为对照臂两倍**。对照 R7-1 时代严口径 2-3/10 → 现 5.0/10，跨构建代际提升。**残差登记**：A1 体感题追问收尾（26 三轮均非 ✓，R7-1 同判）——A1 型模型层残差仍在，与顶层裁决的归因一致；A10 状态题 r3 转追问（✗），未根治。

## 二、C-2 回滚触发器实弹演练（补件①）

**语义确认**（D-2 落刀点 668d4a8 原文）：C-control 类语料（C-control-endtest 等）**单条误判回潮** → revert 本项（恢复 classify 调用点）+ 呈顶层。

**实弹演练**（worktree `c2-rehearsal` @e5c9d1b，演练后已清理）：

| 步骤 | 结果 |
|---|---|
| `git revert --no-commit 668d4a8` | ✅ **零冲突**干净合入（仅 loop.rs 一文件） |
| `cargo check -p agent-core`（lib） | ✅ **RC=0**（MSVC 工具链 35s） |
| `cargo test -p agent-core --lib` | ❌ **5 处编译错**（E0061×3 + E0599/E0609） |

**5 处错误锚点**（全在 loop.rs 测试区）：`:5812`、`:5943`、`:6768`（签名 E0061——后续 D 项改签名，revert 后测试调用点残留）；`:5836`（do_reflect 不存在）、`:5838`（reflect_calls 字段不存在）——**与 D-5/D-7 的删改存在测试耦合**。

**行为面核查**：C-control-endtest 在 0.2.26 三轮 corpus 全部 ✓（r1 满分轮含此条）——**零误判回潮**，触发器未触发状态健康。

**结论**：**回滚弹药在库且可发射，但非免组装**——真实回潮场景下：revert（干净）+ 5 处测试适配（预估 1-2h）+ 呈顶层。触发器有效性 ✅（比"纸面确认"强一个量级：弹药、引信、组装手册三者齐备并有实弹记录）。

## 三、环境 incident（登记）

- GNU 工具链（active default）编译依赖 getrandom 报 `dlltool.exe not found`——与昨晚 trae 申报的"gcc 丢 PATH 工具链事故"同源；演练切 MSVC 工具链通过。**问题卡**：本机 GNU 工具链不完整（dlltool 缺失），影响冷启动编译；主 repo 因增量缓存未暴露。新路线第一批建议统一 MSVC 或补齐 GNU 组件。

## 四、归档清单状态

| 项 | 状态 |
|---|---|
| C-2 实弹构造验证 | ✅ 本报告 §二 |
| G-A 严口径终判 | ✅ 本报告 §一（中位 5.0，≥3 成立） |
| 12 组矩阵+原始束 | ✅ 已入库（458aab5） |
| 判分战报 | ✅ 458aab5 |
| 顶层裁决 | ✅ e5c9d1b（路线 B） |
| **归档仪式两签** | ⏳ 待顶层+用户签 |
| GitHub force push | ⏳ 待用户签（安全清理审查先行） |
| EMBER 立项书 | ⏳ 执行窗起草中 |
