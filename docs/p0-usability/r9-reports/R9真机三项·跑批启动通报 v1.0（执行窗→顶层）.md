# R9真机三项·跑批启动通报 v1.0（执行窗→顶层）

- **出品**：执行窗/验收窗　**日期**：2026-09-09 00:40
- **依据**：顶层批复与真机验收授权（33657c6）；验收报告 105ef84
- **性质**：启动通报（跑批过夜进行中，判分战报待数据齐后另出）

---

## 一、批复件核验

33657c6 已入库核验：message 与顶层转述一致；三件批讫（R6-8 追认 / xray 锁附重建条件 / service 集成重建=D2 第一批·归档仪式前）落笔确认。

## 二、部署（0.2.26，md5 双验）

| 项 | 值 | 核验 |
|---|---|---|
| 部署目标 | .131 `/home/wutao/hearth-0.2.26/`（新增目录，git archive HEAD=33657c6 源码包） | ✓ |
| 源包 md5 | `7add4df0f49962d426fddba18fbc2795`（本机打包=.131 落地，双端一致） | ✓ |
| 引擎身份 | `hearth 0.2.26` / bin md5 `49495b001cb114433cefc7020f07cfbd` | ✓ 留档 |
| loop.rs 远端再证 | **8,114 行**（.131 与 .133 双机解包各验一次） | ✓ |
| .131 磁盘 | cargo clean 释放 **68.8G**（构建缓存，可再生），118G 盘现余 67G | ✓ |

## 三、跑批管线（.133，R7/R8 同款协议）

- **执行器**：复用 `.133:/home/wutao/codex-r6/regression/run-regression.sh`（协议保真：fresh session 一次性进程 / 随机隔离 cwd / `--approve-within session` / 逐条 elapsed+terminal 落盘 / 引擎身份留档 / flock 防并发）
- **对照设计**：手术后 B 臂已删（0.2.26 唯一行为=A 臂转正后的生产默认路径，**不设 SINGLE_LOOP env**），故双臂=双构建对照：
  - 新臂 = **0.2.26**（hearth-0.2.26，源包 7add4df0）
  - 对照臂 = **0.2.25 手术基线 93e7e57**（hearth-0.2.25-base，源包 md5 `dcd7f227e044d6890cf6ebf1d05df302`，loop.rs=13,055 远端再证 ✓）
- **矩阵**：`blindpack-15` + `corpus c`（fixture tui-sample）× 各 3 轮 × 双构建，串行 flock；每轮顺序 26-blind → 26-corpus → 25-blind → 25-corpus（首轮结束即有全套一轮数据）
- **预算时长**：≈8 小时（0.2.25-B 臂 corpus c 为大头，R7-1 实测约 109 分钟/轮）——过夜跑，预计 08:30-09:00 完成
- **产物**：`.133:/home/wutao/regress-run/r9-p{26,25}[c]-r{1,2,3}-*.log` + `results/<TS>-arm*/`（每条 elapsed/terminal/summary）+ `r9-engine-identity.txt` + `r9-post.done` 完成标记

## 四、判据对表（数据齐后的判读口径）

| # | 批复判据 | 判读方式 |
|---|---------|---------|
| ③ | 盲测 C YES≥13 / G-A≥3 | C 语料 14 条逐条判 YES（判分口径=包C 流程）；G-A=盲测 A 组 10 条严口径（首屏直接结论句）✓≥3（R7-1 基线 A≈2-3/10） |
| ③+ | C-2 回滚触发器实弹 | 跑批日志观察触发器路径（668d4a8 落刀点）；语料病态条目触发观察，不足则专项构造场景补测 |
| ④ | 墙钟双臂中位 ≥50% | 0.2.26 vs 0.2.25-base 各 3 轮中位数（blind 与 corpus c 分别对表）；旧 −73.6% 不复用 |
| ⑤ | SINGLE_LOOP=0 回退 | 0.2.26 全部日志 grep 回退事件=0 |

## 五、登记事项

1. **并发登记**：.133 上有他窗 `cargo test --lib -p tools-builtin`（codex_cli 仓库，mock 单测非 LLM 任务）与跑批并存——CPU 竞争轻微，无 LLM 并发违规（"严禁同机并发 LLM 任务"不触发）。
2. **.133 codex-r6 git 状态**：HEAD 停在 cf1e88c（线C D-1 中间态，trae 施工残留）——本跑批仅使用其 runner/corpus 静态文件，不依赖其 git 状态；部署构建使用独立目录（hearth-0.2.26 / hearth-0.2.25-base）。
3. **.133 磁盘**：66%（40G 空闲），两个 release 构建约 6G，充足。
4. **启动插曲（两次，均已闭环）**：① nohup 启动命令两次 channel 超时（paramiko channel 与后台进程 stdout 纠缠），复连验证进程均实际存活——虚惊；② **管线 v1 实挂**：总管脚本 `set -u` 下双引号内 `$i` 未绑定报错退出（R8 原脚本无 set -u 故未暴露此坑）——构建阶段已完成（两引擎 1m48s/1m49s 构建成功），v2 修复（单引号块令 `$i` 内层展开）后重启，**跑批已实证运行**（首目录 20260908T162948Z-armB，A1=35s/calls=2、A2=40s，与 R7-1 基线同量级）。
5. **引擎身份留档（.133）**：0.2.26 = md5 `e23bbb49c5fab0acf18e994a383ad8de`；0.2.25-base（93e7e57）= md5 `93e505c3947587d0b07a8f65c0c843dd`。与 .131 构建（49495b00）md5 不同系两机独立构建（时间戳嵌入），版本字符串双验一致。

## 六、下一步

1. 数据齐后（r9-post.done）：判分战报（C YES / G-A / 墙钟中位 / SINGLE_LOOP grep / C-2 触发观察）出 v1.0 呈顶层
2. 三项全过 → 归档仪式两签 → GitHub 首次正式同步
3. 过程异常（构建失败/语料跑挂/配额）→ 即时战报申报，不静默
