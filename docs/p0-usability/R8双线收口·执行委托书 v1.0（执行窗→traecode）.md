# R8 双线收口·执行委托书 v1.0（执行窗→traecode）

> **派发**：执行窗（WorkBuddy）　**日期**：2026-09-07　**收件**：traecode 执行窗
> **依据**：《Hearth双线收口·长程任务书顶层签发件 v1.0（顶层）》（四签已落，即刻生效）——本委托书只做"给 trae 的施工切片"，任务书全文条款自动生效。
> **你的范围（只此两包）**：**包D（即刻）→ 包A（R7-5，.133）**。
> **不归你**：包B（独立新窗口，.131，含裸跑基线四组对照）——**弹性条款未触发前禁碰**；即便顶层日后触发弹性条款，你也只跑测，Q1-Q5 判分必须外包。包C（G-W1 判分）归砺。**包E（线C 手术）三闸全绿前禁动刀**。
> **环境/陷阱不重复**：R5/R6/R7 三份委托书全部纪律继续生效（HEARTH_BIN 铁律、flock、门禁环境变量、引擎身份留档）。

---

## 一、包D · 卫生清理（即刻做，分钟级，签 3 已授权）

1. **fmt 收编**：工作树 `crates/agent-core/src/context.rs`、`crates/codex-cli/src/render.rs`（验收窗已核=纯 cargo fmt 零语义）→ 单独 commit `style: cargo fmt`（顶层裁决：收编不还原）。
2. **.131 引擎身份修复**：`/usr/local/bin/hearth`（现 0.2.23 unknown，md5 34f6f807）更新为 ebd2f52 构建或移除；`~/.bashrc:127` alias `hearth→codex-r4 旧构建` 改指 codex-r6；**验证**：`bash -lc 'hearth --version'` = 0.2.25 (ebd2f52)。
3. **根目录清扫**（顶层增 3）：仓库根 33 个 `_r7_*.sh` 移入 `docs/data/r7-20260907/scripts/`；`_QUARANTINE_undesired_209d/`（76 文件）**零删除**——原样移入 `docs/data/r7-20260907/` 保留现场，归属与最终处置随战报呈顶层裁。**移前确认零引用**（runbook/文档 grep 一遍）。
4. **防再犯**（签发件原文）：一次性脚本今后落 .133/.131 或 `.workbuddy/tmp/`，不落仓库根目录。
5. 战报：一处 commit + 双机验证输出 + 清单（移了什么/剩了什么）。

## 二、包A · R7-5 探索校准（.133，本轮主工程）

### A-1 · "No response requested." 裸透传修复

- **根因（验收窗已查实，勿重查浪费额度）**：`grep -r "No response" crates/**/*.rs` **0 命中**——模型自吐的 Codex 风格填充文本，hearth 对"空内容轮"（无文本无工具调用）无显式处理，裸透传且计有效步。
- **病理锚点**：R7-1 盲测 A3-A 终态（`✓ Done (22 steps)`，calls=11，正文即该句）；历史同款 r42-A.log:3625、r4-replay-B.log:704（均在 plan 相位后）。
- **修法**：llm-gateway 响应归一化层识别空内容轮 → 重试一次（提示"请给出实质回应或调用工具"）或渲染 `[模型本轮无实质回应]` 并触发策略换向；**空轮禁计有效步**。
- **授权边界**：改 llm-gateway / agent-core，**不涉 FZ-RFC-1（sandbox 零触碰）**。
- **判据**：修复后回归集重跑，该句在 .terminal 出现 = 0（或仅显式标注形式且不计步）；门禁绿（新基线 531，新测试计入后报新数并标注口径）。

### A-2 · 探索行为校准（三杠杆，逐项单独 commit + 单独数据，禁一包全改）

1. 单循环 system prompt 增补收敛指引：目标不明→先问；同一目标连续 3 步无新事实→强制收口（提问或 end_turn）；
2. R5-3 same_tool_repeat 机制在 A 臂的接线**核验**（grep 生产路径，给出 :line 锚点；若断线则修）；
3. R6-6 预算交接提示在 A 臂的注入点核验（病理：A4 预算耗尽前未收口）。

### A-3 · 数据协议（签 1 修订口径，已生效）

- **主判据**：墙钟耗时降 ≥50%（**≥3 轮中位数**）+ 失败数不升 + 质量不回退（C 语料 YES ≥7、G-A 严口径不低于 2-3）。
- **calls 降为必录观察指标**——每份战报仍须报数，不得消失；结构性判据（A 臂每步 1 次调用）独立成立不受修订影响。
- **轮次计划**：基线 = R7-1（已入档）+ **补跑 2 轮双臂盲测**（改前）；改后 3 轮双臂盲测。每轮 15 条 fresh session、cwd 隔离、引擎身份留档。C 语料校准后复跑 1 轮双臂（质量不回退判据）。
- **并发红线**：.133 跑测期禁同机 LLM 任务；多轮之间 flock 串行。

## 三、战报与验收秩序

- 包D 一件、包A 逐件（A-1 / A-2 三个杠杆）**独立 commit + 独立战报**，五要素证据包；禁攒总报。
- 战报口径分树：门禁读数引用 **531 = R7 门禁**（新测试计入后报新数并注明）；禁与 524/486 混标。
- 数据落盘：新时间戳目录 + tar **拉回仓库** `docs/data/`（R7-1 已修正此流程，沿用）。
- 验收窗（本窗）独立复算核验；里程碑宣告权归砺；R7-4 翻默认议题**在包A+包B 数据齐前不得重议**（防墙头草条款，签发件 §一-1）。

## 四、一句话

> 包D 扫地十分钟先交差；包A 三件事——空轮不裸传、探索会收敛、数据按新口径跑三轮中位数。改完的 A 臂拿"耗时中位数降 50% + 零质量回退"来见验收窗，calls 只报数不判生死。

---
*执行窗派发 · 2026-09-07 · 依据：顶层签发件四签 + 拟稿包A/包D 原文 + 5cd63e9 验收锚点*
