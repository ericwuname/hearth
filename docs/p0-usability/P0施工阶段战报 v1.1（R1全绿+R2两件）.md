# P0 引擎复活线 · 施工阶段战报 v1.1（执行窗 → 顶层/砺）

> **承接**：R1 诚实包战报 v1.0（R1 三件 + 492/0）　**本篇增量**：R2 包两件（R2-2 / R2-4）
> **分支**：`p0-usability-01`　**累计门禁**：**493 passed / 0 failed**（`FMT=0 / CLIPPY=0`）
> **施工窗口**：2026-09-04 00:58 – 02:40（用户全权委托，自主推进）

## 一、进度总览（任务书 §三 四包）

| 包 | 项 | 状态 | commit |
|---|---|---|---|
| **R1 诚实** | R1-1 完成语义重造 | ✅ | `bc89678` |
| R1 | R1-3 Observe 实职化 | ✅ | `af60c4a` |
| R1 | R1-2 守门恒真修复 | ✅ | `113b869` |
| **R2 状态** | **R2-2 硬切片并入压缩** | ✅ | `52509d2` |
| R2 | **R2-4 否定检测+去重** | ✅ | `ac568f2` |
| R2 | R2-1 SessionLedger（大件） | ⬜ 下一刀 | — |
| R2 | R2-3 语义摘要 | ⬜ | — |
| R1/R2 交叉 | R1-4 known-failing（依赖 R2-1） | ⬜ | — |
| R3 沟通 | R3-1~R3-4 | ⬜ | — |
| R4 验收 | G 门 + W 判据 + 双会话重放 | ⬜ | — |

## 二、本篇增量两件（病理 → 语义 → 判据）

### R2-2 硬切片并入压缩路径（commit `52509d2`）

- **病理**：`MAX_HISTORY_MSGS=40` 切片只在 Message 级裁剪，早轮事实不进 prompt 也**不落盘**——会话重启即事实销毁（E18；0.9-0.4 会话实证其后果：上一会话的 103% 假 README 以文件产物形态**跨会话存活**并污染事实基础——假事实能跨会话、真事实反而不能，保护完全反向）。
- **修复**：Turn 级识别完全落在被切区域的前缀 Turn → 送入与压缩归档**同一文件**（`archive/<sid>.jsonl` 追加式、会话隔离）；切片提示从"别担心记录还在"升级为"**已归档至 `<路径>`，可用 bash grep 检索找回**"（与压缩路径同款可行动提示）；内存侧不移除（R2-3 语义摘要的活）。
- **判据**：30 turn×2 消息触发切片 → 归档文件含早轮事实标记 + 提示含 grep 通道（测试锁定）。

### R2-4 否定检测+提醒去重（commit `ac568f2`）

- **病理**："不应该是"包含子串"应该是"→ 弱证据词 `contains()` 误报，真机会话实测 **18 次同警告刷屏**（E16）。
- **修复**：弱证据词的**未被否定的出现**存在才触发——否定前缀（不/没/未/非/别/勿/无）紧邻 = 用户表达确定性判断而非转述传闻；多词命中只取第一条（同警告单轮 ≤1 次）。
- **判据**："结果不应该是乱码" → 零误报（旧代码必红）；"据说这个库不应该是线程安全的" → 仍触发（**不误杀真传闻**）。

## 三、门禁纪律（第 1-4 条）

| 纪律 | 读数 |
|---|---|
| 三零门禁 | `FMT=0` / `CLIPPY=0`（触达面 agent-core+codex-cli+service+planner）/ `TEST=0`，全量 workspace 证据 `.131:~/fa/p0_gate_r1full.log`（02:39） |
| 口径纪律 | tag 树 486/0（未动）｜p0 分支 **493/0**——分列，禁混标 |
| 断言纪律 | 本阶段 +3 测试函数（r22×1；r12×2 已在 R1 战报）全部能失败：r22 在旧代码下归档文件不存在必红；r12 中文守门在旧代码下恒真必红 |
| golden anchor | **8/493**——r22 锚定 0.9-0.4"假事实跨会话传播"铁证；r24 锚定 E16 十八次误报的真实会话日志；+R1 的 6 条 |

**增量链**：`486 + 2（R1-1）+ 2（R1-3）+ 2（R1-2）+ 1（R2-2）+ 0（R2-4 断言并入既有测试）= 493` ✅

## 四、下一步

1. **R2-1 SessionLedger**（五栏：未完成/已知失败/承诺/已验证事实/待验证，切片后注入，条目只 close 不 remove）——大件，下一窗口首刀；
2. R1-4 known-failing（串行依赖 R2-1，砺 A-3）——G-B 一票否决门的机制基础；
3. R2-3 语义摘要 → R3 包（收尾三行/goal_drift 拦截/视觉反转/口径治理）→ R4 总验收（双会话重放 + 砺判读 + 外部交叉）。

> 判读分离：本报告为施工证据；里程碑宣告（R1 绿=不再说谎、R2 绿=不再失忆）归砺。
