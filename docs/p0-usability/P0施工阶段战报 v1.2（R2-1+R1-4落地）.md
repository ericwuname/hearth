# P0 引擎复活线 · 施工阶段战报 v1.2（执行窗 → 顶层/砺）

> **承接**：顶层《R1包与R2两件顶层验收批复 v1.0》（五件全验收 + R2-1 批准为下一刀）
> **分支**：`p0-usability-01`　**累计门禁**：**497 passed / 0 failed**（FMT=0 / CLIPPY=0）
> **施工窗口**：2026-09-04 00:58 – 04:20（用户全权委托，自主推进；本篇为批复后增量）

## 一、本篇增量两件（批复后施工）

### R2-1 SessionLedger（commit `ae9c0d8`）——大件，顶层三条硬约束全落实

| 硬约束 | 落实 |
|---|---|
| ① 五栏 | 未完成 / **已知失败**（R1-4 数据源）/ 承诺 / 已验证事实 / 待验证——`LedgerColumn` 五枚举 |
| ② 注入在切片之后 | build_messages：切片 note 之后紧跟账本注入（**顺序断言**锁定）；早轮被裁，账本仍在 prompt——G-D 门"列 5 项→5 轮继续→零遗忘"的机制基础 |
| ③ 只 close() 不 remove() | entries Vec **只追加无删除通道**——"未完成清单蒸发"（0.9-0.3 十项全蒸发）在**数据结构层面**不可能复发；渲染时"已关闭 N 条"计数可见（关闭≠删除的可见证明） |

**自动派生**：do_plan decompose 后 `sync_from_task_graph`——Failed 节点 → 已知失败栏、未完成 → Pending、Completed/Skipped → 关闭对应条目（终态只迁移不删除）；去重 = 同栏同文本前缀已有开放条目。

**跨重启通道**：`RunState.ledger`（`#[serde(default)]`）随会话落盘/恢复自带；serde 往返测试锁定（关闭状态不复活）。

### R1-4 known-failing 报告层拦截（commit `6e905de`）——G-B 一票否决门落地

- **病理**：0.9-0.3 会话"已知 0/16 失败项却标 ✅100%"——失败记录后没有任何机制阻止完成声明。
- **机制**：R2-1 KnownFailing 栏 = 数据源；completed 终态 summary **强制携带** `known_failing_open` 清单；CLI 投影遇未清已知失败 → **禁止裸 ✓**，改显示"⚠ G-B 拦截：N 项已知失败未复测通过，本完成声明不可信、禁止计入验收通过：[清单]"。
- **边界**（任务书指定）：拦截在**报告层**，不改控制流——失败项修不好时不得死锁完成路径（终态九态封闭集不动，C-12 裁定遵守）。

## 二、全线路进度

| 包 | 项 | 状态 | commit |
|---|---|---|---|
| R1 诚实 | R1-1 / R1-3 / R1-2 | ✅（已验收） | bc89678 / af60c4a / 113b869 |
| R2 状态 | R2-2 / R2-4 | ✅（已验收） | 52509d2 / ac568f2 |
| R2 状态 | **R2-1 SessionLedger** | ✅ **本篇** | ae9c0d8 |
| R1×R2 | **R1-4 known-failing** | ✅ **本篇** | 6e905de |
| R2 状态 | R2-3 语义摘要（内存侧） | ⬜ 下一刀 | — |
| R3 沟通 | R3-1 收尾三行 / R3-2 goal_drift 拦截 / R3-3 视觉+降噪 / R3-4 口径治理 | ⬜ | — |
| R4 验收 | G 门 + W 判据 + 双会话重放 | ⬜ | — |

**R2 包语义已基本齐**（R2-3 内存侧摘要是余项）；**"不再失忆"的三大机制**（SessionLedger 注入 / 硬切片归档 / 跨重启 serde）全部就位。

## 三、门禁纪律（第 1-4 条）

| 纪律 | 读数 |
|---|---|
| 三零门禁 | `FMT=0` / `CLIPPY=0` / `TEST=0`（全量 workspace，04:18，`.131:~/fa/p0_gate_r1full.log`） |
| 口径纪律 | tag 树 486/0（未动）｜p0 分支 **497/0**——分列禁混标 |
| 断言纪律 | 本篇 +4 测试（r21×3 + serde×1）全部能失败：injection_after_slice 在旧代码下（无账本注入）必红；close_never_removes 在有 remove 通道的实现下必红 |
| golden anchor | **12/497**——r21×3 锚定"十项清单蒸发"+"FileBrowser 已知失败"病理；serde×1 锚定"假事实跨会话/真事实不能"病理；+ 此前 R1/R2 的 8 条 |

**增量链**：`486 + 2 + 2 + 2 + 1 + 0 + 3 + 1 = 497` ✅（R1-1/R1-3/R1-2/R2-2/R2-4/R2-1/R1-4）

## 四、下一步

1. **R2-3 语义摘要**（内存侧：`summarize_turn` 从"目标 60 字/工具名/路径"升级语义摘要）——R2 包收口；
2. **R3 包**四件（收尾三行 / goal_drift 升级拦截 / 视觉反转+降噪三档 / 完成度口径治理）；
3. **R4 总验收**：G-A~G-H 八门（G-B 一票否决——机制已就位）+ W-A~W-F 六判据 + **双会话场景重放**（0.9-0.3/0.9-0.4 用户轮次重放新引擎）+ 砺判读 + 外部交叉（拆解二 Q-1~Q-6 同口径）。

> **里程碑**：任务书 §四 里程碑门——R1 绿=不再说谎（已施工+已验收，宣告权在砺）；R2 绿=不再失忆（机制齐，待 R2-3 收口+砺判读）。
