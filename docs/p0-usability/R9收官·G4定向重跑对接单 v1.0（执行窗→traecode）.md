# R9收官 · G4定向重跑对接单 v1.0（执行窗→traecode）

> **拟件**：执行窗/验收窗　**日期**：2026-09-08　**对象**：traecode 执行窗（.131）
> **性质**：顶层《Hearth判分与拍板·收官流程任务书 v1.0》**增补 1（v1.1，执行窗口执行令）**的对接转达 + 执行注记 + 闸 3 体感材料核验附录。**条款以顶层原文为准，本单不增不减不改语义，只做转达与落地注记。**
> **依据链**：844dabb（判分批复）批 2 裁决 2 + 批 4 三前置 → 顶层增补 1（2026-09-08 16:26 签发）→ 本对接单。

---

## 一、任务定位与当前状态（trae 必读上下文）

- **判分已回填并顶层入账**：G1 53.3% / G2 100% / G3 95.0% / G4 88.3%（q1q5 已回填，拍板数据半边闭合）。
- **批 4 三前置状态**：① 验收抽验 ≥10/60 ✅ 用户裁决完成；② **G4 定向重跑 = 本令，你的当前任务**；③ 拍板件 v1.1 回填（跑测窗）待 ② 完成。
- **背景**：包B G4 跑批漏设 `HEARTH_ALLOW_NO_CGROUP=1` → cgroup 创建 Permission denied → bash/grep 全程 RT4 fail-closed 瘫痪。受影响条目中三条最重：**A8（三点指令落地）、B1（双 curl 验证）、B5（探查任务）**。本令=环境影响注记性重跑，把"G4 环境折扣"从推断变实测注记。
- **预期记分影响 = 零**（判分不变、不重判；唯一例外=反转证据，见 §四）。

## 二、施工单（顶层增补 1 §二/§三 转达，逐项核对后才许开跑）

**环境配置五查**（全部通过才开跑）：

```bash
export HEARTH_ALLOW_NO_CGROUP=1              # ← 上次事故根因缺失项，本次必须显式设置
export HEARTH_SINGLE_LOOP=1                  # 与原 G4 跑批同口径（A 臂单循环）
export HEARTH_BIN=/usr/local/bin/hearth      # 显式路径，与原跑批一致
unset HEARTH_URL                              # 防环境残留指向非预期网关
md5sum /usr/local/bin/hearth                  # 必须等于 5c7aef0307f1dbe7556441cc432311a9（5b45c57 口径）；不符 → 停跑申报顶层
```

**协议复刻**：fresh session、cwd 随机父目录、timeout 420s、flock 串行、A8/B1/B5 三题从 `regression/corpus/blindpack-15.jsonl`（md5 828c8827）**逐字取用禁改写**。

**跑前自检**：任选一题先跑通，确认日志中**无** `cgroup ... Permission denied (os error 13)` 且 bash/grep 实际可执行——再跑其余两条。若降级开关设置后 bash 仍瘫痪 → 停跑按失败申报（这也是有效数据）。

**产物与入账格式**：
- 落盘 `docs/data/r9-pkgB-20260908/results/G4-rerun/{A8,B1,B5}/`（新目录）；每条四件套 `.log/.rc/.terminal/.elapsed` + 目录级 `identity.txt`（版本/构建/全量 md5 三行）。
- 对照表逐条三列：`原跑行为（fail-closed）｜重跑行为（修复环境）｜差异归因（环境/非环境）`。
- 交付战报 `R9包B·G4定向重跑战报 v1.0（执行窗→顶层）.md` + 数据目录一并 commit。

## 三、禁区（顶层增补 1 §五，红线原样）

1. 禁改 `results/G1~G4/` 原始数据与 `r9-judging/` 判分产物——只读基准，写入仅限 `G4-rerun/`；
2. 禁动 loop.rs / 任何源码（手术触发锁定，开刀申报前零行触碰）；
3. 禁并发跑 LLM 版本冻结类测试（.131 并发纪律不变）；
4. 引擎不变：0.2.25（5c7aef03），禁顺带升级/改动任何代码——**纯数据采集，不是施工**。

## 四、判定纪律（增补 1 §三原样）

重跑产物**不判分**（Q1-Q5 已入账）。唯一例外 = **反转证据**：修复环境后出现与原判分基础事实矛盾的行为（例：B1 example.com 修复后仍失败且根因明确非 cgroup；A8 三点在工具可用下仍不落地且归因非环境）→ 当轮战报**显式申报**，由顶层裁是否影响拍板。

## 五、执行注记（执行窗补充，非顶层条款）

1. **G2/G1 组跑批脚本可复用**：.131 `~/r9-pkgB/`（r9pkgb.sh / run_all.sh / collect_r9.sh）+ 本地 `.workbuddy/tmp/r9/`——重跑仅取 G4 分支三条，脚本改动最小化；一次性改动不落仓库根目录。
2. **对照基准取数**：原跑 `G4/{A8,B1,B5}.log` 中 fail-closed 证据已在前案核验（7c423cb），对照表"原跑行为"列可直接引用原日志行号。
3. **战报新增记录项**（沿 a6ed0a9 惯例）：①五查逐项结果 ②跑前自检日志摘录 ③三列对照表全量 ④反转证据检查结论（即使为零也要显式写"无反转证据"）。

## 六、附录：闸 3 体感材料核验注记（执行窗→顶层，随单呈报）

用户手工测试转录已出：`release/手工测试v0.2.23-2026-0.9-0.8.txt`（4,596 行，md5 `a657440b71cd37d4647dff226ed5099b`），原样入库 `docs/data/手工测试v0.2.23-2026-0.9-0.8.txt`。执行窗核验结果：

### 6.1 三会话结构与引擎口径

| 会话 | 时间（北京） | 引擎（--version 实录） | 口径判定 | 主题 |
|---|---|---|---|---|
| 1 | 09-08 ~02:50 起 | `hearth 0.2.25 (ebd2f52)` | ⚠️ 旧构建（R7 口径 9ec07d9d），非 5b45c57 | 自我能力认知（30 步检索版本/文档） |
| 2 | 09-08 ~15:20 起 | `hearth 0.2.25 (5b45c57)` | ✅ **R8 门禁口径** | 联网能力（web_fetch 审批/白名单）、harness 资料整理 |
| 3 | 09-08 15:18 登录后 | `hearth 0.2.25 (5b45c57)` | ✅ **R8 门禁口径** | web_search 工具构造、模型层 vs 架构层诊断 |

- **文件名笔误登记**："v0.2.23"实为 **0.2.25**（三会话 --version 全实录）；"2026-0.9-0.8"格式笔误。
- **闸 3 口径匹配**：顶层闸 3 = "用户体感 0.2.25（cgroup 正常环境）"。会话 2/3 用 5b45c57 构建（=0.2.25 引擎 md5 5c7aef03 同构建 commit）+ 全程无 cgroup 瘫痪 → **构成闸 3 证据主体**；会话 1 ebd2f52 旧构建如实登记（同版本号，体感结论不受影响）。**裁定权在顶层。**

### 6.2 环境纪律实证（正面）

- 三会话全程 `🔒 landlock+seccomp (fail-closed)` 正常；无 cgroup Permission denied 瘫痪——与包B G4 事故环境形成对照，反证那次确为漏设开关的环境事故。
- UNVERIFIED 诚实标注全程渲染；任务级 deadline（901s ≥ 900s）失败诚实呈现并给出 resume 建议。

### 6.3 实质观察 3 笔（呈顶层，术后挂账候选——手术卡已冻结零重写，本单不擅自加项）

| # | 观察 | 证据（行号） | 初步定性 |
|---|---|---|---|
| 1 | **egress 审批放行链路断裂**：用户 approve `web_fetch www.google.com` → 系统回"🔓 已放行并写入配置（egress-allowlist）" → 重试仍报"域名不在 HEARTH_EGRESS_ALLOWLIST 白名单" | :1687-1720（approve 与放行提示）、:1707-1709（重试被拒） | 疑似审批写入与运行中白名单不同步（持久化文件 vs 进程内存/重启前不生效），P1 级 UX bug 候选 |
| 2 | **账本未完成项雪崩**：目标修订（revision 2"原目标保留为锚点"）后旧目标 pending 项不随修订清理，逐轮累积至 **38 项未完成**，与"Task completed——目标达成"矛盾呈现；末轮"完成决策 =（无记录）" | :4570-4576（38 项全列）、会话 1/2 各轮"还剩什么"递增 | RC-Ω 家族：修订语义与账本清理脱钩 + 收尾"达成"与账本存项的呈现矛盾 |
| 3 | 模型层残差（已知 A1/A10 型）：Agnes 在账本中自记"Respond … as Agnes-2.5-Flash, developed by Sapiens AI"；工具构造任务理解漂移 | :364、会话 3 中段 | 模型层，非引擎层，不进手术 |

### 6.4 给顶层的两行建议

1. 闸 3 材料合格性：会话 2/3 满足口径，**建议认定闸 3 体感证据到位**（是否即判"双闸齐"由顶层裁——批 6 载明闸 3 与判分并行不互斥，两闸证明齐后 trae 开刀申报）。
2. 观察 1/2 建议记入 **D2 历史 40 条小包或 0.2.26 候选清单**（手术卡不动）；如顶层认为影响手术验收判读，可在开刀申报前以增补形式显式追加，勿静默改卡。

## 七、口径分树（禁混标）

5c7aef03=0.2.25 引擎（5b45c57 构建，重跑口径）；9ec07d9d=ebd2f52 旧构建（会话 1 所用）；5fd058b8=包B 数据包；828c8827=blindpack-15 题包；a657440b=体感转录入库件；wc -l=唯一报数口径。

---
*执行窗 · 2026-09-08 · 本单=转达+注记，顶层增补 1 条款效力优先；trae 完成后走既有链路：战报→执行窗验收→呈顶层→三前置齐→签 R7-7 → 开刀申报*
