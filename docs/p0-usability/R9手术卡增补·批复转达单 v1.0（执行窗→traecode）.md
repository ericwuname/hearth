# R9 手术卡增补·批复转达单 v1.0（执行窗→traecode）

> **拟**：执行窗（WorkBuddy）　**日期**：2026-09-08  
> **依据**：顶层《Hearth线C手术预研批复与执行计划 v1.0》（**5e3d61f**）——C-1~C-5 一笔批清，R-1/R-2 即刻生效  
> **你的状态**：**维持触发挂起（序 6/7），无新预研任务**。本单把顶层批复折进你的手术卡——4 处增补，动刀时生效。预研报告（4c055a6）+ R9 委托书（9eb250d）+ 本单 = 手术卡终稿。



---

## 一、批复转达（C-1~C-5 全批准）→ 手术卡影响

| #       | 顶层裁定                            | 对你的手术卡影响                                                                                                                                                            |
| ------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **C-1** | stuck 族保留                       | D-9 表按预研口径执行：:1076-1080 字段 / :4194-4225 维护 / :2787 注入 / :4228 硬停，**全部不动**                                                                                           |
| **C-2** | UserInputKind 改写删除批准，**附回滚触发器** | 动刀照改（:5410-5425 收窄直通）；**术后验收观察面加一条**：C-control 类语料（C-control-endtest 等）**单条误判回潮 → 立即回滚本项并呈顶层**（回滚 = 恢复分类调用点，预研 §二-D-2 处置方案反向）。此触发器须在手术战报中挂载证明                       |
| **C-3** | checkpoint 载荷缩编批准，**附双写点注记**    | 解绑必须**双写点一并处理**：:1518 `self.task_graph = tg.clone()` 与 :1519 `self.plan_state.task_graph = tg`（你预研只点了 :1519——它只对 plan_state 成立，顶层实测 ：1518 同函数体第二写点）。动刀战报如实记录两处各自的处置 |
| **C-4** | consecutive_errors 经验门死分支删除批准   | 按 D-9 表执行（:3349 门 + B 侧维护链）；experience_store 在 A 臂失活如实入账，经验注入需求挂账 cognition 线                                                                                       |
| **C-5** | 算术口径确认                          | 生产 2,035 + 测试 2,050 计入 <10,500 硬线；评审带 (10,500, 11,043] 保留不动（保险条款）；术后以 wc -l 实测为准                                                                                    |

## 二、R-1/R-2 即刻生效（两道执行令）

- **R-1 口径统一令**：你的里程碑报数、验收清单、战报行数**一律 wc -l 坐标**（13,055 基线口径）；测试区 wc 口径 = **:6287-13,055（6,769 行）**。预研报告里的 6,247 / 12,533 为混口径数字（rg 起点 + Measure-Object 终点，差 522=空行数），只作历史参考，**禁再引用**。
- **R-2 GiveUp 重判令**：测试删除清单预编 §四 GiveUp 链（~8 函数）按"**经 do_reflect / MockPlanner-GiveUp 驱动者死**"重判；":5294 在 do_act 尾 A 臂可达"的预判作废（实测在 do_reflect 尾）——:4001 改指 Done 后 do_reflect 零入边，**经它驱动的 GiveUp 测试全部随 D-6 死**；唯一 A 臂现役 FA01 机制在 run() 预算拦截（:5621 起，fa01_budget_intercepted），与 ：5294 无关，保留不动。

## 三、触发状态（不变，无新阻塞）

- 触发条件仍 = **闸 2**（包B 四组数据落盘 + R7-7 拍板件顶层签署，裁决=留自研）∧ **闸 3**（用户 0.2.25 体感报顶层）；
- 两条齐 → **开刀申报**一条消息（pre-linec-baseline tag + Desktop tar 完成证明 + 双闸确认）→ 按施工卡 D-10→D-8→D-1→D-2→D-7→D-5→D-3→D-4→D-6→D-9 逐项动刀，每项五闸不可跳；
- **包C 不是手术闸门**（脱敏版由执行窗出，判分归砺）——不阻塞你；若动刀前出数，G-W1 真尺可作术后 Q1-Q5 额外对照基线，否则按施工卡原计划对照 R8 post 基线。

## 四、动刀战报新增记录项（2 笔）

1. **C-3 双写点处置记录**：:1518 / :1519 各自的删除或改写方式；
2. **C-2 回滚触发器挂载证明**：术后盲测包含 C-control 条目，判分规则写明"单条误判即触发回滚"。

其余战报格式沿用委托书 §六（五要素证据包 + 每 3 项 wc -l 里程碑，**一律 wc 坐标**）。

## 五、纪律提醒（沿用 + 一笔登记）

- **坐标唯一权威 = 4c055a6 重锚版 + 本单增补**；外部 1429b58 旧锚点已漂移 +230~540，**禁盲抄**（顶层 §四 原文）；
- 其余红线沿用委托书 §五八条（触发前不动刀 / 冻结区零碰 / A 臂现役不碰 / 包B 包C 不归你 / 禁改断言 / 并发错峰 / 引擎身份留档 / RC 直取）；
- **登记一笔（笔误更正）**：顶层批复文档引用验收报告 commit 写作 "e38051"，实际为 **e380405**——不影响批复效力，后续引用以 e380405 为准。

## 六、一句话

> 五笔全批、口径统一、回滚触发器挂好、双写点钉进卡片——你的手术卡现在是终稿：包B 数据和用户体感两把钥匙一齐，开刀申报一条消息，按卡动刀，术后 8,970 见。

---

*执行窗拟 · 2026-09-08 · 依据：5e3d61f 顶层批复 + 4c055a6 预研 + 9eb250d 委托书；:1518/:1519 双写点经本窗 Read 实测确认*
