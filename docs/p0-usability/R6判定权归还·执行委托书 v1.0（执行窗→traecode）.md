# R6 判定权归还 · 执行委托书 v1.0（执行窗 → traecode）

> **签发**：执行窗（WorkBuddy）2026-09-06　**受令**：traecode（R5 施工窗口延续）
> **上位文件**：《Hearth判定权归还长程任务书 v1.0（顶层·R6）》（一次签发令）+ **《R5智能性根治·执行委托书 v1.0》**（环境/铁律/陷阱清单**全部继续有效**，本件不重复，只写增量）
> **基线**：0.2.25（R5 后，门禁 524/0，loop.rs 12,165 行）　**分支**：`p0-usability-01` 延续
> **一句话**：**把判断还给模型，框架只留三类护栏——预算、安全、不可恢复错误。允许"做不到"，不允许"框架替模型宣布做不到"。**

---

## 〇、本包与 R5 的三点不同（先读懂再动刀）

1. **每件战报必须回答"删了多少"**——★ 行数判据已是门禁纪律第 6 条：`wc -l crates/agent-core/src/loop.rs` **每包净减 ≥200 行**（累计目标 <9,000）。行数在涨 = 范式没变 = 判为补丁。R5 净增 945 是反面教材。
2. **真机回归集（R6-9）与 mock 门禁分列**——524/0 证明的是内部逻辑自洽，不是真机可用（H1 实锤）。**你的 mock 门禁继续跑，但"真机成功率"从此是并列的验收指标。**
3. **阶段 2 是判别实验**：R6-1 落地后重跑 G-A 语料——G-A ≥7/10 → 框架层占比大（砺"模型层"裁定修正）；仍 <7/10 → 模型层占比大（外部主张修正）。**双方已预注册认输条件，你只管如实出数据。**

## 一、施工项（R6-1 ~ R6-11，阶段即依赖序）

### 阶段 0 · 解自擒（低风险，立即开工）

**R6-1 ⭐ give_up 三出口从"终止"改为"注入事实由模型决定"**
- 坐标：`loop.rs:4901` 附近 `ReflectVerdict::GiveUp` 分支（原代码直接终止 run ok=false）
- 改法（外部处方原文）：GiveUp 时不终止——把事实注入上下文（"你已连续 N 次在 `<tool>` 失败；错误类型 `<error_kind>`；已尝试 `<commands>`；当前产物 `<files>`。请判断：换一种做法 / 向用户说明卡在哪、为什么、建议怎么办"）→ `continue` 回模型，由它 end_turn 或继续
- **一改解三结**：框架自擒断裂 / 假失败消失 / 终止有了面向用户的解释
- 判据：C 语料 L285 场景重跑（curl 被拒 3 次）：❌仍 `Task failed` = 未修；✅输出"外网验证失败（白名单拒绝裸主机名），已试 A/B，建议改完整 URL 或查白名单"

**R6-2 删除 T4 强制 GiveUp**（连续 2 次 TaskGraph hash 相同 → 强制放弃）
- 坐标：`loop.rs:3084-3085`
- 判据：T-B 纯读任务不再被 stalled 终止

**R6-3 补 TodoWrite 工具**（让模型自持任务清单——**T4 停滞检测失去输入源，自擒从源头断**；planner 独立 LLM 分解调用随之减少）
- 坐标：`tools-builtin` 新增（参照 Claude Code TodoWrite 语义：创建/更新/完成清单项）
- 判据：模型可自主维护清单；C 语料不再出现"同一草案连出 3 次"

**R6-4 结构化诊断日志**：每轮一行 JSON（相位/耗时/决策依据/verdict）落盘
- 判据：事后可复盘任一 run 的相位/耗时/决策

### 阶段 1 · 信息不再丢弃

**R6-5 工具结果直接回灌 messages**（tool_result as user message，照 Claude Code 口径）——**取代 R5-1 的 scratch 中转**
- 判据：同错重复 3→≤2（**R4.2 计数验收在此收口**）

**R6-6 预算可配置 + 耗尽优雅降级**：默认 50 改配置项；耗尽 = 交还控制权 + 当前状态 + 建议，**非 `Task failed`**
- 坐标：`agent-types/src/lib.rs:330`（max_steps: 50 硬编码）
- 判据：预算耗尽时用户拿到"做到哪了 + 建议"

**R6-7 上下文分页替代硬截断**：大输出落盘 + 行号范围 + 摘要 + 续读工具
- 坐标：`loop.rs:5886` truncate（6000/4000/1500）+ R5-5 的 read 分页衔接
- 判据：引擎不再自报 `chars truncated`（C 语料 L1481 曾自报 82,204 chars）

**R6-8 one-shot checkpoint**（增量落盘，kill 可续）——可随时插入
- 判据：kill 后 `resume` 续上

### 阶段 2 · 判别实验（数据裁决砺 vs 外部的分歧）

**R6-1 落地后重跑 G-A 语料（10 题）+ 真机回归集**：
- G-A ≥7/10 → **框架层占比大**（砺"模型层"裁定修正）
- 仍 <7/10 → **模型层占比大**（外部框架层主张修正）
- **双方已预注册认输条件——你只管如实出数据，不做倾向性解读**

### 阶段 3 · 手术（天花板级，**A/B 门控**——盲测包 A 组跑分显著优于现状才合入）

**R6-9 单循环改造**：Plan/Act/Observe/Reflect 合为一次模型调用
- 目标形态（外部处方原文）：`loop { resp = llm.chat(messages, tools); if resp.tool_calls.is_empty() { break } results = exec_all(resp.tool_calls); messages.push(tool_results_as_user_message(results)); if budget_exhausted { break } }`——**唯一框架护栏 = budget**
- **B4 同名异构随之消解**（Observe 相位撤销，观察职能归 observer crate 经事件流对接）
- 判据：LLM 调用次数降 ≥50%；Reflect 26s 相位消失

**R6-10 删除 140 词表意图分类**（R6-9 后不需要提前分流，自然消失——**净删除**）
- 判据：C 语料 14 句全部正确响应；分类准确率 29%→N/A

**R6-11 终止条件改 `stop_reason`**：模型不带 tool_calls 即结束——**r5-11 随之可删（净删）**
- 判据：纯问答零写盘正常结束

### 收尾

- 拆 loop.rs（12,165 → <9,000 的一部分）：R6 全判据绿后单独立项，纯重构不改行为
- B4 降级预案（预登记）：R6-9 若延期，最小修复 = 内核经 trait 接口消费 observer 事件（不破坏独立 crate 铁律）

## 二、贯穿纪律（每件战报必附）

1. **★ 行数读数**：每件完工附 `wc -l crates/agent-core/src/loop.rs`——**删了多少是本包第一验收指标**；
2. R4.2 遗留收口（并入本包验收）：R5-1/3 真机同错计数、R5-7 A/B 三指标、专项二 0.2.25 复验；
3. G/W 门不回退（G-B 一票否决持续）；引擎身份可验证（HEARTH_BIN env + --version 0.2.25 起 bump）；
4. "门禁全绿"表述一律带**"内部逻辑自洽"限定语**（H1 重新定性后的表述纪律）。

## 三、总验收（预注册 8 条，照抄任务书 §三）

1. C 语料 L285：不再 Task failed，给自然语言卡点说明+建议；2. T-B 纯读不被 stalled 终止；3. 同错 ≤2 且第二次命令不同；4. **真实成功率 G-W1 ≥70%**（R6-9 回归集产出，首份真尺）；5. LLM 调用次数降 ≥50%；6. **行数判据**：每包净减 ≥200、累计 <9,000；7. 不回退四硬层 + G-A 缓解如实记录；8. 引擎身份可验证 + 回归集语料 git 化。

**诚实边界随报告同呈**：外部分析覆盖率 ≈5%；llm-* provider 层未深审（bridge temperature 0.8 列附带核验）；比例型指标单语料不外推；分歧裁决前两条叙事并行。

## 四、签发栏

| 窗口 | 意见 | 日期 |
|---|---|---|
| 顶层（人工窗） | ☑ R6 判定权归还任务书签发 | 2026-09-06 |
| 执行窗（traecode） | ⬜ 阶段 0 即刻开工 | |
| 砺·评审 | ⬜ 随批判读 + 里程碑判词 | |
