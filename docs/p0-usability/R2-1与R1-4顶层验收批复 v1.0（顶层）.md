# R2-1 SessionLedger + R1-4 known-failing · 顶层验收批复 v1.0（顶层）

> **受审件**：`P0施工阶段战报 v1.2（R2-1+R1-4落地）`
> **日期**：2026-09-04 04:30　**结论**：⬛ **两件抽验 4/4 源码实证，验收通过。R2 包语义基本齐（余 R2-3）；批准下一刀 R2-3，随后进入 R3 包。**

---

## 一、抽验记录（4/4 属实）

| # | 战报主张 | 顶层实测 | 结果 |
|---|---|---|---|
| 1 | 五栏枚举 | `agent-types/src/lib.rs:486-495`：`Pending / KnownFailing / Commitment / VerifiedFact / 待验证`——注释直书"R1-4 数据源：未复测通过前禁止标 ✅" | ✅ |
| 2 | 只 `close()` 不 `remove()` | `impl SessionLedger` 方法面仅 `add()`（push entries）与 `close()`（置 `closed_at_step`）——**API 层面无删除通道**，"十项蒸发"在数据结构上不可复发 | ✅ |
| 3 | 注入在切片之后（顺序硬约束） | 测试 `test_r21_injection_after_slice`（loop.rs:10513）+ 位置断言 `:10599`「账本注入必须在切片提示之后（注入时点硬约束）」 | ✅ |
| 4 | R1-4 报告层拦截 | `loop.rs:5455`（completed summary 强制携带 `known_failing_open`）+ `run_local.rs:762`（CLI 投影"⚠ G-B 拦截：N 项已知失败未复测通过，本完成声明不可信、禁止计入验收通过"） | ✅ |

增量链 `486+2+2+2+1+0+3+1=497` ✅；golden anchor **12/497**（r21×3+serde×1 锚定"清单蒸发/已知失败/跨会话传播"三病理）。

## 二、验收意见

| 项 | 裁定 |
|---|---|
| R2-1 SessionLedger | ☑ **验收通过**。三条硬约束（五栏/切片后注入/只 close 不 remove）全落实且各有测试锁定；`sync_from_task_graph` 自动派生（Failed→已知失败、Completed/Skipped→关闭）设计正确——账本不靠模型自觉，靠任务图终态迁移；serde 跨重启通道使"重启失忆"同被封。**"未完成清单蒸发"病理在机制层终结** |
| R1-4 known-failing | ☑ **验收通过**。G-B 一票否决门的机制基础落地；**报告层拦截不改控制流**的边界判断正确（终态九态封闭集不动，C-12 遵守）——失败项修不好时不得死锁完成路径，这个克制是对的。**"0/16 标 ✅100%"病理从本 commit 起有了结构性终结点**，最终检验在 R4 双会话重放 |
| 里程碑 | "R2 绿=不再失忆"机制三件（账本注入/切片归档/跨重启 serde）已齐，**正式宣告待 R2-3 收口 + 砺判读**（判读分离不变） |

## 三、下一步批准

1. **R2-3 语义摘要（内存侧）= 下一刀**：`summarize_turn` 从"目标 60 字/工具名/路径"升级语义摘要——R2 包就此收口。验收锚点：压缩后"之前做了什么、为什么"可答（W-D 前置）。
2. R2 收口后进 **R3 包**四件（收尾三行 / goal_drift 拦截 / 视觉+降噪 / 口径治理），轻量项可并作一轮。
3. **R4 总验收**保持既有口径：G-A~G-H + W-A~W-F + 双会话场景重放 + 砺判读 + 外部交叉（拆解二同口径）。R2-1/R1-4 的真机检验归 R4，不在施工层自证。
4. 门禁纪律照旧：增量勾稽 + golden anchor 读数随每份战报。

## 四、节奏提示（顶层责任，非催促）

三小时四件（含大件）+ 全量门禁，速度远超预期；**速度本身不是验收标准**——R2-3 与 R3 包虽轻，仍按"每件：病理→语义→判据"格式交战报，勿因进度顺而压缩判据设计。砺判读与 R4 是下一道真实关口，施工窗的证据格式越规范，判读越快。

## 五、签发栏

| 窗口 | 意见 | 日期 |
|---|---|---|
| 顶层（人工窗） | ☑ R2-1/R1-4 验收通过；R2-3 下一刀；里程碑宣告待砺 | 2026-09-04 |
| 砺·评审 | ⬜ 里程碑判读（"不再失忆"）待 R2-3 收口后出 | |
