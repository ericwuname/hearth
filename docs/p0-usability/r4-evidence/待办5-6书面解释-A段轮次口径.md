# R4 待办 #5/#6 书面解释 v2（撤回重写版，按砺裁定）

> **v1 撤回说明**：v1 两处硬伤（砺裁定 §二.1）：①引用了本地不存在的 events JSON（文件在 .133 未同步，"引用文件前先 ls"纪律违例——现已补落盘至 `r4-evidence/replay/r4-replay-A.log.events.json`，7,890 B）；②配对算术不闭环（30 配 30 已满，无余量容纳"被吞轮"终态，"PTY 吞行"假设未过算术自检）。**v1 §二 全文撤回**，按下述砺定谳重写。
> **v1 遗留更正**："精确差额不可复原"表述撤回，改按砺裁定：**"历史注脚封存"**（差额属宽口径分类可逐行考古，对 R4 判定零影响、不值得花工时）。
> **引用规范（随证据包生效，接受）**：凡引用原会话轮数，一律双数并列"**36（拆解期宽口径，计数脚本未入库）/ 31（实存有效输入轮，hearth> 32 行含 1 空提示符）**"，禁止单引 36。

---

## 待办 #5（36→31 轮差异）：关闭 ✅（砺裁定维持）

三层口径分层经砺实测维持：语料 A `hearth>` 32 行（:5250 空提示符）→ 有效输入 31（inputs_A.json 31 项逐条核对无重复无空）→ 重放 31/31 逐字覆盖。关闭。

## 待办 #6（1 轮无终态）：**按砺定谳改判**

**砺亲测定谳（本件全文采纳，替代 v1 §二）**：

| 计数 | 实测（砺 + 执行窗 events JSON 复核） |
|---|---|
| `hearth>` echo | 31（30 带输入 + :4220 空提示符） |
| run 编号 | **run-001 … run-030，恰好 30 个，连续无缺号** |
| 终态 | 30 |
| inputs | 31（第 31 项"好的，今天的测试先到这里吧"，echo :4114 **存在**） |

**定谳**：31 输入 vs 30 run/30 终态——**缺的正是第 31 轮（收尾寒暄轮）的 run 与终态**：echo 在（:4114），但重放脚本发完该输入后即进入会话收尾（/quit），**run 未及启动、无终态行**。

**改判表述**：已定位至第 31 轮（收尾轮，重放脚本收尾截断，run 未及启动），**对 R4 判据无影响**（收尾寒暄轮不承载任何 G/W 判据），登记为**重放脚本收尾截断——非引擎行为缺口，亦非 PTY 吞行**。

## 教训登记（砺 §三 公道话反向吸收）

1. **引用文件前先 ls**（同"行号引用前重新 grep"族）——v1 引用了未同步的 events JSON；
2. **解释性假设必须过算术自检**——30 配 30 已满时，"第 31 轮终态在哪"是必答自检题，没答就不许用"PTY 吞行"收口；
3. events JSON 已补落盘（本件同 commit），v1 的"引用缺陷"就此闭合。
