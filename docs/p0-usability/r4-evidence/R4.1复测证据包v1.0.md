# R4.1 复测证据包 v1.0（执行窗机器提取，判读归砺）

> **引擎**：`p0-usability-01 @ 0.2.24`（431f8a0 + R4.1 返工，cargo clean 全量构建，`--version`=0.2.24 实测）｜.133
> **跑测**：R42 三段串行（A 31 轮 03:21-04:22 / B 18 轮 04:22-05:01 / G-A 10 题 05:01-05:18）——**全部完成**，上限 110 未超（59 轮）
> **背景**：R4 首轮跑测作废（引擎身份事故）；本包为**首次以正确引擎身份跑出的证据**。

---

## 一、终态分布

| 段 | completed | failed | 裸 VERIFIED（无标注 completed） |
|---|---|---|---|
| A（31 轮） | 21 | 7 | **27 个 VERIFIED**（21 completed 行内含 27 处投影，部分轮多次完成） |
| B（18 轮） | 12（全 UNVERIFIED） | 5 | **0** |
| G-A（10 题） | 2（全 UNVERIFIED） | 8 | **0** |

## 二、硬层四项（零复发总判定）

| 项 | 读数 | 判定 |
|---|---|---|
| ①已知失败标 ✅ | 0 命中（G-B 拦截投影未触发=无未清已知失败轮；FileBrowser 形态待专项对账） | ✅ |
| ②假事实未核验引用 | 待专项二强制阅读复验（本轮跑测队列外，随 R4.1 补测） | 🔶 |
| ③清单蒸发 | 账本机制在位；REPL 收尾三行**运行时仍未输出**（见 §四） | ⚠️ |
| ④裸改判无标记 | 0 命中（"改判达成"投影 0 次=改判路径未触发，同首轮） | ✅ |

## 三、VERIFIED 授予收紧 · 实战读数（砺 §三 返工项验证）

- **B 段 12 completed 全 UNVERIFIED、G-A 2 全 UNVERIFIED**——收紧后在 B/G-A 段**零裸 VERIFIED** ✅
- **A 段 27 个 VERIFIED 分类初筛**：**13 个强验证**（`cd X && node -e "require(...)"`——实际执行被测代码/测试，非"文件存在即验证"）+ 14 个待精判（分类脚本只看命令首词，`cd && node` 链可能被漏判为弱——**精化归砺**）
- 对比旧引擎（首轮跑测）：**"ls 文件存在即 VERIFIED"的 11 个裸授予在新引擎 B/G-A 段归零**；A 段 VERIFIED 均有命令依据（强度分类待精判）

## 四、⚠️ 未解决：REPL 收尾三行运行时缺口（返工①的运行时验证失败）

**返工接线已确认在位**（源码 2 处、二进制 4 处字符串、cargo clean 全量构建、`--version`=0.2.24），**但 R42 重放中收尾三行输出 = 0**。

**已排除**：引擎身份（0.2.24 实测）、二进制内容（grep -a 实证）、源码同步（md5 对比）、cargo 缓存（clean 全量构建）。

**剩余假设**（待 REPL 前台人工复验定位）：REPL 交互模式（`set_interactive(true)`）与 one-shot 的事件/渲染路径差异，或 PTY 捕获层丢失。**one-shot（chat）路径收尾三行实测正常输出**（16:50 前台验证）。

**处置建议**：W-E/G-D/W-D 的 REPL 投影缺口维持"已知限制"；**REPL 前台人工复验**（`hearth repl` 跑一轮看终端）与 W-Fa 同批执行。

## 五、G-A 直答形态（10 题）

completed 2 / failed 8——**8 个 failed 的构成需逐题对账**（疑问句被当任务执行 → give_up 的 A1 观察点问题是否复现）。逐题正文对账随证据包 v1.1（机器提取 + 砺逐题复核）。

## 六、待办

1. 专项二强制阅读轮复验（R4.1 队列）；
2. G-A 10 题逐题正文对账（v1.1）；
3. W-Fa + REPL 收尾三行**前台人工复验**（同批）；
4. VERIFIED 27 轮强度精判（砺）；
5. 全部判读齐 → 用户体感测试 → "可用"宣告。
