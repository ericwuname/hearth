# R4 证据 · 语料 A 重放初判数据（执行窗机器提取，判读归砺）

> **跑测**：方案二定稿执行｜引擎 `70730e7`｜.133｜A 语料 31 轮逐字重放（07:00–07:40，单 session `d569cb31`）
> **证据文件**：`r4-replay-A.log`（263,440 B，sha256 见文件头注释提取脚本产出）｜`analysis-A-giveup-pairs.md`（give_up→终态对账）
> **性质**：机器提取数据 + 执行窗读数；**判读权在砺**

---

## 一、终态分布（30 终态行 / 31 用户轮）

| 终态 | 计数 | 细分 |
|---|---|---|
| Task completed | 18 | UNVERIFIED "目标达成（未验证）" **7**；VERIFIED "目标达成" **1**（25 步）；其余 10 为含 R3-1 之外的 completed 行（待砺对账） |
| Task failed | 12 | 全部携带 R3-1 口径提示"报告含已完成/剩余工作，可直接下指令继续" |

## 二、G-C 判读材料（give_up→终态对账）

| 项 | 读数 |
|---|---|
| give_up 出现 | **13 处**（0.9-0.3 原会话 10 处——重放中 give_up 位置随 LLM 非确定性自然漂移，符合预期） |
| **改判路径触发** | **0 次**（diagnostics.log：`GIVE_UP_ROUTED_TO_DONE` 重放期间 0 条新记录，仅存 09-02 旧记录 2 条；`R1_1_REROUTE_REJECTED` 0 条） |
| give_up 后终态 | 8 failed；3 个 give_up 行后最近的 completed-UNVERIFIED **经核实为后续轮次正常完成（假配对，非改判）**；1 VERIFIED；其余配对见对账表 |
| **裸改判（③臂）** | **0 命中** ✅——13 处 give_up 无一被静默改判为无警示完成 |
| 判读提示 | 改判分支未触发 ≠ ③臂通过 certify——按定稿 G-C v2：S1/S2'/S3 对应轮次处置由砺逐场景判；改判未触发的场景按 N/A 出口处置（预注册） |

## 三、其他门机器读数（A 段）

| 门 | 读数 | 初判 |
|---|---|---|
| **G-H Observe** | ⚠️ diagnostics 检查待做（`observe_verdict` 每轮写入的日志形态需按新引擎日志格式定位）——B 段跑完后统一提取 | 待判 |
| **R3-1 收尾三行** | ⚠️ **`── 收尾` 计数 = 0**——**REPL 路径疑似未接 R3-1 渲染**（收尾三行代码在 one-shot 终态渲染函数，REPL 走 render_agent_event 路径） | **W-E 在 REPL 形态部分不满足——如实登记，判据冻结期不修代码，修复进 R4 判读后的返工包** |
| R3-2 goal_drift | `goal_drift` 出现 **4 次**（警示链在） | ✅ 正向信号 |
| R1-1 投影 | "未验证"7 处、裸"目标达成，产物见报告$"（VERIFIED 且非改判）1 处 | 无裸 UNVERIFIED 违规 |
| 渲染瑕疵 | failed 行出现双 ✗ 前缀（"✗   ✗ Task failed"）——error() 加 ✗ 与上层模板叠加 | UI 小瑕疵，登记不阻塞 |

## 四、与 0.9-0.3 原会话的对照锚点（砺判读用）

| 维度 | 0.9-0.3 原会话 | A 重放（新引擎） |
|---|---|---|
| give_up 处置 | 7/28 假完成（静默改判） | **0 假完成**（13 处 give_up 全部如实呈现） |
| 已知失败 | 0/16 标 ✅ | 待专项一场景判读（FileBrowser 轮次对账） |
| 完成核验 | 0/28 经核验 | completed 18 中 UNVERIFIED 7 + VERIFIED 1 + 其余待对账 |

## 五、待办（B 完成后）

1. B 语料重放日志同口径提取（专项一 FileBrowser / 专项二 103% 假事实两专项主战场）；
2. G-H diagnostics 提取（observe_verdict 形态定位）；
3. G-A/W-A 疑问句 10 题对账（盲测包重放——方案一 §二）；
4. W-Fa `--quiet` 静默计数跑测；
5. 全部证据包交砺判读。
