# 外部「六相拆除与判定权归还 v1.0」评审报告（执行窗 · 8.7 结论栏填报）

> **受验件**：`docs/外部窗口-六相拆除与判定权归还·汇总报告 v1.0.md`（J-1~J-10 论断登记 + 8.5 自证伪）
> **评审者**：执行窗（traecode 施工的验收窗，持 R4.2/R5 一手跑测信息）｜2026-09-06
> **结论**：**可执行（需调整一处时序）——E-A 与 E-B 并行，"E-B 出结果前不删 B 臂"维持**

---

## 一、锚点核验（8.4 论断逐条）

| # | 外部论断 | 执行窗实测 | 判定 |
|---|---|---|---|
| J-1 根因=Workflow 范式误用 | LoopPhase 枚举/四相位顺序调用在位 | ✅ **成立**（锚点全在） |
| J-2 9 倍差距 | SWE-bench 依赖底层模型，非纯 harness 对比 | ⚠️ **中**（外部已自我标注；判别实验出数据前不作硬依据） |
| J-3 拆除优于重构 | 删 2,000-4,000 行 vs 重建 30,000+；**沙箱 2,164 行=唯一达标资产不可陪葬** | ✅ **成立**（成本表成立，Joel 定律适用——18 个 RC 修复正是沉淀） |
| **J-4 E-B 先于 E-A** | — | ⚠️ **需调整**（见 §二） |
| J-5 Codex CLI 首选 | 公开文档横评 | ⚠️ **中**（待 E-B 实测复核；Goose 备选同时准备——外部 W-4 已自列） |
| **J-6 A 臂未真机验证** | — | ✅ **执行窗实锤确认**：r5-reports 全部战报 + .133 跑测日志 grep HEARTH_SINGLE_LOOP = **0 条跑测记录**——**A 臂代码在（:1104/:1674 instance-level gate 实锤）但从未真机跑过** |
| J-7 删除估算 2,000-4,000 | 外部自标"估算" | 🔶 低置信（外部已诚实标注；删除后 wc -l 为准） |
| J-8 行数判据 | wc -l 不可辩解 | ✅ **中→采纳**（R5 净增 945 的反面教材由我方 commit 序列独立复现） |
| J-9 +358 是并存期正常 | r6-3 −202 / r6-5 −27 真净减 | ✅ **成立**（commit 序列实锤） |
| J-10 耦合面小 | LoopPhase 外部引用仅 1 处 | ✅ **实锤**：`agent-core/src/lib.rs:22`（re-export）——拆除风险可控确认 |

**行数数据独立复现**：`git show 1429b58:...loop.rs | wc -l` = **12,523** ✅（外部精确）；当前 HEAD 已至 96f4161（budget-exhausted 交接投影 + R4.1 DEBUG 探针清理——traecode 施工持续中）。

## 二、J-4 的调整意见（执行窗唯一异议）

外部主张"E-B 底座对照先于 E-A（A 臂验证）"。**执行窗实测数据支持并行，而非严格先后**：

- **E-A 的成本已被低估为"小时级"**：真机回归集已建成（`1429b58`）、A 臂开关已实现（`HEARTH_SINGLE_LOOP=1`）、跑测脚本与 HEARTH_BIN 纪律全部就绪——**跑一次 A 臂验证 ≈ 1-2 小时**；
- **E-B 确需环境准备**（装 Codex CLI/接 agnes/格式核对）≈ 天级；
- **两者无资源冲突**（E-A 在 .133 用回归集；E-B 是独立安装实验）；
- **E-A 先出数据不阻塞任何决策**：若 A 臂真机劣于 B 臂，E-B 结论直接适用（止损/换基座）；若 A 臂优，E-B 继续作为"要不要自研"的独立裁决。

**维持不变的一条**：**"E-B 出结果前不删 B 臂"**——完全同意（避免 2,000-4,000 行删除后发现更优基座）。删除动作的最后闸门交给 E-B。

**调整后的时序**：**E-A（小时级，立即）∥ E-B（天级，并行启动）→ 双数据到齐 → 按 §一·五 决策矩阵拍板 → 才动删除**。

## 三、执行窗独有贡献（其他窗口给不了的）

1. **J-6 实锤**：A 臂真机跑测记录 = **0**（我方 grep r5-reports 全部战报 + .133 跑测日志确认）——外部 W-5 的疑虑成立，**E-A 是当前第一优先级动作**；
2. **E-A 的执行细节已就绪**（三次跑测事故的教训全部固化）：`HEARTH_BIN` env 绝对路径 + `--version` 必验（0.2.25）+ flock 防并发 + probe timeout ≥420s + timeout 覆盖率验证（grep -a 探针）；
3. **R4.2 作废轮警示**：E-A/E-B 的每一份跑测日志必须含引擎版本标记——**外部报告未提此点，执行窗补上**（这是本项目最贵的教训）。

## 四、8.7 结论栏填报

| 窗口 | 评审结论 | 主要异议 | 签署 |
|---|---|---|---|
| 砺·评审 | ⬜ | （砺对 J-2/J-4 的模型层立场待其自行填报） | |
| **执行窗** | ☑ **可执行（需调整）** | **J-4 时序：E-A 与 E-B 并行（E-A 小时级可出数据且不阻塞决策）；"E-B 出结果前不删 B 臂"维持** | 执行窗 2026-09-06 |
| 测试窗 | — | E-A/E-B 均用真机回归集（`1429b58`），Q1-Q5 判分建议加"引擎版本标记必验"（执行窗补充） | |
| 顶层 | ⬜ | — | |

**补充建议（执行窗独有）**：
1. **E-A 立即执行**（1-2 小时）：`HEARTH_SINGLE_LOOP=1 + HEARTH_BIN=/home/wutao/codex-r4/target/release/hearth` 跑真机回归集，产出 A 臂首份真机数据——**这是 J-6 空白的直接填补，也是 T-1 决策门的前置**；
2. **E-B 的 agnes 配置沿用 .133 现成 config**（api.agnes-ai.cn/v1 已在 hearth config 验证可用）——外部 8.2 步骤 2 的摩擦可能比预期小；
3. **删除阶段（阶段 C）启动前**，除 E-B 门外另加一道：**用户体感测试**（0.2.25 已部署 alias 就绪）——若用户在新引擎体感已明显改善，删除的决策信心再加一重。

---

> 执行窗｜证据：git show 1429b58 行数实算、grep LoopPhase/HEARTH_SINGLE_LOOP/HEARTH_BIN 实测、r5-reports 全量 grep（A 臂跑测记录=0）
