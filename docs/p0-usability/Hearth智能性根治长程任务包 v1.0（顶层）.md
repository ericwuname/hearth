# Hearth 智能性根治长程任务包 v1.0（顶层·一次签发）

> **应令**：用户 2026-09-06 02:26——"不要第一批第二批，出个长程任务包"。**本包一次签发全部施工项，不分批**；包内"顺序"是依赖图，不是审批切分。
> **上位依据**：《Hearth可用性根因分析-对照WorkBuddy（外部窗口）》（五轮、13 条根因）+ 执行窗验证报告（10/10 锚点）+ R4/R4.1 动态跑测证据（两链互证）+《R4.1证据包·砺判读》。
> **分支**：`p0-usability-01` 延续；**日期**：2026-09-06　**基线**：0.2.24（sticky VERIFIED 修复后，501/0）

---

## 〇、定性：与已宣告成果的关系（一句话）

R1–R4 修好了**诚实性层**（不说谎/不失忆/不淹没，宣告路径 (b) 引擎行为层达标）；**本包根治智能性层**——用户体感差的剩余瓶颈（同错不教学、环境盲、信息残缺、高温空转）。两层分开记账，宣告 (b) 不因本包推翻，本包不因 (b) 减项。

**根因-施工项全映射**（13 条根因无遗漏）：

| 外部根因 | 状态 | 本包落点 |
|---|---|---|
| 一 六次独立 LLM 调用 | 🔶 条件触发 | T-1（决策门=R5-7 A/B 数据） |
| 二 环境盲 | ⬜ | R5-4 |
| 三 死流程 prompt | ⬜ | R5-10 |
| 四 工具契约贫瘠 | ⬜ | R5-9 |
| 五 完成判定 keyed to 写盘 | 🔶 半修 | R5-11（R1-1 已重造改判语义，本件补纯回答型） |
| 六 Observe 空转 | ✅ R1-3 真修复（外部 §十四自我修正确认） | 消费端缺口→R5-1 |
| 七 历史硬切片 | ✅ R2-2/R2-3（写入+摘要侧） | 读回侧→R5-6 |
| 八 截断无行号 | ⬜ | R5-5 |
| 九 VERIFIED 不看退出码 | 🔶 R4.1 半修 | R5-8 |
| 十 错误回喂无引导 | ⬜ | R5-2 |
| 十一 temperature | ⬜ | R5-7 |
| 十二 宪法无执行点 | ⬜ | R5-12 |
| **十三 事实不入决策**（本轮最有价值） | ⬜ | **R5-1/R5-2/R5-3（通路层，优先最高）** |

---

## 一、包 A · 通路层（根因十三——"它有记忆，但记忆没接线到大脑"）

> 外部定位：Observe 判定、失败分类、恢复策略、切片归档**事实已生成**，但消费者止步于测试断言和给人看的报告，**没有一条通路喂回下一次 LLM 决策**。成本低得反常（注入模板有既有先例）。

| # | 项 | 坐标（施工前重 grep） | 语义 | 判据（可证伪，预注册） |
|---|---|---|---|---|
| **R5-1** | **事实注入**：`observe_verdict` + `last_failure_class` + `last_recovery_strategy` 注入下一轮 Plan/Act messages | `build_messages()`（~:2745 有 `retrieval_context`/`lsp_diagnostics` 注入先例，照抄） | 策略算出来必须回到决策者 | C 语料 L285 场景：同错重复 **3→≤2**，且**第二次命令必须与第一次不同** |
| **R5-2** | **错误回喂结构化**：`ERROR: {raw}` → `ERROR[class=..] strategy=.. suggestion=..` | `loop.rs:2684-2697` | 恢复引导随错误一起到达 | 同 R5-1 判据 |
| **R5-3** | **强制换策略**：`same_tool_repeat >= 2` 触发换策略（而非只累加到 give_up） | `loop.rs:4498-4505` | same_tool_repeat 累积成"换个做法"而不是放弃 | 全语料"同错重复 3 次"场景数 → **0** |

**施工纪律（外部建议采纳）**：R5-1 判据必须**能失败**——同错 3 次现在可复现，改完实测不降即视为未修。**不靠"代码里写了"验收，靠语料计数验收。**

## 二、包 B · 信息完备层

| # | 项 | 坐标 | 语义 | 判据 |
|---|---|---|---|---|
| **R5-4** | **环境上下文注入**：cwd/项目根/技术栈/顶层文件树（限 N 条）/git 分支进 system prompt | `loop.rs:2337` 附近 | 第一轮不再瞎猜 | **用户原话场景单次判负**："我们的 hearth TUI 项目做的进度如何了"——首轮不得搜 `*.go` |
| **R5-5** | **read 加 offset/limit + 行号**；truncate 口径（6000/4000/1500）同步审视 | `tools-builtin/src/read.rs`、`truncate_tool_output :5658` | 解"82k 字符截断"死结（引擎亲口承认过） | 大文件可分页读取且带行号；截断提示含"如何读剩余部分" |
| **R5-6** | **archive 读回通道**：R2-2 已落盘（archive/<sid>.jsonl），本件补检索恢复——切片提示的 grep 通道升级为结构化读回（按相关性或摘要注入） | `context.rs:366`（写侧已有，读侧全仓为 0） | 事实换个地方销毁 → 事实可找回 | 跨 10 轮以上对话问"还记得早期事实吗"可答（引用归档内容） |

## 三、包 C · 行为校准层

| # | 项 | 坐标 | 语义 | 判据 |
|---|---|---|---|---|
| **R5-7** | **temperature 分档**：Act 0.0–0.2 / Plan 0.2–0.3；主循环告别 0.5 高温；`max_tokens` 收敛审视；bridge 侧 0.8 一并核对 | `loop.rs:3362`、`:1381`、bridge | 消灭随机性空转燃料 | C 语料 A/B：规划次数/工具出错/Plan 耗时三指标实测对照 |
| **R5-8** | **verification 看退出码**：非零退出码不得点亮 VERIFIED | `:910` / `:2717` / `:5405` | 失败命令拿 VERIFIED 的合法性来源切断 | 构造"失败命令 + VERIFIED"场景必红（机器断言） |
| **R5-9** | **工具描述重写**：每工具补"何时用/何时不用/示例/边界/错误解读" | `tools-builtin/src/{read,bash,grep,glob,...}.rs` 各 `description()` | 弱模型表现的直接杠杆 | 各工具描述含五要素；G 门回归全绿 |
| **R5-10** | **prompt 死流程→原则**：删 `follow strictly`/`NEVER more than once`/`MUST end by write_file`；保留安全边界，改给决策原则 | `loop.rs:2334-2383` | 解除假完成的机制压力 | 既有回归测试全绿（含 R1/R2 全部门）；"问答类目标"不再被压去写盘 |
| **R5-11** | **纯回答型完成**：以"用户目标是否满足"为准，问答/诊断类目标允许零写盘完成 | Reflect 决策依据；**与 R1-1 三臂语义不回退**（改判仍 UNVERIFIED+不计成功率） | "问进度"不再被当任务跑 5 相位 | 疑问句/诊断类目标零写盘 completed 路径存在且 G-C 门不回退 |
| **R5-12** | **宪法配执行点**：第 1 条 Verify、第 5 条 Outsider view 落进架构执行位 | `constitution.rs` ↔ 相位机 | 团队不缺正确认知，缺的是把认知落进机制 | 宪法条文↔执行点映射表落盘；可抽查触发 |

## 四、清欠件（宣告 (b) 的欠账，与本包同批施工/复验）

| # | 项 | 来源 |
|---|---|---|
| Q-1 | R3-1 REPL 前台复验 + W-Fa（分钟级，真终端看一眼定位渲染链 vs 捕获层） | 砺 R4.1 判读 #2 |
| Q-2 | 专项二（假事实强制阅读）0.2.24 复验，1 轮 | 砺 #3 |
| Q-3 | VERIFIED 审计重分类（`bash -c` 解包语义；与 sticky 修复同批自动重跑；初筛分类器首词法同病一并修） | 砺 #4 |

## 五、条件触发项（唯一两项——这是"数据决定立项"，不是分批）

| # | 项 | 触发条件 | 说明 |
|---|---|---|---|
| **T-1** | **P0-3 六相收敛**（Plan 内联进 Act 思维链；Observe 启用或删除） | **R5-7 的 A/B 数据**：若温度分档后规划空转仍高 → 立项（架构级，届时单独立项书）；若大降 → 降级观察 | 外部原设计：P0-0 是 P0-3 的**低成本探路**——先消灭随机性燃料，再判断架构是否真要动 |
| **T-2** | 拆 `loop.rs`（11,220 行单文件按相位分模块） | 本包全部判据验收 + 砺判读之后单独立项 | 可维护性工程；**纯重构不改行为**为硬约束。登记在案，防"每次修复都在加重病灶" |

## 六、总验收（预注册，跑前锁死）

| # | 判据 | 通过线 |
|---|---|---|
| 1 | **用户原话场景**："我们的 hearth TUI 项目做的进度如何了" | 首轮不得搜 `*.go`；首屏有结论句——**单次可判负** |
| 2 | C 语料 L285 | 同错重复 ≤2 且第二次命令不同 |
| 3 | 全语料 | "同错重复 3 次"场景数 = 0；规划草案重复率如实报告 |
| 4 | **不回退**：G-B/C/E/F + W-B/C + 零复发四硬层 | 500/0 门禁口径不倒退；G-B 一票否决持续有效 |
| 5 | R5-8 专项 | "失败命令+VERIFIED"= 0 |
| 6 | R5-6 专项 | 跨 10 轮"早期事实"可答 |
| 7 | 引擎身份 | 版本号可区分（0.2.25 或 0.2.24.x，执行窗定） |
| 8 | 砺判读 + 用户体感测试 | 体感结论并入（模型上限边界重申：G-A 8/10 线不因 harness而解，砺条款维持） |

**"智能性改善"宣告的操作定义**：上述 1-7 全绿 + 砺判读确认 → 呈用户。诚实边界随报告同呈：本包治**通路/信息/校准**，模型生成质量上限（agnes-2.5-flash）另账，由校准观测与模型渠道解决。

## 七、施工顺序（依赖图——一次签发，顺序施工）

```
包A通路层 R5-1→R5-2→R5-3 ─┐
包B R5-4→R5-5→R5-6 ────────┼→ R5-7（A/B 数据出口）→ T-1 决策门
包C R5-8→R5-9→R5-10→R5-11→R5-12 ─┘
清欠 Q-1/Q-2/Q-3（并行，分钟~小时级）
全部判据绿 → 砺判读 → 呈用户 → T-2 单独立项
```

各包内部无审批间隙：**每件完工交战报（病理→语义→判据），砺随批判读，顶层只看里程碑与例外**。例外（判负、判据失灵、范围外发现）即时上报，不攒。

## 八、签发栏

| 窗口 | 意见 | 日期 |
|---|---|---|
| 顶层（人工窗） | ☑ **长程任务包一次签发**：R5-1~R5-12 十二件 + 清欠三件 + 条件触发两项；总验收预注册；撤回"第一批/第二批"表述 | 2026-09-06 |
| 执行窗 | ⬜ 按依赖图施工 | |
| 砺·评审 | ⬜ 随批判读 | |
