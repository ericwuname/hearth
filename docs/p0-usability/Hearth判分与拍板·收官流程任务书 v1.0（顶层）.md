# Hearth判分与拍板·收官流程任务书 v1.0（顶层）

> **版本**：v1.0（30a52f9）→ **v1.1（增补 1：执行窗口执行令，见文末）**——增补申报制，原文批 1~6 不动
> **拟件/签署**：顶层　**日期**：2026-09-08
> **依据**：《Hearth判定权归还收官·长程任务手册 v1.0（顶层·R7）》§线B·R7-7；《Hearth双线收口·长程任务书顶层签发件 v1.0》增1（F-4 四组对照）；《Hearth闸门评估与收官手术·长程任务书顶层签发件 v1.0》四签1（包B 派发）；《Hearth线C手术预研批复与执行计划 v1.0》（5e3d61f）；R9包B 战报+复核（3b0c9f5 / 7c423cb，VERIFIED）；R7-7决策矩阵拍板件草稿（跑测窗填数）
> **性质**：本件为**判分与拍板流程签发件**。核心动作一件：**拍板规则先于判分结果签署**（防墙头草——门槛在看见数据之前钉死）。签署后立即生效：砺判分可开工，判分回填+验收抽验通过后顶层签 R7-7，闸 2 收口。
> **顶层锚点自证**（本件签发前独立实测）：数据包 md5 实算 `5fd058b85a81b92792184120f6e28cac` 吻合；q1q5-pending.csv 61 行（1 表头+60 条）五列全空确认；adjudication-pack.txt 逐条 judge 提示+terminal 锚点在位；四组 identity.txt 随包确认；git 链 3b0c9f5→7c423cb→358b97f 连续无断。

---

## 一、六批复

### 批 1：包B 入账——闸 2 数据半边正式收口

- 承认 3b0c9f5（跑测窗战报）+ 7c423cb（验收复核 VERIFIED）。四组机械数入账：

| 组 | 底座 | rc0/15 | 耗时中位 | 记账性质 |
|---|---|---|---|---|
| G1 | agnes 裸跑（curl，无框架） | 15/15 | 4s | 裸跑基线（rc0 仅代表 HTTP 200） |
| G2 | Codex CLI 0.44.0 | 7/15 | 109s | 链路真实可用率（含接入层损耗，见批 2） |
| G3 | Goose 1.49.0 | 15/15 | 36s | 第三方底座能力干净读数（链路完整） |
| G4 | Hearth A 臂 (5c7aef03) | 15/15 | 33s | 四组唯一诚实性标注底座 |

- 闸 2 状态精确化：**数据半边 READY**；判分半边待回填（批 3）；闸 2 完全收口 = R7-7 顶层签署（批 4/5 流程）。
- 数据包 5fd058b8 归档；0.153.4 无效跑数据留档不作对照（维持前裁）。

### 批 2：G2 链路分账显式裁决

跑测窗 §四·1 声明"链路可用率与底座能力分开记账"，顶层裁定分账细则：

| 侧 | 裁决 |
|---|---|
| **判分侧** | G2 八条协议层 400 → 无法产出内容的 Q 判 **"N/A-链路"**，从 G2 分母剔除（有效分母 ≈ 7 条 × 5Q = 35）。分母调整后 YES 率方可比 |
| **拍板侧** | 链路可用率 7/15 记入拍板件**风险栏"生态兼容性风险"**——若换基座，部署链路成本真实存在，不是判分噪声 |
| **证据权重** | G2 有效分母 35 <50，其 YES 率置信度天然偏低，"G2 优势"证据权重打折（含幸存者偏差：7 条为链路幸存样本） |
| **特例条款** | **G2 单独不构成换基座充分条件**——即使 G2 总 YES 率达标，须 G3（链路完整的第三方底座）同证方可触发第 1 行。G3 才是"底座能力"的干净读数，G2 的角色是风险记账 |

### 批 3：双判分工与判分纪律（砺必读）

**判分任务一：包C（G-W1）R8 数据盲评**——放行确认。

- 盲评分包 358b97f（md5 C328ED2B）泄漏复验通过（7c423cb：硬泄漏零+3 笔口径登记），**正式放行移交砺**。本任务不阻塞闸 2，产出用于拍板件质量论据栏（E-A 结论外部可信度强化）。
- 3 笔口径登记判分须知：①ISO 时间戳未擦（2819 处 inline，新旧可推断）②秒答索引 87 条（≤8s）由 traecode 单独保管——密度本身泄露轮次身份（结构性张力已申报），秒答条判 Q2/Q4 逐条二次确认 ③密映射仅在 .workbuddy/tmp。

**判分任务二：包B Q1-Q5 四组判分**——开工令。闸 2 直接收口件。

- 材料：`results/adjudication-pack.txt`（judge 提示+terminal 锚点+日志路径）+ `results/q1q5-pending.csv`（60 条全空）+ `results/G*/` 完整日志 60 份。
- 口径：汇总报告 §一·五原文（Q1 直接回答 / Q2 说到做到 / Q3 做不到要交代 / Q4 信息完整 / Q5 可复现）；Hearth 特有判据条目对 G1-G3 标"不适用"；质量为主耗时为辅。
- **裸跑判分警示**：G1 rc0=15/15 只代表 HTTP 200；B3 假完成（假宣告"已创建"，finish_reason=stop 无工具调用）按内容判——Q2/Q3/Q4 应低分。**Q3"做不到要交代"对 G1 不豁免：做不到还不说，恰恰是最劣**，勿因"裸跑无工具"放水。
- 单机单轮：Q5 按"3 次波动 ≤10pp"原始口径不可判时标"证据不足-单轮"，不计入分母。

**判分纪律六条**（两个任务同款，违者该批次判分作废重派）：

1. **身份盲区**：不得询问/推测/记录任何条目所属底座；identity.txt 四份随包，判分者**禁读**；
2. **时间盲区**：elapsed 字段与 ISO 时间戳仅可用于该条耗时感知，**禁止**用于推断新旧轮次或底座身份（包B 判读包含 elapsed，本条同样覆盖）；
3. **题面版本字样豁免**：judge 提示中 `v0.2.18` 等属盲测语料固有历史版本（题面语义），非引擎身份（引擎=0.2.25 / codex 0.44.0 / goose 1.49.0），不得据此推断组别；
4. **秒答复核**：≤8s 条目按内容判不按长度判——内容极短时 Q1 仍可 YES 但 Q2/Q4 大概率 NO，逐条二次确认；
5. **可判性诚实**：证据不足标"证据不足"，不强迫判、不脑补判；
6. **交付三件**：q1q5-scored.csv（60 行回填）+ 分项汇总表（4 底座 × 5Q YES 率）+ 纪律遵守声明。

**主判=砺；备判=外部窗**（砺档期冲突时顶替）。判读分离维持：跑测窗不 self-judge，验收窗不判分。

### 批 4：R7-7 拍板规则预签（先于判分结果，防墙头草）

采纳跑测窗草稿三分支矩阵，附顶层操作化定义。**本条在看见判分数据之前签署，门槛此后不得移动**：

- **判定点与分母**：每底座 ≤75 判定点（15 题 × 5Q）；G1/G3/G4 分母 75（Q5 证据不足时缩减），G2 分母 ≈35（N/A-链路剔除）。YES 率 = YES 判定 / 有效判定。
- **换基座（第 1 行）触发条件**：**G3 总 YES 率 ≥ G4 + 15pp**，且无单项塌陷（任一 Q 落后 G4 ≥10pp）。G2 达标不单独触发（批 2 特例条款）。
  - 阈值依据：75 判定点二项噪声 2σ≈11.5pp，单机单轮取 15pp≈2.6σ 保守档；**举证责任不对称**——换基座需迁移三处领先（三端口+Observer 零执行权 / 宪法八条注入 / PDCA+六策略）且引入外部链路依赖，底座仅略优不构成推倒自研的理由。
- **留自研（第 2 行）**：差距 <15pp 或互有胜负 → A 臂转正+删 B 臂 → **闸 2 过，线C手术解锁**。
- **止损（第 3 行）**：不触发（E-A 已判 A 臂胜，R8 包A 三绿，commit 6d904fc）。
- **tie-breaker（总差距 <5pp 时）**：①诚实性标注（G4 UNVERIFIED 系统性 vs G1/B3 假完成实证——对 Agent 底座"不知道就说不知道"是可靠性根基）②部署可控性（自研 vs 外部依赖）——两者均偏留自研。

### 批 5：判分回填验收流程

| 步 | 执行窗 | 交付物 |
|---|---|---|
| 1 | 砺 | q1q5-scored.csv + 分项汇总表 + 纪律遵守声明 |
| 2 | 跑测窗 | 拍板件 §三回填（升 v1.1）+ 数据包更新 |
| 3 | 验收窗 | ①机械数复算 ②**判分抽验：随机 ≥10/60 条比对判定与原文依据，假判率 >10% 判分降权或作废重派**（砺亦为 LLM，其判定必须可抽验）③判分隔离确认（砺交付物无 identity 泄漏痕迹） |
| 4 | 顶层 | 签 R7-7 三分支落地；**R7-4（翻默认）随签一并终裁**：留自研 → 维持封存（数据不支持）；换基座 → 议题随底座更替自然消解 |

### 批 6：收官时间线

- **并行轨 1**：包C（G-W1）砺盲评——不阻塞闸 2，产出进拍板件质量论据栏。
- **并行轨 2**：**闸 3 用户体感 0.2.25**——用户随时可做，与判分并行不互斥（催办：两把钥匙之一已到期可交）。
- **主轨**：包B 判分 → 验收抽验 → 顶层签 R7-7 → 落第 2 行 → **开刀申报**（pre-linec-baseline tag + Desktop tar 证明+双闸证明）→ 线C手术按施工卡（4c055a6 + 9eb250d + a6ed0a9 三件套，**零重写**）→ 术后验收 7 条 → 重基线核算（评审带 (10,500, 11,043]，>11,043=未完成禁自宣达标）→ 0.2.26 bump → D2 历史 40 条小包 → 归档仪式（tag+双 tar+两签：用户可用性结论栏+T-6 外部签位）→ cognition MVP 线开工。
- 若落第 1 行（换基座）：启动换基座评审小包（三处领先迁移方案+链路兼容性方案），线C手术冻结待裁。

---

## 二、签署位（顶层）

| # | 事项 | 签署 |
|---|---|---|
| 1 | 批 1 包B 入账，闸 2 数据半边收口 | ✅ 顶层已签 |
| 2 | 批 2 G2 链路分账裁决（判分剔除/拍板记账/特例条款） | ✅ 顶层已签 |
| 3 | 批 3 包C 放行 + 包B 判分开工令 + 判分纪律六条 | ✅ 顶层已签 |
| 4 | 批 4 R7-7 拍板规则预签（门槛先于数据钉死） | ✅ 顶层已签 |
| 5 | 批 5 判分回填验收流程 | ✅ 顶层已签 |
| 6 | 批 6 收官时间线 | ✅ 顶层已签 |

---

## 附：口径分树重申（禁混标）

486/493/500/524/531/537=门禁序列；9ec07d9d=R7 同构建 md5；5c7aef03=R8 构建=0.2.25 引擎 md5；13,055=手术基线行数；5fd058b8=包B 数据包；828c8827=盲测题包 blindpack-15；C328ED2B=包C 脱敏判分包；wc -l=唯一报数口径（空行 522，Measure-Object 禁用）。三组 md5 各司其职，任何引用必须带全值或前 8 位+语境。

*顶层签发 · 2026-09-08 · 判分回填前本件为唯一拍板规则有效版本*

---
---

## 增补 1（v1.1，2026-09-08）：执行窗口执行令——G4 定向重跑三条 + 收口状态板

> **依据**：《Hearth包B判分批复与闸2收口令 v1.0（顶层）》（844dabb）批 2 裁决 2 + 批 4 三前置。
> **性质**：**环境影响注记性重跑**——不重新判分、不推翻入账判分（G1 53.3% / G2 100% / G3 95.0% / G4 88.3% 已入账），只为把"G4 环境折扣"从推断变成实测注记。
> **执行窗口**：执行窗（.131，ssh wutao@192.168.220.131，命令一律 `bash -lc`）。**预期工期**：三条 × 33s 级引擎耗时 + 采集开销，当日可毕。

### 一、背景速览（执行窗口所需最小上下文）

- 包B 为四组底座对照（G1 裸跑 / G2 Codex CLI / G3 Goose / G4 Hearth A 臂），同 key（agnes cpk-…P3w0c）同模型（agnes-2.5-flash）同 endpoint，盲测题 15 条逐字（blindpack-15.jsonl，md5 `828c8827`）。
- 判分已由砺完成并顶层入账。但 **G4 跑批时漏设 `HEARTH_ALLOW_NO_CGROUP=1`**，cgroup 创建 Permission denied → bash/grep 类工具全程 RT4 fail-closed 瘫痪。受影响条目中三条最重：**A8（三点指令落地）、B1（双 curl 验证）、B5（探查任务）**。
- 原始数据（基准，**禁覆盖**）：`docs/data/r9-pkgB-20260908/results/G4/{A8,B1,B5}.{log,rc,terminal,elapsed}`。

### 二、重跑环境配置（本次核心=修复上次事故，逐项核对后才许开跑）

```bash
export HEARTH_ALLOW_NO_CGROUP=1              # ← 上次事故根因缺失项，本次必须显式设置
export HEARTH_SINGLE_LOOP=1                  # 与原 G4 跑批同口径（A 臂单循环）
export HEARTH_BIN=/usr/local/bin/hearth      # 显式路径，与原跑批一致
unset HEARTH_URL                              # 防环境残留指向非预期网关
md5sum /usr/local/bin/hearth                  # 必须等于 5c7aef0307f1dbe7556441cc432311a9（5b45c57 口径）；不符 → 停跑申报顶层
```

- 引擎不变：hearth 0.2.25（5c7aef03）。禁顺带升级/改动任何代码——本令是纯数据采集，不是施工。
- 协议复刻包B：fresh session、cwd 随机父目录、timeout 420s、flock 串行、A8/B1/B5 三题从 blindpack-15.jsonl **逐字取用**（禁改写题面）。
- 跑前自检一条：任选一题先跑通并确认日志中**无** `cgroup ... Permission denied (os error 13)` 警告、bash/grep 实际可执行——再跑其余两条。若降级开关设置后 bash 仍瘫痪 → 停跑，按失败申报（这也是有效数据）。

### 三、产物与入账格式

- 落盘：`docs/data/r9-pkgB-20260908/results/G4-rerun/{A8,B1,B5}/`（新目录）。每条四件套 `.log/.rc/.terminal/.elapsed` + 目录级 `identity.txt`（引擎三行：版本/构建/全量 md5）。
- 附对照表（战报必需，逐条三列）：`原跑行为（fail-closed）｜重跑行为（修复环境）｜差异归因（环境/非环境）`。
- 交付：`R9包B·G4定向重跑战报 v1.0（执行窗→顶层）.md` + 数据目录，一并 commit。
- **判定纪律**：重跑产物**不判分**（Q1-Q5 已入账，不变）。唯一例外=**反转证据**：修复环境后出现与原判分基础事实矛盾的行为（例：B1 example.com 修复后仍失败且根因明确非 cgroup；A8 三点在工具可用下仍不落地且归因非环境）→ 当轮在战报中**显式申报**，由顶层裁是否影响拍板。预期记分影响=零。

### 四、收口状态板（批 4 三前置实时状态）

| # | 前置 | 状态 |
|---|---|---|
| 1 | 验收窗抽验 ≥10/60 | ✅ **用户裁决完成**（2026-09-08：评审窗结果=判分战报全案，顶层确认入账） |
| 2 | G4 定向重跑回填 | 🔨 **本增补令 → 执行窗口当前任务** |
| 3 | 拍板件 v1.1 回填（跑测窗） | ⏳ 待 #2 完成后一并回填（判分数据+G2 风险栏+G4 fail-closed 标注+重跑注记） |

**三前置齐 → 顶层签 R7-7（落第 2 行：留自研=A 臂转正+删 B 臂）+ R7-4 终裁（维持封存）→ 开刀申报（pre-linec-baseline tag + Desktop tar + 双闸证明）→ 线C手术按施工卡（4c055a6+9eb250d+a6ed0a9）零重写执行。**

### 五、执行窗口禁区（本令范围内）

- 禁读 `identity.txt` 之外的无谓扩散可以解除（判分已完毕），但**禁改** `results/G1~G4/` 原始数据与 `r9-judging/` 判分产物——只读基准，写入仅限 `G4-rerun/`。
- 禁动 loop.rs / 任何源码（手术仍触发锁定，开刀申报前零行触碰）。
- 禁并发跑 LLM 版本冻结类测试（.131 并发纪律不变）。

*增补 1 · 顶层签发 · 2026-09-08 16:26 · 本增补与 844dabb 批复配套生效*
