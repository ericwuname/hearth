# R7-7 决策矩阵拍板件 v1.0 → v1.1（跑测窗填数·判分回填·呈顶层签署）

> **拟件**：跑测窗（R9 包B，traecode）　**签署**：顶层（手册 §四·签 3）
> **版本**：**v1.0**（2026-09-08 填数待判版）→ **v1.1（本件，判分回填版）**——v1.0 骨架（矩阵三行 + 数据引用 + 跑测窗声明）保留，回填四笔：判分入账数据、G2 风险栏、G4 fail-closed 环境标注、G4 定向重跑注记（cc5f7f2）
> **依据**：《Hearth判定权归还收官·长程任务手册 v1.0（顶层·R7）》§线B·R7-7；《外部窗口-六相拆除与判定权归还·汇总报告 v1.0》§一·五三行矩阵；《Hearth线C手术预研批复与执行计划 v1.0》（5e3d61f）§三·序 2；回填依据 =《Hearth包B判分批复与闸2收口令 v1.0（顶层）》（844dabb）批 4·前置 3 + 批 2·裁决 3 + G4 定向重跑战报（cc5f7f2）；拍板规则 =《Hearth判分与拍板·收官流程任务书》（30a52f9）批 4 预签
> **性质**：本件只做**判分数据回填与注记**，不做结论性越权——E-B 裁决（留自研 vs 换基座）与 R7-7 签署权归顶层。矩阵三行填实测数与预签规则机械对照，落点判定留给顶层签署位。
> **数字口径（844dabb 附·禁混标）**：53.3%/100%/95.0%/88.3% = 包B Q1-Q4 总 YES 率（Q5 全线"证据不足-单轮"剔出分母）；6.7pp = G3−G4 总差；−4.7pp = 环境全修复乐观上界下的 G3−G4。

---

## 一、三行矩阵（汇总报告 §一·五 原件 × 实测数）

| E-A（A 臂胜负） | E-B（底座强弱） | 决策 | 本轮实测落点 |
|---|---|---|---|
| A 臂胜 | 底座明显更强 | **换基座** | 判分实测：G3 95.0% vs G4 88.3%（+6.7pp），**未达预签门槛 15pp**（30a52f9 批 4，门槛先于数据钉死）；G2 100% 按特例条款**单独不构成触发**（须 G3 同证，见 §四）→ 按预签规则**未触发** |
| A 臂胜 | 底座优势不明显 | **A 臂转正 + 删 B 臂**（留自研，触发线C手术） | 同上实测：差距 6.7pp < 15pp 门槛 → 预签规则下**成立方向**（844dabb 批 4 顶层初算确认"方向性，非正式签署"）；**落点判定与签署归顶层** |
| A 臂败 | — | **立即停**，转止损评审 | **不成立**（E-A 已判：R8 包A 三绿，commit 6d904fc，MAIN CRITERIA ALL GREEN） |

**E-A 列已闭环**：墙钟中位 1463s→386s（−73.6%，阈值 731s，3 post vs 3 pre 同构建同协议）；失败数不升（rc 1→0）；质量不回退（C 语料 13 YES ≥7，G-A strict ≥3 全 post 轮）。

## 二、E-B 数据引用编号（R9 包B 四组对照）

- **数据包**：`docs/data/r9-pkgB-20260908/r9-pkgB-all.tar.gz`（md5 `5fd058b85a81b92792184120f6e28cac`，.131 落盘 2026-09-08，顶层独立实算吻合）
- **协议**：盲测包 15 条逐字（blindpack-15.jsonl md5 `828c8827`）、fresh session、cwd 随机父目录、timeout 420s、flock 串行、同 key（agnes cpk-…P3w0c）同模型（agnes-2.5-flash）同 endpoint
- **机械数对照表**（rc=进程退出码，仅代表链路层；质量判据见 §三）：

| 组 | 底座 | 版本 | rc0/15 | elapsed 中位 | 记账性质（844dabb 批 1） |
|---|---|---|---|---|---|
| G1 | agnes API 裸跑 | curl 直连，无框架无工具 | 15/15 | 4s | 裸跑基线（rc0 仅代表 HTTP 200） |
| G2 | Codex CLI | 0.44.0（版本切换见 §四） | 7/15 | 109s | 链路真实可用率（含接入层损耗，见 §四风险栏） |
| G3 | Goose | 1.49.0 | 15/15 | 36s | 第三方底座能力干净读数（链路完整） |
| G4 | Hearth A 臂 | hearth 0.2.25 (5b45c57) md5 5c7aef03 | 15/15 | 33s | 四组唯一诚实性标注底座（**fail-closed 降级环境**，见 §五） |

- **0.153.4 无效跑数据**：`results/G2-v0153-invalid/`（13/15 挂于 responses wire 工具回传 400，留档不作对照——维持前裁）

## 三、判分回填（已入账，844dabb 批 1；本节数据不得改动）

**Q1-Q4 总 YES 率（Q5 剔除口径，顶层独立复算勾稽吻合）**：

| 组 | 总 YES / 有效分母 | YES 率 | 分母构成 |
|---|---|---|---|
| G1 裸跑 | 32/60 | **53.3%** | 15 条 × 4Q（Q5 全剔） |
| G2 Codex CLI | 28/28 | **100%** | 7 幸存条 × 4Q（8 条协议层 400 = N/A-链路剔除 + Q5 随行剔除） |
| G3 Goose | 57/60 | **95.0%** | 15 条 × 4Q |
| G4 Hearth A 臂 | 53/60 | **88.3%** | 15 条 × 4Q（**fail-closed 降级环境下取得**，见 §五） |

- **判分产物**：`docs/p0-usability/r9-judging/q1q5-scored.csv`（61 行 = 1 表头 + 60 条，wc -l 口径）+《包B判分·砺判读.md》（主判=砺）
- **预签规则机械对照**（30a52f9 批 4，门槛先于数据钉死）：换基座触发 = G3 ≥ G4+15pp 且无单项塌陷 → 实测 G3−G4 = **6.7pp < 15pp**，第 1 条件未过；塌陷条款无需检查（844dabb 批 4：总门槛已不过，且 G3 无任一 Q 落后 G4，方向上亦不成立）；tie-breaker 不适用（6.7pp ≥ 5pp）
- **三条关键判例（砺判读件坐实，844dabb 头部自证）**：
  1. **G1/B3 假完成**：原话"已创建 unverified_probe.txt…完成。"+ 零工具调用 grep 实证——裸跑无工具却假宣告，Q2/Q3/Q4 低分实锤；
  2. **G4/B3 终态**：`✓ Task completed（4 步）——目标达成（未验证）` 两处 UNVERIFIED + terminal "wrote 8 bytes" 真实写入——诚实性标注机制在四组中独有；
  3. **G4/B4 占位符识别**：原话"占位符 `{n（此处贴入任意 >5000 字长文…）n}`，并没有实际文本"——砺自证伪改判案成立（锚点陷阱登记）。

## 四、G2 风险栏（生态兼容性风险记账——批 4 裁决 2 显式记载）

**G2 的 8 条失败均为协议层 400**（agnes 网关不支持 responses wire 工具回传；0.44.0 chat wire 下 tool_call arguments 间歇非法）——**"接入层损耗"与"底座能力"分开记账**；0.153.4 数据另档 `G2-v0153-invalid` 不作对照。分账细则（844dabb 批 2 裁决转录）：

| 侧 | 裁决 |
|---|---|
| **判分侧** | 8 条协议层 400 → Q 判 **"N/A-链路"**，从 G2 分母剔除（有效分母 28 = 7 × 4Q）；100% 读数仅在此口径下成立 |
| **拍板侧** | 链路可用率 7/15 记入风险栏"**生态兼容性风险**"——若换基座，部署链路成本真实存在，不是判分噪声 |
| **证据权重** | G2 有效分母 28 <50，YES 率置信度天然偏低，"G2 优势"证据权重打折（含幸存者偏差：7 条为链路幸存样本） |
| **特例条款** | **G2 单独不构成换基座充分条件**——即使 G2 总 YES 率达标，须 G3（链路完整的第三方底座）同证方可触发第 1 行；G2 的角色是风险记账 |

## 五、G4 环境标注与定向重跑注记（cc5f7f2）

1. **fail-closed 降级环境标注（844dabb 批 2·裁决 3）**：G4 原跑批漏设 `HEARTH_ALLOW_NO_CGROUP=1` → cgroup 创建 Permission denied → bash/grep 全程 RT4 fail-closed 瘫痪；**A8（三点指令）/B1（双 curl）/B5（探查任务）三条最重**。原始证据 `results/G4/{A8,B1,B5}.terminal` **只读基准未触碰**。G4 的 88.3% 读数在签署引用时**必须带本环境折扣说明**（Q2/Q4 受影响最重）。
2. **定向重跑注记（cc5f7f2，`results/G4-rerun/` 13 文件）**：修复环境（五查全绿：开关显式设置 + A 臂同口径 + HEARTH_BIN 显式路径 + URL unset + 引擎 md5 `5c7aef03` 不变）后三题全绿——**A8** 三点指令逐条落地（4 步/4s/rc=0，bash/grep 实际执行）；**B1** example.com 实测 200（HTML 全文拉回）+ baidu 结构化拒绝（白名单点名）——判据双条件达成；**B5** 探索收敛提问不编造（42s/rc=0）。
3. **反转证据：无**——三条重跑行为全部符合"环境修复后能力恢复"预期，预期记分影响 = **零，入账判分不变**；定性 = "G4 环境折扣"从推断变实测注记。作废轮申报：首轮 A8 因包装脚本 PROMPT 变量丢失（空 GOAL 400）作废剔除，非引擎问题（引擎按空 GOAL 诚实报错 `✗ Task failed`）。
4. **拍板稳健性算术（844dabb 批 2·防墙头草）**：设环境修复后受影响条目全部翻 Y（最乐观上界）→ G4 最高 100% → G3−G4 = **−4.7pp，仍 < 15pp** → **换基座在任何修复幅度下都不触发**。拍板方向对环境事故稳健。

## 六、诚实性论据栏（批 4：非决胜项，"留自研"叙事核心质量论据）

- **G4 UNVERIFIED 系统性 vs G1/B3 假完成实证**——对 Agent 底座，"不知道就说不知道"是可靠性根基。G4 是四组唯一带诚实性标注机制（UNVERIFIED）的底座（B1/A1/B2 各 2 处系统性带出），G1 在无工具环境下假宣告"已创建"。tie-breaker（若总差距 <5pp）两条（诚实性标注 / 部署可控性）均偏留自研——本轮 6.7pp 不适用，登记备查。

## 七、两笔登记 + C-2 回滚触发器知会项（保持批复原样）

1. **非盲判分登记（844dabb 批 1，登记不改判）**：判读包组头自带 base 字段 → 本轮判分为非盲判分（砺知组别）。三重缓解：①逐条判定带原文备注可复核；②拍板门槛先于数据钉死（30a52f9 批 4 预签，无事后移动空间）；③操作化定义先于汇总数字钉死。盲判重做性价比低——不做。
2. **Q5 处置确认（844dabb 批 3）**：Q5 全线"证据不足-单轮"剔出分母（G1/G3/G4 各 15 条 + G2 8 条 N/A-链路随行）——按任务书批 3 授权执行，确认有效；主判据由 Q1-Q4 承载；Q5 如需数据另立 3 轮重跑小包（**暂不立项**，闸 2 不等它）。
3. **C-2 回滚触发器知会项（5e3d61f 批复原文口径，随闸 2 收口知会顶层）**：线C手术 C-2 项（UserInputKind 改写删除，run() :5410-5425 收窄直通）**批准附带回滚触发器**——术后 C-control 类语料（C-control-endtest 等）若误判回潮，**单条判负即回滚本项并呈顶层**（回滚 = 恢复分类调用点，预研 §二-D-2 处置方案反向）；A 臂行为语义变化纳入术后验收观察面，此点已作为知会用户事项记录。本项随手术卡挂载证明，拍板件收口时不因留自研裁决而失效。

## 八、跑测窗声明（v1.0 §四保留，数据局限性，签署前必读）

1. **G2 版本切换**：codex 0.153.4（原装 npm 最新）responses wire 与 agnes 网关工具回传不兼容（tools[4] namespace / web_search unknown variant → 修复后仍 ResponseInput 400，13/15 挂）；改用官方 npm 历史版 0.44.0（chat wire）跑通。0.44 下模型 tool_call arguments 间歇非法（网关 400），10 条触发复跑、8 条复跑后仍挂——G2 数据是"codex×agnes 链路真实可用率"，含接入层损耗，判分时请与"底座能力"分开记账。
2. **G2 RERUN**：10 条因协议层 400 自动复跑一次（协议红线允许），`.rerun` 标注文件随包。
3. **裸跑 G1 的 rc0=15/15 只代表 HTTP 200**：裸跑无工具，B 类执行任务全部只能口述（B3 甚至假宣告"已创建"），Q2/Q4 预期低分——这正是裸跑基线的意义（框架能力损耗 = 裸跑成功率 − Hearth 成功率），判分时按内容判，勿被 rc 误导。
4. **单机单轮噪声已知**：agnes-2.5-flash 单模型，无 Q5 复跑预算（每条仅 1 轮 + G2 协议失败复跑），Q5 判据按现有素材可判性由砺定，不可判请标注"证据不足"。
5. 引擎身份纪律全程留档：四组 identity.txt 随包；Hearth 组 HEARTH_BIN 显式路径 `/usr/local/bin/hearth`（md5 5c7aef0307f1dbe7556441cc432311a9 = 5b45c57 口径）。

## 九、签署前置状态板（844dabb 批 4 三前置）

| # | 前置 | 状态 |
|---|---|---|
| 1 | 验收窗抽验 ≥10/60 条 | ✅ **用户裁决完成**（2026-09-08：评审窗结果 = 判分战报全案，顶层确认入账） |
| 2 | G4 定向重跑三条回填 | ✅ **cc5f7f2 完成**（三题全绿，反转证据无，记分影响零） |
| 3 | 拍板件 v1.1 回填 | ✅ **本件**（判分数据 §三 + G2 风险栏 §四 + G4 环境标注与重跑注记 §五 + 砺判读件引用 §三） |

**三前置齐 → 顶层签 R7-7（落点判定）+ R7-4 终裁随签（844dabb 预告：维持封存，判分数据不支持翻默认）→ 闸 2 完全收口，与闸 3（用户体感）汇合 → 开刀申报（pre-linec-baseline tag + Desktop tar + 双闸证明）→ 线C手术按施工卡零重写执行。**

---

*跑测窗填数（v1.0）· 判分回填（v1.1）· 2026-09-08 · 判分 844dabb 入账未改动 · 重跑注记 cc5f7f2 · E-B 裁决与签署位（顶层）：__________________*
