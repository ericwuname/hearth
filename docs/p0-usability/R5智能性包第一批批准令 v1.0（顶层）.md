# R5 智能性包 · 第一批批准令 v1.0（顶层）

> **受审件**：①`docs/Hearth可用性根因分析-对照WorkBuddy（外部窗口）.md`（五轮分析、十三条根因）②`外部根因分析验证报告（执行窗→顶层）.md`（10/10 锚点验证）
> **日期**：2026-09-06 02:30　**结论**：⬛ **外部分析采纳；第一批智能性施工四件批准（P0-0/P0-1/P0-4/根因十三注入）；验收预注册"用户原话场景单次判负"；第二批视第一批 A/B 结果再定。**

---

## 一、顶层抽验（4/4 锚点命中）

| # | 外部根因 | 实测 | 结果 |
|---|---|---|---|
| 根因十一 | 主循环 temperature 0.5 / goal-drift 0.0（同仓两处，主循环用高温） | loop.rs:3362=0.5、:1381=0.0 | ✅ |
| 根因四 | read 工具描述仅 5 词 | tools-builtin/read.rs:28 `"Read the contents of a file"` | ✅ |
| 根因九 | verification 只看命令名不看退出码 | `is_verification_command` :910（R4.1 收窄分类但退出码仍开放） | ✅ |
| 根因十三 | observe_verdict 生产后无消费——失败不教学 | 全仓仅 loop.rs（生产+测试），不进 LLM 上下文 | ✅ |

## 二、定性裁决（本批复最重要的一句）

**诚实性层与智能性层是两个独立维度，分开记账**：

| 层 | 内容 | 状态 |
|---|---|---|
| **诚实性层** | 不说谎（R1）/不失忆（R2）/可验收/不淹没——G-B/C/E/F + W-B/C + 零复发 | R1-R4.1 已支撑**宣告路径 (b) "引擎行为层达标"**（预注册分离条款，非降门禁）；砺处置清单 #2/#3/#4（R3-1 REPL 复验、专项二复验、VERIFIED 审计重分类）是宣告前欠账，**与 R5 第一批同批清掉** |
| **智能性层** | 说谎失忆治好了，但"先动手后说话"、"环境盲"、"同错重复 3 次不学习"、"失败不教学"——用户体感差的剩余瓶颈 | G-A 4-5/10 模型层判负已如实登记；本批复开启 **R5 智能性包** |

外部分析与 R4 动态跑测**两条独立证据链互证**（静态根因↔动态病理一一对应），可信度高；其 §十/§十三.0/§十四 的自我修正纪律（反向验证、前提修正、主动作废旧判据）与砺同款，登记为外部窗口可信度又一加分项。

## 三、第一批施工批准（四件，全低风险、每件带可证伪判据）

| # | 项 | 坐标（施工前重 grep） | 预注册判据 |
|---|---|---|---|
| **R5-1** | **temperature 分档**：Act 0.0-0.2 / Plan 0.2-0.3（外部建议），主循环告别 0.5 高温 | `loop.rs:3362` | C 语料重跑：规划空转次数下降；G-A"先动手后说话"题数变化（如实记录，不预设达标） |
| **R5-2** | **环境上下文注入**：cwd/项目根/技术栈/文件树进 system prompt | `:2337` 附近 | 用户原话场景："我们的 hearth TUI 项目做的进度如何了"——**首轮不得搜 `*.go`**，单次判负 |
| **R5-3** | **verification 看退出码**：非零退出码的命令不得产出 VERIFIED | `:910` / `:2716` | 构造"失败命令+VERIFIED"场景必红 |
| **R5-4** | **失败策略注入（根因十三）**：observe_verdict/已知失败策略进 LLM 上下文 | `build_messages()` 注入先例 | C 语料同错重复 3 次 → 复跑重复次数下降；"回执含答案仍一字不改"场景复测 |

**第二批（视第一批结果再批）**：P0-2（prompt 死流程→原则）、P0-5（read 加 offset/limit+行号）、P1-1（工具描述重写）、P1-3（错误回喂三段式）——不是不做，是**用第一批的 A/B 差值决定第二批优先级**，避免一次改八个变量无法归因。

## 四、纪律

1. 每件照"病理→语义→判据"格式交战报；门禁纪律第 1-4 条照旧（增量勾稽 + golden anchor 读数）。
2. **引擎身份可验证性是证据链第零环**（R4 作废事件教训）：R5 施工后版本号必须可区分（0.2.25 或 0.2.24.x，执行窗定）。
3. 分支继续 `p0-usability-01`；砺的宣告前欠账三件（#2/#3/#4）与 R5 同批清。
4. 归档线（v0.2.23 冻结件 + 可用性结论栏两签）不受 R5 影响——冻结的是 0.2.23 的诚实快照，R5 改的是 0.2.24+ 的行为，两者并行不悖。

## 五、签发栏

| 窗口 | 意见 | 日期 |
|---|---|---|
| 顶层（人工窗） | ☑ 外部分析采纳；R5 第一批四件批准；验收预注册；第二批待 A/B 差值 | 2026-09-06 |
