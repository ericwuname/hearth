# P0 引擎复活线 · R1 诚实包施工战报 v1.0（执行窗 → 顶层/砺）

> **受命**：《Hearth对话可用性根治总纲与长程任务书 v1.0（顶层）》§三 R1 包（用户 2026-09-04 00:57 全权委托执行窗）
> **分支**：`p0-usability-01`（基线 `117a947`，A-1 裁 a：引擎无修法 A）
> **施工窗口**：2026-09-04 00:58 – 02:15　**状态**：**R1 包施工+门禁全部完成**
> **性质**：施工证据报告；**判读归砺**（里程碑门"R1 绿=不再说谎"的正式宣告权在砺）

---

## 一、一句话

R1 诚实包三件（R1-1 完成语义重造 / R1-3 Observe 实职化 / R1-2 守门恒真修复）**全部落地**，全量门禁 **492 passed / 0 failed**，增量链 `486+6=492` 双向验算成立，golden anchor **6/492**（每条新判据都锚定两份真实会话的实证病理）。

## 二、三件施工明细（每件：病理 → 语义 → 判据）

### R1-1 完成语义重造（commit `bc89678`）

| 项 | 内容 |
|---|---|
| 病理 | rc52 改判：写过文件 + 0 错误 + 相关性过 → 改判 completed——外部拆解实证为 **7/28 假完成的机制源头**（写盘行为被当完成事实） |
| 新语义 | ①**改判时点产物存活重验**（`verify_written_files_sync`，存在且非空）——缺失/空 → 拒绝改判维持 failed；②改判成功**强制 UNVERIFIED + `success_rate_counted=false`**（A-4(b) 验收门）；③Done 相位盲区C 校验保留（纵深防御） |
| 投影 | 改判终态显示"⚠️ **改判达成（未验证·不计入成功率）**：本 run 曾放弃，因存在产物而改判；产物合格性未经核验，请人工复核" |
| 测试 | +`test_r11_reroute_rejected_when_artifact_deleted`（写后删→failed）+`..._when_artifact_empty`（空壳→failed）；rc47/rc52 hydration 测试**强化**为 tempdir 真写盘 |

### R1-3 Observe 实职化（commit `af60c4a`）

| 项 | 内容 |
|---|---|
| 病理 | `do_observe` 首行 `let _has_error = ...` **计算后丢弃**（dead code）——Observe 沦为 LSP/检索收集器，真机相位矩阵判定输出全 0 条/0ms（E12 源码证据即这个下划线） |
| 新语义 | Act 执行、**Observe 判定**（判读分离）：①is_error 结构化捕获（错误模式扫描只对 is_error 项做归类补全——**防 E16 型误报**）；②error_kind 归类（`inferred:` 前缀，panic/compile_error/permission/not_found/unclassified——**中英双语**）；③产物存活复核（复用 R1-1 原语） |
| 产出 | scratch `observe_verdict` **每轮写入**（G-H：Observe 有可审计判定输出）+ 异常轮 ThinkSummary 投影（正常轮静默——W-F 降噪） |
| 插曲 | 首轮测试红：中文错误"**未找到**命令"被归 unclassified——与 A-2 同族的**英文中心主义**，当场修正为双语模式（此失败案例本身即"中文场景"重要性的实证） |

### R1-2 守门恒真修复（commit `113b869`）

| 项 | 内容 |
|---|---|
| 病理 | `extract_mentioned_files` = 空格分词 + `contains('.')` → **中文目标恒返回空集 → 守门跳过 = 恒真**（A-2 铁证；0.9-0.3 全中文会话写错文件零拦截的直接机制） |
| 新语义 | **字符级扫描整句**（不依赖分词）：中文句内嵌文件名（"创建 plan.html"/"请写report.md报告"粘连形态）必提取；扩展相对路径形态（旧字符集不含 `/` 必漏）；排除 URL 段；段级过滤沿用旧语义 |
| 已知取舍（注释载明） | 版本号"v0.2.23"也会被提取——**误收紧（守门变严）的代价 = 改判被拒 → failed/UNVERIFIED（诚实方向），远小于假完成（恒真方向）** |
| 测试 | +中文四态提取（内嵌/粘连/路径/URL）+ **中文目标+写错文件 → 守门红**（集成负向门）；既有英文测试全部保持绿（无回退） |

## 三、门禁证据（门禁纪律第 1-4 条逐条）

| 纪律 | 读数 |
|---|---|
| **1 三零门禁** | `FMT_RC=0` / `CLIPPY_RC=0`（agent-core+codex-cli+service 触达面）/ `TEST_RC=0`，证据 `.131:~/fa/p0_gate2.log`、`p0_gate3.log`、`p0_gate4.log`、`p0_gate_r1full.log` |
| **2 口径纪律** | 分树分口径：**tag 树 486/0**（`117a947` 基线，未动）｜**p0 分支 492/0**（本包施工后）——两数并列，不得混标 |
| **3 断言纪律** | 本轮 +6 测试全部**能失败**（r11 两门在旧语义下必红：写后删会被改判 completed；r12 两门在旧语义下必红：守门恒真） |
| **4 golden anchor** | **6/492**——r11×2 锚定 0.9-0.3"写后删仍报完成"铁证；r12×2 锚定"全中文会话守门恒真"铁证；r13×2 锚定 E12 相位矩阵 Observe 0ms 铁证 |

**增量链双向验算**：`486 + 2（R1-1）+ 2（R1-3）+ 2（R1-2）= 492` ✅；agent-core 分量 `130 → 136`。

## 四、G 门预演（正式判读归砺）

| 门 | 预期 | 依据 |
|---|---|---|
| **G-C**（give_up 改判 3 场景 ≥2 呈现未完成） | 预期 **3/3 呈现未完成**（重验失败→failed；重验过→UNVERIFIED 不计成功率——改判场景**0 个无保留完成**） | R1-1 语义 |
| **G-H**（Observe 有实际判定输出，0ms 消失） | 预期通过（observe_verdict 每轮写入 + 异常轮投影） | R1-3 |
| **G-B**（已知失败标 ✅ 一票否决） | R1-4（known-failing）依赖 R2-1 SessionLedger，**未在本包**——按依赖图串行 | 任务书 §四 |
| **A-2 守门恒真** | 中文目标守门**能红**（测试锁定）——"按句核验"的完整实现（LLM 辅助语义核验）为下一刀，本刀先消灭恒真 | R1-2 分层 |

## 五、施工过程披露（诚实边界）

1. **git checkout 事故一次**：Windows 下 34-commit 大跨度 checkout 被 120s 超时杀死，遗留半切状态 + 损坏 index；"Already on" 的 `checkout -f` 与 DIRTY_COUNT=0 **均为假象**，随后发现工作树缺文件。处置：抢救未提交编辑 → `reset --hard` → 恢复编辑 → diff 验证（83 行=恰好我的编辑）。**教训：大跨度切分支分步执行 + 每步加长超时；checkout 被杀后必须验证关键文件物理存在**。
2. **两次测试先红后绿**：r13 中文归类（英文中心主义）、rc47/rc52 hydration（旧测试只 push 记录不落盘——被 R1-1 存活重验照出）。两红均为**测试强化**而非语义回退。
3. **范围纪律**：本包未动 planner schema（STOP-1/2）、未动 T4 阈值、未给 Hearth 引擎写 cognition 适配器（三红线自检过）。

## 六、下一步（按任务书 §四 依赖图）

```
✅ R1-1 → ✅ R1-3 → ✅ R1-2 ─→ R2-1(SessionLedger) → R1-4(known-failing)
R2-2(独立·最小) ──────────┤
R2-4(独立) ────────────────┤→ R3-1..R3-4 → R4 总验收
```

- **R2-2**（硬切片并入压缩，`archive_compacted_turns` 已存在，复用非新建）为下一刀；
- R2-1 SessionLedger 为大件（新模块）；
- **里程碑"R1 绿=不再说谎"请砺判读确认**（本报告为施工证据，非宣告）。
