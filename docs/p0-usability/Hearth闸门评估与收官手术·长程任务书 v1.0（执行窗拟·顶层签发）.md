# Hearth 闸门评估与收官手术·长程任务书 v1.0（执行窗拟·顶层签发）

> **拟稿**：执行窗（WorkBuddy）　**日期**：2026-09-08　**状态**：待顶层签署
> **输入**：①R8 首轮验收（61dea7c，VERIFIED——闸 1 数据侧收官）②R7 手册三闸与线 C 预写细则 ③六相拆除汇总报告 §3.4（D-1~D-10 删除清单原文）④R8 包A-2 战报（**D-9 冲突发现**，见 §三-签 3）
> **定位**：R8 双线收口任务书的**接续件**——包A/包D 已验收入账，本任务书把剩余全程一次签清：包B 派发协议、包C 判分协议、闸门评估流程、线 C 手术施工卡（触发式生效）与术后收尾。签发后包B/包C 即可并行派发，手术待闸。
> **关键实测**：loop.rs 当前 **13,055 行**（61dea7c 实测；R8 净增 543 行为校准机制与测试）——手术硬线 <10,500，**净减目标 ≥2,558 行**，比手册估的 12,523 基线多削 543。

---

## 〇、三闸现状（顶层登记口径）

| 闸 | 内容 | 现状 |
|---|---|---|
| 闸 1 | 干净对照支持 A 臂 | ✅ **数据侧收官**（R8 包A：墙钟中位 −73.6%、C 语料 13 YES、失败数不升；61dea7c 独立复算） |
| 闸 2 | E-B 决策=留自研 | ⏳ 待包B 四组对照（裸跑/Codex CLI/Goose/Hearth-A）+ R7-7 拍板件顶层签署 |
| 闸 3 | 用户体感 0.2.25 | ⏳ 待用户实操（双机 alias 已就绪，不阻塞包B/包C） |

---

## 一、顶层签署区

| # | 事项 | 内容 | 签署 |
|---|---|---|---|
| **签 1** | 包B 派发确认 | 四组对照协议（§二）发独立窗口；agnes 探活/VPN 由用户侧保障 | ________ |
| **签 2** | 线 C 手术启动令（触发式） | 闸 2+闸 3 齐即生效，无需二次签署；手术按 §四施工卡执行 | ________ |
| **签 3** | **D-9 冲突裁定** | 外部清单 D-9"删计数器体系"与 R8 实测**直接冲突**：`a_arm_act_tally`（convergence 收口 + strategy-forced 换法）正是 −73.6% 的功臣机制。裁：**A 臂现役计数机制保留**，D-9 收窄为"删 B 臂侧计数残留"（预估减量相应下调） | ________ |
| **签 4** | 风险项处置授权 | 隐藏依赖不可安全拆除的 D 项，允许 `#[deprecated]` 冻结而非硬删（逐项申报，禁批量） | ________ |

---

## 二、包B · 四组底座对照（独立窗口，.131）

**协议全文沿用 R8 双线收口任务书 §三 + 顶层签发件增 1**，此处只列执行卡：

1. **预检三件**（缺一不开工）：探活 agnes（期望 401，不通找用户挂 VPN）；node/npm 版本留档；Codex CLI 与 Goose `--version` 留档。
2. **四组 × 15 条逐字**：裸跑（agnes API 直连，无框架）/ Codex CLI / Goose / Hearth-A 臂。后三组每条 fresh session、cwd 隔离（/tmp 随机父目录）、原装默认设置禁调参。
3. **每份日志首行** = 组名+底座版本+模型+时间戳（引擎身份纪律对底座同款适用）。
4. **计分**：每条 成功/失败 + Q1-Q5（按汇总报告 §一·五定义照抄）+ calls（可测则测）+ 耗时；Hearth 特有判据条目对底座标"不适用"，禁硬套。
5. **产出**：四组对照表落盘 + tar 拉回仓库 `docs/data/`；R7-7 决策矩阵拍板件（三行矩阵填实测数）呈顶层签署。
6. **判分外包**（签发件弹性条款）：Q1-Q5 判分交砺/外部窗，跑测窗不_self-judge。
7. 诚实边界：agnes-2.5-flash 单模型；单机单轮（噪声已知——引用 R8 判据修订理由，四组对比以质量判据为主、耗时为辅）。

## 三、包C · G-W1 真尺判分（砺主评，材料已就绪）

1. 材料：`docs/data/r8-20260907/r8-all.tar.gz`（md5 7d4453b8）——postc 双臂 28 份 C 语料 terminal + 判读证据包（cpack/gapack 已附）。
2. 脱敏：验收窗出脱敏版（抹臂标/引擎行/耗时）后移交砺。
3. 判分：逐条按 corpus-c.jsonl judge 字段 YES/NO/PARTIAL/环境受限；**与执行窗自判差 >2 条逐条复议留痕**。
4. **秒答条重点复核**（trae 偏差 #3 + 验收窗认同）：post 轮多条 2-6s 收口——重点核对 A1/A4/A10 等反问/直答是否命中判据而非敷衍收口。
5. 顺带收口两笔登记：B 臂判负计数 ±1（战报 7→4 vs 实测 6→5）；summary.csv exit_code 弱仪器问题（判负以终端终态为准）。

## 四、包E · 线 C 手术施工卡（闸 2+闸 3 齐，签 2 触发生效）

> **动刀对象**：B 臂六相机制残余。**手术基线：13,055 行（61dea7c）→ 硬线 <10,500**。锚点均为 96f4161/外部报告时代坐标，**下笔前逐项重新 grep**（R8 后 loop.rs 行号整体 +543 漂移）。

### 4.0 动刀前置（一次做齐）
- tag `pre-linec-baseline` + 源码 tar 日期戳存 Desktop（用户既定纪律）；
- 手术分支（禁带斜杠名）或主线直做——顶层定；
- 事件投影先改后删：`Event::Phase` 消费端仅 session.rs:1002 / api/src/lib.rs:265（96f4161 坐标），先改 2 处再删枚举。

### 4.1 逐项施工卡（建议顺序：低风险→高风险，每项独立 commit）

| 序 | D 项 | 内容 | 施工法 | 预估减量 |
|---|---|---|---|---|
| 1 | D-10 | emit_think_summary 相位套话 | grep 各相位尾部队列→删套话分支；A 臂投影保留 ThinkSummary 主体（R8 budget 注入不在其列，**勿误删**） | −60+ |
| 2 | D-8 | B 臂机关（写盘逼迫/forced-replan/MUST tool_calls，:3541-3648 注释自述块） | 逐段 grep 确认 A 臂旁路属实→整块删 | −107 |
| 3 | D-1 | goal_requires_product() + 98 词表 | 全仓 grep 调用面→A 臂无需分流→删 | −140+ |
| 4 | D-2 | UserInputKind 三分类 + 42 词 | 同上（C2 误判 29% 的病灶源头） | −70+ |
| 5 | D-7 | ReflectVerdict 三值决策（agent-types） | agent-types 独立 crate，跨 crate 引用先清 | −80+ |
| 6 | D-5 | LoopPhase 枚举 + step() match | **最后删**（R8-A1 仍用 LoopPhase::Error 路径；投影 2 处先改） | −120+ |
| 7 | D-3 | planner.decompose() 调用链 | A 臂 needs_decompose=false；planner crate 分解逻辑联动删 | −300+ |
| 8 | D-4 | task_graph/TaskGraph/plan_state | TodoWrite 已替代清单职能；**checkpoint 挂钩处先解绑再删**（R6-8 保留项） | −400+ |
| 9 | D-6 | do_observe()/do_reflect() | B 臂相位本体；B4 同名异构随之消解；**error_kind 归类缺口随删接受**（R8-A2 遗留已登记） | −500+ |
| 10 | D-9 | ~~计数器体系~~ → **收窄（签 3）**：仅删 B 臂侧计数残留 | **A 臂现役 `a_arm_act_tally` 保留**（−73.6% 功臣机制，R8 实测） | 原 −200+ 下调 |

### 4.2 每项施工闸（不可跳）
1. 动刀前全仓 grep 该对象全部引用（含测试/文档/宏）→ 引用清单入战报；
2. 单独 commit，diff 越界审查（禁夹带）；
3. 全量门禁（537 基线，B 臂测试**同步删除**，禁改断言使其绿——纪律红线）；
4. 盲测包 15 条冒烟（分钟级，**冒烟红即停**，连续两项红 → 手术暂停评审）；
5. 每 3 项 `wc -l` 里程碑报数一次。

### 4.3 保留清单（手册原文，违反=手术失败）
预算+优雅降级 / 沙箱全家（Landlock+seccomp+cgroups，4.5/5）/ observer 事件流 / checkpoint / 诊断 JSONL / 大输出落盘 / 真机回归集 / **R8 校准机制（a_arm_act_tally + 预算双闸，签 3）**。

### 4.4 术后验收（手术专用预注册）
1. `wc -l crates/agent-core/src/loop.rs` < 10,500（硬线），净减 ≥2,558 实测；
2. 门禁绿（B 臂测试删除清单在案，零改断言）；
3. Q1-Q5 不回退：术后盲测包 + C 语料双臂各 1 轮，与 R8 post 基线对照（C YES ≥13 不降、G-A ≥3）；
4. A 臂墙钟中位数不劣化（单轮 >2× 中位即警）；
5. `HEARTH_SINGLE_LOOP=0` 逃生门实测回退一次后按手册保留一个版本周期；
6. 版本号 bump（建议 0.2.26）+ `--version` 跟踪验证（R7-3 机制）；
7. D-1~D-10 逐项留痕 commit 链 + 隐藏依赖处置清单（含 #[deprecated] 冻结项，签 4）。

## 五、术后展望（挂账不施工，防丢失）

- D2 历史 40 条硬切 → A 臂长任务上下文策略小包（线 C 后）；
- cognition MVP 线：包B 四组数据天然合流（Codex CLI 底座方案已批）；
- 归档线两签：用户签可用性结论栏 + T-6 外部签位 → tag+双 tar 仪式；
- 追问类回归子集（模型层）→ cognition 线输入。

## 六、纪律（沿用，违者按手册罚则）

判读分离 / 并发红线（包B 装底座错峰 .133 跑测）/ 冻结区零触碰（手术全程不涉 sandbox）/ 诚实边界 / 引擎身份留档 / 一次性脚本不落仓库根目录。

## 七、一句话

> 闸 1 已收官，数据只剩包B 一件、体感一件；手术清单十项里有一项（D-9）被 R8 实测否了半条——**删 B 臂的刀已经磨好，但 13,055 行的起点比手册预估高了 543 行，刀要落在 2,558 行以上才算根治**。四包并行：包B 外援、包C 立尺、你的体感、然后开闸动刀。

---
*执行窗拟 · 2026-09-08 · 依据：61dea7c 验收 + R7 手册 + 汇总报告 §3.4 清单原文；loop.rs 13,055 行为 61dea7c 实测*
