# 执行窗汇总报告（2026-09-03 呈顶层窗口）

> **出品**：执行窗口　**日期**：2026-09-03 17:00　**读者**：顶层窗口（决策用）
> **性质**：**决策汇总件**——不含论证过程，只给"现在是什么状态、还差什么、请你批哪几项"
> **配套（不重复）**：`docs/P4-P5合并总报告（完整现状·执行窗）.md`（完整履历 401 行）、`docs/core-freeze-candidate/`（冻结八文件 + 归档附页 + 外部评估包）、`docs/core-freeze-candidate/evidence-index.md`（证据索引）

---

## 一、一句话

**两个总包均已达终态，L1 决胜局 D2 终判成立（修法 A 有效），外部评估闭环完成；现在只差顶层/外部的签字与归档仪式执行——请批 §四 的五项，即可进入归档 + cognition MVP。**

## 二、三行速览

| 问题 | 现状 |
|---|---|
| P4 / P5 做完了吗 | **都达终态**：P5 = 段 1 完工发版（486/0）+ 段 2 顶层取消；P4 = 改道完成（根因源码级坐实 + 八文件齐 + 三轮盲测入档） |
| 根因对不对、修法有没有用 | **D2 终判 = 成立**：B 臂 27%→80%（4/5）、A 臂 4/6 = 67%，**均达预注册阈值**；追问类可比样本 pre 0/3 → post 2/3（**方向为改善**） |
| 还差什么 | 5 项待批（§四）：归档仪式执行授权、RFC-1 的 S-2 补测、U-44 派工、外部评估签位、MVP 验收口径确认 |

---

## 三、本轮闭环清单（2026-09-02 → 09-03，全部已落地）

| # | 产出 | 状态 |
|---|---|---|
| 1 | **冻结八文件**（含 open-deviations v1.1、simuser-status、evidence-index、independent-review-request、freeze-candidate） | ✅ |
| 2 | **统一台账 U-01~U-47**（四账并账，唯一登记处） | ✅ |
| 3 | **L3 知识迁移卡 ×5**（护栏范式 / 完成判定 RC1 修正版 / 三层死锁 / 代理指标陷阱 / 激励结构） | ✅ |
| 4 | **外部评估材料包 v1**（十节、Q1-Q6 预注册）+ 三家回执闭环 | ✅ |
| 5 | **L1 修法 A**（非产物救回口）实施 + 门禁 **491/0** + 5 个新测试 | ✅ |
| 6 | Phase D 补测四项（P-D5 归因反转 / P-D1 / P-D6 qiyuan-8b 正口径 / P-D3 introspect） | ✅ |
| 7 | 追问类回归子集 + **pre-fix 基线补齐** + A 臂补跑至 n=6 | ✅ |
| 8 | **driver 硬断言**（`wait_terminal_strict` + 显式标记映射 + 删失信息记录） | ✅ |
| 9 | **L1 归档附页**（D2 终判 + 三条必须同时引用的限制） | ✅ |
| 10 | 顶层指令的执行窗六项（U-06 降级 / RFC-1 措辞 / docs 数 52 / RC51 锚点 / W1 重写 / 追问类跑批） | ✅ |

---

## 四、请顶层批的五项（决策请求）

| # | 请求 | 选项 | 执行窗建议 |
|---|---|---|---|
| **R1** | **归档仪式执行授权**（D1 已预授权，等外部签位） | A. 批准执行 `git tag v0.2.23-final` + archive 存 Desktop｜B. 暂缓 | A（数据已齐，归档仪式是最后一步不可逆动作） |
| **R2** | **RFC-1 的 S-2 补测**（唯一未解除的签字阻塞） | A. 执行窗现在补 spawn 级集成测试｜B. 随归档放弃 | A（1 个用例成本，解除的是安全回归风险） |
| **R3** | **U-44 派工确认**（原 P5 段 2 取消后悬空） | A. 并入 `l1-b3-rescue` 分支｜B. 随归档放弃 | A（修法 = run 起始落会话头，`run_local.rs:365` 已接线，**不是补接线**） |
| **R4** | **外部评估签位** | 用户转发后外部窗签 | —（执行窗已备齐材料） |
| **R5** | **MVP 验收口径确认**（D3 附加条件） | 确认：cognition MVP 验收**必须含同一份 A 组盲测复跑（全新 session）** | 确认（防"跳过实验"退化为"跳过验证"） |

---

## 五、数据与结论（附限制，不得只引结论）

### 5.1 D2 终判（砺 v3）：**成立**

| 条件 | 阈值 | 实测（n=6） |
|---|---|---|
| B 臂 ✓率 | ≥3/5 | **4/5 = 80%** |
| A 臂 ✓率 | ≥2/3 | **4/6 = 67%** |

**必须与结论同时引用的三条限制**：
1. **零余量**：4/6 恰在阈值上，n=6 时 95% CI 约 30%–90%，单个样本翻转即跌破；
2. **两侧非同类证据**：pre 是产物救回（有产物事实 + fact_check），post 是非产物救回（仅"有工具交换"弱信号 + **UNVERIFIED**）；
3. **剔除非中性（收窄未消除）**：补跑批**零删失**却复现同样 67% → 不支持"改善来自剔除"，但信息性删失风险未完全排除。

### 5.2 追问类（首次拿到可比对照）

#28 ✗→✗、**#30 ✗→✅（25 交换/51 步/UNVERIFIED）**、**#32 ✗→✅（1 交换/6 步/UNVERIFIED）**。
✅ 允许表述："同条目可比 3 条，pre 0/3 → post 2/3，**方向为改善**"。⛔ 禁止表述："追问类成功率提升到 67%"。

### 5.3 L1-b 第二修法：**暂不立项**（转观察项）

有效样本中**零工具交换的追问任务一次都没出现**——v1 曾声称的"零交换"来自被截断日志（已作废）。源码层面零交换确被救回守卫排除，但"是否真实发生、占比多少"无证据，**价值不可量化**。

---

## 六、下一步时序

```
现在 ── 顶层批 R1-R5 ──▶ 执行窗：S-2 补测 + U-44 并入分支 + 复跑门禁
                    ──▶ 外部签位落笔
                    ──▶ 归档仪式：git tag v0.2.23-final + archive 存 Desktop
                    ──▶ cognition MVP（第二个 1）在主力底座开工
                        （验收必须含 A 组盲测复跑 —— D3 条件）
```

## 七、诚实披露（未决，全部显式）

| # | 项 | 状态 |
|---|---|---|
| 1 | U-06（L6 作为 RC52 根因） | 🔶 未定案 + 有反证（已降级，与 RC-Ω 解耦） |
| 2 | W1 反应回路改造 | 仅设计小样（v0.3），立项依据已改为"仅可用性" |
| 3 | #21 个案（PLAN 相位 T4 stall，与修法 A 无关） | 与 b5 同型，U-48 候选待砺裁 |
| 4 | 口径争议（61vs63 / 18vs12 / 497vs502） | 度量对象不同，未裁决 |
| 5 | 样本量 | A n=6、B n=5、追问类可比 3 —— 均偏小 |
| 6 | U-27 全量测试挂起 / U-26 seccomp 杀 cargo | 登记待修 |

---

> **执行窗**　2026-09-03
> 本件为决策汇总；每条结论的原始数据与复算入口见 `evidence-index.md`，完整履历见 `P4-P5合并总报告（完整现状·执行窗）.md`。
