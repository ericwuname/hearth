# FREEZE CANDIDATE · freeze-candidate（Node 15 八文件之八）——v0.2.23-final 归档版草案

> **执行窗 2026-09-03**｜**状态：DRAFT——按路径书 v1.0 §五.1 降级口径起草；归档仪式（git tag + archive）为不可逆动作，待 L1/L2 数据 + 顶层终审后才执行**
> **禁自行宣布 FREEZE**：归档版同样走顶层终审（本文件是候选，不是宣告）。

## 1. 候选对象与形态

| 项 | 内容 |
|---|---|
| 候选 | **v0.2.23-final（归档版）**——主线 commit `117a947`（gates 486/0，.131 复跑确认） |
| 分支处置 | `l1-b3-rescue`（commit `0ce93aa`，gates 491/0）——L1 决胜局载体；**不入本归档**，随 L1/L2 总决策处置（成功→合入后续主线；失败→分支留档） |
| 归档仪式（待批） | git tag `v0.2.23-final` + `git archive` 日期戳 tar 存 Desktop（既有惯例） |

## 2. 八文件完成度

| 文件 | 状态 |
|---|---|
| architecture-status | ✅ 起草 |
| behavioral-status | ✅ 起草（含熟悉版/首正式轮/正式轮三轮数据） |
| projection-status | ✅ 起草 |
| open-deviations | ✅ v1.1（13+3 项口径，随补测更新至 U-47） |
| simuser-status | ✅ 本轮起草 |
| evidence-index | ✅ 本轮起草 |
| independent-review-request | ✅ 本轮起草 |
| freeze-candidate | ✅ 本文件 |

## 3. 归档前必须闭合的项（硬清单）

> **执行窗 2026-09-03 18:35 重排**：本表从"现状罗列"改为**可追责归属表**。原表格只有"责任/状态"，导致阻塞无法定位到人、也无法定位到"对方须交付什么"，是"冻结一直在原地打转"的直接成因。下表强制给出**阻塞方须交付的最小动作**。

> **执行窗 2026-09-03 20:10 更新**：顶层签发件 v1.0 已落定 T-1/T-2/T-3 三项裁决，本表据此刷新。硬清单**剩余 2 项**（#5 砺、#6 砺）为纯判读动作，#4 仅余外部签位。

| # | 项 | 归属（阻塞方） | 阻塞方须交付的**最小动作** | 状态 |
|---|---|---|---|---|
| 1 | L1 post-fix 复测数据 + 砺判定 | 执行窗收数 → **砺**判读 | **砺**：裁定是否接受"标注"作为 legacy 采集的处置（砺 §2 处置 2 原文为**「标注 _或_ 补跑」二选一**） | ✅ D2 终判已成立（B 4/5、A 4/6 均达阈值）——**必须与三条限制同时引用**：(a) A 臂零余量 (b) 两侧计数非同类证据 (c) 剔除无效样本非中性，见 `L1-归档附页-D2终判与限制.md`。<br>🔶 严格口径补跑产出 B1（`completed / 124 次工具交换 / UNVERIFIED / censoring=[]`）后暂停；**签发件 §三维持"当前不启动"**——标注已做且经顶层抽验属实，补跑属可选加固 |
| 2 | ~~L2 底座对照数据（Codex CLI/Goose/OpenCode × A 组盲测）~~ | ~~未认领~~ → **顶层** | ~~指派或豁免~~ | ✅ **已裁：豁免独立轮**（签发件 **T-1**）——**移出硬清单**，登记入 **§3.4「已知缺口并声明」**。豁免的是"独立 L2 对照轮"，**不豁免对照数据本身**：最小子集并入 **cognition MVP 验收口径**回收（主力底座天然即对照面，一份跑测两用）。**执行窗配套施工已完成**（本表移出 + §3.4 登记） |
| 3 | 引擎总决策（归档 vs 回主线） | **顶层** | ~~一句话裁决~~ | ✅ **已裁 (a) 归档主线**（签发件 **T-2**）：主线 `117a947` 打 tag `v0.2.23-final`；`l1-b3-rescue` **留档不合并、不删**，状态="已验证待合并"，cognition 底座实验若需可 cherry-pick。<br>**归档仪式触发条件 = T-3① 签位完成 且 T-6 外部签位到位**（后者为硬前置，不放松） |
| 4 | FZ-RFC-1/2b 双签 | **顶层 + 外部 AI 窗**（执行窗已签） | **外部窗**：出评估意见并签署（T-6） | 🔶 **顶层位已准签**（签发件 **T-3①**，S-2 阻塞移除经抽验属实）；**外部签位待到位**——RFC-1 仍为双签制，**外部签位到位前不产生完整效力** |
| 5 | G1-C3 复核 → OD-3 闭环 | **砺** | **砺**：给 OD-3 闭环判词 | ⏳ 照发。**签发件补充**：判词须**显式注明 V 值修正状态**（材料报 `0.065` vs JSON 实际 `0.1047`，口径差是否已回写材料），否则闭环不完整；OD-10 随带 |
| 6 | PhaseD 补测报告 v2 判读（U-06/U-47/U-07 定级） | **砺** | **砺**：三条 U 项的最终定级 | ⏳ 照发。**签发件补充**：出口 IP 变更（挪威→上海电信）**顶层初判不构成不可比因素**（provider/model/key 三者未变），**默认按可比处理，勿为此阻塞 T-5**（初判不代裁，砺若发现时延分布异常可单独提出）；W1 立项理由仍**只能是可用性**，禁引"RC52 架构治愈" |
| 7 | U-44 去向确认 | ~~顶层定~~ **顶层** | ~~并入或另开分支~~ | ✅ **已裁：并入 `l1-b3-rescue`**（签发件 **T-3②**）。施工与门禁验证均已闭合（19:17，.131）：`a7db27a` + `8a4df33`，`FMT_RC=0`/`CLIPPY_RC=0`/`TEST_RC=0`。随分支留档，状态="已验证待合并"，**不进 v0.2.23-final tag** |

### 3.1 冻结路径的结构性判断（执行窗）

| 归属 | 项数 | 项 | 状态 |
|---|---|---|---|
| **顶层窗口** | **0** | #2 已裁豁免、#3 已裁归档、#7 已裁并入分支、#4 顶层位已准签 | ✅ **四项全部裁完** |
| **砺·评审** | 2 | #5 G1-C3/OD-3 闭环判词、#6 PhaseD v2 三条 U 定级 | ⏳ 待判读（**当前唯一实质性判读阻塞**） |
| **外部 AI 窗** | 1 | #4 双签的**外部签位**（T-6） | ⏳ 待签 —— **R1 归档仪式的硬前置** |
| **未认领** | **0** | #2 已裁豁免，不再有无人认领项 | ✅ |
| **执行窗** | 0 | 签发件配套施工已完成（本表刷新 + §3.4 登记 + RFC 签位登记） | ✅ |

> **结论（2026-09-03 20:10，顶层签发件 v1.0 签发后刷新）**
> - **顶层侧 4 项裁决已全部落定**，执行窗配套施工同步完成。**"冻结一直在原地打转"的结构性成因已被解除**——此前是 6/7 项卡在外部且 #2 无人认领，现已归零。
> - **剩余阻塞只剩两条线**：**砺的 2 项判读**（#5/#6）+ **外部窗的 1 个签位**（T-6）。
> - 🔓 **cognition MVP 已解冻**：签发件 §三明载「T-2 已裁 (a) 即视为解冻」，且**归档仪式不阻塞 cognition 开工，两者可并行**。新战场可执行窗自主推进，无需再等归档。
> - **R1 归档仪式触发条件**＝T-3① 签位完成（已准签）**且 T-6 外部签位到位**。**只差外部签位一项。**
> - **B 臂补跑维持不启动**（签发件 §三维持原判），除非砺在 T-5 判读中认为必须补跑。

### 3.2 本批数据采集的环境变更登记（2026-09-03）

| 项 | 历史基线 | 本批（VM 重启后） | 是否影响可比性 |
|---|---|---|---|
| 出口 IP | 45.88.202.26（挪威，用户 VPN） | **183.192.126.72（上海电信，直连）** | ⚠️ VPN 随宿主重启断开；Agnes 探活 401 = 可达，provider 未变。**待砺裁定是否需要标注** |
| B 臂后台任务 | 已跑部分 | **随 VM 重启归零 → 重跑后仅产出 B1 即主动暂停**（详见 #1） | 无影响：严格口径补跑属**可选加固**，砺 §2 处置 2 允许「标注」替代「补跑」，标注已完成 |
| 全量门禁数字 | 486/0（**tag 树**） | **493 / 0 / 1 ignored**（**分支树**，2026-09-03 19:22，.131 复验） | ✅ **分树并列**，见 §3.3 |
| hearth 二进制 | `~/hearth-l1`（9/3 01:21 构建） | 同（未重建） | 无影响 |
| `HEARTH_ALLOW_NO_CGROUP` | 真机跑批必须 `=1` | 同（本批启动脚本已显式 export） | 无影响 |

### 3.3 全量门禁复验 · **分树口径**（2026-09-03 19:21–19:22，.131）

> **口径修正（依顶层签发件 v1.0 · T-2）**：门禁数字须**分树表述**。归档报告两数并列、各注口径，**不得混标，不得以分支树 493 冒充 tag 树读数**。486 / 491 / 493 三者各自合法；"497"无出处，维持禁引。

| 树 | commit | 门禁读数 | 分解 |
|---|---|---|---|
| **tag 树**（v0.2.23-final） | `117a947` | **486 / 0** | 130 agent-core + 356 rest |
| **分支树**（`l1-b3-rescue`） | `18ccd85` | **493 / 0 / 1 ignored** | 135 agent-core + 358 rest |

**增量来源与勾稽**（执行窗 2026-09-03 20:10 复核**签发件所载数字，结论：与实测完全一致，无差异**）：

| 增量 | 来源 | agent-core | rest | 合计 |
|---|---|---|---|---|
| 基线（tag 树） | `117a947` | 130 | 356 | **486** |
| +5 | 修法 A（`0ce93aa`，L1 非产物救回五测试） | 135 | 356 | 491 |
| +1 | U-44（`a7db27a` / `8a4df33`） | 135 | 357 | 492 |
| +1 | S-2（`crates/sandbox/tests/s4_spawn_env.rs`） | 135 | 358 | **493** |

> ✅ 双向验算同真：`130 + 356 = 486`、`135 + 358 = 493`、`486 + 5 + 1 + 1 = 493`。
> ⚠️ **执行窗自纠**：本窗早前曾记「491 = agent-core 135 + workspace 356」——该分解把**分支树的 agent-core** 与 **tag 树的 rest** 混算，虽总和凑巧正确但**分解错误**。正确分解如上表。签发件 T-2 所载 130/356 为 tag 树值，实测 135/358 为分支树值，**两者不冲突**（差 5 即修法 A 五测试）。

| 项 | 值 |
|---|---|
| 命令 | `cargo test --workspace`（门禁作用域：`HEARTH_URL` unset / `HEARTH_CGROUP_BASE=/sys/fs/cgroup/hearth` / `HEARTH_ALLOW_NO_CGROUP` **unset**） |
| 实测证据 | `.131:/home/wutao/fa/gate_full.log`；本机副本 `docs/data/gate-full-20260903.log`（含 env 回显行） |
| U-44 单独门禁 | `FMT_RC=0` / `CLIPPY_RC=0` / `TEST_RC=0`，证据 `docs/data/u44-gate-20260903.log` |

> **注**：复验脚本首版有一处自读自写缺陷（`grep` 的输入与输出同为 `gate_full.log`），导致汇总行不可信；已改为**在日志写完后**重新提取，上表数字为干净提取结果。此缺陷一并记录，避免后人复用脚本时被误导。

### 3.4 已知缺口并声明（豁免项 · 顶层签发件 v1.0 · T-1）

| # | 项 | 豁免依据 | 代价声明 | 回收路径 |
|---|---|---|---|---|
| 2 | **L2 底座对照数据**（Codex CLI / Goose / OpenCode × A 组盲测） | 顶层签发件 v1.0 **T-1** 裁 **(b) 豁免** | 归档版 v0.2.23-final **不含**相对成熟 harness 的**定量**对标结论；**后续若需对标须另起一轮** | 对照数据**最小子集**并入 **cognition MVP 验收口径**（A 组盲测复跑、全新 session、主力底座）——主力底座天然即对照面，**一份跑测两用，不再单开 L2 轮** |

> **说明**：豁免的是"**独立 L2 对照轮**"，**不豁免对照数据本身**——数据经 cognition MVP 验收回收，属改道而非弃权。执行窗配套施工（本表移出硬清单 + 本节登记）已完成。

## 4. 归档包内容清单（待终审通过后打包）

1. 主线源码 archive（`117a947`）+ tag `v0.2.23-final`；
2. `l1-b3-rescue` 分支引用（不合并）；
3. evidence-index 全量数据（release/ 手工测试 ×5、docs/core-revalidation/ 报告链、docs/core-freeze-candidate/ 八文件、unified-ledger）；
4. 知识迁移卡 ×5（L3）；
5. 测试基建 `tools/simuser/`（随源码归档，底座上可复用其语料与判据）。

## 5. 诚实声明

- 本候选**不是**原 Node 15 口径的"CORE FREEZE CANDIDATE"——原流程（八文件+双签+全量 campaign）已被顶层路径书降级改道，本文件按降级口径起草；
- 引擎的最终处置（归档 vs 回主线）**不由本文件决定**，由 L1/L2 数据 + 顶层总决策决定；
- 全部已知缺口显式列于 §3，无隐藏项。
