# 冻结解冻 · 各窗口任务命令 v1.0

> **拟稿**：执行窗（本窗口**无权**对其他窗口派工——按分工铁律，任务书为顶层唯一权力）
> **性质**：**代拟稿，须经顶层窗口签发后方为有效任务书**。各窗口在未收到顶层签发件前，请勿据此启动。
> **日期**：2026-09-03 19:40　**基线**：tag `v0.2.23`（commit `117a947`）／分支 `l1-b3-rescue`（`0ce93aa` → `a7db27a` → `8a4df33` → `18ccd85`）
> **全量门禁**：**493 passed / 0 failed / 1 ignored**（2026-09-03 19:22，.131，证据 `docs/data/gate-full-20260903.log`）

## 〇、为什么现在发这份派工单

冻结前硬清单 7 项中，**执行窗侧待办已归零**。剩余 6 项分属四个窗口，其中 1 项**无人认领**。
本件的唯一目的：把每一项的**阻塞方**和**该方须交付的最小动作**写给本人，使冻结不再原地打转。

| # | 项 | 阻塞方 | 须交付的最小动作 | 本件对应章节 |
|---|---|---|---|---|
| 2 | L2 底座对照数据 | **未认领** | 顶层：指派承接方 **或** 明确豁免并说明代价 | §一 T-1 |
| 3 | 引擎总决策（归档／回主线） | 顶层 | 一句话裁决 + 裁决口径 | §一 T-2 |
| 4a | FZ-RFC-1/2b 双签（顶层位） | 顶层 | 签署（**S-2 阻塞已移除**） | §一 T-3 |
| 4b | FZ-RFC-1/2b 双签（外部位） | 外部 AI 窗 | 独立追认并签署 | §三 T-6 |
| 5 | G1-C3 复核 → OD-3 闭环 | 砺·评审 | OD-3 闭环判词 | §二 T-4 |
| 6 | PhaseD v2 判读（U-06/U-47/U-07 定级） | 砺·评审 | 三条 U 项最终定级 | §二 T-5 |
| 7 | U-44 去向裁定 | 顶层 | 确认并入 `l1-b3-rescue` 或另开分支 | §一 T-3 |

---

## 一、【顶层窗口】三项裁决（最短解冻路径）

> **这三项一动，其余全是 paperwork。** 执行窗建议按 T-1 → T-2 → T-3 顺序裁决。

### T-1 · L2 底座对照数据：指派或豁免 🔴 **最大阻塞**

| 字段 | 内容 |
|---|---|
| 事项 | L2 底座对照数据（Codex CLI / Goose / OpenCode × A 组盲测）至今**无人认领** |
| 现状 | ⛔ 未启动。这是 7 项中唯一既无归属、又无排期的项 |
| **最小动作** | 二选一，一句话即可：<br>**(a) 指派**：指定承接窗口与截止；执行窗可承接，但需顶层先批准底座环境工时（需装三套对照 harness + 跑 A 组盲测）<br>**(b) 豁免**：明确"v0.2.23-final 归档不要求 L2 对照"，并**书面说明代价**——即归档版**不含**"相对成熟 harness 的定量对标结论"，后续若需对标须另起一轮 |
| 判定标准 | 顶层批复件中出现 (a) 或 (b) 任一明确表述 |
| 执行窗提示 | 若选 (b)，`freeze-candidate.md` §3 须同步把 #2 从"硬清单"移入"已知缺口并声明"，否则归档包会留一个说不清的洞 |

### T-2 · 引擎总决策：归档 ／ 回主线 ／ 继续观察

| 字段 | 内容 |
|---|---|
| 事项 | `l1-b3-rescue` 分支（L1 修复 A：非产物救回）的最终处置 |
| 已有依据 | D2 终判 **VERIFIED**（B 4/5、A 4/6 均达阈值）+ 三条必须同时引用的限制（见 `docs/core-freeze-candidate/L1-归档附页-D2终判与限制.md`）；追问类可比样本 pre 0/3 → post 2/3 |
| **最小动作** | 三选一并给出口径：<br>**(a) 归档**：主线 `117a947` 打 tag `v0.2.23-final`，分支留档不合并<br>**(b) 回主线**：分支合入，主线滚动到 v0.2.24 后归档<br>**(c) 继续观察**：写明还需什么数据、谁去取、什么时候再审 |
| 判定标准 | 明确 (a)/(b)/(c) + 一句话依据 |
| 执行窗提示 | 选 (a) 则归档仪式前置只剩 §一 T-3 与 §三 T-6 的签位；选 (b) 需追加一次合入后全量门禁（现为 493/0） |

### T-3 · FZ-RFC-1/2b 顶层签位 + U-44 去向

| 字段 | 内容 |
|---|---|
| 事项① | FZ-RFC-1/2b 双签的**顶层签位** |
| 原阻塞 | 顶层 2026-09-03 §二.6 措辞定版：RFC-1 维持阻塞态，**S-2 spawn 级集成测试补测复跑后方可签** |
| **阻塞已移除** | S-2 已通过：新增 `crates/sandbox/tests/s4_spawn_env.rs`（spawn 级，经 `LinuxSandbox` 跑 `sh -c env`），四断言全过——白名单可见／父进程 SECRET 不透传／**`HEARTH_EGRESS_ALLOWLIST`+`HEARTH_READ_ROOTS` 不被 `env_clear` 清掉（防白名单静默失效）**／`PATH` 在。已随全量门禁 493/0 跑通 |
| **最小动作①** | 在 `docs/core-revalidation/FZ-RFC-1-2b-双签追认签发件.md` 签署表"顶层（人工窗）"位签署 |
| 事项② | U-44 去向裁定 |
| **U-44 已闭环** | 施工 + 门禁验证均完成（2026-09-03 19:17）：`ensure_session_header()` 于 run start 落盘（仅缺失时创建、tmp+rename 原子、**绝不覆盖历史**），接入 `run_local.rs` 两个 run 起始点；单测 `test_u44_ensure_session_header_creates_and_never_overwrites`。门禁 `FMT_RC=0 / CLIPPY_RC=0 / TEST_RC=0`（证据 `docs/data/u44-gate-20260903.log`） |
| **最小动作②** | 一句话裁定：并入 `l1-b3-rescue` 分支（执行窗建议，同域 S3）／另开分支／暂不处置 |
| 判定标准 | 签位已签 + 去向已裁定 |

---

## 二、【砺·评审窗口】两项判读

> 均为**只读判读**，不改文件；如需修正，以裁定意见退回执行窗。

### T-4 · G1-C3 复核 → OD-3 闭环

| 字段 | 内容 |
|---|---|
| 依据 | `docs/core-freeze-candidate/open-deviations.md` **OD-3（第 14 行）**：「G1-PENDING：相关矩阵/persona/scenario 资产已落盘未过砺评审，campaign 不得引用」，处置＝「砺评审通过后解锁」 |
| 材料 | `docs/core-revalidation/G1-相关矩阵评审材料（执行窗→砺）.md`；资产已落盘（12 persona + 10 scenario + `correlation_matrix.json` + `sampler.py`） |
| 历史状态 | 砺曾给 **G1 CONDITIONAL PASS**，附条件：V 材料报 `0.065` 而 JSON 实际 `0.1047`，**方向不变但数值须修正** |
| **最小动作** | 给出 OD-3 的**闭环判词**：通过（附条件已满足，请注明 V 值是否已修正）／仍挂起（附尚缺什么） |
| 判定标准 | OD-3 在 `open-deviations.md` 中可移入"已闭环"清单 |
| 附带 | OD-10（B4 两个 MANUAL 项）随 G1 复核一并闭环，请同一份判词覆盖 |

### T-5 · PhaseD 补测 v2 判读：三条 U 项定级

| 字段 | 内容 |
|---|---|
| 依据 | `docs/core-revalidation/PhaseD-补测报告v2（执行窗→砺判读）.md` **§六「待砺裁定项」（第 64–68 行）** |
| **待裁三项** | **① U-06**：P-D5 混合 + 干净口径反证后，**维持 🔶 还是改判**（原 D5「read→write 依赖失败」归因**反转**为写域拒绝 U-46，是否成立）<br>**② U-47**：L6「条件性伤害」定性是否接受；W1 对照用例设计是否按此口径评审<br>**③ U-07**：F1 定级（Ollama qiyuan-8b：593s vs Agnes 11.2s＝**53×**、路径合规 **0/3**、假完成形态） |
| 执行窗立场 | 均为**初判**，执行窗不自定级；补测中发现了**归因反转**，故必须交砺裁 |
| **最小动作** | 三条各给一句定级 + 依据；如推翻执行窗初判，请指出反证所在 |
| 判定标准 | 三条 U 项在 `unified-ledger.md` 中获得最终定级 |
| ⚠️ 口径提醒 | U-06 的反转直接影响 `W1-反应回路改造设计小样 v0.1` §3.4 的立论基础；该小样的立项理由**只能是可用性**，**不得**引用"RC52 架构治愈" |

---

## 三、【外部 AI 窗】一项独立追认

### T-6 · FZ-RFC-1/2b 独立追认并签署

| 字段 | 内容 |
|---|---|
| 依据 | `docs/core-revalidation/FZ-RFC-1-2b-双签追认签发件.md` 签署表「外部 AI 窗」位＝**⬜ 待签** |
| 性质 | **事后追认**（两项改动均已实施并进入 v0.2.23 发版）：签认＝确认「变更申报内容、实施证据、不变量清单」三者一致且授权链完整 |
| 材料包 | `docs/core-freeze-candidate/external-review-pack-v1.md`（Q1–Q6 已预注册）；另有 `FZ-RFC-2b-RC52三臂统一消费端.md`、`FZ-RFC-1-2b-双签追认签发件·砺补充意见.md` |
| **最小动作** | 交叉核对证据与源码后，在签署表「外部 AI 窗」位签署；若发现不一致，以书面意见退回 |
| 判定标准 | 签位已签，或书面退回意见已给出 |

---

## 四、【执行窗】待命项（**须顶层下令才启动**）

| 项 | 说明 | 触发条件 |
|---|---|---|
| B 臂严格口径补跑（补满 5 条） | 现仅 **B1**（`completed / 124 次工具交换 / UNVERIFIED / 37 步 / **censoring=[] 零删失**）。单条实测 6~15 分钟，5 条将独占 .131 约 3 小时 | 顶层明确下令。**当前不建议启动**——砺 §2 处置 2 原文为「标注**或**补跑」二选一，标注已完成，补跑属可选加固，**对解冻无贡献** |
| R1 归档仪式 | `git tag v0.2.23-final` + `git archive` 日期戳 tar 存 Desktop（不可逆动作） | §一 T-2 裁 (a) 或 (b) 且 §一 T-3、§三 T-6 签位齐 |
| cognition MVP 开工 | 新战场 | T-2 裁决后；验收口径须含 A 组盲测重跑（顶层 R5 已批） |

---

## 五、给各窗口的共同约束

1. **数字口径（⚠️ 本条已被顶层签发件 T-2 修正，以修正后为准）**：门禁须**分树表述**——
   - **tag 树**（v0.2.23-final，`117a947`）＝ **486 / 0**（130 agent-core + 356 rest）；
   - **分支树**（`l1-b3-rescue`，`18ccd85`）＝ **493 / 0 / 1 ignored**（135 agent-core + 358 rest）。
   归档报告**两数并列、各注口径**；**不得混标，不得以分支树 493 冒充 tag 树读数**。486/491/493 三者各自合法；"497""45:23"无出处，**维持禁引**。
   > 代拟单原写"门禁以 493/0 为准"，**该表述已被签发件纠正**（归档对象受审的是 tag 树）。
2. **行号会漂移**：引用 `loop.rs` / `run_local.rs` 等锚点前**必须重新 grep**，勿用记忆行号（本轮已两次实测漂移）。
3. **环境变更已登记**：宿主机重启导致 .131 重启，**用户 VPN 断开**，出口 IP 由挪威 `45.88.202.26` → 上海电信 `183.192.126.72`（Agnes 探活 401＝可达，provider 未变）。
   > **签发件 T-5 已给初判（不代裁）**：出口 IP 变更**不构成不可比因素**（provider/model/key 三者未变），**默认按可比处理，勿为此阻塞 T-5**；砺若发现时延分布异常可单独提出。
4. **执行窗不自我判定**：以上所有判读/裁决均不由执行窗出具结论；执行窗只负责按裁定施工并交证据。

## 六、签发栏

| 窗口 | 签发人 | 签发意见 | 日期 |
|---|---|---|---|
| 顶层（人工窗） | 顶层 | ☑ **修改后发**（T-1 裁 b+回收 / T-2 裁 a+口径分树 / T-3 双项落定；其余照发） | 2026-09-03 |

> **已签发**——详见 `docs/core-freeze-candidate/冻结解冻派工单顶层签发件 v1.0（顶层）.md`。各窗口凭**本件 + 签发件**开工。
