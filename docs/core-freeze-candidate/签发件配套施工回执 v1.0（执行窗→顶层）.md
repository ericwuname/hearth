# 签发件配套施工回执 v1.0（执行窗 → 顶层）

> **对象**：`冻结解冻派工单顶层签发件 v1.0（顶层）.md`
> **施工窗口**：执行窗　**日期**：2026-09-03 20:20　**基线**：`117a947` / 分支 `l1-b3-rescue`（`18ccd85`）
> **性质**：施工完成报告 + 证据；**不含任何判读/裁决结论**（判读归砺，裁决归顶层）

## 一、施工清单（逐项对照签发件）

| 签发件条款 | 要求动作 | 状态 | 落点（文件 / 位置） |
|---|---|---|---|
| **T-1 配套施工令** | `freeze-candidate.md` §3 把硬清单 #2 移入"已知缺口并声明"，注明豁免依据＝本件 T-1 | ✅ 完成 | `freeze-candidate.md` §3 表格 #2 行（标注"已裁豁免 → 移出硬清单"）+ **新增 §3.4「已知缺口并声明（豁免项）」**（含豁免依据／代价声明／回收路径三列） |
| **T-2 口径修正** | 门禁改为**分树表述**，两数并列、各注口径，不得以 493 冒充 tag 树读数 | ✅ 完成 | `freeze-candidate.md` §3.3 重写为「全量门禁复验 · **分树口径**」+ 增量勾稽表；§3.2 末行同步；**代拟单 §五.1 已标注"本条被签发件 T-2 修正，以修正后为准"** |
| **T-3① 签位登记** | 在 `FZ-RFC-1-2b-双签追认签发件.md` 顶层位登记签发件为准签凭证 | ✅ 完成 | 该件签署表「顶层（人工窗）」位由 ⬜ 未签署 → **✅ 准签**（附凭证指针 + S-2 移除说明 + "外部签位到位前不产生完整效力"警示），并附执行窗登记记录 |

## 二、数字复核结论（防墙头草：复核基于实测，不基于签发件自述）

签发件 T-2 载「tag 树 = 486/0（**130** agent-core + 356 rest）」，与执行窗今日实测分支树 **agent-core = 135** 表面差 5。执行窗**未直接采信也未直接质疑**，先做了分解验算：

| 树 | agent-core | rest | 合计 |
|---|---|---|---|
| tag 树（`117a947`） | **130** | **356** | **486** |
| + 修法 A（`0ce93aa`，L1 非产物救回五测试） | 135 | 356 | 491 |
| + U-44（`a7db27a`/`8a4df33`） | 135 | 357 | 492 |
| + S-2（`s4_spawn_env.rs`） | 135 | **358** | **493**（＝今日实测值） |

**三式双向验算同真**：`130+356=486`、`135+358=493`、`486+5+1+1=493`。

> **结论：签发件数字无误，与实测无差异。** 130 是 **tag 树**值、135 是 **分支树**值，差 5 正是修法 A 的五个测试。
>
> ⚠️ **执行窗自纠一处**：本窗早前曾记「491 = agent-core **135** + workspace **356**」——该分解把**分支树的 agent-core** 与 **tag 树的 rest** 混算，虽总和凑巧正确，但**分解错误**，会误导后续任何按分解做的推断。正确分解如上表，已写入 `freeze-candidate.md` §3.3 并显式标注自纠。

## 三、顺带修复（执行窗自身造成的结构损坏）

| 问题 | 成因 | 修复 |
|---|---|---|
| `freeze-candidate.md` §3.2 表格被截断：`hearth 二进制`、`HEARTH_ALLOW_NO_CGROUP` 两行掉到了 §3.3 之后，表格断裂 | 执行窗前一轮编辑时，把 §3.2 末行替换成含 §3.3 引用的内容，未同步搬走后续行 | ✅ 两行已归位至 §3.2 表格内，§3.2/§3.3/§3.4 层级恢复完整 |

> 主动披露：该项**非签发件要求**，是执行窗自查发现的自身缺陷，已修，不留隐患给归档包。

## 四、剩余阻塞（施工完成后）

| 阻塞线 | 项 | 归属 | 备注 |
|---|---|---|---|
| **判读线** | #5 G1-C3/OD-3 闭环判词（须注明 V 值 0.065→0.1047 修正状态） | 砺 | 签发件 T-4 照发 |
| **判读线** | #6 PhaseD v2 三条 U 定级（U-06／U-47／U-07） | 砺 | 签发件 T-5 照发；出口 IP 顶层初判**不构成不可比**，勿为此阻塞 |
| **签位线** | #4 外部签位（T-6，FZ-RFC 独立追认） | 外部 AI 窗 | **R1 归档仪式的唯一剩余硬前置** |

**执行窗侧：签发件所指配套施工已全部完成，无待办。**

## 五、请示一项（执行窗不自行开工）

签发件 §三载：「**cognition MVP 开工：待命，T-2 已裁 (a) 即视为解冻**；归档仪式不阻塞 cognition 开工（两者可并行）」。

据此，**cognition MVP 在法律上已解冻**，且可与归档并行。执行窗**暂不自行开工**，请顶层确认以下两点后下令：

1. **开工时点**：现在即开工，还是等 R1 归档仪式完成后再开工？
2. **验收口径确认**：A 组盲测重跑（全新 session，R5 已批）＋ T-1 的对照数据回收（主力底座天然即对照面，一份跑测两用）——**这两项是否即构成 MVP 验收的完整口径**，还是另有验收项？

> 理由：MVP 属新战场、涉及三红线（接口只用标准协议／MVP 先标准底座+适配器／范围收敛三件），开工前确认口径可避免返工。执行窗不自我判定。

## 六、非阻塞挂账（已阅，登记防遗失）

签发件 §四三项挂账，执行窗已阅并记录于本回执，待归档后随 cognition 窗口处置：
1. 追问类回归子集 5 条（A5/A6/A7/A9/A10，黄金集 #21/27-30/32）；
2. U-06 台账行更新、#21/b5 同型 T4 stall（U-48 候选）交砺、口径三争议维持登记；
3. U-27/U-26 登记待修。

---

**提交**：本轮回执 + 配套施工改动（`freeze-candidate.md` §3/§3.1/§3.2/§3.3/§3.4、`FZ-RFC-1-2b-双签追认签发件.md` 签署表、代拟单 §五.1/§六）。
