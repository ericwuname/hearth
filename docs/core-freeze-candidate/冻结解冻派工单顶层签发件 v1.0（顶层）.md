# 冻结解冻派工单 · 顶层签发件 v1.0（顶层）

> **对象**：`docs/core-freeze-candidate/派工单-冻结解冻各窗口任务命令 v1.0（执行窗代拟·待顶层签发）.md`
> **签发结论**：⬛ **修改后发**——两项核心裁决当庭落定（T-1 / T-2），一处口径修正（§五.1），其余照发。
> **日期**：2026-09-03　**基线**：v0.2.23（commit `117a947`）／分支 `l1-b3-rescue`（`18ccd85`）
> **性质**：本件与代拟单合并后即为有效任务书；各窗口以本件签发栏为开工凭证。

---

## 〇、签发前抽验记录（防墙头草条款：签发基于证据，不基于执行窗自述）

| # | 代拟单主张 | 抽验方式 | 结果 |
|---|---|---|---|
| 1 | S-2 补测已落地（`crates/sandbox/tests/s4_spawn_env.rs`） | Glob 本机源码树 | ✅ 文件在 |
| 2 | U-44 施工完成（`ensure_session_header()` 接入 run 起始） | grep `crates/` | ✅ 命中 `codex-cli/src/run_local.rs` + `session_store.rs` |
| 3 | 全量门禁 493 passed / 0 failed | 读 `docs/data/gate-full-20260903.log` 尾部 | ✅ `TOTAL_PASSED=493`，时间戳 2026-09-03T19:22:53+08:00，与代拟单一致 |
| 4 | 矩阵 Runner 6 处切 strict（砺 §2 处置 3） | grep `tools/simuser/` | ✅ `run_rc52_matrix.py` 6 处调用点全走 `send_and_wait_terminal_strict` / `wait_terminal_strict`；`driver.py` 的 `TERMINAL_MARKER_MAP` 在 |
| 5 | B 臂采集口径标注（砺 §2 处置 2，"标注或补跑"二选一取标注） | grep 附页 | ✅ `L1-归档附页-D2终判与限制.md` 已写入 legacy 口径标注+缺陷方向（低估=保守）+ censoring bias 限制 |
| 6 | 附页"已修 ✅"改"部分已修"（砺 §2 处置 5） | grep 附页 | ✅ 补正措辞已入文 |

**结论：代拟单"执行窗侧待办已归零"的主张属实，抽验 6/6 全中。** 具备签发条件。

---

## 一、三项裁决（代拟单 §一，当庭落定）

### T-1 · L2 底座对照数据 → 裁 **(b) 豁免独立轮 + 经 cognition 验收回收** 🔴 已解

| 字段 | 裁定 |
|---|---|
| 选择 | **(b) 豁免**，但非弃权：豁免的是"独立 L2 对照轮"，不豁免对照数据本身 |
| 代价声明（照执行窗提示采纳） | 归档版 v0.2.23-final **不含**相对成熟 harness 的定量对标结论；后续若需对标须另起一轮 |
| 回收路径 | 对照数据最小子集并入 **cognition MVP 验收口径**（A 组盲测复跑、全新 session、主力底座）——主力底座（Codex CLI 等）天然就是对照面，一份跑测两用，不再单开 L2 轮 |
| 配套施工令 | 执行窗按代拟单 §一 T-1 提示办理：`freeze-candidate.md` §3 把硬清单 #2 从"硬清单"移入"已知缺口并声明"，注明豁免依据=本件 T-1 |
| 依据编号 | 双底座裁定（docs/Hearth 双底座架构裁定 v1.0）三红线之"MVP 先标准底座"；战略转向路径书 1+1>2 框架——对照的意义在证明 cognition 层增值，不在给引擎归档背书 |

### T-2 · 引擎总决策 → 裁 **(a) 归档主线，分支留档不合并**

| 字段 | 裁定 |
|---|---|
| 选择 | **(a)**：主线 `117a947` 打 tag `v0.2.23-final`；`l1-b3-rescue` 分支**留档不合并、不删** |
| 依据（按防墙头草条款引证据编号） | ①外部评估与 FZ-RFC 的受审对象=117a947（external-review-pack-v1 八锚点均锚定该 commit），归档包必须与受审对象逐字节一致；②D2 终判 VERIFIED 但**三条限制同封存**（附页明载：零余量、两侧非同类证据、收窄未消除）——零余量不满足"回主线"所需的余量条件，回主线属风险动作而非收口动作；③修法 A / U-44 / S-2 以分支 commit 链（`0ce93aa→a7db27a→8a4df33→18ccd85`）永久留档，登记状态="已验证待合并"，cognition 底座实验若需可 cherry-pick |
| **口径修正（对代拟单 §五.1）** | 原"门禁以 493/0 为准"**修正为分树表述**：**tag 树（v0.2.23-final）=486/0**（基线门禁，130 agent-core + 356 rest）；**分支树=493/0**（含 U-44 + S-2 + 修法 A）。归档报告两数并列、各注口径，**不得混标也不得以 493 冒充 tag 树读数**。486/491/493 三个数字各自合法，"497"无出处维持禁引 |
| 归档仪式触发条件 | 本件 T-3①签位完成 **且** T-6 外部签位到位（外部签位是硬前置，维持《执行窗汇总报告批复》原判不放松） |

### T-3 · FZ-RFC-1/2b 顶层签位 + U-44 去向 → **双项落定**

| 事项 | 裁定 |
|---|---|
| ① RFC-1 顶层位 | **准签**。S-2 阻塞移除已抽验属实（抽验 #1/#3/#4）；RFC-2b 维持 09-03 批复"即签"口径。执行窗在 `FZ-RFC-1-2b-双签追认签发件.md` 签署表"顶层（人工窗）"位登记本件为准签凭证；**注意 RFC-1 仍是双签制，外部签位（T-6）到位前不产生完整效力** |
| ② U-44 去向 | **并入 `l1-b3-rescue`**（采纳执行窗建议，同域 S3）。与 T-2 裁 (a) 自洽：U-44 随分支留档，状态="已验证待合并"，不进 v0.2.23-final tag（原 PhaseD 终审即定位其修法归 P5 段 2，提前施工不改变去向） |

---

## 二、判读项批复（代拟单 §二/§三）

| 项 | 批复 |
|---|---|
| T-4（砺：OD-3/OD-10 闭环判词） | **照发**。补充：判词须显式注明 V 值修正状态（0.065→0.1047 口径差是否已回写材料），闭环才算完整 |
| T-5（砺：U-06/U-47/U-07 三条定级） | **照发**。⚠️ 口径提醒照抄采纳：W1 小样立项理由只能是可用性，禁引"RC52 架构治愈"。**补充一项初判供砺参考（不代裁）**：出口 IP 变更（VPN 断开，挪威→上海电信）**不构成不可比因素**——provider/model/key 三者均未变、Agnes 探活 401 可达，可比性阻断条件是"换 provider=换模型层"而非出口 IP；砺若发现时延分布异常可单独提出，但默认按可比处理，勿为此阻塞 T-5 |
| T-6（外部 AI 窗：FZ-RFC 独立追认） | **照发**。材料包 external-review-pack-v1 + Q1–Q6 预注册六问口径不变；用户转发材料包即触发 |

---

## 三、执行窗待命项（代拟单 §四）批复

| 项 | 批复 |
|---|---|
| B 臂严格口径补跑（补满 5 条） | **维持"当前不启动"**。采纳代拟单解读：砺处置 2 原文为"标注**或**补跑"，标注已做且抽验属实（#5），补跑属可选加固、对解冻无贡献。严格版补跑目录 `~/fa/l1-b-strict/` 的 B1 单条结果（censoring=[] 零删失）已足以为 legacy 口径标注提供佐证。若砺在 T-5 判读中认为必须补跑，另行下令 |
| R1 归档仪式 | **待命，触发条件=T-3①+T-6 双签位齐**。动作照代拟单：`git tag v0.2.23-final` + `git archive` 日期戳 tar 存 Desktop。归档报告门禁口径按本件 T-2 修正执行（486/0 与 493/0 分列） |
| cognition MVP 开工 | **待命，T-2 已裁 (a) 即视为解冻**。三红线与验收口径照双底座裁定执行；验收须含 A 组盲测重跑（全新 session，R5 已批）+ 本件 T-1 的对照数据回收。归档仪式不阻塞 cognition 开工（两者可并行） |

## 四、非阻塞挂账（不进冻结硬清单，登记防遗失）

1. **追问类回归子集 5 条**（A5/A6/A7/A9/A10，黄金集 #21/27-30/32）：归档后随 cognition 底座实验窗口择期跑测，修法 A 边界量化用。
2. U-06 台账行更新、#21/b5 同型 T4 stall（U-48 候选）交砺、口径三争议（61vs63/18vs12/28.6%）维持登记待裁决。
3. U-27/U-26 登记待修。

## 五、签发栏

| 窗口 | 签发人 | 签发意见 | 日期 |
|---|---|---|---|
| 顶层（人工窗） | 顶层 | ☑ **修改后发**（T-1 裁 b+回收 / T-2 裁 a+口径分树 / T-3 双项落定；其余照发） | 2026-09-03 |

> 各窗口凭本件+代拟单开工。砺·评审自 T-4/T-5 起；执行窗自 T-1 配套施工 + RFC 签位登记起；外部 AI 窗由用户转发材料包后启动 T-6。
