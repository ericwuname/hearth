# FREEZE CANDIDATE · open-deviations（先行件）

> **Node 15 八文件之一（先行起草）**｜执行窗 2026-09-02（**v1.1 修订**：按裁决书 §2 O-1~O-7 更新）｜基线 tag `v0.2.23`
> **状态**：DRAFT——随 Step 2/3 数据滚动更新；**每条必须显式披露，不得隐瞒**
> **分级**：🔴 阻塞冻结 / 🟡 带入冻结需顶层知情 / 🔵 观察项
> **当前口径**：**开放 11 项 + 已闭环 7 项**（**v1.2 修订 2026-09-03**：OD-3 / OD-10 按砺 T-4 判词闭环并移入下方已闭环清单；**OD-13 维持开放**，不得顺带闭环。v1.1：新增 OD-12/OD-13，OD-6/OD-11 升 🟡，OD-8 附顶层语义裁定）

## 开放偏差清单

| # | 级 | 偏差 | 证据 | 处置 |
|---|---|---|---|---|
| OD-1 | 🟡 | **B 臂（前缀重放/继续型续作）失败模式残留**：产物为 0 的续作轮 reflect 反复 replan → give_up 终止；PLAN 阶段 stall 到不了 reflect 臂，三臂 RC52 路由不触发（设计内边界，但用户可感） | 盲测 v0.2.23 #7/#8/#10 FAILED + Node14 B 5/5 | 带入冻结如实登记；v0.3 候选修复 |
| OD-2 | 🟡 | **FZ-RFC-1/2b 双签追认未闭环**（时点=Step 2 campaign 期间，Node 15 前硬门槛） | 放行令 §2 COND-6 | campaign 运行期间完成追认，未闭环则 Node 15 顺延 |
| ~~OD-3~~ | ✅ | ~~**G1-PENDING**：相关矩阵/persona/scenario 资产已落盘未过砺评审，campaign 不得引用~~ → **已闭环**（2026-09-03 砺 T-4 判词收讫生效），**campaign 引用已解锁**。⚠️ **随附口径铁律：引用矩阵精确 V 值必须同时带「n=273、抽样噪声 ±0.15」——闭环 ≠ 解禁精确值引用，矩阵只定方向**。详见下方已闭环清单 | 顶层签发件 §一.1；判词出处＝砺本件 §六 | ✅ 已闭环，移入已闭环清单 |
| OD-4 | 🟡 | **workspace 全量测试挂起**（RT4-OOM 探针 socket 死锁，2 次复现）——门禁被迫拆分口径 | 2026-09-01 门禁记录 | 缺陷登记待修；测试期避开全量口径 |
| OD-5 | 🔵 | **沙箱内 cargo test exit 159 core dump**（seccomp 杀子进程 syscall，v0.2.23 盲测 #27 路径现场出现）——bash 工具 seccomp 白名单对 cargo 构建链不全 | 盲测日志 L1062 | 登记；与 getent 家族同源（v0.2.22 修了 send 侧，clone/构建链 syscall 待核） |
| OD-6 | 🟡 | **「✗ Done (N steps)」渲染语义混乱**（failed 轮也打 Done 记号，用户主诉"需要猜"的渲染成因之一）——**2026-09-02 顶层裁决 O-1 批准升级 🟡**（A 组正式盲测 L551 用户原话直接证据） | 盲测 #12 用户原话 + A.txt L551 | 禁改令内登记，v0.3 投影层修复候选 |
| OD-7 | 🔵 | **PLAN 阶段 stall 无 💭 依据投影**（do_plan_inner 失败仅裸错误行）；A 组正式盲测复现（A3 未先 introspect） | 盲测 #12 T4 现场输出 + A.txt | 禁改令内登记 |
| OD-8 | 🔵 | **空输入触发 FAILED**（#5 空输入 33 步失败；golden #05/#18/#34/#41 同项）——**顶层裁决 O-7 已定语义：NOP + 轻提示**（"输入为空，如需继续请输入内容"），不 FAILED 不静默；v0.3 修复方向，登记不即修 | 盲测统计 + 裁决书 O-7 | 顶层已裁定，v0.3 修复候选 |
| OD-9 | 🔵 | **B1 判定器 give_up 主导性检查**（COND-4）落地前，历史 B1 数据判读歧义保留 | 砺评审报告 §裁定二 | 测试窗 campaign 判定前落实 |
| ~~OD-10~~ | ✅ | ~~**B4 两个 MANUAL 项**（R1 十域场景映射、四大域 ≥10% 场景级映射）~~ → **随 OD-3 同批闭环**（2026-09-03 砺 T-4 判词，OD-10 随同闭环） | validate_assets.py；顶层签发件 §一.1 | ✅ 已闭环，移入已闭环清单 |
| OD-11 | 🟡 | **VM 源树落后/缺失**——砺报告 T-4 实测 `.131 ~/codex` NO_CODEX（后核实为 `git log` 误报：tar 同步法无 `.git`，树存在但 simuser 资产缺）——**2026-09-02 15:40 已解除**：E7 增量同步双 VM（.131 `~/codex` / .133 `~/codex_t`），simuser Stage 2 全套 + 语料 JSON 在位，`crates/*.rs` touch 强制重建；gate 复跑已发起（`~/gate-rerun-0902.log`） | 2026-09-02 同步记录 | ✅ 前置解除；gate 486/0 复跑结果回填后本项可闭环 |
| OD-12 | 🟡 | **失败时丢失已成功并行结果**（裁决书 O-3 新登记）：B1 白名单拒绝致 give_up 后，已成功的 example.com 200 子结果未随终态投影（任务列表仍 [Pending]）——违反"产物清单投影"精神 | B.txt L55/L77（顶层抽验 T-3） | v0.3 修复候选 |
| OD-13 | 🔵 | **多行粘贴 `{` 入口未触发**（裁决书 O-4 新登记）：前导文本干扰入口检测 → 截断防护无从验证（B4 场景实测） | B.txt（B4 项实测） | v0.3 修复候选；盲测包 v1.1 已补 BP-1 备选观察点。<br>⛔ **2026-09-03 顶层签发件 §一.1 明示：维持开放，不得随 OD-3 顺带闭环**（砺范围声明已被采纳） |

## 已闭环（本轮确认，防止陈旧记忆误报）

- ~~bash 出网零管控~~ → egress 白名单 23 域收口（v0.2.22+配置），盲测 B1 探针可复验。
- ~~"key 直透子进程"🔴~~ → 实测不成立，S4 env_clear 兜底已落地（FZ-RFC-1）。
- ~~超长写毁原文件 / kill 丢整轮 / 零快照~~ → S1/S2/S3 落地。
- ~~C 条件 BLOCKED~~ → R-1 解封（砺裁定 + J-1/J-2/J-3 首跑验证前置）。
- ~~宪法 FALLBACK 静默~~ → C-3 告警落地（v0.2.21）。
- ~~**G1-PENDING（OD-3）**~~ → **2026-09-03 砺 T-4 判词收讫生效，闭环**（顶层签发件 §一.1 采纳），campaign 引用解锁；判词出处＝**砺本件 §六**。
  ⚠️ **随附口径铁律（不可省略）**：引用相关矩阵的**精确 V 值必须同时带「n=273、抽样噪声 ±0.15」**——**闭环 ≠ 解禁精确值引用，矩阵只定方向**。
- ~~**B4 两个 MANUAL 项（OD-10）**~~ → **随 OD-3 同批闭环**（2026-09-03 砺 T-4 判词）。

> **v1.2 范围声明**：本轮**仅** OD-3 / OD-10 闭环。**OD-13（多行粘贴 `{` 入口，🔵）维持开放**——顶层签发件 §一.1 明示不得顺带闭环，砺范围声明已采纳。
