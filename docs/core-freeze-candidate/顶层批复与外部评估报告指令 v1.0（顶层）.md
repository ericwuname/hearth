# 合并终审顶层批复 + 外部评估报告指令 v1.0（顶层）

> **签发**：顶层　**日期**：2026-09-03　**收件**：执行窗口（主）／砺／外部 AI 窗（知会）
> **审批对象**：①《合并终审报告》（执行窗 09-03 04:10）②《FZ-RFC-1/2b 双签追认签发件》③砺补充意见
> **一句话**：**六项决策 D1-D6 全部批复（§二），FZ-RFC 按砺条件签（RFC-2b 顶层即签、RFC-1 待 S-2 补测后签）**（§三）；**执行窗按 §四规格产出《外部评估材料包 v1》**——给 Claude/ChatGPT 的零上下文评估件，这是外部签字（签字栏第四行）的前置。

---

## 〇、顶层抽验记录（2026-09-03 06:10 本地实测）

| # | 主张 | 实测 | 判 |
|---|---|---|---|
| 1 | `l1-b3-rescue` 分支存在 | `git branch` 命中（当前分支） | ✅ |
| 2 | 冻结八文件齐 | `docs/core-freeze-candidate/` 实测 9 件（八文件 + unified-ledger 并账产物） | ✅ |
| 3 | L3 知识卡五张 | `docs/Hearth 知识迁移卡×5（L3·归档蒸馏）.md` 存在（最新产出） | ✅ |
| 4 | evidence-index | 存在（48 行） | ✅ |
| 5 | L1 门禁 491/0、B 臂 4/5 | VM 侧数据（`~/fa/l1-postfix/`），本地不可验 → **列入外部评估包必附原始数据**，外部模型可凭复现命令独立核验 | ⚪ 转外部 |

---

## 一、FZ-RFC 双签批复

| 项 | 顶层裁定 |
|---|---|
| **RFC-2b**（RC52 三臂统一消费端） | **顶层即签 ✅**——砺判证据充分（四锚点全中），残留 OD-1 已显式登记 |
| **RFC-1**（env_clear + 白名单） | **有条件签**：S-1（白名单数字修正为"4 项白名单 + 1 项工具层 PATH"）**立即改**；**S-2（spawn 级集成测试）必须补**——砺指出的反向安全回归风险（`tool_env` 注入被清 → egress 白名单静默失效）是真实缺口，1 个用例的成本必须花；S-3~S-6 随附。补测复跑 gate 后顶层签 |
| 双签时序 | 外部 AI 签位随《外部评估材料包》（§四）一并征签——外部模型独立核验砺 §6 八查清单后签 |

---

## 二、六项决策单批复（合并终审报告 §六）

| # | 决策 | **顶层批复** | 附加条件 |
|---|---|---|---|
| D1 | v0.2.23-final 归档 | **批准（预授权）** | 归档仪式在《外部评估材料包》返回且外部签位完成后再执行——外部独立复核是归档前置 |
| D2 | L1 预签规则 | **批准** | B 臂 4/5 已达标；A 臂数据落地即按规则自动判定，不再开会 |
| D3 | L2 底座对照跳过 | **批准（有条件）** | 同意"选型并入 MVP 实测"，但 **MVP 验收必须含同一份 A 组盲测复跑（全新 session）**——保留可证伪性，防止"跳过实验"退化为"跳过验证" |
| D4 | U-44 并入 l1-b3-rescue | **批准** | 半小时级，随 L1 结论一起归档 |
| D5 | 知识卡五张 | **批准**（顶层已验存在） | 卡 2 按 RC1 修正版口径为准（砺补意见与 Claude 批判已吸收） |
| D6 | 产出秩序令 v2 | **批准** | 与 v1.1 治理条款（辩护/修正通道）合并执行 |

---

## 三、给砺的待签项确认

G1-C3 复核 / PhaseD 补测判读 / L1 post-fix 数据判读（按 D2 规则）——**随本批复一并放行**，砺按既定判据执行，出具一页判定即可，不需新流程文档。

---

## 四、【核心指令】《外部评估材料包 v1》规格（执行窗产出）

**目标读者**：零上下文的外部模型（Claude / ChatGPT）。它们**不认识**任何内部编号、窗口角色、文档路径、项目黑话。这份材料的质量标准：**一个从未见过本项目的外部 AI，读完即可独立评估并给出可复核意见。**

**文件**：`docs/core-freeze-candidate/external-review-pack-v1.md`

### 4.1 十节结构（必含）

1. **项目一句话 + 评估请求**：Hearth 是什么（27 crates Rust 自研 coding agent harness，对标 Codex CLI/Claude Code）；请外部评估的 4-6 个具体问题（见 4.3）；
2. **两个月时间线**：v0.2.x 迭代 → 用户盲测暴露 80% 失败率 → P4/P5 双总包 → 外部三报告对抗 → 根因三层死锁 → L1 修法 A → 战略转向（1+1 分离 / 双底座）→ 现在的归档决策点。每行带日期；
3. **关键事实与数据（全部带锚点）**：三层死锁（目标路由/产物判定/完成判定，各带 loop.rs 行号——**标注"核验当日实测"**）、497:0（测试数:黄金集断言数）、L1 修法 A（B 臂 27%→80%，门禁 491/0）、RC1 教训（事实校验 vs LLM 自报）、F1 弱模型放大（53×耗时 0/3 合规）、OR=75.1 p=1.14e-10；
4. **对抗证据全景（关键节——不许只放我方叙事）**：
   - 外部三报告结论摘要（终审"继续但 W1 唯一主线" / 深挖"三层死锁+三断点" / 批判"不应继续存在"+其撤回的五条过度批评）；
   - Claude 两段批判（墙头草 15 分钟转向 / "更便宜"账目矛盾 / RC1 发现）；
   - 砺的关键反对意见（FZ-RFC 补充意见 S-1~S-6 全文引用；对内部 L6 "零模型调用"错误的更正过程）；
   - 内部各窗角色与分工（一段）；
   - **顶层认错记录**（墙头草、账目矛盾、抽验口径缺陷——原样放入，不美化）；
5. **已做决策与依据**：D1-D6、双底座架构（hearth-cognition 独立成体 + 三底座可切换 + 三红线）、L1 修法 A 内容（非产物救回口：criteria 空 + 0 errors + 工具交换 ≥1 → Done+UNVERIFIED）；
6. **未决与分歧清单（显式，不隐藏）**：61vs63 字段口径差、18vs12 终止出口口径差、L2 跳过的争议（D3 的取舍与保留条件）、B 臂 post-fix 4/5 样本量小、A 臂数据未落地；
7. **FZ-RFC 双签待决项**：RFC-1/2b 申报内容 + 砺补充意见全文要点 + S-2 补测的现状；
8. **可复现验证入口**：砺 §6 八查清单（grep 命令原样）+ 门禁复现命令（`unset HEARTH_URL` + `HEARTH_CGROUP_BASE` 模板，注明两作用域）+ 数据文件路径（`~/fa/l1-postfix/`、`docs/data/`）+ **行号漂移警告（核验当日实测，勿用记忆行号）**；
9. **请外部模型回答的问题清单**（见 4.3）；
10. **诚实披露**：样本量小（B 臂 5 跑）、自指涉（本材料也由参与方制作）、口径争议未裁决项、congition/外部评估本身也是文档生产的自指问题。

### 4.2 写作纪律

- **自包含**：每个内部编号（RC52/U-44/OD-1/L1/D2…）首次出现必须一句话解释；
- **表格优先**，关键数字加粗，禁止散文式论证；
- **引用内部文档一律附路径 + 一句话内容摘要**；
- 篇幅 300-500 行——**详细到能独立评估，短到外部模型读得完**；
- 产出后登记 evidence-index，并同步本机 `docs/data/`（若需携带原始数据摘录）。

### 4.3 请外部模型回答的问题清单（预注册，产出前写定不得事后增删）

- **Q1**：给定三层死锁证据链与 B 臂 27%→80% 复测数据，"框架替模型做完成判定 + 问答路径缺失"的根因诊断是否成立？有哪些**未被排除**的替代解释？
- **Q2**：RC1 教训（完成置位生产者=事实校验，非 LLM 自报）与"模型驱动节奏"的合成架构是否正确？在 Codex CLI/Goose 等底座上实现时，如何避免重蹈"模型自报假完成"？
- **Q3**：FZ-RFC-1/2b 事后追认：给定申报内容、砺补充意见（S-1~S-6）与八查清单，是否同意签署？S-2 补测是否为必要前置？
- **Q4**：v0.2.23-final 归档 + 引擎降级为可选底座 + cognition 独立成体的路线，有哪些**未识别的风险与盲点**？
- **Q5**：双底座三红线（接口/顺序/范围）是否足以防"两头烧"与"wrapper 变厚"？需要补哪条？
- **Q6**：多窗口治理（顶层/执行/测试/评审 + 外部交叉）在 497:0 与 45:23 的激励结构证据下，最优先的流程修正是什么？

---

## 五、时序

```
今天：执行窗产出 external-review-pack-v1（本指令 §四规格）→ 用户转发 Claude/ChatGPT
外部意见返回 → 砺 + 顶层联合判读 → RFC-1（S-2 补测后）+ 外部签位落笔
       → D1 归档仪式执行（git tag v0.2.23-final + archive 存 Desktop）
       → 认知层 MVP（L2.5）在主力底座开工
```
