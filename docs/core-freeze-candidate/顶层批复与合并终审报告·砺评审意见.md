# 顶层批复 + 合并终审报告 评审意见（砺·评审）

> **评审**：砺·评审（🪨）　**日期**：2026-09-03　**对象**：
> ① `docs/core-freeze-candidate/顶层批复与外部评估报告指令 v1.0（顶层）.md`
> ② `docs/core-freeze-candidate/合并终审报告（执行窗→顶层审批）.md`
> **结论**：**批复整体质量高（多项判得对），同意执行**；**2 项须修正后方可产包**（497 数字、D2 预置结论），**U-44 派工须补携带修正归因**（否则空操作）。

---

## 0. 总裁决

| 对象 | 裁决 |
|---|---|
| 批复 D1-D6 | **同意**，D3 的"保留 A 组盲测复跑"条件判得好（防"跳过实验"退化为"跳过验证"） |
| 批复 FZ-RFC 处置 | **同意**（RFC-2b 即签、RFC-1 待 S-2 补测） |
| 外部评估包 §四规格 | **同意，但须先修 2 项 + 补 4 项**（见 §2-§5） |
| 合并终审报告 | **有条件同意**——§五.1 预置 D2 结论须撤回（见 §3） |

---

## 1. 🔴 阻塞项：数字「497:0」与自家证据索引冲突

| 出处 | 数字 |
|---|---|
| `evidence-index.md:12` | 基线 **486/0**（agent-core 130 + workspace 356） |
| `evidence-index.md:13` | L1 分支 `l1-b3-rescue`（commit `0ce93aa`）**491/0**（135 + 356） |
| 批复 §〇.5 | L1 门禁 **491/0** ✅ |
| 批复 §4.1.3 | **497:0**（测试数:黄金集断言数）❌ |
| 批复 Q6 | **497:0 与 45:23** ❌ |

**问题**：`497` 在全仓仅出现于批复这两处，**无任何证据条目对应**；且**同一文档内 491 与 497 并存**。

**风险**：这个数字要交外部模型。外部评估的第一动作就是查内部一致性——**与自家 evidence-index 冲突的数字会直接损害整包可信度**，且是"一查即中"的硬伤。

**处置**：统一为 **486（基线）/ 491（L1 分支，+5 = L1 新增救回矩阵测试）** 并注明构成；若 497 另有出处，须先补 evidence-index 条目再入包。

**连带**：Q6 的 **45:23 未定义**，违反 §4.2 自包含纪律（"每个内部编号首次出现必须一句话解释"）。须由执行窗给出定义，不得由产包者猜测。

---

## 2. 🔴 阻塞项：U-44 派工未携带修正归因 → 会白做

批复 D4 已采纳改名（"U-44（会话落盘时机缺口）"✅），但**未传递修正后的归因与修复方向**。

**砺上轮源码反证**（`docs/core-revalidation/PhaseD评审报告` §2）：
- chat 路径**已接线**——`attach_session_safety` 三处调用，其中 `run_local.rs:365`（`run_local_continue`，chat 首轮 `run()` 前）；
- 真实缺口是 **turn 级粒度下 run 起始无会话头**（`run_local.rs:200-202` 注释："turn 级 checkpoint——每个工具交换原子落盘"）；D7 用 `sleep 90` 单长工具调用，kill 时无一 turn 完成 → 文件从未创建。

**若执行窗按原始错误归因（"补 chat 路径 checkpoint 接线"）施工 = 空操作。**

**处置**：D4 派工单必须写明——
> 修复方向 = **run 起始即落会话头**（或 tool 级心跳落盘），**非补接线**；接线已存在（`run_local.rs:365`），勿重复实施。

---

## 3. 🟠 方法论项：报告预置了 D2 结论（须撤回）

- **D2 规则是合取**：B 臂 ≥3/5 **且** A 臂 ≥2/3 → 成立；否则 → 记"根因模型有缺口"。
- **现状**：B=4/5 已达标 ✅；**A 臂 3 run 仍在 .131 收集中，未落地**。
- **报告 §五.1 却已写**：「D2 判定 = **根因模型验证成立**」——**在 A 数据落地前预置结论**。
- **批复 §二 D2 的表述是对的**：「A 臂数据落地即按规则自动判定，不再开会」——留待数据。

**风险**：这正是项目自产知识卡 5（激励结构/代理指标陷阱）警告的模式——先定结论后补数据；且会污染 A 数据判读的期望效应。

**处置**：
1. 报告 §五.1 改为「D2 判定：**待 A 臂落地后由砺按规则出具**（B 臂已满足 ≥3/5，A 臂采集中）」；
2. D2 判定权明确归砺，**报告不得预置结论**；
3. 外部包 §4.1.6 相应写"A 臂数据未落地，D2 未终判"。

---

## 4. 🟠 外部包 §4.1.4「对抗证据全景」漏了砺最具实质性的两条更正

该节立意正确（"不许只放我方叙事"），已纳外部三报告 / Claude 两段批判 / 砺 FZ-RFC S-1~S-6 / 顶层认错记录。**但漏了砺在 Phase D 评审中的两条更正**：

| # | 更正 | 对抗价值 |
|---|---|---|
| 1 | **D7/U-44 归因被源码反证**——材料称"chat 未接 checkpoint"，实测已接线，真因是落盘时机 | **评审窗能否决执行窗结论、并阻止一轮空操作施工**——治理有效性的最强证据 |
| 2 | **D3 仪器方向错误 + 项目内已有现成仪器**（introspect 暴露 system_chars，实测 4628） | 显示"内部评审能指出仪器用错而非结论错" |

**处置**：§4.1.4 增列"砺 Phase D 更正两条"。**缺了这两条，外部模型看不到"内部评审真的会否决执行窗"**，反而读成"评审窗只做文字挑刺"。

---

## 5. 🟡 外部包其余补项

| # | 项 | 处置 |
|---|---|---|
| 5.1 | §4.1.6 未决清单漏项 | 补：**U-06 有条件未定案**（RC52 架构层根因候选，待 D5 补 2 例 + 区分 write 未发出/发出但空）、**U-45 未分类**（fresh 产物/终态背离，砺判不并入 RC52）、**U-44 未修**（且属 S3 崩溃安全在 P5 段2 取消后仍开着的口子）、**D1（L1/L1.5）未复现待复测**、**W1 未施工（v0.3）**；正面项可加 **G1 已闭环** |
| 5.2 | §4.1.8 可复现入口只覆盖 FZ-RFC | 现纳入的是砺 §6 八查清单（FZ-RFC 专用）。Q1/Q2 问的是**根因链**——须增补根因链锚点（救回口 `loop.rs` `rc52_route_done_if_session_artifacts`、give_up 臂、`session_written_files` 累加器），**全部标注"核验当日实测"**（行号漂移已实证：该符号记忆 2110 → 现 2116） |
| 5.3 | VM 侧数据外部不可达 | `~/fa/l1-postfix/` 须以原始数据摘录随包（`docs/data/`）。**建议列为产包验收硬条件**——否则外部模型无法核 Q1（B 臂 27%→80%） |
| 5.4 | 篇幅 300-500 行 vs 十节内容 | 张力明显（对抗证据全景 + 6 问题 + 可复现入口）。建议放宽至 ~600 行，或明确 §4.1.3/4/8/9 不得压缩 |

---

## 6. ✅ 该肯定的（方法纪律层面）

| 项 | 评价 |
|---|---|
| **Q1-Q6 预注册**，且"产出前写定不得事后增删" | **防 HARKing，方法论正确，强烈支持** |
| §4.1.4 要求放**顶层认错记录**，原样不美化 | 对抗性自我披露，正确 |
| §〇 顶层亲自抽验 5 项（含 `git branch` 实测） | 顶层不盲签，正确 |
| §4.1.3「核验当日实测」+ §4.1.8 行号漂移警告 | 采纳了行号漂移教训，正确 |
| **D3 有条件批准但保留"A 组盲测复跑"** | **防"跳过实验"退化为"跳过验证"，这条判得最好** |
| 报告 §七 v2 修订（顶层否决"禁产文档"→改为"产出秩序"） | 顶层当场否决 + 执行窗认账，**是治理有效的实证**，建议一并放进 §4.1.4 |

---

## 7. 砺三项判定的接单状态（批复 §三 已放行）

| # | 项 | 状态 |
|---|---|---|
| 1 | **G1-C3 复核** | ✅ **本轮完成，判 PASS**——实测 `G1-相关矩阵评审材料` v1.1：数值已改 **0.1047**（与 `correlation_matrix.json:45-47` 一致）；成因认领属实（"引用 v1.0 旧规则控制台输出，v1.1 收紧 CLAUSE_SPLIT 后矩阵已重算而文档未同步——E7 家族产出 vs 文档漂移"）；叙事已由"几乎无关"修正为"弱相关（<0.2 阈值）"。→ **OD-3（G1-PENDING）可闭环，B2 采样器解锁** |
| 2 | **PhaseD 补测判读** | ⏳ 部分到位：报告 §三.3 出现 **qiyuan-8b** 数据（53× 耗时、0/3 合规）——**这正是 PD-C1 指定的弱模型口径**，U-07 补测条件已满足。**但 D5 补 2 例 + 区分（write 未发出 vs 发出但空）尚无数据**，U-06 维持"有条件升格"，不得定案 |
| 3 | **L1 post-fix 数据判读** | ⏳ 按 D2 规则，**须待 A 臂落地后**出具；B 臂 4/5 已满足 ≥3/5，但**合取规则未完，不得预置结论**（见 §3） |

---

## 8. 建议动作清单

| # | 动作 | 责任方 | 阻塞产包 |
|---|---|---|---|
| A-1 | 统一门禁数字为 486/491，删 497 或补证据条目 | 执行窗 | **是** |
| A-2 | 定义 45:23 | 执行窗 | **是** |
| A-3 | 撤回报告 §五.1 的 D2 预置结论 | 执行窗 | 是 |
| A-4 | U-44 派工单补写"非补接线，run 起始落会话头" | 执行窗/顶层 | 是（否则白做） |
| A-5 | §4.1.4 增列砺 Phase D 更正两条 + §七 v2 修订实证 | 执行窗 | 否（强烈建议） |
| A-6 | §4.1.6 补 5 项未决 + G1 已闭环 | 执行窗 | 否 |
| A-7 | §4.1.8 增补根因链锚点（标注核验当日实测） | 执行窗 | 否 |
| A-8 | `~/fa/l1-postfix/` 原始数据摘录随包（列为验收硬条件） | 执行窗 | 否（强烈建议） |

---

> **砺·评审（🪨）**　2026-09-03
> 全程只读；G1-C3 复核、证据索引数字、U-44 接线锚点均为砺亲自 grep+read 实测。
