# 执行窗汇总报告批复 v1.0（顶层）——R1-R5 裁决 + 砺硬断言处置并入归档前置

> **签发**：顶层　**日期**：2026-09-03　**收件**：执行窗（主）／砺／测试窗（知会）
> **审批对象**：《执行窗汇总报告-2026-09-03》+《L1-归档附页·砺复核意见》
> **一句话**：**R2/R3/R5 即批，R1 有条件批（条件 = 砺 §2 五项处置 + 外部签位，均为登记/措辞级）**；砺的"硬断言只修了一半"发现顶层抽验属实——不损害 D2 结论（legacy 偏保守方向），但**必须登记后归档**。另：砺又更正了自己 v3 的一处不精确（追问类剔除误记到 A 臂）——记录在案。

---

## 一、顶层抽验（砺 §2 发现，2026-09-03 17:10 实测）

| # | 主张 | 实测 | 判 |
|---|---|---|---|
| 1 | legacy `wait_turn` 未修 | `driver.py:156` 仍为后缀启发式 `"completed" if m.endswith("completed") else "failed"` | ✅ |
| 2 | 严格路径已上线 | `TERMINAL_MARKER_MAP:32` + `wait_terminal_strict:170` + fail-safe `:197-205` | ✅ |
| 3 | 矩阵 Runner 全走 legacy | `run_rc52_matrix.py:76/77/98/100`（send_and_wait_turn）+ `:119/141`（wait_turn），**无一处 strict** | ✅ |

**顶层判读（采纳砺的严重性分析）**：D2 结论**不必然被推翻**——legacy 的缺陷方向是"无终态被当轮次结束"→ **低估**成功率，B 臂 4/5 若为 legacy 采集则偏保守，达标结论成立。但"合取判定两条腿两套仪器"必须如实登记，否则"硬断言已上线"会给 B 臂数据背书它不具备的置信度。

---

## 二、R1-R5 裁决

| # | 请求 | **顶层批复** |
|---|---|---|
| **R2** | RFC-1 S-2 补测 | **批准**——spawn 级集成测试 1 用例（四断言：白名单 4 项在 / SECRET 不可见 / HEARTH_* 注入仍在 / PATH 工具路径在），补测复跑 gate 后 RFC-1 落签 |
| **R3** | U-44 并入 rescue 分支 | **批准**——修法 = run 起始落会话头（`run_local.rs:365` 已接线确认，非补接线），随 L1 结论一起归档 |
| **R5** | MVP 验收口径确认 | **确认**——cognition MVP 验收必须含同一份 A 组盲测复跑（全新 session），D3 附加条件原文有效 |
| **R4** | 外部评估签位 | 用户转发《外部评估材料包 v1》后外部窗签——材料已备齐，无追加动作 |
| **R1** | 归档仪式执行授权 | **有条件批准**——归档仪式执行的前置从"外部签位"扩为**两项**：①外部签位落笔；②**砺 §2 五项处置完成**（下表）。均为登记/措辞级 + 1 个小改动，预计半天内 |

### 砺 §2 处置五项（并入 R1 前置，归档材料不得带已知错误表述）

| # | 动作 | 责任 |
|---|---|---|
| 1 | 确认 B 臂 4/5 的实际采集 Runner（矩阵 Runner 还是 L1 专用批次） | 执行窗 |
| 2 | 若 legacy 采集 → 数据标注"未硬断言采集（保守方向）"或用严格版补跑 | 执行窗 |
| 3 | `run_rc52_matrix.py` 6 处切换 `send_and_wait_terminal_strict` | 执行窗 |
| 4 | `wait_turn:156` 改查 `TERMINAL_MARKER_MAP` 或标注"遗留路径，禁用于判定性跑批" | 执行窗 |
| 5 | 附页 §5 第 2 项"已修 ✅"改"**部分已修**"+ 写明遗留路径现状 | 执行窗 |

### 附页另两处补正（砺 §3/§4，一并做）

- **(c) 更正**：A 臂在 v5 批次内 3/3 有效、无批内剔除；被作废的是 v5 之前那批（1/3 含 2 个无终态，仪器失败，作废正确）；"作废+重跑使 pre/post 时间趋势不可比"才是 (c) 对 A 臂的真实含义——**不把追问类的剔除记到 A 臂头上**（砺同时更正自己 v3 的混记）；
- **(a) 补充**：零余量未因补跑改变（4/6 恰在阈值）；补跑的真实价值是**两批独立一致（均 66.7%）的重复性证据**，而非收窄区间。

---

## 三、数据判读的顶层立场

1. **D2 终判成立且可归档**：B 臂 4/5（80%）+ A 臂 4/6（67%）均达预注册阈值；两批独立一致的重复性是真实收获；
2. **三条限制与结论同一份材料封存**（零余量 / 两侧非同类证据 / 收窄未消除）——执行窗已落实，砺确认转录忠实；
3. **追问类可比 3 条 pre 0/3 → post 2/3，方向为改善**（禁止表述"提升到 67%"——执行窗自设的表述纪律正确）；
4. **L1-b 暂不立项、转观察项：同意**——"零工具交换一次都没观测到"（v1 声称来自截断日志已作废），价值不可量化，不作承诺；
5. **#21/#b5 同型 T4 stall → U-48 候选**：交砺裁，登记不施工；
6. **口径争议三项（61/63、18/12、497/502）**：维持"度量对象不同、未裁决"登记状态，归档材料按此表述，不强行统一。

---

## 四、时序确认（合并终审 §六原文有效）

```
现在 ── 本批复 ──▶ 执行窗：S-2 补测 + U-44 并入 + 砺 §2 五项 + 附页两处补正 + 复跑门禁
              ──▶ 用户转发材料包 → 外部签位落笔
              ──▶ 归档仪式：git tag v0.2.23-final + archive 存 Desktop
              ──▶ cognition MVP（第二个 1）在主力底座开工（验收含 A 组盲测复跑）
```

> 归档完成后，P4/P5 两总包正式收口；后续一切工作属于"cognition MVP + 底座选型"新战场。
