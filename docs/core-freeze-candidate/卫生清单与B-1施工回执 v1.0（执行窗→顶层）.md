# 卫生清单 W-1~W-6 + B-1 + T-4 配套 · 施工回执 v1.0（执行窗 → 顶层）

> **受命**：顶层《砺评审意见终审与 cognition 开工令 v1.0》§一.2（B-1）、§二（W-1~W-6）、§一.1（T-4 配套）、§三（MVP 验收方案）
> **要求**：「一次做完，做完一次性报回执，不分五次开工」
> **执行窗 2026-09-03 21:30**　**性质**：施工完成报告 + 证据；**不含判读/裁决结论**

---

## 一、卫生清单 W-1~W-6（6/6 办结）

| # | 要求 | 状态 | 落点（当日实测锚点） |
|---|---|---|---|
| **W-1** | 附页 §二(c) 标题下加显式指针 | ✅ | `L1-归档附页-D2终判与限制.md` **(c) 标题下**：注明「**72–74 行＝砺 v3 原文转录，已被 78–83 行更正段取代，以更正段为准；⛔ 引用不得单独摘引 72–74 行**」。另附**语义锚点**（转录段以「本批剔除了 #29…」起首；更正段以「(c) 对 A 臂的准确含义…」起首）——行号会漂移，语义锚点才稳 |
| **W-2** | evidence-index 三处 | ✅ | ①§8 作废「#28/#29 零交换→仍 failed」，注明**来自被截断的无效日志（无终态）非真实零交换**，指向附页 §四（三可比样本全部有交换 25/1/3）；②§9「A 2/3」→**「A 4/6（n=6，16:50 补跑后）」**并禁引旧读数；③数字口径行补注「**docs 50 = 不含新增文档；含则 52**」并同步 T-2 分树口径 |
| **W-3** | 附页 §二(57) 改写 | ✅ | 补入 **B1 严格口径实测佐证表**（`completed` / valid=true / **124 次工具交换** / UNVERIFIED / 37 步 / **censoring=[] 零删失**），结论「与 legacy 方向一致 → legacy 低估未致方向性错误」；**死指针「结果并入后更新本行」已删除**，改为「维持不启动 + 若砺 T-5 认为须补满可下令续跑」 |
| **W-4** | S-2 注释改白名单 2 项 | ✅ | `crates/sandbox/tests/s4_spawn_env.rs` **第 9 行**（文档注释）与**第 61 行**（断言注释）同步改为「白名单 2 项（HOME/TMPDIR；**LANG/LC_ALL 依赖父进程已存在，不作断言**）」 |
| **W-5** | 3 处行号 365→370 | ✅ **＋追加 1 处** | `session_store.rs:45` ✅、`run_local.rs:373` ✅、`evidence-index.md:58` ✅。**实测确认调用点确为 370**（`grep attach_session_safety`） |
| **W-6** | 补记重启/IP 变更时刻回填附页 §一 | ✅ | 附页 §一新增「环境变更与采集窗口登记」表，**全部实测**（`uptime -s` / `last reboot` / `curl ifconfig.me`），见 §四 |

---

## 二、B-1 映射表（🔴 归档前置 —— 已解除）

**结论：砺抓到的「5 条 vs 6 个编号」不是谁数错了，是**两个不同集合被写成了同一集合**。**

| 口径 | 集合 | n | 定义 |
|---|---|---|---|
| **① 追问类全集** | #21/#27/#28/#29/#30/#32 | **6** | `replay41-golden.txt` 内按**内容性质**划分的追问/元诊断类条目 |
| **② A 组盲测追问类子集** | A5/A6/A7/A9/A10 ＝ **#11/#12/#21/#29/#39** | **5** | `盲测包-正式轮-v0.2.23-10+5.md` A 组 10 条中的追问类（**实际跑过的**） |
| **③ pre/post 可比子集** | #28/#30/#32 | **3** | 有双侧有效数据的条目（回执 v5） |

**关键交叉关系**：①∩② ＝ {#21, #29}（仅 2 个）；③ ⊂ ①；**③ ∩ ② ＝ ∅**——**可比子集与 A 组盲测子集完全不相交**。

**回执 v1 第 4 行的错误已定位**：把口径①当成了口径②。**A5/A6 实为 #11/#12，根本不在那六个编号里。** 已按逐字比对重出 A1–A10 ↔ 黄金集完整映射（10 条全对齐），并锁死表述纪律（「0/5」指②、「0/3→2/3」指③，禁止互相代入）。

**落点**：`unified-ledger.md` **新增 §3**（三口径 + 完整映射表 + 交叉关系 + 表述纪律）；`L1-归档附页-D2终判与限制.md` **§三表头**加口径锁定框（含"是否在 A 组 10 条内"新列）。

---

## 三、T-4 配套（OD-3/OD-10 闭环）

| 项 | 处置 |
|---|---|
| **OD-3** | ✅ 移入 `open-deviations.md` 已闭环清单，注明判词出处＝**砺本件 §六**，campaign 引用解锁。**随附口径铁律已抄录**：引用矩阵**精确 V 值必须同时带「n=273、抽样噪声 ±0.15」——闭环 ≠ 解禁精确值引用，矩阵只定方向** |
| **OD-10** | ✅ 随同闭环 |
| **OD-13** | ⛔ **维持开放**，并在表中显式标注「**不得随 OD-3 顺带闭环**」（签发件 §一.1 明示，砺范围声明已采纳） |
| 计数 | 头部口径同步改为 **开放 11 + 已闭环 7** |

---

## 四、两项**执行窗主动追加**的发现（顶层未列，如实上报）

### 4.1 W-5 追加：evidence-index 第 4 处行号偏差（203 → 208）

实测 `grep -n attach_session_safety crates/codex-cli/src/run_local.rs` → **196（调用·fresh）、208（函数定义）、370（调用·continue）**。
原文档写「196、**203**、365」，其中 **203 处并无该符号**，函数定义在 **208**。已一并更正。

### 4.2 W-6 关键判断：**B-8「跨条件稳健性证据」条款不触发** ⚠️

签发件 B-8 载：「若 A 臂两批分处变更两侧且均 67%，按砺口径记为**跨条件稳健性证据**（强于同条件重复）」。

**实测各批次与重启（18:23:36）的相对位置**：

| 批次 | 时点 | 相对重启 |
|---|---|---|
| A 臂 v5 批（2/3） | 16:50 之前 | **重启前** |
| A 臂补跑批（2/3） | 16:50 | **重启前** |
| B 臂 legacy 批（4/5） | 更早 | 重启前 |
| **B1 严格口径** | 18:31 启动 / 18:55 落盘 | **重启后** |

> **两批 A 臂同在重启前（同侧）→ 条款前提不成立，不得援引为"跨条件稳健性证据"。**
> **A 臂两批 67% 一致的性质仍为同条件重复**，价值按附页 §二(a) 原判（重复性证据，非区间收窄）。
> **唯一跨条件观测**是 B1（重启后·直连）与 legacy B 臂（重启前·VPN）方向一致 → 属**方向性佐证**（n=1），**不作统计证据**，不计入 D2 判据。
>
> 出口 IP 变更本身仍按 B-8 **按可比处理**，不阻塞 T-5。

---

## 五、cognition MVP 验收方案（**已交，待批**）

`docs/cognition/cognition MVP 验收方案 v0.1（执行窗→顶层批）.md`

- **三件套判据**（宪法注入 / 技能库 / 校准观测）逐件给出**可判定**的验收标准与"不过的判据"；
- **对照跑测条目已预置**（钉 1 落字）、**注明不作判据**（钉 2）、**责任人＝执行窗、砺只读见证**（钉 3）；
- **三红线自检表**（接口/顺序/范围各一条问句 + 违规处置）；
- A 组盲测复跑按 R5 口径，语料映射引 `unified-ledger.md` §3.2。

**请顶层批三项**：①主力底座（执行窗建议 **Codex CLI**）②A 组盲测复跑通过线（建议不设单一门槛，改"三件套全过 + 复跑完整落盘可判读"）③裁定 §四路线图是否随 T-1 豁免同步修订。

**未批前执行窗不写 MVP 代码**（方案先行）。

---

## 六、归档仪式触发条件（三要件状态）

| 要件 | 状态 |
|---|---|
| W-1~W-6 回执 | ✅ **本件即回执，6/6 办结** |
| B-1 映射表 | ✅ **已交**（`unified-ledger.md` §3 + 附页 §三表头） |
| T-6 外部签位 | ⬜ **未到位 —— 当前唯一无法内生的硬前置**（需用户将 `external-review-pack-v1.md` 转发 Claude/ChatGPT） |

> **执行窗侧：卫生清单与 B-1 全部办结，无待办。归档仪式只差 T-6。**
