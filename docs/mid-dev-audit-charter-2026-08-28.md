# 开发中审计清单（Mid-Dev Audit Charter）

> **定位**：封版前的「在途审计」——不追版本号/文档完整，只追「正在改的代码对不对、有没有埋雷」。
> **日期**：2026-08-28 ｜ **基线**：commit `f0b4a54`（ux-polish-01）
> **与封版全项审计的区别**：封版审计看「能不能交付」；在途审计看「每天都在往主干里塞的东西会不会悄悄烂掉」。

---

## 1. 已在途验证（本次快查结论）

| 项 | 结论 | 证据 |
|---|---|---|
| T10 出网白名单是否已真接线 | ✅ 真接线 | `run_local.rs:174 / :273` 两处 `ToolContext { env: tool_env(cfg) }`；`service/main.rs:406`、`repl.rs:250` 同步注入 |
| T2 错误分类（429→Transient / 5xx→Fatal / 4xx→Param） | ✅ 合理 | `llm-openai/src/lib.rs:371-381`；端点死 vs 抖动区分在 `:444-448` 字符串匹配 |
| `build_dispatcher(cwd)` | ⚠️ 小瑕疵 | `run_local.rs:234` 的 `cwd` 被 `let _ = cwd;` 丢弃，参数未用（无害，待清理） |

---

## 2. 在途阶段建议持续审计的维度

| # | 审计维度 | 为什么现在审（信号） | 怎么审 / 触发 |
|---|---|---|---|
| A | **最新改动正确性**（每日） | 今晨 T1–T10 刚落地，是最高 bug 风险区 | 对 `f0b4a54` 涉及文件做 diff 复核：provider 选择、context 度量、sandbox 注入；VM 跑 `cargo test --workspace` |
| B | **已知挂账项闭环** | T11（白名单审批流未做）、T4（提示词瘦身）、WS12（长文自动分块）都还是「声明」没落地 | 逐项 grep 生产路径调用点，确认「声称完成 ⇄ 真实接线」 |
| C | **新 crate 设计边缘** | observer / project-sync / agent-runtime 是最近落地的高风险器官 | project-sync：哈希链/乐观锁/权限矩阵是否被绕过；observer 是否真零执行权（grep 有无反向依赖 nerve-action） |
| D | **契约漂移** | 前端 demo（demo-v21j-frozen，39 断言）仍是模拟数据，接真流前必须有契约守门 | `EnvelopedEvent` / WP-0 字段改动后，FE 与后端 grep 对齐；未知字段必须忽略（H-12） |
| E | **成本/预算回归** | Agnes 「14 元啥也没干」根因未彻底量化 | 复用 `bench/` 跑一次真实任务的 token / 成本基线，对比 system_text 瘦身前后（T4 数据驱动） |
| F | **未提交漂移** | 62 份文档/归档游离在 git 外 | 每日 `git status` 扫描；成熟文档备份后 commit，避免知识丢失（见 rot-audit） |
| G | **依赖与供应链** | Cargo.lock 可能引进过期/漏洞依赖 | VM 上 `cargo audit` + `cargo udeps`（本地 cargo 不可用，必经 VM） |

---

## 3. 推荐执行节奏

1. **每次提交前**做 A（最新改动 diff 复核）——成本最低、收益最高。
2. **里程碑节点**做 B + C（挂账闭环 + 新器官边缘）——避免「声称完成」类隐性 rot。
3. **封版当天**再做全项审计（你已决定）：代码整洁 / 安全逃逸 / 需求覆盖 / 契约一致 / 灾难恢复 / 供应链，六维一次过闸。

---

## 4. 本次未覆盖、留待封版的（不在途范围）

- G0 真机逃逸探针（seccomp/landlock/cgroup fail-closed）—— 需要在 VM 做红队，归封版全项。
- 需求总账 BE-01~15 逐项覆盖度——归封版全项（B 维度只抽查）。
- 21 个 `.workbuddy/*.tgz` 可恢复性验证——归封版全项 / 归档整理。

> 本清单是轻量、可重复的「每日/每里程碑」审计指南，不等同于封版全项审计。
