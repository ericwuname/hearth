# P4-T1 判读与测试窗下一步指令 v1.0（顶层）

> **签发**：顶层 → 测试窗口（砺代行）　**日期**：2026-09-01
> **一句话**：T1 变体实验**数据收下、结论接受、但不计为矩阵块 A**——它测的是另一条链路；下一步先做**差异考古**（近零成本、最高判别力），再搬运本机既有资产按 v1.1 口径补跑真正的 A/B 矩阵。**黄金集与工具链不是"缺失"，是还没从本机搬过去**——不用从零重建。

---

## 一、顶层对 T1 变体实验的判读

### 1.1 数据可信度：接受

- T0 硬门禁实测扎实：binary sha `fc39c27c…` 精确吻合、cgroup v2 可用。
- 13 样本原始数据落点齐全（`~/fa/p4-rerun/<cond><idx>/` 四件套），A3/B3/C3 的 unknown 用原 log 复核而非信解析器——纪律正确。
- 结论诚实保守：不升格 CONFIRMED、明确声明 Q2 既未证实也未证伪。**这个自我约束是本次报告最有价值的一部分。**

### 1.2 但它不是 T1 矩阵复现——三处口径偏离（关键判读）

| 维度 | v1.1 / campaign directive 口径 | 本次实际跑法 | 后果 |
|---|---|---|---|
| **链路** | REPL（PTY 交互）内任务完成后发"继续"型输入 | `hearth chat '<goal>'` 一次 + `hearth resume <uuid> '继续'` | **resume 显式恢复 goal（🎯 revision 1）= 把 RC52 病灶（planner 不知道任务已完成）结构性绕过**。RC52 原始入口（run-013）是 REPL 多轮会话内的"继续"，chat+resume 是另一拓扑 |
| **任务** | 黄金集任务 | 1 步可验证任务（DONE.txt） | 不触发长会话累积污染动力学（报告已自承） |
| **污染** | 逐字重放 run-001..012 前缀 | 目标前缀改写近似 | 放大器是否激活无法对照（报告已自承） |

**正确读法**：13/13 SOUND = "**简单任务 + resume 链路下机制 sound**"，**不是**"RC52 的 40% 基线被推翻"。两个假说都还活着：

- **H1**：执行窗 40%/100% 是真的，触发条件 = 长会话累积污染（与 RC52 原画像一致）；
- **H2**：执行窗 40%/100% 本身含 DRIVER-INDUCED 或口径 artifact（它有 harness 三版失真前科）。

### 1.3 归档定位

- 本次 13 跑归档为 **"Path-A 简单任务回归"**，数据保留、报告有效；
- **不得计入 campaign 矩阵块 A**——directive T1 说的"13 跑 = 块 A"指按 v1.1 口径的跑法。

### 1.4 goal_drift 信号

5/13（A3/B2/B4/B5/C3）的新遥测，登记立案。判读归判读流程，**测试窗不占主线、不自行立项施工**。

---

## 二、下一步指令（四步串行）

### Step 1 · 差异考古（最先做，近零成本，最高判别力）

执行窗原始矩阵日志就在 `~/fa/ab_A1..A5.log`、`ab_B1..B5.log`（T0 报告实测确认存在）。逐项提取：任务文本、输入序列、链路（chat 还是 REPL）、acceptance 定义、污染前缀内容。与我方 p4-rerun 配置对照，产出**"自变量差异表"**——回答 **40% 与 0% 之间，哪个变量是分母**。这是区分 H1/H2 的最快路径。

### Step 2 · 资产搬运（5 分钟级——不是 Path C 重建）

T0 报告把两件事误判为"缺失"，实际是"还没搬"：

| 资产 | 实况 | 动作 |
|---|---|---|
| 黄金集源文件 | **本机实存** `release/手工测试v0.2.18.txt`（3702 行；顶层 grep 实测 `hearth>` 提示行 **41 条** = directive 声称口径吻合）。VM 上没有是**预期内**的——directive 明写"release/ 不随源码同步，VM 上没有"，本就要求测试窗从本机提取上传 | sftp 本机 → `.131`，按"`hearth>` 行"口径提取落 `~/fa/campaign/golden/replay41.txt`，sha256 记报告头 |
| PTY 工具链 | **本机实存** `tools/simuser/` 四件：driver.py / analyzer.py / run_rc52_matrix.py / metrics.yaml（顶层 ls 实测）。同步管线漏了 tools/ 目录 | sftp 本机 → `.131:~/fa/simuser/`。**落 ~/fa/ 下，不碰 ~/codex**（6 套并行 checkout 混战，禁止往执行窗在制品树里放东西） |

personas/scenarios 本机也是空目录（P4 已自承空壳）——按 directive + 派工单 M-1：块 C 采样按规划书 v1.1 §2.1 占比**自构输入**，不得自造分布。

### Step 3 · 按 v1.1 口径补跑矩阵块 A（A×5 + B×5）

- **A fresh ×5**：全新 REPL（driver.py PTY 链路），小任务完成后发"继续"型输入；
- **B contaminated ×5**：**逐字重放** run-001..012 污染前缀（用 replay41.txt 的真实序列）；
- 部署后若发现 driver.py 与当前 binary 接口漂移：**只登记不修**，报顶层裁量；
- **预注册预期（跑前写下，防事后凑读）**：这批跑出什么都是有效数据——若长会话重放复现失败 → H1 确认、块 A 成立；若仍全绿 → 执行窗 40%/100% 基线存疑（H2 升格），**"复现失败"本身就是 T1 的合法结论，如实报告，不得凑数重跑**；
- C×3 待 run_rc52_matrix.py 正则修复（附录 A R-1）→ diff 贴报告交复核后照旧。

### Step 4 · 判读分离的结构性约束（本条最重要）

测试窗口由砺代行 → **砺不能既产数据又当唯一判官**（自跑自判正是本链路要消除的东西）。约束三条：

1. 正式归因判读必须基于**可复算的原始数据**（四件套已归档，满足此条）；
2. 涉及"升格/推翻执行窗结论"（RC52 CONFIRMED、或 H2 判执行窗数据 artifact）的裁决 → **交外部 AI（ChatGPT/Claude 参谋）交叉或顶层亲自抽验原始数据后定**，砺不出具终审；
3. 顶层远程抽验本次因 .131 SSH 不可达暂缓——VM 恢复后补抽：13 样本目录存在性、A1/C3 log 终态、`find driver.py` 缺失复证。测试窗不必等此项，先走 Step 1-3。

---

## 三、成本口径纠正

T0 报告"多美元量级"提示有误：Agnes 是**会员配额通道**（周 75,000 次 / 每 5h 7,500 次，按次计），无边际美元成本。真正的约束是**时间**（串行跑数小时级，B 前缀重放单跑可达数十分钟）——排串行窗口，勿并行抢状态目录。**Step 1-3 全部值得跑，预算不是理由。**

---

## 四、顶层自己的挂账（本报告衍生，测试窗不用管）

1. **同步管线缺口**：E7 家族新变体——"仪器与部署物脱节"（tools/ 未随 tar 上 VM）。修管线时把 tools/ 纳入同步白名单；
2. **6 套并行 checkout**（~/codex、codex_work、codex_t、codex_new、codex_new5、住户反馈/projects/…）：改错树风险，建议执行窗 Node 13 开工前清理归档；
3. `run_rc52_matrix.py` 在 .131 不存在 → 执行窗 Final Report 所述 C 条件 67% 的实际产生路径需执行窗自证（其报告与部署物可能存在描述出入——E7 家族第四次复查候选）。
