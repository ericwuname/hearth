# 第二份真实会话（0.9-0.4）· 顶层初判与复检派工 v1.0（顶层）

> **对象**：`release/手工测试v0.2.23-2026-0.9-0.4.txt`（2,983 行，.133，`hearth 0.2.23 (unknown)`，REPL 直跑，2026-09-03 21:33 – 09-04 00:32 本地，约 3 小时）
> **日期**：2026-09-04 00:35　**性质**：顶层快速勘验（非深度拆解）+ 复检派工
> **定位**：这是**外部拆解判"不可用"之后、同一引擎（v0.2.23，修法 A/U-44 均未部署）上的第二个真实会话样本**——恰好构成对拆解结论的**跨会话复检**。

---

## 一、顶层快速勘验（grep 级信号，未做逐行拆解）

| 信号 | 0.9-0.4（本会话） | 对照 0.9-0.3（已拆解） |
|---|---|---|
| 用户轮次 / run 数 | 19 轮 / 18 run（run-001…018） | 36 轮 / 31 run |
| give_up 次数 | **6**（:1610/1873/2050/2394/2780/2975） | 10（7 次被改判） |
| 跨轮产物累加器 | **活跃**：`2 个`×36、`3 个`×16、`6 个`×13、`0 个`×15——非零占绝对多数，棘轮结构同在 | 0→13 单调递增 |
| **已知失败项复现** | 🔴 **FileBrowser 依旧 0/16 全挂**（`:1253`，`FileBrowser is not a constructor` ×16）——**与上一会话完全相同的失败项，跨会话未修复** | E3 铁证原点 |
| **假事实跨会话传播** | 🔴 **上一会话产出的 README（报"完成度 103%"）在本会话 :103 被当作事实读回**——假完成数据作为文件产物跨会话存活并污染后续会话的事实基础 | E6/E7 原点 |
| 末轮终态 | run-018：give_up → 依据"连续错误 0、本轮写盘 **0 个**、跨轮产物记录 **0 个**" → **Error（终止）→ ✗ failed**（22 步） | —— |

**末轮观察（重要，机制侧写）**：本轮 give_up 被**正确**判为失败——因为本轮写盘 0 且跨轮产物 0，救回口前置不满足。即：**同一个改判机制，累加器为 0 时行为正确，非零时放行**——与 RC-Ω 根因模型的预测**方向一致**（第二会话的机制侧证据）。

## 二、初步结论（待深度拆解确认）

1. **病理跨会话复现的初步迹象明确**：已知失败项 0/16 不修复、假完成事实（103% README）跨会话存活并污染、累加器棘轮结构——三大核心病理在第二会话均有对应信号。
2. **若深度拆解确认**，外部窗口"单会话不自外推"的自我限制即可解除：**"不可用"结论升级为跨会话稳健**。归档报告可用性结论栏依据随之加一句"第二份真实会话初步复现同型病理（顶层勘验），详见拆解二"。
3. **本会话不改归档线状态**——不可用判定只强化不弱化，R1 两签照旧。

## 三、复检派工（外部窗口 · 深度拆解二）

**预注册问题**（拆解前锁定，防事后挑选）：

| # | 问题 | 判据 |
|---|---|---|
| Q-1 | **R1 改判是否复现**：6 次 give_up 中几次被改判为完成？改判依据串是否同型（跨轮产物 N 个）？ | 逐条列出 + 与 E1 七条对齐 |
| Q-2 | **E3/E5 型是否复现**：已知失败项（FileBrowser 0/16）在本会话内是否被标 ✅？是否有未完成清单蒸发？ | 终态表 vs 实测 |
| Q-3 | **假事实传播链**：103% README 如何进入本会话事实基础？agent 是否基于它做出错误推断？ | 传播路径逐行 |
| Q-4 | **R3 相位空转是否复现**：Observe 全 0ms？Plan 空转比例？ | 相位转移矩阵 |
| Q-5 | **新现象**：末轮 give_up 正确判失败（累加器 0）——本会话内"累加器 0→判失败 / >0→判完成"的分界是否与 RC-Ω 模型预测一致？ | 6 次 give_up 逐条对照 |
| Q-6 | 用户侧代价：推进/质疑轮次比例，与 19%/58% 对照 | 同口径统计 |

**产出**：`docs/外部窗口-会话深度拆解二-20260904（0.9-0.4·跨会话复检）.md`，报批件呈砺，流程同拆解一。

## 四、给砺的预通知

拆解二回报后由砺判读（同拆解一流程）。判读重点：**Q-5 是本会话独有的机制侧机会**——第一会话证明了"累加器非零→改判"，本会话提供了"累加器为零→正确失败"的对照组，若成立，RC-Ω 根因模型获得**双向验证**（正反两向均有会话级证据）。

## 五、登记

`evidence-index.md` §12 增补一行：第二份真实会话落盘 + 本初判件路径。

| 签发 | 意见 | 日期 |
|---|---|---|
| 顶层 | ☑ 初判成立；拆解二派工生效；归档线状态不变 | 2026-09-04 |
