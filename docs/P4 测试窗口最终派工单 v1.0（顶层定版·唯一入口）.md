# P4 测试窗口最终派工单 v1.0（顶层定版 · 唯一入口）

> **签发**：顶层 → 测试窗口（用户转发用）　**日期**：2026-09-01
> **一句话**：两份指令**都要收、接力执行、不二选一**——先执行《Node03 独立复现指令 v1.1》（= T1），闭环后接着执行《Campaign 指令书 v1》（= T2/T3）。本单 = 两份文件的顶层背书 + 核验批注 + 3 条微调条款；**冲突时以本单为准，其余条款原样生效**。

---

## 1. 直接回答：发哪份？怎么发？

**三份一起发给测试窗口，按此顺序读**：

| 顺序 | 文件 | 角色 |
|---|---|---|
| ① | **本派工单** | 唯一入口：先读，看完即知全局 |
| ② | 《P4-Node03独立复现测试指令 v1.1（顶层→测试窗口）》 | **T1 执行**（A/B 复现 → 附录 A 修复 → 砺复核 → C 补跑） |
| ③ | `docs/hearth-test-window-campaign-directive-v1.md` | **T2/T3 执行**（Campaign ≥30 runs + 七场景×三样本） |

**两份指令是接力关系，不是二选一**。执行窗口签发的 Campaign 指令书头部已明写（directive L3-4）：

> 前序指令：《P4-Node03独立复现测试指令 v1.1（顶层→测试窗口）》——**必须先闭环**（见 §2-T1），本指令书与其不冲突、是其后续阶段

其 T1 节（L57-61）完整引用了 v1.1 的流程，并声明 **T1 的 13 跑结果 = campaign 矩阵块 A，不重复跑**（直接计入 ≥30 runs）。执行窗口把顶层 v1.1 整合得正确，无抢权、无矛盾。

**执行顺序总图**：

```
T0 环境锁定（hearth --version = 0.2.20 + binary sha256 前16位 fc39c27c…，不符即停）
  → T1 = v1.1 全流程：A/B 复现 ×10 → 附录 A 仪器修复（单独 commit + diff 贴报告）
        → 交砺·复核通过 → C 补跑 ×3          【数据落 ~/fa/p4-rerun/】
  → T2 = Campaign ≥30 runs：块A 引用 T1 的 13 跑 + 块B 黄金集回放 ≥10 + 块C persona ≥7
  → T3 = 七场景 × 三样本（每类留 成功/失败/反例 三样本）
  → 交付 docs/hearth-p4-campaign-testwindow-report-v1.md（四件套归档 ~/fa/campaign/）
  → 砺·评审判读 → 顶层验收 → Node 13 修复批（v0.2.21）→ Node 14 复测 → Node 15 CANDIDATE
```

---

## 2. 顶层核验批注（directive v1 全文已核验，2026-09-01）

| # | 核验项 | 结论 | 锚点 |
|---|---|---|---|
| K-1 | T1 接力关系 | ✅ 引用准确，13 跑口径 = A×5 + B×5 + C×3，与 v1.1 一致 | directive L3-4, L57-61 |
| K-2 | 版本冻结 | ✅ v0.2.20 + sha `fc39c27c…`，与 Node03 指令 P-1 同一基线 | L6, L38-39 |
| K-3 | 三目录隔离 | ✅ `~/fa/p4/`（旧，C 侧作废）/ `~/fa/p4-rerun/`（复现）/ `~/fa/campaign/`（campaign）物理隔离禁互覆 | L51, L162 |
| K-4 | 判读分离 | ✅ 测试窗口只跑录测归档不自判；正式归因 = 砺，验收 = 顶层 | L7, L21, L144 |
| K-5 | 仪器裁量 | ✅ 附录 A 授权测试窗实施（单独 commit + diff + 砺复核），与 v1.1 §9-2 同款 | L20, L158 |
| K-6 | 预注册预期 | ✅ fresh 预期维持 ~40% 量级（decision 层有意未修 = 因果预测，非修复失败）；偏离即大发现如实报 | L19, L28 |
| K-7 | metrics.yaml | ✅ 文件实存（tools/simuser/metrics.yaml，42 行），口径冻结 v1.0 | L52, L119-128 |
| K-8 | 黄金集源文件 | ✅ `release/手工测试v0.2.18.txt` 实存；**顶层实测 `hearth>` 提示行 = 41 条，与指令书声称的"41 条输入行"精确吻合**；35 逻辑 run 为其归并口径 | L53 |
| K-9 | driver/analyzer 用法 | ✅ PTY driver + send_and_wait_turn + 每 run 独立 cwd，与 P4 Final Report 一致 | L93-117 |
| K-10 | 红线六条 | ✅ 禁改 crates/、禁边测边改、禁自建口径、禁采信自述、禁混表齐全 | L155-162 |

---

## 3. 微调与提示条款（本单唯一增量，M-1 优先看）

| # | 级别 | 条款 |
|---|---|---|
| **M-1** | 🟡 | **块 C persona 语料现状**：`tools/simuser/personas/` 与 `scenarios/` 当前为**空目录**（P4 Final Report 已自承两件空壳）。directive 块 C 要求"按七类 persona 占比采样（规划书 v1.1 §2.1）"——**占比数字本身在规划书里，不依赖 personas/ 文件**：测试窗口按占比**自构输入**即可（继续型 26.9% / 纠偏 10.4% / 状态 8.1% / 高收益探针 5.6% 等放大），三会话形态（fresh repl / resume / chat 单目标）必须都有覆盖。**不得自行编造占比分布**；若需要真实 persona 语料，向顶层要，等待期可先跑块 B。 |
| M-2 | ⚪ | 黄金集源日志行数：顶层 wc -l 实测 **3702** 行（指令书写 3703 = 末行换行口径差）。**以 sha256 为准**，行数差 1 不构成阻断，报告头注明实测值即可。 |
| M-3 | ⚪ | 归档路径 `docs/data/p4-campaign-20260831/` 的日期戳为指令签发日，实际执行日不同**不改目录名**（保持全链路引用一致）。 |

---

## 4. 效力声明

1. 本派工单与 v1.1、directive v1 **无实质冲突**；M-1/M-2/M-3 为唯一增量条款。
2. v1.1 与 directive 的其余条款（铁律、前置检查、口径、红线、验收）**原样生效**，测试窗口照两份原文执行，无需等待顶层重写。
3. 测试窗口对两份文件的理解分歧，一律以本单 §2 批注裁决；仍无法裁决的，停下来问顶层，不猜。

---

## 5. 当前全链状态（2026-09-01，供各窗口对表）

- **执行窗口**：Node 00-09 收工（tag v0.2.20，gate 455→459），球已交出。
- **测试窗口**：本派工单即开工令——T0 → T1 → T2 → T3 串行推进。
- **砺·评审**：待收 T1 附录 A diff 复核 + T2/T3 数据判读。
- **顶层**：T1 复现数据回来后裁决 RC52 升格（CAUSE LIKELY → CONFIRMED）；P5-FOUNDATION-01 段 1（N00/N01 零代码）另行下发执行窗口，与本链并行不冲突。
