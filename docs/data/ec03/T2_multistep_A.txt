```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>计算器</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: #f0f2f5;
            font-family: Arial, sans-serif;
        }
        .calculator {
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.12);
            padding: 24px;
            width: 320px;
        }
        h1 {
            text-align: center;
            margin-bottom: 20px;
            font-size: 22px;
            color: #333;
        }
        .input-group {
            display: flex;
            gap: 10px;
            margin-bottom: 16px;
        }
        .input-group input {
            flex: 1;
            padding: 12px 14px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 16px;
            outline: none;
            transition: border-color 0.2s;
        }
        .input-group input:focus {
            border-color: #4a90d9;
        }
        .buttons {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 10px;
            margin-bottom: 16px;
        }
        .buttons button {
            padding: 14px;
            font-size: 18px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            background: #e8e8e8;
            color: #333;
            transition: background 0.2s;
        }
        .buttons button:hover {
            background: #d0d0d0;
        }
        .buttons button.equal {
            background: #4a90d9;
            color: #fff;
            grid-column: span 2;
        }
        .buttons button.equal:hover {
            background: #357abd;
        }
        .buttons button.clear {
            background: #e74c3c;
            color: #fff;
        }
        .buttons button.clear:hover {
            background: #c0392b;
        }
        .result {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            padding: 16px;
            text-align: center;
            font-size: 20px;
            color: #333;
            min-height: 24px;
        }
        .result.error {
            color: #e74c3c;
        }
    </style>
</head>
<body>
    <div class="calculator">
        <h1>计算器</h1>
        <div class="input-group">
            <input type="number" id="num1" placeholder="第一个数" step="any">
            <input type="number" id="num2" placeholder="第二个数" step="any">
        </div>
        <div class="buttons">
            <button onclick="calculate('+')">+</button>
            <button onclick="calculate('-')">−</button>
            <button onclick="calculate('*')">×</button>
            <button onclick="calculate('/')">÷</button>
            <button class="clear" onclick="clearAll()">清空</button>
            <button class="equal" onclick="calculate('=')">=</button>
        </div>
        <div class="result" id="result">等待计算...</div>
    </div>

    <script>
        function calculate(op) {
            const num1 = parseFloat(document.getElementById('num1').value);
            const num2 = parseFloat(document.getElementById('num2').value);
            const resultEl = document.getElementById('result');
            
            if (isNaN(num1) || isNaN(num2)) {
                resultEl.textContent = '请输入有效数字';
                resultEl.className = 'result error';
                return;
            }
            
            let result;
            switch (op) {
                case '+': result = num1 + num2; break;
                case '-': result = num1 - num2; break;
                case '*': result = num1 * num2; break;
                case '/':
                    if (num2 === 0) {
                        resultEl.textContent = '除数不能为零';
                        resultEl.className = 'result error';
                        return;
                    }
                    result = num1 / num2;
                    break;
                default: return;
            }
            
            // 处理浮点数精度问题
            result = Math.round(result * 10000000000) / 10000000000;
            resultEl.textContent = num1 + ' ' + getOpSymbol(op) + ' ' + num2 + ' = ' + result;
            resultEl.className = 'result';
        }
        
        function getOpSymbol(op) {
            const symbols = {'+': '+', '-': '−', '*': '×', '/': '÷'};
            return symbols[op] || op;
        }
        
        function clearAll() {
            document.getElementById('num1').value = '';
            document.getElementById('num2').value = '';
            const resultEl = document.getElementById('result');
            resultEl.textContent = '等待计算...';
            resultEl.className = 'result';
        }
    </script>
</body>
</html>
```

**自测逻辑说明：**

| 测试场景 | 输入 | 期望结果 |
|---------|------|---------|
| 正常加法 | 5 + 3 | 5 + 3 = 8 |
| 正常减法 | 10 − 4 | 10 − 4 = 6 |
| 正常乘法 | 6 × 7 | 6 × 7 = 42 |
| 正常除法 | 15 ÷ 3 | 15 ÷ 3 = 5 |
| 小数计算 | 2.5 × 4 | 2.5 × 4 = 10 |
| 浮点精度 | 0.1 + 0.2 | 0.1 + 0.2 = 0.3（已处理精度） |
| 除零错误 | 10 ÷ 0 | 提示"除数不能为零" |
| 空输入 | 空 + 空 | 提示"请输入有效数字" |
| 清空功能 | 点击清空 | 输入框清空，结果重置 |

所有逻辑均已在代码中实现并处理边界情况。