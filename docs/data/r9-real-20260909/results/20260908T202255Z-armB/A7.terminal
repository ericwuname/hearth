工作目录是空的，也没有之前任务的上下文残留。根据你描述的问题（出现"✗ Done (5 steps)"并直接退出而非继续修复），这是 **Hearth 智能体自身的运行逻辑**，不是代码 bug。下面解释：
## 为什么会出现"✗ Done (5 steps)"并直接退出？
- 5 步用完 → 强制结束，输出 `✗ Done (5 steps)`
但这不代表不能继续——**你可以重新发起一次新的对话/任务**，带上之前的错误信息，让系统用不同的策略再试一次。✓ Done (8 steps)
  ✓ Task completed（8 步）——目标达成（未验证），产物见报告
