  ✓      1	# hearth-tui 已知缺陷账本
  ✓      1	//! hearth-tui 入口：终端初始化 + 事件循环骨架（fixture 供回归语料上下文用）。
  ✓      1	//! 渲染层（骨架——fixture 只需给语料以"项目存在"的上下文）。
TUI 基础框架可用（键盘上下键滚动、Q 退出），会话列表渲染正常。但 **鼠标交互功能未生效**，属于已知的未完成项，BUG-003 尚未关闭。如需推进，需补全 `Event::Mouse` 分支逻辑并验证终端环境。✓ Done (6 steps)
  ✓ Task completed（6 步）——目标达成（未验证），产物见报告
