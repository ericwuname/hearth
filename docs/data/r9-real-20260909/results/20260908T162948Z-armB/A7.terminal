3. 错误相位触发 `ok=false` 的报告，输出 `✗ Done (N steps)`
| `✗ Done (5 steps)` 是什么？ | 任务因失败终止的状态消息（非成功完成） |
✓ Done (48 steps)
  ✓ Task completed（48 步）——目标达成（未验证），产物见报告
✗   ⚠ G-F goal_drift 疑似：终局产物与原始目标语义相关度存疑（独立判定认为任务被替换/漂移）——请人工核对产物是否确实是你要的
