# L1 追问/元诊断类回归子集 · 数据回执 v1（执行窗 → 砺判读）

> **依据**：顶层《外部评估回执终审与行动指令 v1.0》§四（最高优先动作）
> **对象**：黄金集 #21 / #27 / #28 / #29 / #30 / #32（对应盲测 A5/A6/A7/A9/A10 追问/元诊断类）
> **环境**：.131，L1 分支二进制 `~/hearth-l1`（commit `0ce93aa`），**每条全新 session**（BP-2 纪律）
> **产出纪律**：本回执为**数据报告**，≤1 页，不含论证 —— 判读权归砺
> **原始数据**：`.131 ~/fa/l1-ask/`（逐条 `.log` + `summary.json`）

## 数据（#30 / #32 与 A 臂 3 跑因 SSH 中断尚未回收，回收后补 v2）

| 样本 | 终态 | 工具交换次数 | UNVERIFIED | 步数 | 日志长度 |
|---|---|---|---|---|---|
| **#21**（长追问 + 质疑 give_up 机制） | ✗ failed | **6** | — | — | 4648 |
| **#27**（追问 + 贴 bash exit 127 报错） | ✅ **completed** | **6** | **✅ 是** | — | 4686 |
| **#28**（"能先汇报一下吗"） | ✗ failed | **0** | — | — | 637 |
| **#29**（追问 + 自述指令不规范） | ✗ failed | **0** | — | — | 730 |
| #30 | ⏳ 回收中 | — | — | — | — |
| #32 | ⏳ 回收中 | — | — | — | — |

**pre-fix 基线**（顶层抽验确认）：逐字正式轮 A 组 2✓8✗，其中追问类 **0/5**。

## 读数（执行窗初步，供砺判）

1. **边界探针成立且方向清晰**：
   - **有工具交换**（#27，6 次）→ 修法 A 救回成功，终态 completed + **UNVERIFIED**（诚实标注在位）；
   - **零工具交换**（#28、#29）→ 修法 A **按设计不救**（反空转护栏 `tool_exchanges_this_run ≥1`）→ 仍 failed。
2. **#21 是唯一的反常样本**：6 次工具交换却仍失败——需查其终止出口（是否走 PLAN 相位 stall、或有连续错误）。**这是判读的关键个案**（不自行定论）。
3. **对齐顶层 §四 的分支判断**："若 0 工具交换的追问仍全败 → 修法 A 需扩展为 **L1-b 第二修法**（'模型已输出文字回答'信号入救回条件，即深挖报告的配套信号建议）"。当前零交换样本 2/2 全败，**该分支条件已具雏形**，是否立项由砺裁定。

## 待砺裁定项

1. #21 反常个案的归因（建议：查其终止出口与连续错误计数）；
2. #30 / #32 回收后是否改变结论方向；
3. 是否据此**立项 L1-b 第二修法**（把"模型已输出非空文字回答"纳入救回条件，仍打 UNVERIFIED 标）；
4. A 臂 3 跑（同批次，v4 轮询式驱动）数据落地后与 D2 合取规则（B ≥3/5 **且** A ≥2/3）的判定。

> **执行窗**　2026-09-03　本回执只交数据，不预置结论（砺 A-3 / 顶层 §二.2 纪律）。
