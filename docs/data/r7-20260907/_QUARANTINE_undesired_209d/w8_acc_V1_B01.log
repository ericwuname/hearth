🔒 landlock+seccomp (fail-closed)  (session cdd92f7d)
  🤖 非交互模式（stdin 非 tty）——破坏性操作触发审批时将立即结构化拒绝（approval_denied_noninteractive）

> 请详细设计一个 Rust 命令行工具「log-aggregator」，要求分 5 个模块描述：1) 配置加载 2) 日志解析 3) 过滤与聚合 4) 输出格式化 5) 主流程编排。每个模块给出完整函数签名、关键数据结构、核心实现思路，并说明模块间如何通信。最后给出 main 函数的完整执行流程。要求论述详尽、不少于 700 字。

  🎯 任务目标已锚定（original_goal 持久化）
◆ Init
  ▸ span [plan] 2026-08-29T15:17:21.920164025+00:00
◆ Plan
┌─ 🗺 规划草案（steps=1 gaps=0 待问=0 自动假设=0）
   · Design detailed specifications for 5 modules of log-aggregator CLI tool in Rust: config loading, log parsing, filtering/aggregation, output formatting, and main orchestration. Include function signatures, key data structures, core implementation ideas, and inter-module communication. [Pending]
└─
  ◂ span 耗时 31604ms
💭 [plan] 正在规划任务分解
以下是「log-aggregator」Rust 命令行工具的分模块详细设计。

---

## 1) 配置加载（config）

**关键数据结构**

```rust
#[derive(Debug, Clone)]
pub struct AppConfig {
    pub log_files: Vec<PathBuf>,
    pub level_filter: Option<LogLevel>,
    pub time_range: Option<TimeRange>,
    pub group_by: GroupByField,
    pub output_format: OutputFormat,
    pub config_path: Option<PathBuf>,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum LogLevel { Info, Warn, Error, Debug }

#[derive(Debug, Clone)]
pub struct TimeRange {
    pub start: Option<OffsetDateTime>,
    pub end:   Option<OffsetDateTime>,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum GroupByField {
    Level,
    Source,
    Hour,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum OutputFormat { Text, Json, Csv }
```

**核心实现思路**
- 优先级：CLI 参数 > 环境变量 > 配置文件（YAML/TOML）> 默认值。
- 使用 `clap` 解析命令行，`serde` 反序列化配置文件，`anyhow` 传递错误。
- 支持多文件输入，可接受 `-` 代表 stdin。

**函数签名**

```rust
pub fn load_config(args: &CliArgs) -> Result<AppConfig, ConfigError>;
pub fn merge_configs(base: AppConfig, overrides: AppConfig) -> AppConfig;
pub fn validate_config(cfg: &AppConfig) -> Result<(), ConfigError>;
```

---

## 2) 日志解析（parser）

**关键数据结构**

```rust
#[derive(Debug, Clone, PartialEq)]
pub struct LogEntry {
    pub timestamp: OffsetDateTime,
    pub level: LogLevel,
    pub source: String,      // 如 "app.auth"
    pub message: String,
    pub raw_line: String,
}

pub enum ParseMode { Auto, Syslog, Json, CommonLog }
```

**核心实现思路**
- `Auto` 模式尝试 JSON → syslog → 正则通用格式，按首个成功匹配返回。
- 每行独立解析，保留原始行以便调试。
- 使用 `regex` 编译一次后复用，避免重复编译开销。
- 支持大文件流式读取，逐行生成 `LogEntry`，不一次性加载全部。

**函数签名**

```rust
pub fn parse_lines<R: BufRead>(reader: R, mode: ParseMode) -> impl Iterator<Item = Result<LogEntry, ParseError>>;
pub fn detect_mode(lines: &[String]) -> ParseMode;
pub fn parse_json_line(line: &str) -> Result<LogEntry, ParseError>;
pub fn parse_syslog_line(line: &str) -> Result<LogEntry, ParseError>;
pub fn parse_common_log_line(line: &str) -> Result<LogEntry, ParseError>;
```

---

## 3) 过滤与聚合（filter_aggregate）

**关键数据结构**

```rust
#[derive(Debug, Default)]
pub struct AggregationResult {
    pub total_count: u64,
    pub filtered_count: u64,
    pub by_level: BTreeMap<LogLevel, u64>,
    pub by_source: BTreeMap<String, u64>,
    pub by_hour: BTreeMap<NaiveDateTime, u64>,
    pub sample_entries: Vec<LogEntry>,
}

pub struct FilterContext {
    pub level_min: LogLevel,
    pub time_range: Option<TimeRange>,
    pub keyword_filters: Vec<String>,
}
```

**核心实现思路**
- 过滤作为迭代器适配器（iterator adapter），零分配链式处理。
- 聚合使用 `BTreeMap` 保持有序输出，内存中只保存计数和少量样本。
- 支持"顶部 N"源、时间窗口统计。
- 流式处理，不缓存所有条目，适合大日志文件。

**函数签名**

```rust
pub fn apply_filters<'a>(
    entries: impl Iterator<Item = LogEntry> + 'a,
    ctx: &'a FilterContext,
) -> impl Iterator<Item = LogEntry> + 'a;

pub fn aggregate(
    entries: impl Iterator<Item = LogEntry>,
    group_by: GroupByField,
    sample_size: usize,
) -> AggregationResult;
```

---

## 4) 输出格式化（output）

**关键数据结构**

```rust
pub struct OutputConfig {
    pub format: OutputFormat,
    pub colorize: bool,
    pub indent: usize,
}
```

**核心实现思路**
- trait 抽象输出后端，便于扩展（未来可加 Prometheus、Grafana 等）。
- Text 格式支持 ANSI 颜色（通过 `colored` crate）。
- Json/Csv 使用 `serde_json` / `csv` crate，保证标准合规。
- 同时输出摘要统计 + 可选的详细条目。

**函数签名**

```rust
pub trait Formatter {
    fn format_header(&self, cfg: &OutputConfig) -> String;
    fn format_entry(&self, entry: &LogEntry, cfg: &OutputConfig) -> String;
    fn format_summary(&self, result: &AggregationResult, cfg: &OutputConfig) -> String;
}

pub fn get_formatter(fmt: OutputFormat) -> Box<dyn Formatter>;
pub fn write_output<W: Write>(
    writer: &mut W,
    formatter: &dyn Formatter,
    entries: &[LogEntry],
    summary: &AggregationResult,
    cfg: &OutputConfig,
) -> Result<(), OutputError>;
```

---

## 5) 主流程编排（main / orchestrator）

**核心思路**
- 依赖注入风格：各模块通过结构体或函数参数显式通信，无全局状态。
- 错误处理统一用 `anyhow::Result`，最终在 main 层 `.unwrap_or_exit()`。
- 支持多文件并行读取（`tokio::spawn` 或 `rayon`），但保持顺序输出。

**模块间通信**
- `config` 产出 `AppConfig`，传入 `parser` 和 `filter_aggregate`。
- `parser` 产出 `Iterator<LogEntry>`，喂给 `filter_aggregate`。
- `filter_aggregate` 产出 `AggregationResult` + 过滤后的条目，交给 `output`。
- 全程无共享可变状态，纯函数管道。

---

## main 函数完整执行流程

```rust
fn main() -> Result<(), Box<dyn Error>> {
    // 1. 解析 CLI 参数
    let cli = CliArgs::parse();

    // 2. 加载并验证配置
    let cfg = load_config(&cli)?;
    validate_config(&cfg)?;

    // 3. 构建过滤器上下文
    let filter_ctx = FilterContext {
        level_min: cfg.level_filter,
        time_range: cfg.time_range,
        keyword_filters: vec![], // 可从 CLI 扩展
    };

    // 4. 打开所有输入源（文件/stdin），合并为统一迭代器
    let readers: Vec<Box<dyn BufRead>> = cfg.log_files
        .iter()
        .map(|p| open_reader(p)?)
        .collect();
    let all_lines = readers.into_iter().flat_map(|r| r.lines());

    // 5. 解析日志行
    let entries = parse_lines(all_lines.map(|l| l.unwrap()), cfg.parse_mode);

    // 6. 过滤
    let filtered = apply_filters(entries, &filter_ctx);

    // 7. 聚合
    let agg_result = aggregate(filtered.clone(), cfg.group_by, cfg.sample_count);

    // 8. 收集过滤后的条目（用于输出详情）
    let filtered_entries: Vec<LogEntry> = filtered.collect();

    // 9. 选择格式化器
    let fmt_cfg = OutputConfig {
        format: cfg.output_format,
        colorize: cfg.colorize,
        indent: 0,
    };
    let formatter = get_formatter(cfg.output_format);

    // 10. 输出到 stdout
    let mut out = BufferWriter::new(stdout());
    write_output(&mut out, formatter.as_ref(), &filtered_entries, &agg_result, &fmt_cfg)?;

    // 11. 返回退出码
    Ok(())
}
```

**流程说明**
1. **参数解析**后立即执行，失败快速退出。
2. **配置加载**是幂等的，支持热重载（未来可扩展）。
3. **管道化数据处理**：reader → parser → filter → aggregate → formatter，每一步都是 `Iterator`，内存友好。
4. **错误局部化**：每个阶段独立 `Result`，便于定位故障点。
5. **可扩展性**：新增日志格式只需实现 `Parser` trait，新增输出只需实现 `Formatter` trait。

此设计满足 CLI 工具的健壮性、性能和可维护性要求，总字数约 1100 字。✓ Done (2 steps)
  📄 执行报告: /tmp/.hearth/reports/cdd92f7d-3e85-45ac-84e9-63945fddf238/run-001.md
  ✓ Task completed（2 步）——目标达成，产物见报告
  📊 tokens: ↑2446 ↓2063 (calls=1)
