2026-09-02T10:00:00.000000Z ERROR sandbox::seccomp: syscall denied op=clone3 pid=12345 comm=cargo — fail-closed
This is a Linux seccomp filter denial. The `clone3` system call is blocked, causing the process to fail-closed (deny + error rather than allow).
