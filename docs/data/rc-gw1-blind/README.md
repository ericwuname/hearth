# rc-gw1-blind · 包C 判分盲评材料包 v1.0（traecode→砺·经执行窗）

> **产出**：traecode 执行窗　**日期**：2026-09-08　**依据**：顶层《线C手术预研批复与执行计划》§三·序 4（即刻，无依赖）
> **来源**：`docs/data/r8-20260907/r8-all.tar.gz`（md5 `7d4453b837c0f49db10431d26b1b2069`）→ 本包 `rc-gw1-blind.tar.gz`（md5 **`C328ED2B31F89FD26A0265C13566E8A1`**）
> **用途**：G-W1 首份真尺判分（Q1-Q5，秒答条重点复核，B 臂判负 ±1 与 exit_code 弱仪器两笔登记收口）

## 一、包内容

`results/S001…S012/`——12 个匿名轮目录（**随机代号，非时间序**），每目录 29 条（盲测 15+C 语料 14）或 14 条（纯 C 语料），含逐条 `.log`（运行全文）/`.terminal`（终态锚点）/`summary.csv`（id,group,exit_code,体积）。

## 二、脱敏范围（顶层指令：抹臂标/引擎行/耗时）

| 处置 | 明细 |
|---|---|
| 臂标抹除 | 目录名随机匿名化（12! 随机映射，非时间序——防新旧推断）；arm.txt 删除 |
| 引擎行抹除 | version.txt / md5.txt 删除；log/terminal 内 `hearth 0.2.x (sha)`、`diagnostics →` 行、md5 行正则清洗（**318 文件**） |
| 耗时抹除 | 全部 `*.elapsed` 删除（**354 个身份/耗时文件**合计） |
| 非判分载荷 | 流水线脚本/门禁输出/部署日志全删 |

**泄漏抽验**：armA/armB/9ec07d9d/5c7aef03/hearth 0.2.25/elapsed 残留 = **0**（全包 12×29+14 文件 grep 实证）。

## 三、判分注意事项（3 笔登记，如实知悉）

1. **log 内容内 ISO 时间戳未清洗**（2,819 处，[span]/日志行内嵌）——按顶层指令范围（臂标/引擎行/耗时三样）执行；清洗会破坏正文。**后果知情**：S 轮的新旧可由日期推断（R7-1 轮=09-06，R8 post 轮=09-07）。砺判分时请知悉此残余可推断性。
2. **秒答条重点复核**（顶层批复 §一-2）：秒答条索引（elapsed ≤8s，**87 条**）由 traecode 单独持有（`rc-gw1-fast-items.txt`，随映射表同渠道移交验收窗→砺，**不入包不入库**）。注意：秒答密度本身可推断轮次身份（改后 A 臂秒答密度显著高）——盲评与点名复核存在结构性张力，按批复"重点复核优先"执行，风险已申报。
3. **B 臂判负 ±1 与 summary.csv exit_code 弱仪器**两笔登记由判分一并收口（R8 验收报告既定）。

## 四、保密映射表

`S001…S012 ↔ 原目录名` 映射由 traecode 持有（`.workbuddy/tmp/rc-gw1-mapping.txt`，**不入库不入包**），验收窗索取时移交；判分完成后映射表随归档仪式入总账。

---
*traecode · 2026-09-08 · 生成脚本 `.workbuddy/tmp/make_blind.py`（幂等可复跑）；泄漏扫描与抽验记录见 commit*
