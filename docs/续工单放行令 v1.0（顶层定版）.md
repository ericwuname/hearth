# 续工单放行令 v1.0（顶层定版）

> **签发**：顶层　**日期**：2026-09-02　**收件**：执行窗口（主）／测试窗口（抄送）
> **审批对象**：《续工单 · Node 10 → Campaign → 冻结报告（v0.2.23 基线）》（commit `d644b33`）+ 砺《续工单评审报告》（CONDITIONAL GO）
> **一句话**：**批准生效，按工单四步开工**；砺 6 条批准条件全盘接受，其中 2 项顶层裁定当场定（见 §2）；盲测黄金集剧本已由顶层提取就位（见 §5）。

---

## 1. 审批结论

砺评审报告的事实基线核验（9 项逐条对源）、R-1 解封裁定（附 J-1/J-2/J-3 首跑验证）、B1 改判 RC52_REPRO（附判定器修复条件）——**顶层全部认可，无推翻、无追加**。工单四步计划批准执行。

## 2. 两项顶层裁定（砺点名，当场定）

### COND-2 · 检测器目标数：**定 16，不凑 15**

- 三个新增检测器（`multi_intent_drop` / `paste_error_probe_quality` / `longpaste_truncation`）职责独立，分别盯意图丢失／探针质量／长贴截断，**砍一个换数字好看是自欺**；
- "总数 15"源自总包 Node 10 原文的算术错误（13+3≠15），纠正口径 = 写明 **13 + 3 = 16（含 3 个 Stage 2 新增）**；
- 落实：`simuser-status`（Node 15 八文件之一）里写死 16 与修正链，**16 这个数字漂进冻结包的是正确口径，不是漂移**。

### COND-6 · FZ-RFC-1/2b 双签追认：**时点锁定 = Step 2 campaign 运行期间**

- campaign 跑数是数小时级串行窗口，期间顶层完成 FZ-RFC-1/2b 追认文档并送执行窗双签；
- 硬门槛不变：**Node 15 冻结包归档前必须闭环**，未闭环则 Node 15 顺延，不带病进 CANDIDATE。

## 3. 责任分工（按砺 §5 表格执行，此处重申并补齐顶层项）

| 条件 | 内容 | 责任 | 时点 |
|---|---|---|---|
| COND-1 | harness 源归一：repo 侧 `tools/simuser/` 为唯一真源（`run_rc52_matrix.py` OUT 仍是 `~/fa/p4`，:16 未改——先改 OUT）→ 同步 VM → 跑完回传 repo | 执行窗（repo 侧修正）+ 测试窗（VM 同步与回传） | campaign 开工前 |
| COND-2 | 检测器数 = 16（本令 §2 裁定） | 执行窗 | Node 10 验收前 |
| COND-3 | R-1 三条首跑验证 J-1/J-2/J-3（C1 单跑通过才放 C2/C3），C 数据单独标注"v0.2.23 首次闭合" | 测试窗 | C1 单跑时 |
| COND-4 | `n14_judge.py` give_up 主导性检查（give_up ≥阈值且终态 failed → RC52_REPRO，provider 字符串降注记）+ B1 原始日志回归用例 | 测试窗 | campaign 判定前 |
| COND-5 | VM 实测：binary md5 `34f6f807…` 比对 + 门禁 486/0 复跑（门禁模板 `unset HEARTH_URL` + `HEARTH_CGROUP_BASE`；跑批另设 `HEARTH_ALLOW_NO_CGROUP=1`——两套作用域别套错） | 测试窗 | 开工探活时 |
| COND-6 | FZ-RFC-1/2b 双签追认（时点见本令 §2） | 顶层 + 执行窗双签 | Step 2 期间，Node 15 前 |

## 4. 开工序列

**执行窗口**：
1. Step 1（Node 10 测试基建，repo 侧）：sampler.py 联合分布采样器 → personas/scenarios 资产落盘 → 检测器补 3（按 16 口径）→ G1 硬门禁（矩阵先交砺评审再进 B2）；
2. COND-1 repo 侧：`run_rc52_matrix.py` OUT 改 `~/fa/n14-final`；
3. Step 4 可先行件：八文件中 architecture-status / behavioral-status / projection-status / open-deviations 依 `docs/p5-foundation/specs.md` 与审计文档起草（后五者等 Step 2/3 数据）；
4. Step 3 盲测包：从 `release/replay41-golden.txt`（sha256 `fb0c0ff5…`）抽 10 条 + 5 条新场景，做**双列盲测包**（hearth 输出列 / 评分列）送顶层。

**测试窗口**：COND-5 探活 → COND-1 VM 同步 → Step 2 campaign（≥30 runs 全新 v0.2.23 + 七场景×三样本，禁混表）→ COND-3/4 穿插执行。

**禁改令继续有效**：`crates/` 零改动；发现缺陷只登记不修。

## 5. Step 3 盲测（顶层本人）——剧本已就位

| 项 | 位置 |
|---|---|
| 盲测原始日志（90 分钟全量，3702 行） | `release/手工测试v0.2.18.txt`（本机仓库内） |
| **黄金集剧本（41 条输入行，顶层提取）** | `release/replay41-golden.txt`——每条带序号 + 源日志行号，sha256 前 16 位 `fb0c0ff5ca88b18` 见文件尾 |
| 盲测环境 | `.131`（确认 `hearth --version` = 0.2.23 再开跑）；`hearth repl` 逐条粘贴剧本输入 |
| 打分维度（对照 Q1 三缺陷 + RC52） | 每条记录：①输出是否符合预期 ②是否裸 ERROR 刷屏 ③是否"需要猜结果"（产物清单缺失）④是否 introspect 误路由 ⑤"继续"型输入是否假完成 |

正式 Step 3 评分以执行窗送来的双列盲测包为准；剧本提前给顶层可先跑熟悉版，两轮数据都留档。
