# Hearth 可用性改造长程任务书 v1.0（顶层定版 · 两段式）

> **签发**：顶层　**日期**：2026-09-02　**收件**：执行窗口（主）／砺·评审（判读与复核）／测试窗口（知会）
> **依据**：《hearth-usability-approval-pack-20260902.md》（分析窗口）+ 砺《可用性改造审批材料评审报告》（CONDITIONAL GO）
> **基线**：tag `v0.2.23`（commit `117a947`，gates 486/0）
> **一句话**：**批准立项**。主矛盾定性成立（"每周期模型只说话 1 次、整批盲跑、反馈中段被砍"——调参不可解，须改范式）。施工分两段：**段 1 立即跑**（Phase D 只读实验 + 四账并账，零 crates/ 改动）；**段 2 等 Node 15 双签后解锁**（W7→W2→W5 三个低风险包）；W3/W6/W4/W1 属 v0.3 段 3 另发。**同一窗口期只开一个总包**。

---

## 一、顶层审批裁决（对应砺 T-1~T-7 + 送审物决策矩阵）

| # | 项 | **顶层裁决** |
|---|---|---|
| T-1 | W1-W7 总时机表 | **按本任务书两段式定版**（见 §二/§三/§四） |
| T-2 | W4 审批默认分级 | **用户已拍板 A（2026-09-02）**：非 tty 默认改"命令表级破坏性操作 + 审计放行"（复用 DelegateSession，逐条审计可见），硬红线仍拦。**W4 解锁，排段 3** |
| T-3 | RC52 归因增补 | **批准增补**：归因链新增"架构层候选根因（L6 反应回路缺位）"条目，标注 🔶 待 D5 出数定级；D5 前不改写 CAUSE LIKELY 主结论 |
| T-4 | 四账并账 | **执行窗统一归并、砺复核**：本材料台账（19 项）↔ open-deviations（13 OD）↔ 骨架评审 S 清单 ↔ RC52 归因链。并账映射按砺 §3 末表执行 |
| T-5 | F2 纠正回填 | **批准**："生产续跑已实现"（lib.rs:644/680/702）回填总账，原"未实现"误判条目标注修正 |
| T-6 | 送审物口径修正 | **执行窗修订后存档**：18/19/20 计数统一为台账实数（17✅含2🔧+2🔶+1❌=20 行）、484→486、1324 函数归属改 `check_goal_drift` |
| T-7 | W1 立项 | **批准为 v0.3 A 能力线（成功率主线）立项主载体**——先设计小样（feature 分支+对照测试）→ 砺评审设计 → 再施工 |

**送审物决策矩阵勾选结果**：W1 批准（先设计小样，v0.3）｜W2 批准｜W3 批准带条件（v0.3）｜W4 批准原则、待用户拍板｜W5 批准｜W6 批准（v0.3）｜W7 批准先行｜Phase D 批准立即跑。

---

## 二、段 1（立即执行 · 零 crates/ 改动）

### P1 · 口径修正与四账并账（半天级，先行）

1. 按 T-6 修订送审物口径（存档，不改结论）；
2. 按 T-4 完成四账归并，产出**统一台账**（每条缺陷一个唯一编号，旧编号映射保留）——防 BUG-011/012/013 两本账复发；
3. 按 T-5 回填 F2；按 T-3 增补 RC52 归因条目。
4. 交付：统一台账文档 → 砺复核。

### P2 · Phase D 真机验证（8 实验，核心交付）

| 实验 | 触发方式 | 抓取信号 | 对应候选 |
|---|---|---|---|
| D1 | "用一段话解释 X 模块的风险" | 是否触发 write_file 强制/give_up（loop.rs:3366 注入） | L1/L1.5 |
| D2 | 8000 字符编译错误跑 `bash("cargo build")` | 回灌历史中段是否丢关键行（loop.rs:5278） | L0.5 |
| D3 | 长跑任务 | 每周期 system prompt 体积；compact 后历史保真 | L0.6/L5 |
| D4 | 非 tty（管道）跑含 bash 任务 | 预期 run_abort（loop.rs:3485-3534） | L2.5 |
| D5 | read→edit 依赖两步任务 | 批量内 edit 是否拿到 read 输出（预期失败，capture:None） | **L6 / RC52 架构根因** |
| D6 | 同一多步任务：Agnes vs Ollama qiyuan-8b | give_up 率 / 步数对比 | F1 放大系数 |
| D7 | 长跑中途 kill → `hearth resume <id>` | 无损续跑并收敛 | F2 |
| D8 | 正常编辑任务事件流 | 是否含 diff 类事件（预期无） | L3.5 |

**硬条件（砺 PD-C1/C2/C3，违反即数据作废）**：
- **PD-C1**：D6 只用 Agnes（基线可比）+ Ollama qiyuan-8b（本机）；换 provider = 换模型层，报告须声明不可比；
- **PD-C2**：全部实验跑 **.133**（实验机），**不占 .131**（campaign 专属）；provider 不可达样本必须出失败率分母；
- **PD-C3**：D2/D3 历史回灌抓取**直读 session JSONL**（`~/.config/hearth/sessions/`），不经 hearth 自身工具——防仪器污染。

**验收**：D1-D8 每实验一份结论（✅复现/❌未复现 + 数据），回填发现台账 🔶→✅/❌ 定级；**D5 结论直接决定 RC52 归因是否升格架构层根因、W1 设计小样的优先级**；D6 出 F1 放大系数定量。实验原始数据归档 + 报告交砺判读。

**当前并发检查**：campaign（≥30 runs）在 .131 跑数期间，Phase D 在 .133 并行**允许**（两机分工）；但若 campaign 需要占 .133 则错峰——先保 campaign。

---

## 三、段 2（Node 15 CORE FREEZE CANDIDATE 双窗终审（双签）后解锁 · 动 crates/）

> 解锁硬前置：①Node 15 双签完成；②段 1 的 D5/F1 数据已回填（W2/W5 设计参考 D2/D8 实测）；③P5 段 2 未在同一窗口期开工（并发纪律）。

### N-W7 · 小修包（先行热身，低风险）

| 项 | 内容 | 锚点 | 验收 |
|---|---|---|---|
| W7-a | apply_patch 中文错误路径 panic | `patch.rs:127/135/145`（`&s[..len().min(80)]` 字节切片） | 中文 search 的错误路径不崩（字符安全切片） |
| W7-b | extract_mentioned_files 误判 | `loop.rs:683` | call./done./glob. 不再触发虚假告警 |
| W7-c | 过时注释修正 | `loop.rs:3237`（"唯一 provider.chat 出口"已被 check_goal_drift 绕过——砺 §1 修正项 2） | 注释与事实一致；**只改注释不改语义** |

### N-W2 · 工具结果回灌保真（低风险高收益）

- 目标：消 L0.5 中段截断——超长输出改"错误行/关键行结构化保留 + 全量落盘给路径引用"；
- 锚点：`loop.rs:5278`（truncate_tool_output）、`loop.rs:2512`（record_tool_exchange）；
- 验收：8000 字符编译错误回灌后模型可见完整错误行（D2 复测对照）；历史体积有上限防 L0.6 恶化。

### N-W5 · 透明度包（低风险；直接消解用户 OD-6"需要猜"主诉）

- 目标：① 文件 diff 事件（L3.5，`session.rs:1002-1074` Event 枚举）；② Done/give_up 接手叙事（L3.6，`loop.rs:4802/4953`——"试了 X/被 Y 卡/建议下一步"）；③ 步数叙事（L3）；
- 验收：一次编辑任务事件流含可读 diff；失败终止报告含接手建议字段。
- **约束**：C-1（UNVERIFIED 标注）等 v0.2.23 新投影语义不得回退。

**段 2 门禁（每包）**：build → test → verify；fmt=0 / clippy=0 / tests pass（486 基线 + 新增行为测试）/ 实现率 ≥0.9 / 🔴=0；施工前源码备份（git archive 日期戳存 Desktop）；单独 commit、diff 留痕交砺复核。

---

## 四、段 3 预告（v0.3，另行派工，本单不施工）

| 包 | 内容 | 前置 |
|---|---|---|
| W3 | 提示敕令降级 + 契约路由升级 | 先落 L1.5 误路由用例表再动代码；C-1 语义不回退 |
| W6 | 失败恢复包（give_up 出口收口 + L2.7 恢复覆盖 + L2.6 双计步） | 与 OD-9/COND-4 判定器工作并账施工 |
| W4 | 审批默认分级 | **用户已拍板 A（2026-09-02），解锁可施工**——保守分级：非 tty 默认"命令表级破坏性操作 + 审计放行"（复用 DelegateSession），硬红线仍拦；禁止滑向全放行 |
| W1 | 反应回路改造（v0.3 A 线主载体） | 设计小样（feature 分支 + 对照测试）→ 砺评审 → 施工；验收三条见送审物 §6-W1 |

---

## 五、用户拍板记录（W4，已闭环）

**问题**：非 tty（管道/CI/agent-to-agent）调用默认 `DenyAllNonInteractive`，首条需审批 bash/edit 即 abort（`run_local.rs:332-339`）——自动化场景默认不可用。

**拍板结果**：**用户选 A（2026-09-02）**——保守分级：非 tty 默认改"命令表级破坏性操作 + 审计放行"（复用 DelegateSession 已有机制，delegated 逐条审计可见），硬红线（fork bomb/设备/内核）仍拦。

**W4 验收标准（随拍板生效）**：
1. 非 tty 跑含 bash 的任务不再 run_abort；
2. 硬红线用例仍拦截（3 条潜意识层硬命令 + 既有红线族）；
3. 审计日志完整（delegated 放行逐条可见、可追溯）；
4. 交互模式行为不变（逐工具审批不因本变更受影响）。

**施工注意**：锚点 `run_local.rs:332-339`（默认注入）、`loop.rs:3485-3534`（DenyAllNonInteractive 终止）、`loop.rs:3457-3480`（DelegateSession 放行+审计）；施工前重新 grep 核对锚点；新增行为测试须含"硬红线仍拦"负例（测试必须能失败）。

*（原选项 B 维持现状 / C 全放行已否决，留档备查。）*

---

## 六、红线（全程有效）

1. **并发纪律**：本任务书与 P5-FOUNDATION-01 都动 loop.rs 区域——**同一窗口期只开一个总包**；段 2 与 P5 段 2 错峰，顺序可调但不得并发；
2. **Node 15 前零 crates/ 改动**：段 2 未解锁前，执行窗不得以任何理由动 `crates/`（段 1 全程只读）；
3. **判读分离**：Phase D 数据判读 = 砺；执行窗交数据与初步结论，不自判；
4. **并账强制**：一切新缺陷进统一台账（P1 产出），禁止另立登记文件；
5. **S6 跨版本纪律**：实验数据标注基线版本，v0.2.22 取证结论迁移 v0.2.23 须注明（砺已验零漂移，后续版本变动须重新核对锚点）；
6. 行号锚点引用前重新 grep（漂移常态）。
