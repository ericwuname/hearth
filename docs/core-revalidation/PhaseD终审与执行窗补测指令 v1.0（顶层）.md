# Phase D 终审与执行窗补测指令 v1.0（顶层）

> **签发**：顶层　**日期**：2026-09-02　**收件**：执行窗口（主）／砺·评审（判读）／测试窗口（知会）
> **审批对象**：砺《PhaseD评审报告》（8 实验 6 接受 + 1 反证修正 + 1 仪器改口径 + 1 复测）
> **一句话**：**砺裁决全部维持，6 项移交决策全部当场定**（§一）；执行窗口按 §二补测清单开工（全部只读实验，**crates/ 零改动红线仍有效**）；U-44 修复派工去向已定（§三）。**本轮最值钱的产出是 D7 归因反证——避免了一轮空操作施工。**

---

## 一、移交决策项 · 顶层终审（砺 T-1~T-6 全部裁决）

| # | 项 | **顶层裁决** | 说明 |
|---|---|---|---|
| T-1 | U-44 修复方向 | **定：run 起始即落会话头** | 采纳砺建议。理由：run 启动路径单一、成本最低、覆盖全生命周期（含首个 LLM 调用与长工具调用期间的 kill）；tool 级心跳落盘为增量优化，后置 v0.3 评估 |
| T-2 | U-44 排 P5 段 2 前置 | **采纳：归入 P5 段 2 施工清单** | 数据丢失类与 S3 崩溃安全同族，同域并账施工；**不因 U-44 单独提前动 crates/**（Node 15 双签前零改动红线不变） |
| T-3 | U-45 分类 | **采纳：单列，不并 RC52** | fresh 单轮产物/终态背离 ≠ RC52 续作定义域，并分母会污染既有归因 |
| T-4 | W1 设计小样启动 | **采纳：启动设计（文档级），RC52 根因不定案** | 设计小样 = 设计文档 + 对照测试方案，不动 crates/ 主线；U-06 样本侧补齐（§二 P-D5）后方可在设计文档中引用为 ✅ |
| T-5 | 附-1 写域策略 | **采纳：本轮仅登记，v0.3 统一时顶层定政策方向** | write_file 严 / bash 宽的不对称属设计后果，是否统一 = 政策裁定 |
| T-6 | PD-C1 纪律 | **采纳并重申：偏差须事先报批，不得事后声明** | D6 用 qwen2.5:3b 未守指定口径，降为方向性认可——此教训入纪律：凡实验口径变更，先报批后跑 |

**U-06 状态确认**：有条件升格——机制侧（`capture:None` + `{{key}}` 未接线）已证；样本侧待 §二 P-D5 补齐后 🔶→✅。**RC52 架构根因在 U-06 定案前不写入冻结材料为"已定案"。**

---

## 二、执行窗口补测指令（四项实验 + 四项登记，全部只读）

### P-D5 · U-06 定案补样（最高优先）

- 补 **2 例** read→edit 依赖两步任务（含 1 例 read→**edit** 变体）；
- **必须抓全量 tool_calls 序列日志**，逐例区分：(a) 模型从未发出 write（规划能力问题，非 L6 证据）vs (b) write 已发出但内容空/失败（机制截断，L6 机制证据）；
- 两例均呈 (b) → U-06 ✅ 定案；混合 → 逐例归因，砺判读；
- 交付：tool_calls 原始日志 + 逐例 (a)/(b) 判定 → 砺复核。

### P-D1 · L1.5 误路由复测

- 按 L1.5 误路由用例表 ≥3 例，**必含**两条推演用例：「优化这个函数」（预期误判 QA）、「写下你的结论」（预期误判 product 被逼写文件）；
- 未复现 ≠ 证伪——措辞敏感本身即 L1.5 证据之一，如实记录措辞-路由对照表。

### P-D6 · qiyuan-8b 口径补测（PD-C1 纠偏）

- 用 **qiyuan-8b**（用户本机已装，零成本）重跑 D6 同款多步任务，与 Agnes 侧对照；
- 产出 F1 放大系数精确定级（U-07）；**换任何 provider 先报批**（T-6 纪律）。

### P-D3 · introspect 口径闭环（无需新仪器）

- 改用 `introspect` 的 `system_chars` / `history_chars`（`loop.rs:2307-2309` 已接线）；
- 已有实测锚点可直接引用：用户 B5 现场 `system_chars = 4628`（`手工测试v0.2.23-B.txt:279`）——L5/L0.6 静态敕令体量**已基本坐实**，补 2-3 个长跑采样点即可闭环；
- 顺带排查 D3 原 env 未达问题（`HEARTH_DEBUG_PLANNER_INPUT` 须在**启动 hearth 的 shell** export）。

### 登记项（四笔，进统一台账并交砺复核）

1. **U-44**：按修正归因登记——"会话落盘时机缺口（run 起始无会话头 + 长工具调用期间无落盘）"；**禁止按原归因（chat 路径补接线）施工——已证为空操作**；修复方向 run 起始落会话头，派工去向 P5 段 2（§三）；
2. **U-45**：fresh 场景产物与终态背离（Agnes 3 文件全对但 give_up），单列不并 RC52；
3. **附-1**：工具间写域不对称（write_file 拒 /tmp / bash 可写），🔵 设计后果观察项，v0.3 政策裁定；
4. **OD-1 证据链 +2**：D7v3/v4 resume 旧会话 give_up 两例入链，**不计 RC52 分母**（S6 不混表）。

### 文档修正

- 原材料 L2.5 表述"abort"改为"**结构化拒绝 + 补救指引**"（D4 实证 v0.2.23 语义已进化）——防冻结材料留过时描述。

---

## 三、派工去向速查（更新链路图）

```
段 1（进行中）：
  P1 四账并账（U-44/U-45/附-1/OD-1+2 入统一台账）← 本指令登记项
  P2 Phase D 补测（P-D5 → P-D1 → P-D6 → P-D3）→ 砺判读 → U-06 定案
  W1 设计小样（文档级，可并行启动）→ 砺评审设计
段 2（Node 15 双签后）：
  W7 → W2 → W5（可用性首批）
  + U-44 修复（run 起始落会话头，与 S3 同域并账）← 派工去向已定
P5 段 2（与段 2 错峰，同一窗口期只开一个总包）
段 3（v0.3）：W3 / W6 / W4（已拍板 A）/ W1 施工 / 附-1 写域政策
```

## 四、纪律重申

1. **crates/ 零改动红线仍有效**：本指令全部补测为只读实验；段 2 未解锁前不得动生产码（U-44 修复在 P5 段 2）；
2. **PD-C1 教训入纪律**：实验口径变更（provider/模型/环境）**先报批后跑**，事后声明不构成合规；
3. **判读分离不变**：补测数据交砺判读，执行窗交数据与初步归因；
4. D5 补样期间 .131 若有 campaign 占用，继续走 .133（两机分工不变）。
