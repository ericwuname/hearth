# L1 追问/元诊断类回归子集 · 数据回执 v4（执行窗 → 砺判读）

> **依据**：顶层 §四 + **砺《追问类回归子集·判读 v2》（取代 v1）**
> **v4 相对 v3 的变更**：① **L1-b 由"有条件同意"更正为"暂不立项"**（砺 v2 §3 撤回）；② **D2 状态更正为 SUSPENDED（悬置）**，撤回 v2 回执中的字面"有缺口"读数；③ 可比口径 0/1 不变；④ **硬断言已落地**（driver `wait_terminal_strict`）；⑤ A 臂与追问 4 条重跑已启动。
> **纪律**：只交数据与仪器状态；判定权归砺。

## 一、四项裁定的执行状态

| 砺裁定 | 执行窗处置 |
|---|---|
| #21 归因不变（PLAN 相位 T4 stall，与修法 A 无关） | ✅ v3 已定案（日志未命中三个救回 reason） |
| 可比口径 **0/1** | ✅ v3/v4 结果表按"有效/采集失败"×"有/无基线"两维分栏 |
| **L1-b 暂不立项**（v1 的有条件同意撤回） | ✅ 接单：**不立项**。前置 = 拿到有效零交换样本 + #21 归因落定 |
| **D2 悬置 SUSPENDED** | ✅ 接单：D2 状态一律写 SUSPENDED，**撤回** v2 回执里"B 满足 / A 不满足 → 字面记有缺口"的读数 |

## 二、结果表（两维分栏，不混列）

| 样本 | = A 组 | pre-fix 基线 | post-fix 结果 | 有效性 |
|---|---|---|---|---|
| **#21** | A7（行 398） | ✗ failed | ✗ failed（9 步 / 6 交换 / T4 stall） | ✅ 有效 |
| **#27** | —（不在 A 组 10 条） | 补跑中 | ✅ completed（15 步 / 6 交换 / **UNVERIFIED**） | ✅ 有效（**无基线，不可归因**） |
| #28 | — | 补跑中 | ⛔ 无终态 | ⛔ 无效（采集失败） |
| #29 | A9（行 809） | ✗ failed | ⛔ 无终态 | ⛔ 无效（采集失败） |
| #30 | — | 补跑中 | ⛔ 无终态 | ⛔ 无效（采集失败） |
| #32 | — | 补跑中 | ⛔ 无终态 | ⛔ 无效（采集失败） |

**可比读数（不解读）**：完全可比样本**仅 #21**，pre ✗ → post ✗，**未见改善**。
**#27 的成功属无基线新观测，不得计为修法 A 的提升**（"0/5→1/4"类表述禁止引用）。

## 三、硬断言落地（砺 §5 系统性观察 → 机器化）

**根因模式**：三次同类错误（B1 判定器 / #28/#29 采集失败 / A 臂 1/3），共同点 = **把仪器失败读成实验失败**。

**落地**：`tools/simuser/driver.py` 新增 `wait_terminal_strict()`（+ `send_and_wait_terminal_strict()`），与既有 `wait_turn` 并存、不破坏现有 API：

| 行为 | 说明 |
|---|---|
| 不把"看见 prompt"当作轮次结束 | 短轮上 prompt 先于终态出现 → 这是 A1 与 #28/#29 无终态的直接成因 |
| 命中终态后再静默 8 秒吸干尾部 | 保证报告路径/事件行完整入日志 |
| 超时无终态 → 返回 **`instrument_invalid`** 并写事件 | 调用方**必须**把该样本排除出分母；与 R-1 空 uuid assert 同款思路 |

双 VM 已同步（.131 `~/codex`、.133 `~/codex_t`）。

## 四、进行中的跑批

| 跑批 | 内容 | 落点 |
|---|---|---|
| **pre-fix 基线**（砺 §2 处置 3） | #27/#28/#30/#32 × **基线二进制 `/usr/local/bin/hearth` v0.2.23**（版本已确认） | `.131 ~/fa/l1-ask-prefix/` |
| **v5 重跑**（砺 §4 处置 1 + §2 处置 2） | A 臂 ×3 + 追问 #28/#29/#30/#32 ×1，均用 `wait_terminal_strict` | `.131 ~/fa/l1-rerun-v5/` |

两批完成后，A 臂须有 **≥3 条有终态有效样本**方可由砺按 D2 规则出一页判定。

## 五、L1-b 立项前置（砺定，执行窗记录不自行推进）

1. 拿到**有效**的零交换/有交换分布（v5 重跑）；
2. #21 归因落定（✅ 已完成）；
3. 若确有零交换的追问任务真实失败 → 再议立项。

**立项时的硬性设计约束（预先记录，防止届时放松）**：信号**不得**是"模型已输出非空文字"（= 纯 LLM 自报，违背 RC1 `loop.rs:4134`，且会救活"让我再想想"式空转，违反 `:2183` 护栏意图）。建议形态：
`文本 ≥ 阈值 ∧ 不含空转标记 ∧ 非"仅重述计划" ∧ consecutive_errors == 0 → Done + UNVERIFIED`；**反空转护栏保留**。

> **执行窗**　2026-09-03　本回执只交数据、仪器状态与裁定执行结果，不预置结论。
