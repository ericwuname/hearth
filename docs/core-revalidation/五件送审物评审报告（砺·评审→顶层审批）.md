# 五件送审物评审报告（A/B 手工测试 + open-deviations + 盲测包 + G1 相关矩阵）

> **送审方**：执行窗口 + 用户亲测　**评审**：砺·评审（🪨）　**日期**：2026-09-02
> **呈递**：顶层规划窗口审批
> **基线**：tag `v0.2.23`（commit `117a947`，gates 486/0）
> ** persona 纪律**：只读评审，源码锚点强制复验，不接受执行器自述；本报告每条结论下附源码锚点或测试现场行号。

## 0. 评审范围与锚点核查清单

| 件 | 路径 | 类型 | 状态 |
|---|---|---|---|
| 1 | `release/手工测试v0.2.23-A.txt` | 用户 A 组 5 轮 REPL 现场录 | 已审 |
| 2 | `release/手工测试v0.2.23-B.txt` | 用户 B 组 5 轮 REPL 现场录 | 已审 |
| 3 | `docs/core-freeze-candidate/open-deviations.md` | Node 15 八文件先行件（11 OD + 5 已闭环） | 已审 |
| 4 | `release/盲测包-正式轮-v0.2.23-10+5.md` | 10 黄金 + 5 新场景，五维评分 | 已审 |
| 5 | `docs/core-revalidation/G1-相关矩阵评审材料（执行窗→砺）.md` | G1 硬门禁送审物（8 件资产） | 已审 |

**本次新核验源码锚点（grep + read 实测）**：

| 锚点 | 位置 | 用途 |
|---|---|---|
| `[goal_drift]` observe-only 警示 | `crates/agent-core/src/loop.rs:5143-5144`（触发条件 `:5135` `steps >= GOAL_DRIFT_MIN_STEPS=20` `:425`） | 验证 B5 测试中触发的 RC31 机制 |
| bash 结构化出网拒绝 | `crates/tools-builtin/src/bash.rs:97-100`（`denied` + `allow.len()`） | 验证 B1 沙箱拒绝格式 |
| web_fetch 结构化出网拒绝 | `crates/tools-builtin/src/web.rs:101-104` | 同源路径，对照 |
| Q11 单一主体声明强制 | `tools/simuser/sampler.py:17-18, 41-44, 152, 156` | 验证 G1 sampler 纪律 |
| "不独立采样"设计原则 | `tools/simuser/sampler.py:5-7` | 验证 G1 方法论核心 |
| Cramér's V 规则版本 | `tools/simuser/correlation_matrix.py:59` `RULE_VERSION = "corr-rules-v1.1"` | 验证 G1 资产一致性 |
| V 矩阵 JSON 实际值 | `tools/simuser/correlation_matrix.json:62-63`（length×multi_intent=0.3815）；`:45-47`（mode×multi_intent=**0.1047**） | **G1 数值核对——发现差异** |
| B3 双向断言 | `tools/simuser/test_analyzer_b3.py:3` | G1 自检 |
| B4 MANUAL 项 | `tools/simuser/validate_assets.py:83`（R1 十域）、`:87`（四大域 ≥10%）、`:114`（MANUAL 不计退出码） | G1 自检 |

---

## 1. 用户 A 组手工测试（v0.2.23-A.txt，5 轮 REPL）

### 1.1 逐轮发现

| 轮 | 输入摘要 | 步数 | 终态 | 关键观察 | 盲测期望达成 |
|---|---|---|---|---|---|
| A1 | 现在怎么样啊，身体通不通 | 2 | ✓ Done（未验证） | LLM 未进入工具即 Done，本质是交互层 NOP；但"（未验证）"投影标注正确 | ✓ 不应规划 4+ 步——达成（2 步） |
| A2 | 木有特定的检查项，你自己先自检一下 | 38 | ✓ Done（未验证） | **意图理解偏移**：LLM 把"自检"理解为扫描 `.trae-cn-server` 等无关目录；严重超长但终态正确 | ⚪ 开放式自检——部分达成（计划投影可见，但偏移） |
| A3 | 故意编的 stalled 日志（探针质量） | 5 | ✗ Done failed | **核心缺陷复现**：未先 introspect/查源码核实，直接 give_up；`do_plan_inner failed` 仅裸错误行 | ✗ 应先 introspect/复现——**未达成** |
| A4 | 那你继续跑吧 | 5 | ✗ Done failed | **B 臂主缺陷完美复现**：继续型指令直接 give_up，无恢复尝试，无原因说明 | ✗ 继续型是否假完成/失败时是否说清原因——**未达成** |
| A6 | "✓ Done (21 steps)…我需要猜你的结果" | 49 | ✗ budget_exhausted | **v0.2.18 主诉复验**：仍需猜；预算耗尽前无清晰汇报 | ✗ 本轮是否仍需猜——**未达成** |
| A7 | 多意图长输入（give_up 为何不重规划） | 6 | ✗ Done failed | 多子句意图部分接收，但 reflect 阶段 give_up；至少一子句未被回应 | ✗ 逐子句是否都有回应——**未达成** |
| A9 | 策略对话+诚实性（"AI 说能完成长程任务了"） | 25 | ✗ Done failed | **诚实性 ✓**：给出"频繁 Done(5 steps) 失败模式"诚实分析；**但自身又陷入同一模式** | ⚪ 是否正视问题不粉饰——达成；恢复能力 ✗ |

**注**：用户 A 组 5 轮实际包含 7 条盲测输入（A1/A2/A3/A4/A6/A7/A9）。盲测 A5/A8/A10 未跑。

### 1.2 A 组综合

- **5 轮中 4 轮 ✗ Done，1 轮 ✓ Done 但超长（38 步）**。
- **B 臂 give_up 主缺陷完全复现**（A4 5 步直接放弃），与 OD-1 登记一致。
- **探针质量缺陷未改善**（A3 未先 introspect 即放弃），与 OD-7 登记一致。
- **"需要猜"问题仍存**（A6 budget_exhausted），与 OD-6 登记一致。
- **v0.2.23 新机制可观察生效**：`目标达成（未验证）` 投影标注在所有 ✓ Done 轮出现（A1/A2/B3 见 §2）。

---

## 2. 用户 B 组手工测试（v0.2.23-B.txt，5 轮 REPL）

### 2.1 逐轮发现

| 轮 | 输入摘要 | 步数 | 终态 | 关键观察 | 盲测期望达成 |
|---|---|---|---|---|---|
| B1 | curl example.com + curl baidu，两次结果都贴 | 5 | ✗ Done failed | **沙箱设计正确生效**：baidu 拒绝文案 `出网被拒：目标域名 ["www.baidu.com"] 不在 egress_allowlist 白名单（23 条）内。需要放行请让用户在 config.toml 的 egress_allowlist 中补入该域名。`（`bash.rs:97-100`，denied=["www.baidu.com"] allow.len()=23）；**但 agent give_up 后未把 example.com 的 200 成功结果贴给用户**——用户明确要求"两次结果都贴"，子点未达成 | ⚪ 结构化拒绝并点名域名——**完美达成**；贴出两次结果——**未达成** |
| B2 | 三步做网页（plan.html） | 10 | ✓ Done | **完美完成**：三步计划→执行→汇报；PDCA span 全可见，💭 推理投影存在；**轻度瑕疵**：第 1 步计划草案残留上一轮 "curl example.com"（stale context），第 2 步修正为 write_file——不影响终态 | ✓ 推理显式显示——**达成** |
| B3 | unverified_probe.txt 写 PROBE_OK，不用检查 | 6 | ✓ Done（未验证） | **投影标注机制成功证据**：产物 8 字节写入，终态标 `目标达成（未验证）` 而非裸 ✓；C-1/C-12 投影标注可观察 | ✓ UNVERIFIED 标注——**完美达成** |
| B4 | `{...}` 多行粘贴 + 三点总结 | 7 | ✓ Done（未验证） | **未进入多行粘贴模式**：agent 把指令本身当成任务执行（又写了 unverified_probe.txt），`{` 入口未触发；这是**多行粘贴入口 UX 缺陷**，非截断缺陷 | ✗ 是否有截断标记——**未达成（入口未触发，谈不上截断）** |
| B5 | 故意编的 seccomp 错误日志（探针质量） | 32 | ✓ Done | **关键观察**：`💭 [done] [goal_drift] ⚠ 终局产物与 original_goal 语义相关度存疑——请人工核对 run report（observe-only 警示，不改变终态）` 触发（源码 `loop.rs:5143-5144`，触发条件 `steps >= GOAL_DRIFT_MIN_STEPS=20` `:5135`）；**但 agent 未先 introspect 查源码核实日志真伪**，直接进入"修复 seccomp"路径——探针质量缺陷再次复现 | ⚪ goal_drift 机制——**达成**；先 introspect 核实——**未达成** |

### 2.2 B 组综合

- **5 轮中 3 轮 ✓ Done（B2/B3/B5），2 轮 ✗ Done（B1/B4）**。
- **B1 的 ✗ Done 实际是沙箱正确拒绝**（设计生效），但 agent give_up 后未贴出 example.com 成功结果——**新发现缺陷**：失败时丢失已成功的并行结果。
- **B3 UNVERIFIED 投影标注完美工作**——C-1/C-12 机制可观察证据。
- **B5 goal_drift observe-only 警示触发**——RC31 机制工作证据（源码锚点 `loop.rs:5143-5144`）。
- **B4 多行粘贴入口未触发**——`{` 检测可能被前导文本干扰，需 v0.3 修复。
- **B4 盲测包设计建议**：原期望"是否有截断标记/静默截断"，但实测根本没进入粘贴模式。盲测包应增加备选观察点"如果用户没有粘贴长文而是把指令本身当任务执行"。

---

## 3. open-deviations.md 评审

### 3.1 11 项开放偏差逐项核对

| # | 级 | 偏差 | 证据复验 | 砺裁决 |
|---|---|---|---|---|
| OD-1 | 🟡 | B 臂失败模式残留 | 用户 A 组 A4 完美复现（5 步 give_up）；盲测 #7/#8/#10 FAILED | 🟡 正确；v0.3 候选修复 |
| OD-2 | 🟡 | FZ-RFC-1/2b 双签未闭环 | 与砺上轮 COND-6 一致 | 🟡 正确；campaign 期间闭环 |
| OD-3 | 🟡 | G1-PENDING | 本报告 §5 即产出 G1 裁决 | 🟡 正确；本报告裁决后可降级 |
| OD-4 | 🟡 | workspace 全量测试 RT4-OOM（socket 死锁 2 次） | 新登记项，砺上轮未列入 | 🟡 正确；测试期避开全量口径 |
| OD-5 | 🔵 | 沙箱 cargo test exit 159 core dump | 与 v0.2.22 send 侧修复同源，clone/构建链 syscall 未核 | 🔵 正确登记 |
| OD-6 | 🔵 | "✗ Done"渲染语义混乱 | 用户 A6 主诉"需要猜"直接对应；盲测 #12 用户原话 | **🔵→🟡 建议升级**：用户主诉渲染成因，非纯观察项；v0.3 投影层修复候选 |
| OD-7 | 🔵 | PLAN stall 无 💭 依据投影 | 用户 A3 现场 `do_plan_inner failed error=stalled` 仅裸错误行 | 🔵 正确登记 |
| OD-8 | 🔵 | 空输入触发 FAILED | 用户 A 组未测，盲测 #5 有 | 🔵 正确登记；空输入语义待顶层定 |
| OD-9 | 🔵 | B1 判定器 give_up 主导性检查 | 砺上轮 COND-4 延续 | 🔵 正确；测试窗 campaign 判定前落实 |
| OD-10 | 🔵 | B4 两个 MANUAL 项 | `validate_assets.py:83,87` 确认存在；`:114` MANUAL 不计退出码 | 🔵 正确；随 G1 评审人工裁定（§5.3） |
| OD-11 | 🔵 | .133 源码树落后（0.2.20 时代） | E7 三步同步前置 | **🔵→🟡 建议升级**：评审窗开工前置，非纯观察项 |

### 3.2 已闭环项验证（5 项全部属实）

| 项 | 验证 | 结论 |
|---|---|---|
| ~~bash 出网零管控~~ | B1 测试证明 23 域白名单收口；`bash.rs:97-100` + `web.rs:101-104` 双路径结构化拒绝 | ✓ 属实 |
| ~~"key 直透"🔴~~ | S4 env_clear 兜底落地（FZ-RFC-1） | ✓ 属实 |
| ~~超长写毁原文件 / kill 丢整轮 / 零快照~~ | S1/S2/S3 落地 | ✓ 属实 |
| ~~C 条件 BLOCKED~~ | R-1 解封（砺上轮裁定 + J-1/J-2/J-3 首跑验证前置） | ✓ 属实 |
| ~~宪法 FALLBACK 静默~~ | C-3 告警落地（v0.2.21） | ✓ 属实 |

### 3.3 open-deviations 综合裁决

- **清单完整**：11 项覆盖了用户 A/B 测试 + 盲测包现场所有可观察缺陷，无遗漏重大项。
- **分级正确**：🟡/🔵 划分与砺独立判断一致。
- **建议两项升级**：OD-6（用户主诉渲染成因，建议 🟡）、OD-11（评审窗开工前置，建议 🟡）。
- **已闭环 5 项全部属实**——防止陈旧记忆误报的纪律生效。

---

## 4. 盲测包 10+5 评审

### 4.1 结构与口径

| 项 | 验证 | 结论 |
|---|---|---|
| 10 黄金集（replay41 逐字） | A 组覆盖五维与主要缺陷族；输入与用户熟悉版 A 组高度重叠（A1/A2/A3/A4/A6/A7/A9/A10） | ✓ 结构完整 |
| 5 新场景（对准 v0.2.23 三新能力 + 两盲区） | B1 出网白名单 / B2 推理显式 / B3 UNVERIFIED 标注 / B4 超长粘贴 / B5 假报错探针 | ✓ 对准正确 |
| 五维评分（0-2 × 5 = 满分 10） | ①输出符合预期 / ②无裸 ERROR / ③不用猜结果 / ④introspect 不误路由 / ⑤继续型不假完成 | ✓ 口径清晰 |
| Strong #10 F0 缺陷率挂钩 | 正式轮数据计入冻结材料，熟悉版数据留档 | ✓ S6 跨标签纪律严格执行（v0.2.21 10 runs 不计入 v0.2.23 ≥30） |
| 环境规格 | `.131` 或 `.133` 均 0.2.23，md5 `34f6f807…`，开跑前 `hearth --version` 确认 | ✓ 与 OD-11 一致，开跑前必须 E7 三步同步 |
| 顶层亲测 | 包头部"你已跑的熟悉版数据同时留档"——给用户本人的执行包 | ✓ 符合"顶层亲测"要求 |

### 4.2 盲测包设计建议

| # | 建议 | 理由 |
|---|---|---|
| BP-1 | B4 增加备选观察点"如果用户没有粘贴长文而是把指令本身当任务执行" | 用户 B4 实测未进入多行粘贴模式，原期望"截断标记"无从观察 |
| BP-2 | 正式轮必须用全新 session 跑，不能复用熟悉版 session 数据 | 用户熟悉版 A 组已跑 7/10 黄金输入，存在"模型已见过该 session"污染风险——S6 纪律要求 |
| BP-3 | B1 期望增加子点"失败时是否丢失已成功的并行结果" | 用户 B1 实测 example.com 200 成功但 give_up 后未贴出 |

---

## 5. G1 相关矩阵评审（**硬门禁核心**）

### 5.1 资产清单验证（8 件全部存在）

| 件 | 路径 | 存在 | 关键锚点 |
|---|---|---|---|
| 相关矩阵计算器 | `tools/simuser/correlation_matrix.py` | ✓ | `:59` `RULE_VERSION = "corr-rules-v1.1"` |
| 矩阵产出 | `tools/simuser/correlation_matrix.json` | ✓ | `:2` `rule_version: "corr-rules-v1.1"`；`:7` `single_subject: true` |
| 联合分布采样器 | `tools/simuser/sampler.py` | ✓ | `:5-7` 不独立采样原则；`:17-18, 41-44` Q11 强制 |
| persona ×12 | `tools/simuser/personas/p01..p12.yaml` | ✓ | 头部 G1-PENDING |
| 矩阵场景 ×10 | `tools/simuser/scenarios/s01..s10.yaml` | ✓ | Node 12 七类 + 3 战略类 |
| B3 检测器自检 | `tools/simuser/test_analyzer_b3.py` | ✓ | `:3` 6/6 双向断言 |
| B4 验收自测 | `tools/simuser/validate_assets.py` | ✓ | `:83, 87` MANUAL 项；`:114` MANUAL 不计退出码 |

### 5.2 方法论验证

| 原则 | 源码锚点 | 结论 |
|---|---|---|
| 不对各维度独立采样 | `sampler.py:5-7` | ✓ 设计正确 |
| 逐字采样真实种子话语 | `sampler.py:5-6` | ✓ 措辞纪律 |
| 同一规则函数打标 | `sampler.py:7` + `correlation_matrix.py:155` `label_multi_intent` 等 | ✓ 维度联合分布 = 语料经验联合分布 |
| Cramér's V on rule-labelled cross-tabs | `correlation_matrix.py:181` | ✓ 方法明确 |
| rule_version `corr-rules-v1.1` | `correlation_matrix.py:59` ↔ `correlation_matrix.json:2` | ✓ 一致 |
| Q11 单一主体声明强制 | `sampler.py:17-18, 41-44, 152, 156` | ✓ 纪律生效 |
| 结构性恒等对单独标注 | `correlation_matrix.py:22` + `json:16-20` `category×mode structural: true` | ✓ 不参与独立性结论 |

**方法论裁决：通过。** "不独立采样 + 逐字采样 + 同规则打标"从根本上规避"独立采样拼出假人"风险——这是 G1 门禁最核心的设计正确性。

### 5.3 V 矩阵数值核对（**关键发现**）

| 维度对 | G1 材料报告 | JSON 实际值 | 一致性 |
|---|---|---|---|
| length×multi_intent | 0.381 | 0.3815 | ✓ 一致 |
| category×anaphora | 0.327 | 0.3265 | ✓ 一致 |
| category×style | 0.317 | 0.3173 | ✓ 一致 |
| category×tone | 0.251 | 0.2508 | ✓ 一致 |
| **mode×multi_intent** | **0.065** | **0.1047** | **❌ 不一致** |

**差异分析**：
- 材料报告 0.065，JSON 实际 0.1047，差 0.04（相对差 60%）。
- **方向性结论仍成立**：0.1047 仍 <0.2 弱相关阈值，"战略/执行模态与复合度几乎无关，可独立分层"结论不变。
- 但材料作为冻结资产，数值必须与 JSON 一致——**必须修正**。
- 与材料 §4 项 2"n=273 抽样噪声 ±0.15"形成悖论：既然 ±0.15 噪声，0.065 与 0.1047 之差 0.04 仍在噪声内，方向结论不变；但材料数值声明错误本身是冻结材料瑕疵。

### 5.4 评审请裁决项 5 项

| # | 裁决项 | 砺裁定 | 理由 |
|---|---|---|---|
| 1 | multi_intent 口径（v1.1 硬分隔符 vs R3 人工 21.9%，偏差 +2.6pt） | **接受** | 偏差在合理范围，方向明确（收紧后向人工基线靠拢）；种子集人工重标成本不划算 |
| 2 | n=273 抽样噪声 ±0.15，定方向非定精确值 | **接受** | 但**与 §5.3 mode×multi_intent 数值错误形成悖论**——既然 ±0.15 噪声，0.065→0.1047 之差仍在噪声内，方向结论不变；材料数值仍须修正 |
| 3 | 稀有类放大权重 9pt（B4 实测 9.0%） | **接受** | 权重值合理且与 B4 实测对齐 |
| 4 | 语料边界（五会话源，不含 hearth 本身） | **接受** | 口径明确，Hearth 侧行为由场景驱动 |
| 5 | B4 两个 MANUAL 项（R1 十域、四大域 ≥10%） | **裁定为 MANUAL 项** | R1 十域：`json:103-117` category×mode 列联表显示 12 个 category，覆盖 ≥10 要求（实际 12 域）✓；四大域 ≥10% 场景级映射：需手工核对 scenarios/s01-s10.yaml 的 domain 字段，MANUAL 项本质 |

### 5.5 已知局限诚实披露

| 局限 | 评估 |
|---|---|
| 种子话语为语料作者精选子集，长度/语气分布可能偏代表性 | 风险已知，接受 |
| 规则打标对反讽/上下文语气不敏感 | 确定性纪律代价，知情人裁决，接受 |
| V 矩阵为边际近似，未建模高阶交互 | B2 场景内手写结构补足，接受 |

**三条局限均诚实披露 ✓。**

### 5.6 G1 综合裁决

**CONDITIONAL PASS**——方法论与设计通过，但 mode×multi_intent 数值必须修正后方可解锁 B2 sampler。

**G1-PENDING 解锁条件**：

| # | 条件 | 责任方 |
|---|---|---|
| G1-C1 | 修正材料中 mode×multi_intent 数值（0.065 → 0.1047） | 执行窗口 |
| G1-C2 | 同步修正材料叙事——保留"战略/执行模态与复合度几乎无关，可独立分层"方向结论，去掉"0.065"错误数值 | 执行窗口 |
| G1-C3 | 修正后冻结材料中标注"V 值已 source-anchored 复核（砺 2026-09-02）" | 执行窗口 |
| G1-C4 | 修正后 OD-3 (G1-PENDING) 可降级为已闭环 | 顶层 |

---

## 6. 综合裁决与开放项移交

### 6.1 综合裁决

| 件 | 裁决 | 关键条件 |
|---|---|---|
| 用户 A 组测试 | **如实登记，不改终态** | A4/A6/A7 复现的 B 臂缺陷与 OD-1/OD-6/OD-7 一致，v0.3 修复候选 |
| 用户 B 组测试 | **如实登记，不改终态** | B1 沙箱拒绝设计生效（新发现缺陷：失败时丢失已成功并行结果）；B3 UNVERIFIED 投影标注完美工作；B5 goal_drift 触发（RC31 工作）；B4 多行粘贴入口未触发 |
| open-deviations | **CONDITIONAL ACCEPT** | 11 项分级正确；建议 OD-6/OD-11 升级 🟡；5 已闭环项属实 |
| 盲测包 10+5 | **ACCEPT** | 结构完整，五维口径清晰；3 项设计建议（BP-1/BP-2/BP-3） |
| G1 相关矩阵 | **CONDITIONAL PASS** | 方法论通过；mode×multi_intent 数值必须修正（G1-C1~C4） |

### 6.2 移交顶层的开放项

| # | 开放项 | 砺建议 | 决策方 |
|---|---|---|---|
| O-1 | OD-6 升级 🟡（用户主诉渲染成因） | 升级 | 顶层 |
| O-2 | OD-11 升级 🟡（评审窗开工前置） | 升级 | 顶层 |
| O-3 | B1 新发现缺陷"失败时丢失已成功并行结果" | v0.3 修复候选 | 顶层 |
| O-4 | B4 多行粘贴入口 UX 缺陷 | v0.3 修复候选 | 顶层 |
| O-5 | 盲测包 BP-1/BP-2/BP-3 设计建议 | 正式轮前修订 | 顶层 + 执行窗 |
| O-6 | G1-C1~C4 解锁条件 | 执行窗修正后报砺复核 | 执行窗 → 砺 |
| O-7 | 空输入语义（nop 还是重试提示）OD-8 | 顶层定 | 顶层 |

### 6.3 承接砺上轮裁定（不变）

| 上轮裁定 | 本轮状态 |
|---|---|
| COND-1~6（续工单评审报告） | 不变，仍生效 |
| C 条件解封（R-1 实现 + J-1/J-2/J-3 首跑验证前置） | 不变，仍生效 |
| B1 重分类 RC52_REPRO（判定器 give_up 主导性检查 + B1 回归用例） | 不变，仍生效；OD-9 延续 |
| RC52 维持 CAUSE LIKELY（三臂 campaign + 盲测前不升格） | 不变，仍生效 |
| S6 跨标签纪律（v0.2.21 10 runs 不计入 v0.2.23 ≥30） | 不变，盲测包严格执行 |
| Node 15 仅可声明 CORE FREEZE CANDIDATE | 不变，仍生效 |
| Q11 单一主体声明（campaign 报告禁"真实用户成功率"） | 不变，G1 sampler 已强制 |

---

## 7. 源码锚点清单（本次评审新增核验）

| # | 锚点 | 位置 | 用途 | 核验方式 |
|---|---|---|---|---|
| A1 | `[goal_drift]` observe-only 警示 | `crates/agent-core/src/loop.rs:5143-5144` | RC31 机制验证 | grep + read |
| A2 | GOAL_DRIFT_MIN_STEPS=20 | `crates/agent-core/src/loop.rs:425` | 触发阈值 | read |
| A3 | 触发条件 `steps >= GOAL_DRIFT_MIN_STEPS` | `crates/agent-core/src/loop.rs:5135` | 触发路径 | grep |
| A4 | bash 结构化出网拒绝 | `crates/tools-builtin/src/bash.rs:97-100` | B1 沙箱拒绝格式 | grep + read |
| A5 | web_fetch 结构化出网拒绝 | `crates/tools-builtin/src/web.rs:101-104` | 同源路径对照 | grep |
| A6 | Q11 单一主体声明强制 | `tools/simuser/sampler.py:17-18, 41-44, 152, 156` | G1 纪律 | grep |
| A7 | "不独立采样"设计原则 | `tools/simuser/sampler.py:5-7` | G1 方法论核心 | grep + read |
| A8 | Cramér's V 规则版本 v1.1 | `tools/simuser/correlation_matrix.py:59` ↔ `correlation_matrix.json:2` | G1 资产一致性 | grep + read |
| A9 | V 矩阵 length×multi_intent | `tools/simuser/correlation_matrix.json:62-63` (0.3815) | G1 数值核对 ✓ | read |
| A10 | V 矩阵 mode×multi_intent | `tools/simuser/correlation_matrix.json:45-47` (0.1047) | G1 数值核对 ❌ | read |
| A11 | B3 双向断言 | `tools/simuser/test_analyzer_b3.py:3` | G1 自检 | grep |
| A12 | B4 MANUAL 项 | `tools/simuser/validate_assets.py:83, 87, 114` | G1 自检 | grep |
| A13 | category×mode 列联表 12 域 | `tools/simuser/correlation_matrix.json:103-117` | R1 十域覆盖证据 | read |

---

## 8. 评审结论一句话

**五件送审物整体 CONDITIONAL GO**：用户测试如实登记不改终态；open-deviations 完整分级正确（建议 OD-6/OD-11 升级）；盲测包结构完整（3 项设计建议）；**G1 相关矩阵方法论通过但 mode×multi_intent 数值必须修正（0.065→0.1047）后方可解锁 B2 sampler**。所有裁定承上轮不变（RC52 维持 CAUSE LIKELY、Node 15 仅 CANDIDATE、S6 跨标签纪律、Q11 单一主体声明）。

**呈递顶层规划窗口审批。**

---

> **砺·评审（🪨）**　2026-09-02
> 本报告每条结论附源码锚点或测试现场行号； persona 纪律：只读评审，不接受执行器自述，源码锚点强制复验。
