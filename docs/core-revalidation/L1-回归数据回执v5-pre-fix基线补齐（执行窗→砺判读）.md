# L1 回归数据回执 v5（执行窗 → 砺判读）——pre-fix 基线已补齐

> **依据**：砺判读 v2 §2 处置 3（补 pre-fix 基线）+ §4 处置 1（A 臂须 ≥3 条有终态有效样本）
> **仪器**：`driver.wait_terminal_strict`（硬断言版，v4 落地）；无终态样本一律 `instrument_invalid`，**不计入分母**
> **纪律**：本回执只交数据与实测值，**D2 与 L1-b 的判定权归砺**，执行窗不预置结论
> **原始数据**：`.131 ~/fa/l1-rerun-v5/`（post-fix）+ `~/fa/l1-ask-prefix/`（pre-fix 基线）

## 一、pre-fix ↔ post-fix 可比对照（同条目、同条件、全新 session）

| 条目 | **pre-fix**（基线 `v0.2.23` 二进制） | **post-fix**（L1 分支 `hearth-l1`） | 变化 |
|---|---|---|---|
| **#28** | ✗ failed（12 交换） | ✗ failed（3 交换 / 5 步） | 无变化 |
| **#30** | ✗ failed（17 交换） | ✅ **completed**（25 交换 / 51 步 / **UNVERIFIED**） | **✗ → ✓** |
| **#32** | ✗ failed（4 交换） | ✅ **completed**（1 交换 / 6 步 / **UNVERIFIED**） | **✗ → ✓** |
| #27 | ⛔ 无效（无终态，len 1267） | ✅ completed（15 步 / 6 交换 / UNVERIFIED，v4 批） | 不可比（基线侧无效） |
| #29 | 未跑基线 | ⛔ instrument_invalid | 无效 |

**可比样本 3 条（#28/#30/#32）**：**pre 0/3 → post 2/3**；两个成功样本**均带 UNVERIFIED 诚实标注**。
**注**：#32 仅 1 次工具交换即被救回——修法 A 门槛的下沿样本。

## 二、A 臂（v5，严格等待）

| 样本 | turn1 | turn1 产物 | turn2 | turn2 交换 | 有效性 |
|---|---|---|---|---|---|
| A1 | ✅ completed（UNVERIFIED） | ✅ | ✗ failed | 4 | ✅ 有效 |
| A2 | ✅ completed（UNVERIFIED） | ✅ | ✅ completed（UNVERIFIED） | 6 | ✅ 有效 |
| A3 | ✅ completed（UNVERIFIED） | ✅ | ✅ completed（UNVERIFIED） | 8 | ✅ 有效 |

**有效样本 3/3（全部有终态）；turn2 completed = 2/3；turn1 completed = 3/3（全部 UNVERIFIED）。**

## 三、D2 合取规则的**实测值**（判定权归砺，此处只填数）

| 条件 | 阈值 | 实测 | 样本有效性 |
|---|---|---|---|
| B 臂 ✓率 | ≥3/5 | **4/5 = 80%** | ✅ 有效 |
| A 臂 ✓率 | ≥2/3 | **2/3 = 67%** | ✅ **3/3 有终态**（较上批 1/3 含 2 个无终态已修正） |

合取条件的**两个实测值均已达到各自阈值**——**是否据此判定"根因模型验证成立"，由砺按 D2 规则出具一页判定**（执行窗不预置）。

## 四、L1-b 立项前置（砺 v2 §3）的现状

| 前置 | 状态 |
|---|---|
| ① 拿到**有效**的零交换/有交换分布 | ✅ 已拿到：#30（25）、#32（1）、#28（3）均为**有交换**；**本批未出现零交换的有效追问样本** |
| ② #21 归因落定 | ✅（PLAN 相位 T4 stall，与修法 A 无关） |
| ③ 若确有零交换追问且失败 → 再议 | 当前**未观测到**零交换追问的有效失败样本 → 按砺裁定，**L1-b 维持"暂不立项"** |

## 五、硬断言运行情况（v4 落地的验证）

- #29 → `instrument_invalid`（**正确拦下**，未被计为 failed）；
- PRE#27 → 基线侧无终态，**未计入分母**；
- 其余样本均返回明确终态 → 分母干净。

**结论性一句（供砺参考，非执行窗判定）**：硬断言上线后，同一批实验从"有效样本 2/6"提升到"可比样本 3 条 + A 臂 3/3 有效"，**仪器问题已不再是数据的主要噪声源**——砺 §5 所要求的机器化处置生效。

> **执行窗**　2026-09-03
