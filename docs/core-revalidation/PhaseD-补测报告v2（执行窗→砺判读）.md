# Phase D 补测报告 v2 · P-D5/P-D1/P-D6/P-D3（执行窗 → 砺判读）

> **执行**：执行窗口　**日期**：2026-09-02　**依据**：《PhaseD终审与执行窗补测指令 v1.0》§二
> **环境**：.133（PD-C2 ✓）；crates/ 零改动 ✓；口径变更（P-D6 用 qiyuan-8b 正口径）已由指令预批（T-6 教训已吸收：先批后跑）
> **原始数据**：.133 `~/phaseD/`（summary_v3.json / summary_v4.json / summary_pd3.json + 逐实验 .log 与 .tool_calls.json）
> **台账**：U-44（修正归因）/ U-45 / U-46（=附-1）/ U-47（补样反证）/ U-10 证据链+2 已登记 `docs/core-freeze-candidate/unified-ledger.md`

## 一、P-D5 · U-06 定案补样（⚠ 重大归因反转，交砺裁）

| 例 | 任务 | 结果 | (a)/(b) 判定 |
|---|---|---|---|
| v3-1 read→write（/tmp 目标） | 原样重跑 | result 未创建；**write 从未发出** | (a) 规划问题 |
| v3-2 read→edit（/tmp 目标） | edit 变体 | write_file 发出且**内容正确**（mode=on\nretries=3）但被**读根域拒绝**（/tmp） | (b) 表面成立，**实为 U-46 写域拒绝**——非 L6 |
| **v4-1 read→write（workspace 内，干净口径）** | 同任务 | **✅ 内容完整传递，write 成功** | ❌ 未复现 |
| **v4-2 read→edit（workspace 内，干净口径）** | 同任务 | **✅ 编辑成功**（mode=on 落盘） | ❌ 未复现 |

**结论（初步归因）**：**原 D5（v1）的"read→write 依赖失败"归因反转**——真因是写域拒绝（U-46），不是 capture:None。干净口径下强模型（Agnes）通过**跨周期排序**（read 一个周期、write 下个周期）规避了同批 capture:None。**L6 伤害为条件性**：仅在模型把依赖调用排入**同一批次**时触发（U-47）。**U-06 不定案 ✅**，按指令"混合 → 逐例归因，砺判读"交砺。RC52 架构根因维持不定案（终审 §一确认）。

**对 W1 的直接影响**：设计小样（`docs/core-revalidation/W1-反应回路改造设计小样 v0.1（文档级）.md`）已按"条件性伤害"重构对照用例——T-A 用提示词强制同批依赖构造红例，T-B 跨周期排序为对照组。

## 二、P-D1 · L1.5 误路由复测（3 例 + 指令指定 2 例全含）

| 用例 | write 强制 | 终态 | 实际失败模式 |
|---|---|---|---|
| 「优化这个函数」 | 0 次 | give_up（4 工具调用） | 无上下文指代（"这个函数"）→ 打转后 give_up |
| 「写下你的结论」 | 0 次 | give_up（6 调用） | 同上 |
| 「帮我分析一下刚才失败的原因」 | 0 次 | give_up（12 调用） | 同上 |

**初步归因**：三条用例**均未触发 write 强制**（预期误判 QA/product 未出现）；失败模式统一为**无上下文指代 + 恢复护栏 keyed to criteria 落不到（U-11）→ give_up**。按指令"未复现 ≠ 证伪——措辞敏感本身即 L1.5 证据之一"：措辞-路由对照表已存档（`~/phaseD/pd1_*.tool_calls.json`），**是否构成 L1.5 反证由砺裁定**。

## 三、P-D6 · qiyuan-8b 正口径（F1 定级材料）

| 指标 | Agnes（基线，D6 v1） | **qiyuan-8b（本机 Windows Ollama，正口径）** |
|---|---|---|
| 耗时 | 11.2s | **593s（53×）** |
| 终态 | give_up（但 3 文件全对） | ✓ Task completed（自称完成，无 give_up） |
| 路径合规 | 3/3（/tmp/d6q/a|b|c.txt） | **0/3**（三个全写进 `/home/wutao/a.txt` 同一文件，5 字节） |
| 内容合规 | ALPHA/BETA/GAMMA 全对 | 5 字节单文件 |
| 行为定性 | 产物对、终态错（U-45） | **终态"完成"、产物不合格——假完成形态随模型强度变化** |

**初步归因**：F1 放大成立且方向修正——弱模型不是单纯"更差"，而是**把失败伪装成完成**（✓ Done + 产物不合格），恰是用户五维"需要猜结果"主诉的模型侧放大器。**U-07 升格材料交砺定级**。（环境注：Windows Ollama 401 WARN 为 api_key mismatch 告警，不影响 ollama 无鉴权调用，数据有效；工具调用明细在 `~/phaseD/pd6_qiyuan_retry.log`。）

## 四、P-D3 · introspect 口径闭环（2 采样点 + 顶层锚点）

| 采样 | system_chars | history_chars |
|---|---|---|
| s1（fresh 直问） | 1657 | 1657 |
| s2（3 文件任务后） | 449 | 4734 |
| 顶层锚点（用户 B5 现场） | **4628** | —（`手工测试v0.2.23-B.txt:279`） |

**初步归因**：system prompt 体量随**契约类型**显著变化（1657 vs 449——product 敕令 vs 简约契约），B5 现场 4628 为长跑高点。L5/L0.6 静态敕令体量坐实；**同任务不同契约的体积差本身是 L5 的直接量化**。原始值留砺判读（模型转述数字与 introspect 实际输出的一致性未做二次核验——如实声明）。

## 五、登记与文档修正核对单（终审 §二）

| 项 | 状态 |
|---|---|
| U-44 修正归因登记（禁"chat 路径补接线"） | ✅ unified-ledger 已改 |
| U-45 单列不并 RC52 | ✅ 已登记 |
| 附-1 → U-46 登记（含 D5 反证） | ✅ 已登记 |
| OD-1 证据链 +2（不计分母） | ✅ U-10 已注 |
| U-13 L2.5 表述改"结构化拒绝 + 补救指引" | ✅ 已改 |
| W1 设计小样（文档级） | ✅ 已产出 v0.1 |

## 六、待砺裁定项

1. **U-06**：P-D5 混合 + 干净口径反证——维持 🔶 还是改判（原 D5 归因反转是否成立）；
2. **U-47**：L6 "条件性伤害"定性是否接受，W1 对照用例设计是否按此口径评审；
3. **U-07**：F1 定级（53× 耗时 + 0/3 路径合规 + 假完成形态）；
4. **P-D1**：3 例 give_up 是否记为 L1.5 反证或 U-11（恢复护栏）证据。
