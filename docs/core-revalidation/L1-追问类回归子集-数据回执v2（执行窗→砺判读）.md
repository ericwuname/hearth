# L1 追问/元诊断类回归子集 · 数据回执 v2（执行窗 → 砺判读）

> **依据**：顶层《外部评估回执终审与行动指令 v1.0》§四（最高优先动作）
> **对象**：黄金集 #21 / #27 / #28 / #29 / #30 / #32（追问/元诊断类，pre-fix 0/5）+ A 臂 3 跑
> **环境**：.131，L1 分支二进制 `~/hearth-l1`（commit `0ce93aa`），每条全新 session
> **v2 修订**：**撤回 v1 中"零工具交换 → 仍失败"的初步读数**——复核发现 #28/#29/#30/#32 四条**无终态标记**（既非 completed 也非 failed，步数为 null，日志仅 ~650 字符），属**采集失败**而非"被反空转护栏拦截"。v1 结论作废，以本表为准。
> **原始数据**：`.131 ~/fa/l1-ask/`（`g*.log`、`a*.log`、`summary.json`）

## 一、追问类 6 条（L1 分支，全新 session）

| 样本 | completed | failed | 工具交换 | UNVERIFIED | 步数 | 日志长度 | 有效性 |
|---|---|---|---|---|---|---|---|
| **#21** | ✗ | ✅ | 6 | — | **9** | 4648 | ✅ 有效（**失败个案**） |
| **#27** | ✅ | ✗ | 6 | **✅** | **15** | 4686 | ✅ 有效（**救回成功**） |
| #28 | ✗ | ✗ | 0 | — | null | 637 | ⛔ 无效（无终态） |
| #29 | ✗ | ✗ | 0 | — | null | 730 | ⛔ 无效（无终态） |
| #30 | ✗ | ✗ | 0 | — | null | 758 | ⛔ 无效（无终态） |
| #32 | ✗ | ✗ | 0 | — | null | 645 | ⛔ 无效（无终态） |

**有效结论**：追问类有效样本 **2 条 = #21 失败 / #27 成功（completed + UNVERIFIED，15 步）**。
**"零工具交换的追问类是否仍全败"——本轮数据无法回答**（4/6 采集失败）。L1-b 第二修法的立项条件**既未获支持，也未被否证**。

## 二、A 臂 3 跑（D2 合取规则第二条件）

| 样本 | turn1 | turn1 产物 | turn2 | turn2 工具交换 | 有效性 |
|---|---|---|---|---|---|
| A1 | 无终态（len 639） | ✅ 已落盘 | 无终态（len 974） | 1 | ⛔ 两轮均无终态 |
| A2 | ✗ failed（5 步） | ✅ 已落盘 | ✗ failed（5 步） | 3 | ✅ 有效 |
| A3 | ✅ completed（5 步，UNVERIFIED） | ✅ 已落盘 | ✅ completed（5 步，UNVERIFIED） | 2 | ✅ 有效 |

**turn2 completed = 1/3**（A3）。

## 三、D2 预签规则的**字面**结果（执行窗只算数，不判）

| 条件 | 阈值 | 实测 | 判 |
|---|---|---|---|
| B 臂 ✓率 | ≥3/5 | **4/5 = 80%** | ✅ 满足 |
| A 臂 ✓率 | ≥2/3 | **1/3** | ❌ 未满足 |
| **合取结果** | 两者皆满足 | — | ❌ **按规则字面 = 记"根因模型有缺口"** |

**但执行窗必须同时披露**：A 臂 3 跑中 **1 跑（A1）两轮均无终态（采集失败）**，有效样本仅 2 条（1 成 1 败）。**1/3 这个数字可能低估**，不足以支撑实质结论。

## 四、建议（交砺裁定；执行窗不自行补跑，以免污染判读）

1. **追问类 4 条无效样本**：按 v4 驱动重跑（疑似 wait_terminal 在短轮上提前退出），补齐后再议 L1-b；
2. **A 臂**：补跑至有效样本 ≥3（当前有效仅 2），再按 D2 合取规则出判定；
3. **#21 个案**：9 步、6 次交换仍 failed——建议砺调 `g21.log` 查终止出口（PLAN 相位 stall？连续错误 >0？）；
4. **#27 是正面样本**：15 步、6 次交换、completed + **UNVERIFIED**——修法 A 在真实追问类输入上**确实救回过**，这是 B 臂之外的独立佐证。

> **执行窗**　2026-09-03　本回执只交数据与仪器状态，不预置结论（砺 A-3 / 顶层 §二.2 纪律）。
> **自我更正登记**：v1 基于 #28/#29 得出的"零交换仍全败"读数已作废——当时未注意到这两条同时 `failed=false` 且无步数，属**把采集失败误读为实验失败**，与本项目反复警告的"仪器污染"同类。
