# L1 追问/元诊断类回归子集 · 数据回执 v3（执行窗 → 砺判读）

> **依据**：顶层 §四；**砺《追问类回归子集·判读 v1》**（2026-09-03）
> **v3 相对 v2 的变更**：① **#21 归因定案**（砺 §1 判别式，一次 grep）；② **按"有无 pre-fix 基线"分栏**（砺 §2 口径缺陷处置 1）；③ pre-fix 基线补跑已启动（砺 §2 处置 2）。
> **纪律**：只交数据与仪器状态，不预置结论；v1/v2 的自我更正保留在案。

## 一、#21 归因定案（砺判别式执行结果）

```
grep 'RC52_RESCUE_SKIP|L1_B3_NONPRODUCT_RESCUE|GIVE_UP_ROUTED_TO_DONE' g21.log   → 未命中（空）
终态：✗ stalled: 2 consecutive replans produced identical TaskGraph (3 nodes) — Tier3 T4
reflect 依据：连续错误 1 → 连续错误 2
```

**按砺判别表第三行**：**未命中 → 根本没走到救回函数 → 走 PLAN 相位 T4 stall 出口 → 另一类缺陷，与修法 A 无关**。

补充读数：即使走到救回函数，`连续错误 2 > 0` 也会被 `consecutive_errors == 0` 护栏挡下（**设计正确**）。
**同型关联**：b5（B 臂唯一失败）同为 PLAN 相位 stall ×3 → **#21 与 b5 是同一失败形态**，建议砺考虑单列（暂记 U-48 候选，未登记）。

## 二、结果表（按 pre-fix 基线有无分栏 —— 不混列）

| 样本 | 黄金集↔A 组映射 | pre-fix 基线 | post-fix（L1 分支） | 可比性 |
|---|---|---|---|---|
| **#21** | = **A7**（正式轮行 398） | ✗ failed | ✗ failed（9 步，6 交换，T4 stall） | ✅ **可比 → 未见改善** |
| **#29** | = **A9**（行 809） | ✗ failed | ⛔ 无效（无终态） | ⚠ 半可比（post 侧采集失败） |
| #27 | **不在 A 组 10 条内** | **无基线** | ✅ completed（15 步，6 交换，**UNVERIFIED**） | ❌ **不可比（无基线新观测）** |
| #28 | 不在 A 组 | 无基线 | ⛔ 无效（无终态） | ❌ 不可比 |
| #30 | 不在 A 组 | 无基线 | ⛔ 无效（无终态） | ❌ 不可比 |
| #32 | 不在 A 组 | 无基线 | ⛔ 无效（无终态） | ❌ 不可比 |

**可比口径下的真实读数（执行窗计算，不解读）**：
- 完全可比（pre 与 post 均有效）：**仅 #21 → pre ✗ / post ✗ = 0/1，未见改善**；
- 顶部裁决书所引"追问类 0/5"指的是 **A5/A6/A7/A9/A10**；本轮 post 集合与之**仅 2/6 重叠**（#21=A7、#29=A9）——**"0/5 → 1/4"这类表述不成立，禁止引用**。

## 三、pre-fix 基线补跑（砺 §2 处置 2，已启动）

| 项 | 值 |
|---|---|
| 二进制 | **`/usr/local/bin/hearth` v0.2.23**（基线，非 L1 分支）— 启动前已 `hearth --version` 确认 |
| 样本 | #27 / #28 / #30 / #32 |
| 条件 | 全新 session、同环境、v4 轮询驱动（与 post-fix 同仪器口径） |
| 落点 | `.131 ~/fa/l1-ask-prefix/` |

补跑完成后，#27 的成功才能归因（是修法 A 所致，还是该条目本就可通过）。

## 四、L1-b 第二修法（砺 §3 有条件同意 —— 执行窗接单记录）

**机制层面**：`loop.rs` 救回守卫 `tool_exchanges_this_run >= 1` 在源码层确定性排除零交换样本 → **不需更多样本确认机制**，砺同意立项。

**但信号定义必须改写（砺 🔴 硬性）**：

```
❌ 禁用："模型已输出非空文字回答"   —— 即纯 LLM 自报，与 RC1（loop.rs:4134）直接冲突，
                                      且会救活边 replan 边吐"让我再想想"的空转（违背 :2183 护栏意图）
✅ 建议：实质回答 := 文本长度 ≥ 阈值
                ∧ 不含空转标记（重复 replan / "再想想" 类）
                ∧ 非"仅重述计划未产出结论"
                ∧ consecutive_errors == 0
        → 路由 Done，仍打 UNVERIFIED（verification_evidence 不置位，C-1/C-12 不变）
反空转护栏保留：零工具交换 且 无实质回答 → 维持原 give_up 终止，不得救
```

**立项前置（砺列）**：① #21 归因落定（**已完成，见 §一**）；② #30/#32 回收（**已回收，均无效**）；③ pre-fix 基线补跑（**进行中**）。

## 五、待砺裁定

1. #21 / b5 同型（PLAN 相位 stall 终止）是否单列为独立缺陷（U-48 候选）；
2. pre-fix 基线补齐后，#27 的成功能否归因于修法 A；
3. L1-b 信号定义的阈值与"空转标记"词表（执行窗按砺 §3 形态设计，交砺评审后再施工）。

> **执行窗**　2026-09-03
