# P4 v0.2.23 五件送审物 · 测试窗口测试报告（砺·评审）

> **测试方**：砺·评审（🪨）代行测试窗口　**日期**：2026-09-02
> **基线**：tag `v0.2.23`（commit `117a947`，gates 486/0）
> **前置链**：执行窗交付 → 评测窗评估 → 用户手工 A/B → 砺评审报告（CONDITIONAL GO）→ 顶层审批裁决书（批准 CONDITIONAL GO，7 开放项当场裁决）
> **本报告定位**：测试窗口的**独立验证**报告（与评审报告互补）。纪律：不信报告信源码、源码锚点强制复验、测试必须能失败。每条结论附实测证据或源码行号。

---

## 〇 测试范围与结论速览

| # | 测试项 | 方法 | 结论 |
|---|---|---|---|
| T-1 | 源码锚点强制复验（13 处） | grep + read 本地工作树 | ✅ **全部真实**，与评审报告一致 |
| T-2 | G1 数值修正状态（G1-C1） | 读 `correlation_matrix.json` + G1 材料 doc | ⚠️ **JSON 已对（0.1047），材料 doc 仍写 0.065 → G1-C1 未完成** |
| T-3 | COND-5 探活（binary） | SSH `.131`：`hearth --version` + `md5sum` | ✅ version `0.2.23`、md5 `34f6f807…` 与基线吻合 |
| T-4 | COND-5 门禁 486/0 复跑 | SSH `.131` 查源树 | ⛔ **VM `~/codex` 源树缺失**，gate 复跑受阻 → 强化 OD-11 |
| T-5 | open-deviations 一致性 | 读 `docs/core-freeze-candidate/open-deviations.md` 对照审批裁决书 | ⚠️ **文件 stale**：OD-6/OD-11 未升 🟡，无 OD-12/OD-13 |
| T-6 | 手工 A/B 日志独立抽验 | grep 用户原话/终态 | ✅ 全部吻合评审报告与裁决书 T-1~T-5 |

**一句话**：五件送审物的**事实基础可信**（源码锚点、手工日志、binary 均通过独立验证）；但**两处交付物未追上已批准的顶层裁决**——G1 材料 doc 数值未修正、open-deviations.md 未同步 OD 变更。这两项是冻结链路上的待办，不是新的质量缺陷。

---

## 一 源码锚点强制复验（T-1，全部通过）

评审报告 §0/§7 引用的 13 个锚点，逐一在本地工作树复验，全部真实存在且措辞一致：

| 锚点 | 位置 | 复验结果 |
|---|---|---|
| `GOAL_DRIFT_MIN_STEPS=20` | `crates/agent-core/src/loop.rs:425` | ✅ `pub(crate) const GOAL_DRIFT_MIN_STEPS: u64 = 20;` |
| goal_drift 触发条件 | `loop.rs:5135` | ✅ `if steps >= GOAL_DRIFT_MIN_STEPS` |
| `[goal_drift]` observe-only 警示 | `loop.rs:5143` | ✅ 逐字 `⚠ 终局产物与 original_goal 语义相关度存疑——请人工核对 run report` |
| bash 结构化出网拒绝 | `crates/tools-builtin/src/bash.rs:97-100` | ✅ `出网被拒：目标域名 {:?} 不在 egress_allowlist 白名单（{} 条）内` + `allow.len()` |
| web_fetch 同源路径 | `crates/tools-builtin/src/web.rs:101-104` | ✅ 同源结构化拒绝（裁决书 T-3 旁证） |
| "不独立采样"原则 | `tools/simuser/sampler.py:5-7` | ✅ `不对各维度独立采样…维度联合分布 = 语料本身的经验联合分布` |
| Q11 单一主体强制 | `tools/simuser/sampler.py:17-18,41-44` | ✅ 强制声明在位 |
| Cramér's V 规则版本 | `correlation_matrix.py:59` ↔ `json:2` | ✅ 均为 `corr-rules-v1.1` |
| V 矩阵 mode×multi_intent | `correlation_matrix.json:45-46` | ✅ **实际 `0.1047`**（见 §二） |

**结论**：评审报告的锚点引证经得起独立复验，无虚构锚点。

---

## 二 G1 相关矩阵数值修正状态（T-2，⚠️ G1-C1 未完成）

审批裁决书 §3 要求执行窗完成 G1-C1~C3：**把材料中 mode×multi_intent 的 0.065 改为 JSON 实际的 0.1047**，同步叙事，并加"source-anchored 复核"标注。复验结果：

- **`tools/simuser/correlation_matrix.json:45-46`** → `"mode×multi_intent": { "cramers_v": 0.1047 }` ✅ **JSON 数据正确**（评审报告 §5.3 早已指出 JSON 是对的，错在材料 doc）。
- **`docs/core-revalidation/G1-相关矩阵评审材料（执行窗→砺）.md:31`** → `| mode×multi_intent | 0.065 | 战略/执行模态与复合度几乎无关（可独立分层） |` ❌ **材料 doc 仍写 0.065**。

**判定**：G1-C1（材料 doc 数值修正）**尚未执行**。JSON 已对、doc 未改 → 人类可读的冻结资产仍携带错误数值，方向结论（"弱相关、可独立分层"）虽仍成立，但数值声明与 source 不一致。

**后果**：审批裁决书 §3 的解锁链（`G1-C1~C3 完成 → 报砺复核 → OD-3 降级 → B2 sampler 解锁`）**在 G1-C1 完成前不能推进**。即 B2 sampler 当前仍处 PENDING。

**建议**：执行窗修正 G1 材料 doc 的 0.065→0.1047（分钟级），报砺复核后解锁 B2。

---

## 三 COND-5 探活（T-3 / T-4）

SSH 至 `.131`（执行窗部署机），实跑：

| 项 | 命令 | 结果 |
|---|---|---|
| 版本 | `hearth --version` | `0.2.23 (unknown)` ✅ 与基线吻合 |
| 二进制 md5 | `md5sum /usr/local/bin/hearth` | `34f6f80781e34665920a7cc676d44a98` ✅ 与盲测包声明 `34f6f807…`、评审报告 §0 一致 |
| 源树 | `cd ~/codex && git log -1` | ⛔ **`NO_CODEX`** —— VM 上无 `~/codex` 源树 |

**结论**：
- binary 探活 ✅：部署件版本与 md5 均与 v0.2.23 基线一致，可确认"执行窗交付的二进制 = 测试基线"。
- **gate 486/0 复跑受阻**：`~/codex` 源树在 `.131` 不存在，无法从 VM 复跑 cargo test。gate 486/0 目前仅由评审报告 §0（commit `117a947`）与裁决书 §0 背书，**未经本轮独立复跑**。
- 源树缺失 **强化 OD-11**（".133 源码树落后，评审窗开工前须 E7 三步同步"）——本次验证改在**本地工作树**完成（本地树即项目权威源，锚点复验有效）；但 campaign（≥30 runs）需在 VM 有源树才能跑，故必须先做 E7 三步同步。

---

## 四 open-deviations 一致性核查（T-5，⚠️ 文件 stale）

对照 `docs/core-freeze-candidate/open-deviations.md`（磁盘实际）与审批裁决书 §2/§4（已批准变更）：

| 审批裁决书要求 | open-deviations.md 现状 | 状态 |
|---|---|---|
| OD-6 升 🟡（用户主诉渲染成因） | 文件仍 `🔵`（L16） | ❌ 未同步 |
| OD-11 升 🟡（评审窗开工前置） | 文件仍 `🔵`（L21） | ❌ 未同步 |
| 新增 OD-12（B1 失败丢成功并行结果） | 文件无 OD-12 | ❌ 未同步 |
| 新增 OD-13（B4 多行粘贴入口） | 文件无 OD-13 | ❌ 未同步 |
| OD-8 附顶层语义裁定（NOP+轻提示） | 文件 L18 仍"待顶层定" | ❌ 未同步 |

**判定**：open-deviations.md 仍是**审批前版本**（11 项、OD-6/11 为 🔵）。审批裁决书 §4 已明确指派执行窗更新该文件，但当前交付件未含此更新。

**影响**：该文件是 Node 15 八文件先行件与冻结材料的一部分。若不带更新进入冻结材料，会呈现"顶层已裁决 OD-6/11 升级、新增 OD-12/13，但清单仍旧"的矛盾。

**建议**：执行窗按裁决书 §4 更新 open-deviations.md（OD-6/11 升 🟡、加 OD-12/OD-13、OD-8 附 NOP+轻提示语义），再并入冻结材料。

---

## 五 手工 A/B 日志独立抽验（T-6，全部吻合）

不依赖评审报告/裁决书转述，直接 grep 用户原始日志：

| 裁决书抽验点 | 日志实证 | 结论 |
|---|---|---|
| T-1 A4 give_up（"为啥直接就是give_up"） | `手工测试v0.2.23-A.txt:919` 用户原话 `为啥直接就是give_up`；`:462/507` `give_up` | ✅ |
| T-2 A6 "需要猜"（budget_exhausted 49 步） | `A.txt:551` `我需要猜你的结果`；`:917` `budget_exhausted（49 步）` | ✅ |
| T-3 B1 白名单拒绝（23 条） | `B.txt:55` 逐字 `出网被拒：目标域名 ["www.baidu.com"] 不在 egress_allowlist 白名单（23 条）内` | ✅ 与 `bash.rs:97-100` 一致 |
| T-3 B1 丢成功并行结果（OD-12 证据） | `B.txt:40/77` `curl https://example.com ... [Pending]`（终态仍 Pending，成功未贴出） | ✅ |
| T-4 B5 goal_drift | `B.txt` L459 `💭 [done] [goal_drift] ⚠ 终局产物与 original_goal 语义相关度存疑` | ✅ 与 `loop.rs:5143` 一致 |

**额外发现**：`A.txt:836` 出现 `GIVEUP_UNVERIFIED: no acceptance criteria on product task — give_up carries zero verification evidence` —— 系统已对 give_up **自标注"零验证证据"**。这是对 OD-6/OD-7（give_up 无依据投影）的正面机制证据：v0.2.23 已在 give_up 路径打了可观测标记，但**仍不阻断** give_up（仅标注），故 OD-6/OD-7 仍是 v0.3 修复候选，方向正确。

---

## 六 测试窗口发现与开放项

| # | 发现 | 性质 | 处置建议 |
|---|---|---|---|
| F-1 | G1 材料 doc 仍 0.065（JSON 已 0.1047） | 交付物未追上裁决（G1-C1 未完成） | 执行窗改 doc → 报砺复核 → 解锁 B2 |
| F-2 | open-deviations.md stale（OD-6/11 未升、无 OD-12/13） | 交付物未追上裁决 | 执行窗按裁决书 §4 更新 |
| F-3 | VM `.131` 无 `~/codex` 源树 | 环境缺口，强化 OD-11 | campaign 前先 E7 三步同步 |
| F-4 | `GIVEUP_UNVERIFIED` 自标注出现 | 正面机制证据（OD-6/7 方向对） | 记入冻结材料，v0.3 仍修 |

**F-1/F-2 是"裁决已下、交付未动"的同步缺口，不是新质量缺陷**——五件送审物本身的事实基础（源码、日志、binary）均经独立验证可信。

---

## 七 下一测试阶段：campaign ≥30 runs（计划，待前置）

审批裁决书 §4 指派测试窗口 Step 2：`v0.2.23 全新 ≥30 runs + 七场景×三样本`，穿插 COND-3（C1 单跑 J-1/J-2/J-3）、COND-4（判定器 give_up 主导性）。

**前置（必须先做）**：
1. **E7 三步同步**：把本地工作树同步至 `.131`/`~codex`（F-3/OD-11 硬前置），否则无法从 VM 跑 cargo/campaign。
2. 盲测包 v1.1 已含 BP-1/BP-2/BP-3（裁决书 O-5 采纳）——正式轮须**全新 session**（BP-2），不得复用熟悉版 A 组已跑的 7/10 黄金输入（S6 跨标签纪律）。
3. 判定以 continue 终态为准，driver marker 仅辅助（B1 教训，COND-4）。

**当前状态**：campaign **未启动**——因 F-3 源树缺失，且属多小时 VM 占用，需在 E7 同步后由你确认再后台开跑。本报告先交付可独立完成的验证部分（T-1~T-6）。

---

## 八 测试窗口结论

**五件送审物的事实基础经独立验证可信**（源码锚点 13/13 真实、手工 A/B 日志逐条吻合、部署 binary 版本/md5 与基线一致）。**但两处交付物未追上顶层已批准的裁决**：G1 材料 doc 数值未修正（F-1，阻断 B2 解锁）、open-deviations.md 未同步 OD 变更（F-2）。VM 源树缺失（F-3）使 gate 复跑与 campaign 暂不能从 VM 执行，须先 E7 同步。

**处置**：F-1/F-2 交执行窗按裁决书更新后报砺复核；F-3 消项后启动 campaign（≥30 runs）作为下一测试阶段。在 F-1/F-2 闭合前，**不建议**将五件送审物标记为"全部交付完成"进入冻结材料。

---

> **砺·评审（🪨）**　2026-09-02
> 本报告为测试窗口独立验证，与《五件送审物评审报告》互补；所有结论附源码行号或实测输出，不接受执行器自述。
