# v3.0 主干冻结方案（中文版 / 可执行）

> **状态**：代码改动已全部落地（已 grep 源码核实），VM 三门 gate（fmt / clippy / test）后台运行中，结果将自动回报。
> **基线**：v2-gaps 定版纯净代码
> **目标**：关闭 5 项冻结阻塞/建议项（B1/B2/B3 + S1/S2）→ 主干可冻结为 v3.0
> **原则**：不信报告信源码——以下每条改动均已在真实 `.rs` 源文件 grep 核实，非执行器自报。

---

## 一、目标与背景

- **主干冻结** = 把当前主干标记为 v3.0 稳定候选。冻结前必须消除所有已知的"会阻断冻结 / 应补的契约"项。
- 评审 `handoff-trunk-freeze.md` 初稿时发现其方案含多处**与源码不符**的假设，直接照做会编译失败或产生空壳。本方案 = 初稿的修正执行版（`handoff-trunk-freeze-v2.md`），已落地代码并正在真 Linux VM 验收。

---

## 二、评审发现的阻塞（初稿 → 修正对照）

| # | 初稿问题 | 源码事实 | 本方案修法 |
|---|---|---|---|
| 🔴1 | B1 假设 `Session` 有 `events` 字段 | `session.rs` 只有 `event_tx: broadcast::Sender<AgentEvent>`，**无 `events`** | 给 `Session` 加 `events: Vec<AgentEvent>`，在 9 个 `tx.send` 点同步 push |
| 🔴2 | B1 把 `events` 当 `Vec<{event_type,payload}>` | `AgentEvent` 是 **enum**，无这两个字段 | `get_history` 用 `agent_event_kind(e)`(variant 名) + `serde_json::to_value(e)` 映射 |
| 🔴3 | B3 `#[openapi(paths(...))]` + 含 `Budget` | handler 无 `#[utoipa::path]`；`Budget` 无 `ToSchema` | `ApiDoc` 只放 `components(schemas(...))` **不放 paths**；DTO 与 `Budget` 加 `ToSchema` |
| 🟡1 | B1/B2 错误类型耦合 | B1 新路由返回 `(StatusCode,String)`，B2 改全部为 `Json<ErrorResponse>` | 合并一次改：`get_session_history` 直接用新错误类型 |
| 🟡2 | `require_api_key` 中间件签名 | `main.rs` 用 `from_fn_with_state`，返回 `(StatusCode,String)` | Err 臂改 `api_err`，返回类型同步为 `(StatusCode,Json<ErrorResponse>)` |
| ✅ S2 复核 | 原判 S2 需 LLM | `EvalRunner` 用 `MockProvider`，本地跑，无需联网 | S2 仅把 eval 显式接 CI，无需 env gate |
| 文档 | 验收清单引用不存在的 G6/G7/T1… | v2-gaps 已关 G1/G3/G4/G5/G9/G11 | 验收以本方案 B1..S2 打勾为准 |

---

## 三、逐条修正方案

### B1 — 历史回放端点
- **问题**：初稿假设 `Session` 有 `events` 字段、且把 `AgentEvent` enum 当 `struct{event_type,payload}`。
- **修正**：
  - `Session` 加 `pub events: Vec<AgentEvent>` 缓冲字段；
  - 9 个广播点（`tx.send`）同步 push 到缓冲——主转发用 `session_clone.lock().await.events.push`；**已持锁分支用 `s.events.push` 避免 tokio Mutex 重入死锁**；
  - 新增 `get_history`：in-memory 分支用 `agent_event_kind`(variant 名) + `serde_json::to_value(e)` 映射成 `HistoryEntry`；`MemoryStore` 分支直接读 `StoredEvent` 字段；
  - 新增 `GET /api/v1/sessions/{id}/messages` 路由 + `SessionHistory`/`HistoryEntry` DTO（带 `ToSchema`）。

### B2 — 统一错误码体系（与 B1 合并改，避免类型耦合）
- `api` crate 新增 `ErrorResponse`/`ErrorBody` + `ErrorResponse::new` + 8 个 `ERR_*` 常量（`UNAUTHORIZED`/`SESSION_NOT_FOUND`/`INVALID_PARAM`/`APPROVAL_TIMEOUT`/`LLM_ERROR`/`TOOL_ERROR`/`SANDBOX_ERROR`/`INTERNAL`）；
- `service` 的 `routes.rs` 整体改用 `(StatusCode, Json<ErrorResponse>)`，新增 `api_err()` helper，所有 `Err` 臂改用错误码；
- `require_api_key` 中间件返回类型同步改为 `(StatusCode, Json<ErrorResponse>)`，未授权返回 `ERR_UNAUTHORIZED`；
- `ApiResult` 类型别名定义在 `service`（不把 axum 依赖强加给 `api` crate）。

### B3 — OpenAPI / Swagger（fallback：components only）
- 依赖：`api`/`agent-types`/`service` 加 `utoipa = "5"`；`service` 加 `utoipa-swagger-ui = "7"`（axum feature）；
- 给全部 API DTO 与 `agent_types::Budget` 加 `ToSchema` derive；
- `ApiDoc` 用 `#[derive(OpenApi)]` + `components(schemas(...))`，**故意不放 paths** → 避免要求每个 handler 写 `#[utoipa::path]`，生成合法 OpenAPI 3.0（含全部组件 schema）；
- `main.rs` 挂 `/openapi.json` + `/swagger-ui`（`Router::merge(SwaggerUi)`）。

### S1 — 配置规范化（TTL 参数化）
- `main.rs` 读 `OOM_TTL_SECS` / `OOM_SWEEP_INTERVAL_SECS`，缺省 **3600 / 300 秒**，保持原默认值不变。

### S2 — telemetry eval 接 CI
- 复核确认 eval harness 用 `MockProvider`（`eval.rs` `build_task_script`），全程本地、不联网、无需 env gate；
- `ci.yml` 在 test 步骤后追加 `cargo test -p telemetry -- --nocapture`。

---

## 四、已落地代码改动（grep 源码已核实 ✅）

| 文件 | 改动 | 核实 |
|---|---|---|
| `crates/api/src/lib.rs` | B1 DTO + B2 错误体/常量 + B3 ToSchema（9 处 derive + `ErrorResponse` + 8 常量） | ✅ |
| `crates/api/Cargo.toml` | + `utoipa` | 依赖 |
| `crates/agent-types/src/lib.rs` | `Budget` + `ToSchema` | ✅ |
| `crates/agent-types/Cargo.toml` | + `utoipa` | 依赖 |
| `crates/service/Cargo.toml` | + `utoipa` + `utoipa-swagger-ui` | ✅ |
| `crates/service/src/session.rs` | +`events` 字段；9 点缓冲；`get_history`；`agent_event_kind` | ✅ |
| `crates/service/src/routes.rs` | +`get_session_history`；`api_err`；全部 `Err` 改错误码 | ✅ |
| `crates/service/src/main.rs` | +`GET messages` 路由；+`ApiDoc`/SwaggerUi；S1 env TTL | ✅ |
| `.github/workflows/ci.yml` | S2 eval step | CI |

---

## 五、验证方式（不信报告信源码）

真 Linux VM（`Ubuntu 24.04`，`wutao@192.168.220.131`）跑三门：

1. `cargo fmt --all -- --check` → `FMT_RC`
2. `cargo clippy --workspace --all-targets -- -D warnings` → `CLIPPY_RC`
3. `cargo test --all` → 测试数 **≥ 146 passed / 0 failed**

**致命坑已规避**：tar 保留本机 mtime 早于 VM `target` 产物会让 cargo 跳过重编、复用旧缓存 → gate 解压后先 `find ~/codex/crates -name '*.rs' -exec touch {} +` 强刷 mtime 再编译；并**保留 VM 端 `target` 增量**（不 `rm -rf`）以缩短到几分钟。

---

## 六、验收门槛（全绿即冻结放行）

- `FMT_RC=0` 且 `CLIPPY_RC=0` 且 `GATE_END rc=0`
- `cargo test --all` ≥ 146 passed / 0 failed
- 手动验收（可选）：`GET /messages` 返回 JSON 历史；不存在 session 返回 `SESSION_NOT_FOUND`；`/openapi.json` 有效；`OOM_TTL_SECS` 可覆盖启动

---

## 七、当前进度与下一步

- ✅ 源码改动全部落地（grep 核实）
- ✅ 打包 `codex_v12.tgz`
- 🔄 VM 三门 gate 后台运行中（任务 `zFzfW5`）
- **下一步**：gate 全绿 → 从 VM 拉回 fmt 后源码（防漂移）→ 标记任务完成 → 出验收报告 → 可选打 v3.0 纯净备份包 → 本方案 B1/B2/B3/S1/S2 打 ✅。若遇 🔴 → 按 clippy `error[` 行修本地源码、重传、重跑。

---

### 验收清单

- [ ] B1 `curl .../sessions/{id}/messages` → 返回 JSON 历史（含 phase/token/tool_call/.../done）
- [ ] B2 `curl .../sessions/nonexistent` → `{"error":{"code":"SESSION_NOT_FOUND",...}}`
- [ ] B3 `curl .../openapi.json` → 有效 OpenAPI 3.0（含 components.schemas）
- [ ] B3 `curl .../swagger-ui` 可访问
- [ ] S1 `OOM_TTL_SECS=60 OOM_SWEEP_INTERVAL_SECS=10` 启动无异常
- [ ] S2 `cargo test -p telemetry` 全绿（本地 mock）
- [ ] fmt / clippy / test 三门全绿
