# Codex-Rust v10.0 执行方案（基于 v9.0 Tarball 审计后更新）

> 审计结论：v9.0 tarball = 18,443 LOC / 19 crate / 156 测试 ✅ 过闸
> 工作区状态：已起步 v10.0 Phase 10B（resource-monitor crate 52 LOC）
> 定位：六条基因宪法 → 六个技术模块，16.5 天

---

## 当前进度

| 模块 | 状态 | 说明 |
|---|---|---|
| 10A 编号系统 | ⏳ 未开工 | instance_id + session_id 扩展 |
| 10B 资源监测 | 🟡 已起步 | `crates/resource-monitor` 骨架存在（52 LOC），CPU/mem/disk 采集 + pricing table + ROI 待实现 |
| 10C 基因宪法 | ⏳ 未开工 | constitution.md + 三处注入点 |
| 10D 独立观测 | ⏳ 未开工 | `crates/observer` 独立二进制 |
| 10E 自愈模块 | ⏳ 未开工 | L1/L2/L3 三级 |
| 10F 工具生态 | ⏳ 未开工 | WASM 沙箱 + tool search/install/list |

---

## 六阶段执行（可直接发执行窗口）

### 10A · 编号系统（0.5 天）

**改动**：`main.rs` 启动时生成 `CODEX_INSTANCE_ID = "codex-{hostname}-{uuid_short[:8]}"`，所有 session 前缀加 instance_id。CLI 启动打印 `[instance: {id}]`。

**文件**：`crates/service/src/main.rs`（+5 行）、`crates/codex-cli/src/main.rs`（+3 行）

**验证**：`curl localhost:3000/api/v1/sessions -X POST -d '{...}'` → 返回 `session_id` 含 instance 前缀。`cargo run -p codex-cli -- whoami` → 打印 instance ID。

---

### 10B · 资源监测（3–5 天）

**文件**：
- `crates/resource-monitor/src/lib.rs`（资源快照 + 成本换算 + ROI 公式，~200 行，已有 52 行骨架）
- `crates/agent-core/src/loop.rs`（`do_reflect` 拉取快照 → 写入 `Observation.resource_snapshot`）
- `crates/service/src/routes.rs`（`GET /api/v1/resources`）
- `crates/codex-cli/src/main.rs`（`codex resources` 命令）
- `Cargo.toml` + `crates/resource-monitor/Cargo.toml`（`sysinfo = "0.31"`）

**依赖**：`sysinfo` crate（纯 Rust，Linux/Windows/macOS 全兼容）。

**验证**：`curl localhost:3000/api/v1/resources` → JSON 含 `cpu_percent`/`memory_mb`/`disk_free_gb`。`codex resources` → 终端表格。

---

### 10C · 基因宪法（2–3 天）

**文件**：
- `constitution.md`（仓库根目录，六条宪法全文，~80 行）
- `crates/agent-core/src/loop.rs`（`build_system_prompt` 底部注入压缩版宪法 200 tokens）
- `crates/planner/src/lib.rs`（`reflect()` 末尾追加三条认知追问：真实？未知？局外人？）
- `crates/memory/src/lib.rs`（`CivilizationStore::append_farewell` → 第六条格式模板）

**不需要**：测试宪法是否被遵守（宪法不是规则，无法 assert）。验证方式是 **grep 检查注入点** → 宪法文本在三个位置出现。

---

### 10D · 独立观测（3–4 天）

**文件**：
- `crates/observer/Cargo.toml`（新） + `crates/observer/src/main.rs`（~200 行）
- `Cargo.toml`（workspace members 加 observer）

**Observer 二进制职责**：定时（可配置间隔）GET 拉取 `/api/v1/telemetry` + `/api/v1/resources` + `/api/v1/costs` → 生成 Markdown 报告 → `observer/reports/`。不参与 agent 运行。

**验证**：`cargo run -p observer` → `observer/reports/daily-{date}.md` 生成。主进程崩溃 → Observer 不崩溃（独立进程验证）。

---

### 10E · 自愈模块（2–3 天，依赖 10D）

**文件**：
- `crates/service/src/session.rs`（L1 工具重试 + L2 panic 捕获）
- `crates/agent-core/src/loop.rs`（L1 重试逻辑，max 3 次）
- `crates/observer/src/main.rs`（L3 进程重启：检测 PID 文件 → 不可达 → `/proc/{pid}` 不存在 → 重启 service）

**验证**：模拟工具失败 → 自动重试 3 次。模拟 process kill → Observer 日志记录重启。

---

### 10F · 外部工具生态（5–8 天）

**文件**：
- `crates/memory/src/lib.rs`（`ToolRegistryStore`，~50 行）
- `crates/codex-cli/src/main.rs`（`codex tool search/install/list`，~100 行）
- `crates/tool-runtime/src/dispatcher.rs`（`register_external_tool`，~20 行）
- `tools/registry.json`（示例远程注册表，~20 行 JSON）

**MVP 范围**：WASM 沙箱执行 (`wasmtime` crate) + 工具搜索/安装/列出。远程 registry 用静态 JSON 文件。Agent 自主发现工具（`do_plan` 阶段提示）为 nice-to-have。

---

## 总文件改动

| 文件 | 阶段 |
|---|---|
| `constitution.md` | 10C（新建） |
| `crates/resource-monitor/src/lib.rs` | 10B（扩展 52→250 行） |
| `crates/resource-monitor/Cargo.toml` | 10B（加 sysinfo） |
| `crates/observer/src/main.rs` | 10D（新建 ~200 行） |
| `crates/observer/Cargo.toml` | 10D（新建） |
| `crates/agent-core/src/loop.rs` | 10B/10C/10E |
| `crates/agent-types/src/lib.rs` | 10B（ResourceSnapshot + RoiReport） |
| `crates/planner/src/lib.rs` | 10C（宪法追问） |
| `crates/memory/src/lib.rs` | 10C（farewell）+ 10F（ToolRegistryStore） |
| `crates/service/src/main.rs` | 10A（instance_id） |
| `crates/service/src/routes.rs` | 10B（resources 端点） |
| `crates/tool-runtime/src/dispatcher.rs` | 10F |
| `crates/codex-cli/src/main.rs` | 10A/10B/10F |
| `Cargo.toml` | 10D（observer member）+ 10B（sysinfo） |
| `tools/registry.json` | 10F（新建） |

---

## 提交门槛

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --all
# 期望：三 rc=0, passed ≥ 156
```
