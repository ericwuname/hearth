# v1.2 全局审计 Triage 报告（global-audit-report.md 40 项逐条核验）

> 原则：**不信报告信源码**。审计报告基于 v1.0 假设，本次逐条对照 **v1.1 真实源码**核验，
> 有效项已修复（本轮改动统称 v1.2），全部改动经真 Linux VM `cargo test --all` 回归。
>
> 判定图例：✅已修复 ｜ 🟠有效-接受风险（记录理由） ｜ ❌误报/已过时 ｜ ⏸有效-延后（P3 技术债）

## 一、🔴 阻断项（12）

| # | 发现 | 核验结论 | 处置 |
|---|------|---------|------|
| AUTH-0 | API 零鉴权 | ✅属实。6 个端点无任何鉴权 | **已修复**：`routes.rs` 新增 Bearer 中间件（常量时间比较），由 `API_KEY` 环境变量门控；未设置时保持开放（dev 模式）并在启动时 WARN。auth 层在 CORS 内侧，preflight 不受影响 |
| OOM-1 | Sessions HashMap 永不清理 | ✅属实。grep 确认无 remove/retain | **已修复**：`Session.finished_at` 标记终态（done/error/cancelled/cancel_session 全路径），`cleanup_finished(ttl)` + `start_cleanup_task`（TTL 1h、5min 扫一次）。带断言测试 `test_v12_cleanup_finished_evicts_only_expired` |
| SBOX-1 | edit.rs 绕过沙箱直接 tokio::fs::write | ✅属实但**接受风险**🟠 | 双重防线已存在：① 进程整体受 landlock 约束（workspace 外只读）；② edit 强制 cwd 相对路径 + 组件级穿越检查。已在源码留注释说明；改走 sandbox.exec 落盘留待 owner 决策 |
| APPR-1 | 审批用 JSON 子串 `contains("rm ")` | ✅属实（loop.rs:506-511 在 v1.1 仍未改） | **已修复**：`tool_call_needs_approval()` 语义判定——bash 按 `cmd` 字段**命令位**分词检查危险命令表（rm/dd/mkfs/shutdown/fork 炸弹/管道进 shell/重定向进 /dev），edit 按**路径可疑性**判定（绝对路径或含 `..` 穿越分量才触发审批；workspace 内相对路径写入不拦——edit 工具自身 + landlock 已双重限制在 workspace 内，逐次审批会卡死无人值守运行，真 Linux 集成测试 `test_p1_tools_real_fs` 实际验证了这一点）。修复了漏判 `rm-rf`、误判 `echo rm`、edit 越界写不触发审批三个缺陷。3 组断言测试 |
| STREAM-1 | openai stream spawn 无取消 | ✅属实（JoinHandle 在 :309 被丢弃） | **已修复**：llm-gateway 新增 `AbortOnDropStream`（Drop 时 abort 生产者任务），openai 已接线。断言测试 `test_v12_abort_on_drop_kills_producer` |
| STREAM-2 | hunyuan 同上 | ✅属实（:476） | **已修复**：同上接线 |
| STREAM-3 | ollama 同上 | ✅属实（:426） | **已修复**：同上接线 |
| STREAM-4 | vllm 同上 | ✅属实（:879） | **已修复**：同上接线 |
| MEM-1 | save_session 非原子写 | ✅属实（File::create 直接覆盖） | **已修复**：写 `.tmp` + `std::fs::rename` 原子替换 |
| MEM-2 | 并发写无锁 | ✅属实 | **已修复**：store 内部 `tokio::sync::Mutex` 序列化 `save_session` 与 `append_events` 两条写路径 |
| MEM-3 | 会话文件无大小上限 | ✅属实 | **已修复**：64 MiB 上限，save/append 双路径检查，超限 bail |
| MEM-4 | 单行损坏丢弃整条记录 | ✅属实（`from_str(line)?`） | **已修复**：逐行 match，损坏行 warn+skip。断言测试 `test_v12_memory_corrupt_line_tolerated`（损坏行前后的合法事件均存活） |

## 二、🟡 高优项（15）

| # | 发现 | 核验结论 | 处置 |
|---|------|---------|------|
| SBOX-2 | read.rs 绕过沙箱 | ✅属实，同 SBOX-1 接受风险🟠 | read 是只读操作 + landlock 兜底 + cwd 限制；本轮顺带把穿越检查升级为组件级 |
| SBOX-3 | spawn_sub_agent 无深度守卫 | ✅属实（守卫只在调用点 do_plan） | **已修复**：守卫移入 `spawn_sub_agent` 内部（`self.depth >= 1` 拒绝并 warn），且子代深度强制 `max(depth, parent+1)`，外部调用无法伪造 depth=0 |
| API-1 | 无请求体大小限制 | ❌**误报**：axum 0.7 默认自带 2MB `DefaultBodyLimit` | 已**显式**声明 `DefaultBodyLimit::max(2MB)` 防未来重构丢失 |
| API-2 | 无限流 | ✅属实，**延后**⏸ | tower-http 未启 `limit`/governor 依赖；AUTH-0 落地后未授权流量已被挡。加限流需引新依赖，列入 P3 |
| API-3 | CORS methods/headers 用 Any | ✅属实 | **已修复**：白名单 `[GET, POST, OPTIONS]` + `[CONTENT_TYPE, AUTHORIZATION]`（origin 在 v1.1 已收紧） |
| LLM-1 | reqwest Client::new() 无超时 ×4 | ✅属实（4 处确认） | **已修复**：4 处全部 `connect_timeout 10s` + 总超时（云端 300s / 本地模型 600s） |
| LLM-2 | 工具参数非 JSON 时静默降级 | ✅属实（openai/hunyuan/vllm 3 处） | **已修复**：3 处均改 `unwrap_or_else` + `tracing::warn` |
| MEM-5 | session_meta 字段缺失静默置空 | ✅属实 | **已修复**：`session_id` 缺失时 warn（其余字段仍容忍降级，可恢复优先） |
| CNCL-1 | abort() 对阻塞任务无效 | ✅属实，**接受风险**🟠 | 工具执行经 sandbox spawn 自带 timeout；LLM 调用本轮已加 HTTP 超时 → abort 的悬点在 .await 处都有界。无纯 CPU 阻塞路径 |
| DUP-1/2/3 | llm 三 crate 大量重复代码 | ✅属实，**延后**⏸ | 纯重构无行为收益，风险>收益，列入 P3（可抽 `llm-common` crate） |
| CODEIDX-1 | `child(i).unwrap()` ×3 | ✅属实（116/159/178） | **已修复**：3 处全部 match-continue，索引器不再可能 panic |

## 三、🔵 建议项（13）

| # | 发现 | 核验结论 | 处置 |
|---|------|---------|------|
| CONS-1 | grep 穿越路径静默回退 cwd | ✅属实 | **已修复**：改为返回错误（拒绝语义），测试同步更新 |
| CONS-2 | `contains("..")` 误拦 `test..txt` | ✅属实 | **已修复**：read/edit/grep 全部改组件级 `Component::ParentDir` 检查；grep 新增 `test..txt` 合法性断言。glob 保留 contains（glob 模式非路径，保守拒绝可接受） |
| PERF-1 | has_rg 每次调用都探测 | ✅属实 | **已修复**：`tokio::sync::OnceCell` 缓存探测结果（进程生命周期内 rg 可用性不变） |
| SSE-1 | 序列化失败静默丢事件 | ✅属实 | **已修复**：serialize 失败与 broadcast lag 均 `tracing::warn` |
| STALE-1 | sandbox 头注释"seccomp deferred to P5"过期 | ✅属实 | **已修复**：注释更新为真实现状（landlock+seccomp+cgroups in pre_exec，无 unshare） |
| TRACE-1 | api 引用不存在的 architecture.final.md | ✅属实 | **已修复**：注释改为"本 enum 即契约" |
| WEAK-1 | noop sandbox 测试仅 smoke | ✅属实，**接受**🟠 | NoopSandbox 本身无行为可断言；Linux 侧 landlock/seccomp 有 3 个断言测试兜底 |
| CONS-1(dup) / CONC-1 | `current_phase` dead_code | ✅属实，**接受**🟠 | 字段为状态机保留位，`#[allow(dead_code)]` 已显式标注意图 |
| FALLBACK-1 | OPENAI_API_KEY 回退 "sk-placeholder" | ✅属实，**接受**🟠 | 启动时已 WARN；占位 key 请求必然 401，不会静默成功 |
| URL-1 | base_url 尾斜杠产生双斜杠 | ✅属实 | **已修复**：4 个 provider 构造时 `trim_end_matches('/')` |
| SEC-1 | Ollama 无 api_key | ❌**设计如此**：Ollama 本地服务原生无鉴权概念 | 不改 |
| ORPHAN-1 / DUP-4 | 零散孤儿代码/重复 | ✅属实，**延后**⏸ | 列入 P3 技术债 |

## 四、修复统计

- **✅ 已修复：24 项**（🔴 10 + 🟡 8 + 🔵 6）
- **🟠 接受风险（有注释/有兜底）：6 项**（SBOX-1、SBOX-2、CNCL-1、WEAK-1、CONC-1、FALLBACK-1）
- **❌ 误报/设计如此：2 项**（API-1 axum 默认 2MB、SEC-1）
- **⏸ 延后 P3：4 项**（API-2 限流、DUP-1/2/3、ORPHAN-1/DUP-4）

**🔴 门禁复核：12 项红色发现中 10 项修复、2 项（SBOX-1 属🔴中唯一接受风险项，双重防线+注释）→ 建议门禁翻绿。**

## 五、本轮新增回归测试

| 测试 | 覆盖 |
|------|------|
| `test_v12_memory_corrupt_line_tolerated` | MEM-4 损坏行容忍 |
| `test_v12_approval_bash_destructive_detected` | APPR-1 危险命令命中（rm -rf/rm-rf/sudo/dd/mkfs/fork 炸弹/管道进 shell/shutdown） |
| `test_v12_approval_bash_benign_not_flagged` | APPR-1 误报消除（echo rm/ls/cargo build/grep delete） |
| `test_v12_approval_edit_write_detected` | APPR-1 edit 越界写（绝对路径/`..` 穿越）触发审批、workspace 内写入与只读工具不触发 |
| `test_v12_abort_on_drop_kills_producer` | STREAM-1~4 Drop 即 abort 生产者 |
| `test_v12_cleanup_finished_evicts_only_expired` | OOM-1 TTL 驱逐仅命中过期终态会话 |
| `test_grep_traversal_path_rejected`（改写） | CONS-1 拒绝语义 + CONS-2 `test..txt` 合法 |

## 六、验证（2026-07-28 真 Linux VM 回归，已完成）

- 环境：真 Linux VM（Ubuntu 24.04，wutao@192.168.220.131），`cargo test --all`，增量编译（复用 VM target/）
- **最终结果：✅ 全绿 — 36 个 suite 全部 ok，145 passed / 0 failed，0 编译错误**
- 本轮全部 7 个 `test_v12_*` 新增回归测试在真 Linux 上通过（含 landlock/seccomp 环境）
- **首轮回归抓出并修复 1 个本轮引入的真缺陷**（体现真 Linux 验收的价值）：
  - 现象：`test_p1_tools_real_fs` 失败，tool results 为空
  - 根因：APPR-1 初版把"edit 且 content 非空"一律判需审批 → 无人值守运行卡死在 `check_approval`，10s 超时
  - 修复：edit 审批门改为**路径可疑性**判定（绝对路径 / `..` 穿越分量才触发）；workspace 内相对路径写入放行（edit 工具自检 + landlock 双重防线兜底）。二轮回归该测试通过。
