# Codex-Rust v4.0–v4.1 验收与审计报告

> 日期：2026-07-29 | 基线：v3.1 Post-Freeze | 审计对象：v4.0 硬化 + v4.1 生产就绪

---

## 一、门禁总表

| 阶段 | FMT | CLIPPY | Test | 说明 |
|---|---|---|---|---|
| v3.1 Post-Freeze | 0 | 0 | 149/0 | P1/P2/P3 合入 |
| H2+H3 | 0 | 0 | 149/0 | 子代理标记 + ra 重试初始化 |
| H1 | 0 | 0 | 149/0 | 行级文件变更追踪 |
| **v4.1 终验** | **0** | **0** | **149/0** | P4/P5/P6/P1 + P2 Dockerfiles |

---

## 二、逐项交付物清单

### v4.0 硬化（4 项，全完成）

| # | 项 | 改动文件 | 行数量 | 验收 |
|---|---|---|---|---|
| H2 | 子代理产出标记 | `loop.rs`（2 处 `TaskResult.output` 构造改走 `format_injected_content`） | +2 行 | ✅ |
| H3 | rust-analyzer 重试初始化 | `lsp-bridge/lib.rs`（`ensure_started` 拆分 spawn + init 重试，60s 超时） | +15 行 | ✅ |
| H1 | 行级文件变更追踪 | `loop.rs` `extract_files_from_tool_calls` 重写：从 `ToolCall.args` 提取 `start_line`/`end_line`，从 `ToolResult.output` 增强路径匹配 | +60 行 | ✅ |
| H4 | 覆盖率扫描 | 分析性任务，结果见 §三 | 报告 | ✅ |

### v4.1 生产就绪（6 项，5 项代码实现，1 项文档）

| # | 项 | 改动文件 | 行数量 | 验收 |
|---|---|---|---|---|
| P4 | 健康检查 + 就绪探针 | `routes.rs`（`healthz`/`readyz` handler + auth bypass）; `main.rs`（路由注册） | +30 行 | ✅ |
| P5 | 配置文档 | `docs/configuration.md`（16 个环境变量总表） | 新建 | ✅ |
| P6 | 部署指南 | `docs/deployment.md`（编译/nginx/systemd/Docker/端点表） | 新建 | ✅ |
| P1 | 并发限流 | `routes.rs`（`concurrency_limit` middleware, `AtomicUsize`, 50 并发上限，429 拒绝） | +15 行 | ✅ |
| P2 | Docker 部署 | `Dockerfile`（multi-stage）+ `docker-compose.yml` | 新建 | ✅ |
| P3 | E2E 集成测试 | 延后（需要 mock LLM server + 启动真实 service 进程，估中，不在本次冲刺范围） | — | ⏸️ |

---

## 三、H4 覆盖率扫描

| Crate | LOC | Tests | t/100LOC |
|---|---|---|---|
| agent-core | 2051 | 17 | 0.8 |
| agent-types | 379 | 9 | 2.4 |
| llm-gateway | 772 | 20 | 2.6 |
| tools-builtin | 761 | 20 | 2.6 |
| service | 1187 | 7 | 0.6 |
| sandbox | 772 | 10 | 1.3 |
| telemetry | 422 | 1 | 0.2 |

全仓 11,358 LOC，137 测试函数，整体 t/LOC 1.2。telemetry（0.2）为已知 G10 缺口（eval harness 0 生产调用）。其余 crate 均 ≥ 0.6。

---

## 四、审计锚点核实（给守门员）

| # | 主张 | 源码锚点 | grep |
|---|---|---|---|
| 1 | H2 子代理输出已标记 | `loop.rs` `"sub_agent_output"` 在 `TaskResult.output` 构造中 | grep `sub_agent_output` |
| 2 | H3 `ensure_started` 拆分 | `lsp-bridge/lib.rs` `if !self.init_done` → `self.initialize().await?` | grep `init_done` |
| 3 | H3 60s 超时 | `lsp-bridge/lib.rs` `Duration::from_secs(60)` | grep `from_secs(60)` |
| 4 | H1 `start_line`/`end_line` 提取 | `loop.rs` `line_start`/`line_end` from args | grep `line_start\|line_end` |
| 5 | P4 `/healthz` + `/readyz` | `routes.rs` `pub async fn healthz` + `pub async fn readyz` | grep `healthz\|readyz` |
| 6 | P4 auth bypass | `routes.rs` `req.uri().path() == "/healthz"` | grep `/healthz.*auth\|auth.*healthz` |
| 7 | P1 并发限流 | `routes.rs` `P1_CONCURRENT_COUNT` + `P1_MAX_CONCURRENT` | grep `P1_CONCURRENT` |
| 8 | P5 配置文档 | `docs/configuration.md` 存在且 ≥ 10 个 env var | read file |
| 9 | P6 部署指南 | `docs/deployment.md` 存在且含 nginx/systemd 示例 | read file |
| 10 | P2 Dockerfile | `Dockerfile`（multi-stage build+run） + `docker-compose.yml` | read file |

---

## 五、已知局限（v4.1 终态）

| # | 局限 | 影响 | 缓解 |
|---|---|---|---|
| L1 | P1 文件变更仅文件级（行号为 heuristic） | 同文件不同区域的子代理作业仍报冲突 | H1 已改进：从 ToolCall args 提取行号，非完全精确但优于 0/0 |
| L2 | P2 仅标记 TaskResult 状态行，不标记子代理完整产出文本 | `RunReport.summary` 未经 `format_injected_content` | 低风险：status 行已标记，完整标记属后续迭代 |
| L3 | rust-analyzer 60s 超时在首次扫描时可能不够 | 大型 workspace 首次扫描 >60s → fallback Noop，后续调用重试 | 可接受：进度性重试，最终会成功 |
| L4 | P1 并发限流为全局限流（非 per-IP） | 多用户场景下不能按用户隔离限流 | 可接受：50 并发上限对单机部署充足；per-IP 限流建议在反向代理层（nginx rate limiting）实现 |
| L5 | telemetry crate 0 生产调用 | eval 功能不可观测 | 已在 roadmap E2 记录：retriever 默认开启时评估是否接 telemetry |

---

## 六、未完成项（v4.2+ 延后）

| # | 项 | 原因 |
|---|---|---|
| P3 | E2E 集成测试 | 需要 mock LLM server + 真实进程启动，估中，不在本次冲刺 |
| E1 | retriever 默认开启 | 需要 embed model 可用性检查 |
| E2 | 多 session 工作区隔离 | 需架构设计 |
| E3–E5 | 插件系统/多 Agent/前端 | 大项，按需启动 |

---

## 七、交付物汇总

| 文件 | 类型 | 说明 |
|---|---|---|
| `post-freeze-audit-handoff.md` | 审计报告 | P1/P2/P3 分支验收 |
| `v4.0-roadmap.md` | 路线图 | 已更新（删 H4/H5，6→4 项） |
| `gatekeeper-review-post-freeze.md` | 守门员审查 | ✅ 过闸 |
| `docs/top-level-design.md` | 设计文档 | G2/G6/G7/G10/G12 + T1/T2/T3/T5/T9 全部 ✅ |
| `docs/configuration.md` | 配置参考 | 16 个环境变量 |
| `docs/deployment.md` | 部署指南 | 编译/nginx/systemd/Docker |
| `Dockerfile` | 容器构建 | Multi-stage |
| `docker-compose.yml` | 容器编排 | 单服务 + volumes |
| `v4.1-acceptance-report.md` | 本报告 | 验收 + 审计双用途 |
