# Codex-Rust v1.1 修复实施汇总

> 实施依据：`fix-plan.md`（参照 `audit-report.md`）
> 实施日期：2026-07-28
> 状态：全部 6 轮代码改动已完成；**本地 `cargo test` 未执行**（本机未安装 Rust 工具链，与审计报告一致），已通过逐文件源码审查替代编译验证。

---

## 改动清单（按 fix-plan 轮次）

### Round 1 — 路径穿越防护 (M1)
**文件**：`crates/tools-builtin/src/read.rs`、`edit.rs`、`grep.rs`
- `ReadTool` / `EditTool`：在 `join` 前插入检查，拒绝含 `..` 或绝对路径的输入，直接返回错误。
- `GrepTool`：用户传入的 `path` 若含 `..` 或为绝对路径，记 warn 并回退到 `cwd`，不 panic。
- 新增测试：`test_read_path_traversal_denied`、`test_edit_path_traversal_denied`、`test_grep_traversal_path_falls_back_to_cwd`。

### Round 2 — Glob/Grep 走沙箱 (M7 + M5)
**文件**：`crates/tools-builtin/src/glob.rs`、`grep.rs`
- `GlobTool` / `GrepTool` 结构体增加 `sandbox: Arc<dyn Sandbox>` 字段，构造时 `create_sandbox(SandboxConfig::default())`（与 `BashTool` 完全一致的模式），保留 `with_sandbox` 便于注入/测试。
- `execute` 改用 `self.sandbox.spawn("find"/"rg"/"grep", ...)` 执行，使 Linux 的 landlock/seccomp 同样覆盖这两个工具。
- `has_rg()` 探测仍在沙箱外执行一次（仅探测能力），实际搜索走沙箱。
- 额外修复 M5：`glob` 的 pattern 含 `..` 直接拒绝；新增 `test_glob_traversal_pattern_denied`。

### Round 3 — Tantivy unwrap → 错误 (M3)
**文件**：`crates/retriever/src/lib.rs`
- `bm25_search` 中 `self.index.as_ref().unwrap()` → `ok_or_else(anyhow!("bm25 index not built yet"))?`。
- 文档解析 `searcher.doc(...).unwrap()` 与 `get_first(...).unwrap().as_u64().unwrap()` → `?` + `ok_or_else`，避免 panic。

### Round 4 — 会话取消清理后台任务 (M4)
**文件**：`crates/service/src/session.rs`
- `Session` 增加 `cancel_tx: Option<oneshot::Sender<()>>`。
- `send_message` 在 spawn 前创建 oneshot 通道并存入 session；事件转发循环改用 `tokio::select!` 监听 `internal_rx` 与取消信号。收到取消信号 → 发送 `AgentEvent::Error` → **`agent_future.abort()`** 真正中止后台 agent 任务（不再只删引用）→ 标记 `phase="cancelled"`。
- `cancel_session` 改为发送取消信号（不再仅 `remove`），真正让后台任务停下、停止消耗资源/调用 LLM。

### Round 5 — 审批状态按 Session 隔离 (M2)
**文件**：`crates/tool-runtime/src/dispatcher.rs`、`crates/agent-core/src/scheduler.rs`、`crates/agent-core/src/loop.rs`、`crates/service/src/session.rs`
- `ToolDispatcher.approval` 由 `Mutex<ApprovalState>` 改为 `Mutex<HashMap<String, ApprovalState>>`，**以 session_id 为 key**。
- `set_approval_pending` / `resolve_approval` / `approval_state` / `reset_approval` 全部加 `session_id` 参数；`resolve_approval` 改用 `.cloned()` 取出再匹配，规避借用冲突。
- `Scheduler::check_approval` / `set_approval_pending` 透传 `session_id`。
- `AgentLoop` 增加 `session_id` 字段与 `set_session_id()`；`do_act` 中透传。
- `SessionManager::create_session` 调用 `agent.set_session_id(session_id)`，使 loop 与 dispatcher 的审批 key 一致。
- `submit_approval` 改为 `self.dispatcher.resolve_approval(id, &req.decision)`。
- 新增测试 `test_approval_isolated_between_sessions`（双 session 互不干扰）。

### Round 6 — 低危收尾 (L4/L5/L6)
**文件**：`crates/service/src/session.rs`、`crates/service/src/main.rs`
- L4：`send_message` 中 `s.event_tx.clone().unwrap()` → `ok_or_else(anyhow!("session {} event stream closed", id))?`。
- L5：监听地址端口可由 `PORT` 环境变量配置，默认 3000。
- L6：CORS `allow_origin` 由 `Any` 收紧为 `AllowOrigin::list(...)`，来源由 `CORS_ORIGIN` 环境变量指定，默认 `http://localhost:5173`。`Any` 仍需保留给 `allow_methods`/`allow_headers`。
- L2（CostMeter provider 校准）按计划**跳过**（影响微小，延后 v1.2）。

---

## 编译/测试验证状态

- ⚠️ 本机未安装 Rust（`cargo`/`rustc` 均不存在），**未能执行 `cargo test --all`**。
- 已逐文件审查所有改动，确认：
  1. 所有 `ToolDispatcher` 审批方法调用点均已更新到新签名（无遗留老签名调用）。
  2. `Arc::from(sandbox::create_sandbox(...))` 模式与既有 `BashTool` 完全一致，可编译。
  3. 修正了一处 `resolve_approval` 中 `state.get()` 不可变借用与 `state.insert()` 可变借用冲突（使用 `.cloned()` 取出所有权）。
  4. `AllowOrigin::list(Vec<HeaderValue>)` 符合 tower-http 0.6 API。
- **下一步**：在有 Rust 的工具链上运行 `cargo test --all`，预期 133 个原有测试 + 新增安全测试全绿（Round 1-3 + Round 6 风险低，可快速进入 v1.1 闸门）。

---

## 发布建议（与 fix-plan 一致）

| 版本 | 内容 | 闸门条件 |
|------|------|----------|
| v1.1 | Round 1-3 + Round 6 | 全部现有测试通过 + 新增安全测试通过 |
| v1.2 | Round 4-5 | 并发测试 + 压力测试通过 |
