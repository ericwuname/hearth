# Codex-Rust v5.0 执行计划

> 基线：v4.1 定版（gatekeeper-review-v4.1.md 过闸，149 passed / 0 failed）
> 日期：2026-07-29
> 目标：E1(retriever默认) + E2(session隔离) + E3(插件) + E4(多Agent) + **CLI 替代前端(E5)**
> 估时：12–18 天

---

## 0. 为什么是 CLI 而不是前端

| 前端 (E5) | CLI (新) |
|---|---|
| 需要 React/Vue + TS + 构建工具链 | 纯 Rust，零外部前端依赖 |
| 浏览器 `EventSource` 不支持 POST SSE | CLI 用 `reqwest` streaming，POST SSE 无问题 |
| 需要 CORS/认证各种浏览器策略 | CLI 直接 HTTP，无浏览器限制 |
| 给最终用户用的 GUI | 给开发者的终端工具——**更贴合 codex 定位** |
| 30+ 天 | 5–7 天 |

CLI 完成后，Gemini 前端可以直接用同一个 `/api/v1/*` + `/openapi.json` 契约对接——CLI 本身就是 API 的"活文档"。

---

## 一、CLI 设计规格

### 1.1 新 crate：`crates/codex-cli`

```
crates/codex-cli/
├── Cargo.toml
└── src/
    ├── main.rs          # clap 命令路由
    ├── client.rs        # CodexClient: HTTP+SSE 封装
    ├── render.rs        # 终端渲染（颜色/格式化）
    └── repl.rs          # 交互式 REPL
```

### 1.2 命令设计

```
codex
├── chat <goal>          # 一次性：创建 session → 发消息 → SSE 流 → 退出
│   [--provider openai]  #   覆盖默认 provider
│   [--model gpt-4o]     #   覆盖默认 model
│   [--budget 20]        #   最大步数
│
├── repl                 # 交互式 REPL：多轮对话 + 审批交互
│
├── sessions             # 列出所有 session 及其状态
│
├── history <id>         # 查看 session 消息历史
│
├── approve <sid> <aid>  # 审批一个 pending 操作
│   [--deny]             #   拒绝审批
│
├── cancel <id>          # 取消 session
│
└── status <id>          # 查看 session 状态
```

### 1.3 环境变量

| 变量 | 默认 | 说明 |
|---|---|---|
| `CODEX_URL` | `http://localhost:3000` | 服务地址 |
| `CODEX_API_KEY` | 无（可选，服务端 API_KEY_REQUIRED=1 时必设） | 认证 token |

### 1.4 SSE 流渲染规则

```
Phase(Init)   →  [dim]◆ Init...[/dim]
Phase(Plan)   →  [dim]◆ Planning[/dim]
Token(delta)  →  [dim]{delta}[/dim]          (不换行，流式累积)
ToolCall      →  [yellow]⚙ {name}({args})[/yellow]
ToolResult    →  [green]  ✓ {output}[/green]  (截断 200 字符)
NeedApproval  →  [bright red]⛔ {action} — 用 `codex approve {sid} {aid}` 审批[/bright red]
Reflection    →  [cyan]  {verdict}[/cyan]
Done          →  [bold]✓ Done ({steps} steps)[/bold]
Error         →  [red]✗ {message}[/red]
```

### 1.4.1 SSE 帧解析器（client.rs 核心）

POST `/api/v1/sessions/{id}/messages` 返回 `text/event-stream`。需要**状态机拆帧**：

```
CONNECT → read line
  "event: Phase"      → phase = "Phase"
  "event: Token"      → phase = "Token"
  "event: ToolCall"   → phase = "ToolCall"
  "data: {...}"       → 累积 JSON
  "" (空行)            → 触发回调(phase, data_json) → 清空状态 → 下一帧
  EOF                 → Done
```

关键：**不能用简单的 `reqwest::Response::text().await`**——那会等整个 body 收完。必须用 `response.bytes_stream()` 逐 chunk 读，自己在内存拼行。`futures::StreamExt` 做 `while let Some(chunk) = stream.next().await`。

估量升为 **中+**（~80 行而非 ~30 行）。

### 1.4.2 REPL 并发模型（repl.rs 核心）

REPL 需要 **两个并发任务** + **一个 mpsc 通道**：

```
Task A (SSE reader):  tokio::spawn 持续读服务端 SSE 事件 → 渲染到终端
Task B (stdin reader): tokio::spawn 阻塞读 stdin 行 → mpsc::Sender
Main:                 mpsc::Receiver 收用户命令 → approve/deny/quit
```

当 Task A 收到 `NeedApproval` 事件时：
1. 打印审批提示（红色 `⛔` + `sid` + `aid`）
2. Task B 继续监听 `approve <aid>` 或 `/deny <aid>`
3. Main 收到命令 → 调 `POST /api/v1/sessions/{sid}/approvals` → Task A 收到 ToolResult 继续

断开重连：SSE 流断开时 Task A 用指数退避重试（1s → 2s → 4s → 8s → 放弃）。

### 1.4.3 错误处理矩阵

| 场景 | 行为 |
|---|---|
| 服务不可达 | 重试 3 次，间隔 1s/2s/4s → 放弃，打印 "server unreachable" |
| 401 Unauthorized | 立即退出，打印 "check CODEX_API_KEY" |
| SSE 中途断开 | 自动重连（`GET /sessions/{id}/messages` 恢复历史 → 继续 SSE） |
| 审批超时（30s 无操作） | 打印 "approval timeout" + 自动 deny |
| Ctrl+C | 调 `POST /cancel` → 优雅退出 |

### 1.5 REPL 模式

```
$ codex repl
Connected to http://localhost:3000  (provider: openai, model: gpt-4o)

codex> build a Rust HTTP server
[创建 session abc123...]

◆ Planning
  Building the server...
⚙ bash(cargo init http-server)
  ✓ Created binary project
⚙ edit(src/main.rs)
  ✓ Wrote 45 lines
⛔ bash(rm -rf /) — 用 `codex approve abc123 x1` 审批

codex> approve x1
  ✓ Deleted temp files

◆ Planning
  ...
✓ Done (12 steps)

codex> /sessions          ← 斜杠命令
codex> /history abc123    ← 查看历史
codex> /quit              ← 退出
```

### 1.6 依赖

```toml
# ── workspace 根 Cargo.toml 新增 ──
clap = { version = "4", features = ["derive"] }
colored = "2"

# ── crates/codex-cli/Cargo.toml ──
[dependencies]
tokio = { workspace = true, features = ["full"] }
reqwest = { workspace = true, features = ["stream"] }
serde_json = { workspace = true }
clap = { workspace = true }
colored = { workspace = true }
futures = { workspace = true }
api = { workspace = true }
anyhow = { workspace = true }
tracing-subscriber = { workspace = true }
```

**别忘了**：根 `Cargo.toml` `members` 数组加 `"crates/codex-cli"`。

### 1.7 验证方式

```bash
# 启动服务（另一终端）
API_KEY=test cargo run -p service

# CLI chat 一次性
CODEX_API_KEY=test cargo run -p codex-cli -- chat "say hello"

# CLI repl 交互
CODEX_API_KEY=test cargo run -p codex-cli -- repl

# CLI 辅助命令
cargo run -p codex-cli -- sessions
cargo run -p codex-cli -- history <session-id>
```

---

## 二、E1–E4 详细规格

### E1 — retriever 默认开启

**当前**：`RETRIEVER_ENABLED=1` 时才接线 → 生产默认无语义检索。

**目标**：`main.rs` 中 retriever 默认 `set_retriever`（不用 env gate），首次 `build()` 在 `do_plan` 触发时自动 index cwd。

**改动**：
- `main.rs` 去掉 `RETRIEVER_ENABLED` gate，改为无条件接线；若无 `OPENAI_API_KEY` → warn + 跳过 embed（BM25-only，不 panic）
- TantivyRetriever 新增 `search_bm25(query, limit)` 方法（不调 embed fallback）——当 `embed_provider.is_none()` 时自动走 BM25
- `do_plan` 中首次调用 `retriever.search` 时若 `doc_count()==0`，自动调 `retriever.build(index, chunks)`
- **自动 index guard**：文件数 > 500 时不自动 index，改为 warn "workspace too large for auto-index, run `codex index` manually"

**估量**：中（~40 行）。（原估 15 行过于乐观。）

---

### E2 — 多 session 工作区隔离

**当前**：所有 session 共享 `cwd`（`main.rs` 单例 `ToolContext::default()`），文件操作互相干扰。

**目标**：每个 session 分配独立临时目录 `<cwd>/sessions/<session_id>/`，session 结束/取消时清理（可选保留）。

**改动**：
- `SessionManager::create_session` 创建 `cwd/sessions/<uuid>/` 目录
- `Session` struct 加 `workspace_dir: PathBuf`
- 工具执行时 `ToolContext.cwd` 改为 session 专属路径
- `SessionManager::cancel_session` + `cleanup_finished` 中清理目录
- 可选：`CODEX_KEEP_WORKSPACES=1` 保留目录以便调试
- **Sandbox 衔接**：`SandboxConfig::writable_paths` 需增加 `sessions/<id>/` 路径，否则 landlock 会阻止工具写入 session 专属目录。在 `do_plan` 调 `ctx_mgr.set_sandbox_config(writable_paths: vec![session_workspace])` 即可

**估量**：中（~100 行）。

---

### E3 — 工具插件系统

**当前**：5 个内建工具在 `main.rs:120-124` 硬编码注册。加新工具需改 `main.rs` + 重新编译。

**目标**：`Tool` trait 的外部动态加载——通过配置文件或目录扫描，运行时发现并注册新工具。

**方案（推荐：WASM 插件——轻量、沙箱内建）**：
- 定义 `wasm-tool` 接口：WASM 模块导出 `name()`, `description()`, `execute(args_json) -> result_json`
- `tools-builtin` 新增 `WasmTool` struct 实现 `Tool` trait，内部嵌 WASM runtime（`wasmtime` 或 `wasmer`，~200KB 额外）
- `main.rs` 启动时扫描 `PLUGIN_DIR`（默认 `./plugins/`），加载 `.wasm` 文件
- 每个 WASM 模块运行在独立 sandbox（与内建工具共享 landlock/seccomp/cgroups）

**估量**：大（~500 行 + 新增 wasmtime 依赖）。如时间紧可降级为方案 B：**配置文件 + 内建注册**（YAML/TOML 描述工具的 name/description/capabilities，`main.rs` 硬编码的注册改为从配置读取——不改`Tool` trait 本身，只改注册方式）。方案 B 估量：小（~30 行）。

**建议**：先出方案 B（v5.0），方案 A（WASM）延后至 v5.1。

---

### E4 — 多 Agent 协作（去深度限制）

**当前**：`spawn_sub_agent` 中 `depth >= 1` 即禁止派生。只有两层（主→子），子代理不能再生孙子。

**目标**：最多三层深度（`MAX_DEPTH = 2`），允许主→子→孙。更深层的 agent 负责更细粒度的子任务。

**改动**：
- `loop.rs:318` `depth >= 1` → `depth >= MAX_DEPTH`（常量 `MAX_DEPTH: u32 = 2`）
- 子 agent 未完成父任务时不允许撤回——父 agent 收集结果后统一做 merge（已有 P1）
- Agent 间通信：`tokio::sync::broadcast`（多对多）而非 `mpsc`（一对一），所有同深度 agent 共享一个 broadcast channel。父 agent 作为 Sender，子 agents 作为 Receiver
- 通信协议：
  ```
  Task { id, parent, instruction, files, deadline }  // 父→子
  TaskResult { id, success, output, files_changed }   // 子→父
  Heartbeat { id, status }                            // 子→父（存活信号）
  ```
- AgentLoop 新增 `send_to_peer(&self, agent_id, msg)` 方法（通过全局 `AgentRegistry` 路由）

**估量**：大（~350 行）。`broadcast` 替代 `mpsc`，protocol enum 定义，`AgentRegistry` 路由。

---

## 三、推进顺序

按依赖性排序——CLI 先做（不依赖 E1-E4，独立可测），再按风险从低到高推 E1-E4：

```
Phase A: CLI       (6–8 天) ← 最优先，独立交付物
Phase B: E1+E2     (3–4 天) ← 小+中，风险低
Phase C: E3(方案B) (0.5 天) ← 配置化注册，极低风险
Phase D: E4         (4–6 天) ← 大，依赖 E2(session 隔离)
```

### 为什么 CLI 先做？

1. CLI 不依赖 E1-E4，可以立即开工
2. CLI 完成后可以**即刻用于验收 E1-E4**——每项改完用 `codex chat` 跑一遍
3. CLI 本身会成为后续所有迭代的**回归测试工具**（比 curl 好用）
4. CLI 做完后，E5(Gemini 前端) 有完整的 API 用例参考

---

## 四、提交门槛

每阶段完成后在 VM 执行：

```bash
cd ~/codex && source $HOME/.cargo/env
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --all
```

**期望**：三 rc 为 0，passed 数 ≥ 149（新增测试会增加）。

对于 CLI，额外验证：
```bash
# 启动服务
API_KEY=test cargo run -p service &
# CLI 基本命令
CODEX_API_KEY=test cargo run -p codex-cli -- chat "say hi"
CODEX_API_KEY=test cargo run -p codex-cli -- sessions
```

---

## 五、交付物

| 阶段 | 文件 | 说明 |
|---|---|---|
| Phase A | `crates/codex-cli/*`（新 crate） | CLI 二进制 |
| Phase B | `main.rs`, `session.rs`, `loop.rs` 改动 | retriever 默认 + session 隔离 |
| Phase C | `main.rs` 改动 + `plugins.toml` 示例 | 工具配置化注册 |
| Phase D | `loop.rs`, `agent-types` 改动 | 多层 Agent + 通信 |

---

## 六、总结

| 阶段 | 项 | 估时 | 优先级 |
|---|---|---|---|
| Phase A | **CLI**（新 crate, 5 命令 + REPL + SSE 帧解析 + 并发） | 6–8 天 | 🔴 最优先 |
| Phase B | E1(retriever 默认 + BM25 降级) + E2(session 隔离 + sandbox) | 3–4 天 | 🟡 |
| Phase C | E3(工具配置化，方案 B) | 0.5 天 | 🟡 |
| Phase D | E4(多层 Agent + broadcast 通信) | 4–6 天 | ⏸ |

**CLI 先行，独当一面。做完后整个项目有一个完整的、不依赖浏览器的交互界面——这才是 codex 该有的样子。**

> v5.0-plan 修订记录（2026-07-29）：
> - §1.4.1 SSE 帧解析器设计 + 估量升中+
> - §1.4.2 REPL 并发模型（双线程 + mpsc）
> - §1.4.3 错误处理矩阵
> - §1.6 补 workspace 依赖（clap/colored）+ members 提醒
> - E1 补 BM25 fallback + auto-index guard
> - E2 补 sandbox workspace path per session
> - E4 补 broadcast channel + AgentRegistry 路由设计
