# Codex-Rust v7.0 规划：从"四线基建"到"AI 操作系统"

> 基线：v6.0 定版（gatekeeper-review-v6.0-final.md 过闸）
> 日期：2026-07-29
> 定位：v6.0 把四条路铺好了——现在是让车跑起来，然后让司机有仪表盘、乘客有安全带、路网能互联互通

---

## 一、v6.0 遗留：v6.1 运行时集成（已知缺口，不需重新规划）

| # | 从 v6.0 来 | 做什么 | 文件 | 估 |
|---|---|---|---|---|
| L1 | Bridge 未集成 | `SessionManager` 新增 `create_bridge` → 接线 BridgeSession | `session.rs`, `routes.rs` | 2天 |
| L2 | Civ 自动触发 | `loop.rs` `do_reflect`/`do_observe`/任务完成点插入 `maybe_post_to_civ_line` | `loop.rs` | 1天 |
| L3 | WorkLine 调度器 | `tokio::spawn` cron 扫描 `category=Scheduled` 节点 → 到期执行 | `main.rs` 或新 `scheduler.rs` | 1天 |
| L4 | Bridge dead code | L1 闭环后自动消除（crate 被引用即不再 dead） | — | 0 |

**→ v6.1 合计 4 天，直接做，不需要本文档详细规划。以下是从 v6.1 以后的全新阶段。**

---

## 二、系统中还差什么（全景扫描）

### 2.1 可观测性

| 缺什么 | 现状 |
|---|---|
| 日志有 `tracing`，但没有**结构化** | 全靠 `info!("...")` 自由文本，无法按 session_id / agent_id 检索 |
| telemetry crate 从未用于生产 | `EvalRunner` 只在 CI `cargo test` 中跑一次，线上不可见 |
| 无 Agent 回放/调试 | 想复现一个 session 的决策过程 → 只能手动读 JSONL |
| 无仪表盘 | 没有 "今天跑了多少个 session / 平均步骤数 / 错误率" 之类的指标 |

### 2.2 测试与质量

| 缺什么 | 现状 |
|---|---|
| **P3 E2E 集成测试** | 从 v4.1 延后至今，从未做过一次"启动服务→发 HTTP→验证完整 SSE 流"的测试 |
| 无回归测试用例库 | 每次迭代靠三门（fmt/clippy/test）+ 人工守门员审计，没有自动化回归套件 |
| 无性能基准 | 不知道 100 并发 session 下 sandbox spawn 多快、retriever 搜索多慢 |

### 2.3 安全与韧性

| 缺什么 | 现状 |
|---|---|
| 限流是全局 50 并发，不区分用户 | 一个恶意用户占满 50 → 其他人全 429 |
| 无 session 资源配额 | 用户可以创建无限 session 消耗磁盘（JSONL 有 64MiB 上限但可创建无限个） |
| 无优雅降级 | Ollama 挂了 → session 直接报错，不会自动 fallback 到 OpenAI |
| 无审计日志 | 谁在什么时候执行了什么命令？无法追溯 |

### 2.4 开发体验

| 缺什么 | 现状 |
|---|---|
| CLI 能用但不"舒服" | `codex repl` 做完了，但没有语法高亮、没有自动补全、没有历史搜索 |
| 无 session 导入导出 | 想把一个 session 的分析结果分享给同事 → 没法导出 |
| 无 agent 模板 | 每次都要手动指定 `--provider` `--model` `--budget` |
| 无 model benchmark | 不知道 gpt-4o vs claude vs deepseek 在同一个 task 上谁更快/更准/更便宜 |

### 2.5 多用户

| 缺什么 | 现状 |
|---|---|
| **整个系统是单用户设计** | 一个 `API_KEY`，一个 `MEMORY_DIR`，一个 cwd |
| 无用户概念 | `SessionManager` 不知道"谁"创建的 session |
| 文明线全局共享 | 没有 per-user 文明线（当前设计是全局的，需要分区） |

---

## 三、v7.0：可观测性 & 质量（5–8 天）

**为什么这是下一阶段的正确方向**：v6.1 把四线接进运行时后，系统就开始"自己跑"了。如果看不到它在干什么、跑得怎么样、哪里出了错——后续的所有优化都是盲猜。

### 3.1 结构化日志 + 关联追踪

**目标**：每条日志带 `session_id` / `bridge_id` / `agent_depth`，可按 session 检索全链路。

```rust
// 从
info!("Agent step completed");
// 到
info!(session_id = %sid, step = 3, phase = "act", tool = "bash", "agent step");

// CLI 新增
codex logs <session_id>       // 按 session 过滤日志
codex logs --follow            // 实时 tail
```

**改动**：`main.rs` tracing 初始化加 `JsonLayer`（可选，`CODEX_LOG_FORMAT=json`），loop.rs 中所有 `info!`/`warn!` 补 structured fields。

### 3.2 Telemetry 复活

**目标**：`EvalRunner` 不只是 CI 中的一次性测试，而是**生产运行中持续采集**的指标管线。

```rust
// telemetry 不只是 eval harness，也是实时指标采集器
pub struct TelemetryCollector {
    session_count: AtomicU64,
    avg_steps: AtomicU64,          // 滚动平均
    error_rate: AtomicU64,         // 错误 / 总步骤
    tokens_used: AtomicU64,        // 累计 token
    active_sessions: AtomicU64,    // 当前活跃数
}

// API
GET /api/v1/telemetry → { sessions, avg_steps, error_rate, tokens }
```

**CLI**：`codex stats` — 终端仪表盘，彩色柱状图显示关键指标。

### 3.3 Agent 回放/调试模式

**目标**：能**重新播放**一个 session 的完整决策链，步进查看每个 phase 的状态。

```
codex replay <session_id>       // 从 MemoryStore 加载，逐步回放
codex replay <id> --step 5      // 跳到第 5 步
codex replay <id> --inspect     // 每步显示完整 Observation / PlanState
```

**实现**：基于已有的 `JsonlMemoryStore::load_session`，新增 `ReplayRunner`（复用 `AgentLoop` 的事件流，但不实际执行工具——从 `StoredEvent` 中读到结果直接回放）。

### 3.4 E2E 集成测试（P3，终于不再延后）

**目标**：一条命令启动 mock 服务 → 发 HTTP → 验证完整流程。

```rust
// tests/e2e.rs
#[tokio::test]
async fn test_e2e_create_session_send_message_sse_completes() {
    // 1. 启动 mock LLM server (axum, 返回脚本化响应)
    // 2. 启动 codex-service (使用 mock server 的 base_url)
    // 3. POST /api/v1/sessions → 创建
    // 4. POST /api/v1/sessions/{id}/messages → SSE 流
    // 5. 验证: Done 事件 + steps ≥ 1 + ToolResult 存在
    // 6. GET /api/v1/sessions/{id}/messages → 历史非空
}
```

**覆盖场景**（至少 5 个）：
- 正常 chat → Done
- chat 含 bash tool → ToolCall + NeedApproval + approve → Done
- chat 含错误 → Error 事件 → session status = error
- 取消 mid-stream → Done(ok=false)
- 历史回放 → messages 数组非空

**Mock 方案**：内嵌 `axum::Router` + `tokio::test`，mock LLM server 返回脚本化 JSON 响应，不依赖外部网络。mock 代码放 `tests/common/mock_llm.rs`，5 个场景各一个 `#[tokio::test]`。

### 3.5 CLIPPY 归零（质量门禁硬约束）

**目标**：三门全绿——FMT=0 _and_ CLIPPY=0 _and_ TEST=0。当前 CLIPPY_RC=101（codex-cli test-target unused_import + bridge dead code）。

**改动**：
- `codex-cli`：`#![allow(unused_imports)]` 已加 crate 级，确认对所有 target 生效
- `bridge`：v6.1 L1 集成后 crate 被引用，dead code 自动消除
- 加 CI 严格模式：`cargo clippy --workspace --all-targets -- -D warnings`（已存在，修到通过）

### 3.6 代码覆盖率门槛

**目标**：`cargo tarpaulin --out html`，行覆盖 ≥ 40%，CI 中自动跑（可选门禁，不硬阻断但报警）。

```
cargo tarpaulin --workspace --skip-clean -o html -o json
# 输出: tarpaulin-report.html + coverage.json
# CLI: codex coverage  → 显示最近一次覆盖率报告摘要
```

### 3.7 Telemetry API schema 定版 + Prometheus 兼容

**目标**：明确 `GET /api/v1/telemetry` 返回格式，同时支持 Prometheus `/metrics` 端点。

```json
// GET /api/v1/telemetry
{
  "session_count": 1234,
  "active_sessions": 5,
  "avg_steps_per_session": 8.3,
  "error_rate": 0.02,
  "total_tokens": 567890,
  "uptime_seconds": 86400
}

// GET /api/v1/metrics (Prometheus text format)
// codex_sessions_total 1234
// codex_active_sessions 5
// codex_error_rate 0.02
// ...
```

### 3.8 ReplayRunner 实现方案细化

**目标**：新增 `ChannelFeeder`——从 `StoredEvent` 注入"假工具结果"，复用 AgentLoop 但不执行真实沙箱。

```rust
// crates/agent-core/src/replay.rs（新文件）
pub struct ReplayRunner {
    events: Vec<StoredEvent>,
    position: usize,
}
impl ReplayRunner {
    /// 逐步回放，feed 预录的 ToolResult 给 AgentLoop
    pub async fn step(&mut self, agent: &mut AgentLoop) -> Option<AgentEvent>;
}
```

### 3.9 性能调优短周期（v7.1，v7.0 完工后立即启动）

**触发条件**：v7.0 telemetry 上线后，从指标中提取 top 3 瓶颈。

**候选优化项**（2-3天）：
| 项 | 预期手段 |
|---|---|
| Sandbox spawn 速度 | process pool 预创建 / `clone()` 替代 `spawn()` |
| Retriever 搜索延迟 | 热缓存 + BM25 降级阈值（< 10 条结果时跳过向量搜索） |
| SSE 流延迟 | 批量 flush（每 50ms 合并 token 再 push） |

---

## 四、v8.0：多用户 & 平台化（5–8 天）

### 4.1 多用户

**目标**：一个 codex 服务支持多个独立用户，各自有独立的 API key、session 列表、文明线和工作线。

```rust
pub struct UserContext {
    pub user_id: String,       // 从 API key 解析
    pub memory_dir: PathBuf,   // per-user MEMORY_DIR
    pub workline: WorkLineStore,
    pub civ_line: CivilizationStore,
}

// API_KEY 从单值 → HashMap<"api_key", "user_id">
// curl -H "Authorization: Bearer user1-key-xxx" → user_id="user1"
```

### 4.2 Per-user 文明线 + 工作线

文明线和工作线从全局共享 → **每个 user 一条** + 可选"全局公告板"（只读，admin 发布）。

### 4.3 Agent 模板

**目标**：预配置好的 agent 设置，一行命令启动：

```yaml
# templates/code-reviewer.toml
[agent]
name = "Code Reviewer"
provider = "openai:gpt-4o"
system_prompt = "You are a senior code reviewer. Focus on security, performance, and readability."
budget = 10

[tools]
disabled = ["bash"]   # 审查员不能执行命令
```

```bash
codex chat --template code-reviewer "review src/main.rs"
```

### 4.4 Webhook 通知

**目标**：长任务完成后通知外部系统（Slack/钉钉/CI pipeline）。

```rust
POST /api/v1/sessions  body: { "webhook_url": "https://..." }
→ 当 session status = Done/Error 时，自动 POST 结果到 webhook_url
```

---

## 五、v9.0：体验 & 生态（按需启动，无固定排期）

| 项 | 说明 |
|---|---|
| **CLI 增强** | 语法高亮、Tab 补全、历史搜索（`rustyline`）、彩色仪表盘 |
| **Session 导出** | `codex export <id> --format markdown` → 生成可读的对话记录 |
| **Model Benchmark** | `codex bench "write a quicksort" --providers openai:gpt-4o,deepseek:chat` → 比速度/质量/成本 |
| **Plugin Marketplace** | 社区贡献的 agent 模板 + 自定义工具，一键安装 |
| **语音接口** | `codex voice` → 语音输入 → whisper 转文本 → agent 执行 → TTS 输出 |
| **Vector DB 升级** | Tantivy 内存索引 → 持久化索引 + 增量更新（支持大型项目 > 10 万文件） |

---

## 六、推进顺序总图

```
v6.0 (现在) ──→ v6.1 运行时集成 (4天)
                    │
                    ├──→ v7.0 可观测性+质量 (5-8天) ← 下一阶段主攻
                    │       │
                    │       ├── 结构化日志 + 关联追踪 (3.1)
                    │       ├── Telemetry 复活 + /api/v1/metrics (3.2+3.7)
                    │       ├── Agent 回放/调试 → codex replay (3.3+3.8)
                    │       ├── E2E 集成测试 + mock LLM (3.4)
                    │       ├── CLIPPY 归零 (3.5)
                    │       └── 代码覆盖率 ≥ 40% (3.6)
                    │
                    ├──→ v7.1 性能调优 (2-3天)  ← v7.0 指标驱动
                    │
                    ├──→ v8.0 多用户+平台化 (5-8天)
                    │       │
                    │       ├── 多用户 → per-user API key
                    │       ├── Per-user 文明线+工作线
                    │       ├── Agent 模板
                    │       └── Webhook 通知
                    │
                    └──→ v9.0+ 体验+生态 (按需)
                            CLI增强 / 导出 / Benchmark / 语音 / Vector DB
```

---

## 七、与顶层设计的关系

`top-level-design.md` 的全 12 缺口 + 11 To-Be 已在 v5.0 前 100% 闭环。本文档中的所有项目都是**原顶层设计范围之外的新能力**——代码从"够用"走到"好用"。

建议在 `top-level-design.md` 新增 §16 "v6.1+ 演进路线图"，引用本文档。

---

## 八、总结

| 阶段 | 定位 | 一句话 |
|---|---|---|
| **v6.1** | 运行时集成 | 四线通车——Bridge 入 session、Civ 自动触发、WorkLine 调度 |
| **v7.0** | 可观测性 | 看得见系统在干什么——结构化日志、实时指标、回放调试、E2E 测试、CLIPPY清零、覆盖率 |
| **v7.1** | 性能调优 | v7.0 指标驱动——sandbox pool、retriever 缓存、SSE 批量 flush |
| **v8.0** | 多用户平台 | 从单人到多人——多用户隔离、agent 模板、webhook |
| **v9.0+** | 体验生态 | 用得舒服——CLI 增强、导出、benchmark、语音 |

**v6.0 是 AI 有了"社群"（四线）。v7.0 是社群有了"镜子"（能看到自己在做什么）。v8.0 是镜子前站了不止一个人。**

> 修订记录（2026-07-29）：
> - §3.5 CLIPPY 归零（质量门禁硬约束）
> - §3.6 代码覆盖率门槛（tarpaulin ≥ 40%）
> - §3.7 Telemetry API schema + Prometheus /metrics
> - §3.8 ReplayRunner ChannelFeeder 实现方案细化
> - §3.9 性能调优短周期 v7.1（sandbox/retriever/SSE）
> - §二 补 v7.1 入口
> - §六/八 更新推进图与总结表

---

## 九、任务管理约定（工作线机制）

本文档中所有阶段（v6.1 / v7.0 / v7.1 / v8.0 / v9.0）均采用以下工作流：

1. **执行前**：用 `TaskCreate` 登记任务（含描述、估时、依赖关系）
2. **每阶段开工**：`TaskUpdate status=in_progress`
3. **VM 验收通过**：三门全绿（FMT/CLIPPY/TEST）→ `TaskUpdate status=completed`
4. **阶段竣工**：同步源码 + git commit + zip 备份

这样做的好处：用户不需要记忆"做了什么"，任务列表即看板。完工后自动归档到工作线（v6.1 L3 闭环）。
