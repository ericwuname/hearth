# Codex-Rust v10.1 执行方案（守门员修正版）

> 基线：v10.0 定版（gatekeeper-review-v10.0-final.md 过闸，156 passed）
> 修正：2026-07-29 — 原版 4 项事实错误已修正，scope 从 410 行砍至 ~200 行
> 估时：2–3 天 → 3 个 phase

---

## 守门员审计摘要

| 原 Plan 声明 | 事实 |
|---|---|
| "crates/observer 已有部分模板" | ❌ observer 目录/文件均不存在，需从零建 |
| "execute_plan 无需改动" | ❌ 该函数根本不存在，需全新实现 |
| "run_session 加 catch_unwind" | ❌ run_session 方法不存在，需新建 |
| "tools/registry.json +30" | ❌ 文件/目录均不存在 |
| "wasmtime 加 dep" | ❌ resource-monitor 无此依赖 |

**结论**：原计划对代码库现状的假设多处不实。以下为修正后的可执行方案。

---

## Phase 1：Orchestrator → AgentLoop 集成（🔵2，1 天）

### 1.1 新增 `orchestrator::execute_plan`

在 `crates/agent-core/src/orchestrator.rs` 末尾新增：

```rust
pub fn execute_plan(
    store: &mut impl FnMut(&str, serde_json::Value) -> Result<serde_json::Value, String>,
    steps: Vec<TaskStep>,
) -> TaskReport { ... }
```

适配 `ToolDispatcher::dispatch(name, args, ctx) → Result<String>` 的签名差异（String ↔ Value）。

### 1.2 AgentLoop 接入

`do_act` 开头检测 `self.pending_plan` 非空 → 调 `execute_plan` → 批量执行 → observe。

### 验证

新增 1 个集成测试：3 步顺序 task（bash → edit → bash），断言 `TaskReport { ok: true, completed: 3 }`。

### 文件

| 文件 | 改动 |
|---|---|
| `crates/agent-core/src/orchestrator.rs` | +40（execute_plan 实现） |
| `crates/agent-core/src/loop.rs` | +15（do_act 接入点） |
| 测试 | +30 |

---

## Phase 2：ROI 评估 + 资源告警（🔵3，0.5 天）

### 2.1 ROI 报告

在 `resource-monitor` 新增 `RoiReport` struct + `GET /api/v1/costs/roi?session_id=X` 端点。

```rust
pub struct RoiReport {
    pub session_id: String,
    pub tokens_total: u64,
    pub cost_est_usd: f64,      // tokens * rough rate
    pub steps: u32,
    pub roi_score: f64,         // 0..1, simple heuristic
}
```

### 2.2 资源告警

`ResourceSnapshot` 新增 `is_critical()` 方法（内存>80% / 磁盘<1GB → true）。

### 文件

| 文件 | 改动 |
|---|---|
| `crates/resource-monitor/src/lib.rs` | +30 |
| `crates/service/src/routes.rs` | +15 |
| 测试 | +20 |

---

## Phase 3：L2 自愈 — panic 捕获（0.5 天）

### 实现

在 `Session` 新增 `run_session` 方法，用 `std::panic::catch_unwind(AssertUnwindSafe(|| { ... }))` 包裹 AgentLoop::run。

panic 时：
1. 写 Error 事件到 event stream
2. 标记 session status = error
3. 写文明线 `CivEntry { event_type: "CRASH", ... }`

### 文件

| 文件 | 改动 |
|---|---|
| `crates/service/src/session.rs` | +25 |
| 测试 | +15 |

---

## 总改动清单

| 文件 | Phase | 估行 |
|---|---|---|
| `crates/agent-core/src/orchestrator.rs` | P1 | +40 |
| `crates/agent-core/src/loop.rs` | P1 | +15 |
| `crates/resource-monitor/src/lib.rs` | P2 | +30 |
| `crates/service/src/routes.rs` | P2 | +15 |
| `crates/service/src/session.rs` | P3 | +25 |
| 测试（3 处） | P1+P2+P3 | +65 |

**总计 ~190 行，2 天。**

## 删除项（原 Plan 🔵1, 🔵4, L3）

| 原项 | 删除理由 |
|---|---|
| 🔵1 tool search/install | WASM 沙箱安全性未解决；代码 `curl\|sh` 风险过高。v10.0 已实现 `codex tools` 列表。 |
| 🔵4 Observer 独立二进制 | 创建独立进程 + 部署管理 1 天不够。v10.0 tokio 后台任务已满足观测需求。 |
| L3 进程重启 | 需要 systemd/supervisor 集成，不是代码问题。 |

---

## 提交门槛

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --all
# 期望：三 rc=0, passed ≥ 159 (156 + 3 new)
```
