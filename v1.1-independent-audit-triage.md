# v1.1 独立审计报告 — 逐条采信/驳回裁定

> 作者：守门员（源码直读验证）
> 方法："不信报告信源码"。每条指控均打开真实源文件核对，给出采信/驳回结论与定级。
> 配套代码改动：见各 `X? fix` 注释（sandbox/lib.rs、tools-builtin/grep.rs、tools-builtin/glob.rs、service/session.rs）。

## 总判定

独立审计整体**有效**：自审确实存在"只看修了什么、不看引入什么 / 只看主路径不看旁路 / 只问测试绿不绿不问覆盖什么"的盲区，抓出 6 个真实问题（X1/X2/X4/X5 确凿，X7 真实但低危，X3 为潜在隐患）。

但本报告**未做到全对**，有 2 处需要纠正/降级：
- **X6 前提错误**：其声称"ctx.cwd 是 current_dir()（绝对路径）"——实测 `ToolContext::default()` 的 cwd 是 `"."`（相对），且 `find . -path "./pattern"` 因 find 给路径加 `./` 前缀而**正常工作**。X6 不是缺陷，审计推理方向反了。
- **X1 严重性低估**：报告指出 cgroup 目录泄漏，但漏了一个更严重的点——超时分支**连子进程都没 kill**，孤儿进程也一直活着。已一并修掉。

## 逐条裁定

| 编号 | 审计定性 | 源码核定结论 | 我的定级 | 处置 |
|------|---------|------------|---------|------|
| X1 | 🔴 cgroup 超时未清理 | **确凿 + 更严重**：超时后 `spawn_blocking` 被 detach，子进程未 kill，cgroup 也未删 | 🔴（升级：进程+目录双泄漏） | **已修**：`Arc<AtomicU32>` 捕 pid，超时分支 `SIGKILL`+`cleanup_cgroup`；新增回归测试 `test_linux_sandbox_timeout_reaps_cgroup` |
| X2 | 🟡 approval HashMap 永不清理 | **确凿**：`reset_approval` 全项目仅测试调用；生产 `send_message` 三条结束路径均无调用 | 🟡 | **已修**：`send_message` 克隆 `dispatcher`，在 cancelled/done/error 三路 `reset_approval(&session_id)` |
| X3 | 🟡 cancel_tx 覆盖成僵尸 | 部分确凿：**当前不可触发**。状态机 `agent.take()` 使第二次 `send_message` 走 "agent not available" 分支，到不了覆盖 `cancel_tx` 那行 | 🟢（潜在隐患，非当前 bug） | **已加防御**：覆盖前 `if is_some()` 打 warn，避免静默丢弃 |
| X4 | 🟡 取消不发 Done 事件 | **确凿**：cancel 分支只发 `Error` 就 return，正常结束路径发 `Done`，协议不一致 | 🟡 | **已修**：cancel 分支补发 `Done{ok:false,status:"cancelled"}` |
| X5 | 🟡 has_rg() 在沙箱外执行 | **确凿**：`has_rg()` 用 `tokio::process::Command` 直接跑 `rg --version`，PATH 投毒可逃逸 landlock/seccomp | 🟡 | **已修**：`has_rg` 改为经 `sandbox.spawn` 探测 |
| X6 | 🔵 find 参数顺序/相对路径 | **驳回（审计前提错）**：生产 cwd 是 `"."` 而非绝对；`find . -path "./pattern"` 因 `./` 前缀匹配而**正常**。**非缺陷** | 🔵（仅注释澄清） | **加注释**说明相对/绝对 cwd 均可用，不改正逻辑 |
| X7 | 🔵 cancel_session 对 running 的竞态 | **确凿但低危**：`cancel_session` 设 `running=false` 与 spawned task 设 `running=true` 有窗口；不影响取消功能（靠 `cancel_tx` abort） | 🔵 | **已修**：`running=true` 提前到 `send_message` 加锁处，与 `cancel_tx` 同锁 |
| X8 | 测试数量对不上 | **非代码问题**：属审计文档自洽性。已核实无编造；本轮测试按真实 `cargo test` 结果计数 | — | 本文档精确计数，不修正旧报告 |

## 审计未提及、我新发现的问题

### N1（新增，真问题）：SessionManager.sessions HashMap 永不清理
`service/src/session.rs:36` `sessions: RwLock<HashMap<String, Arc<Mutex<Session>>>>` —— session 创建后**从不移除**。与 X2 同源（注册表只增不删），但 X2 只修了 approval 子表，session 主表仍是泄漏。
- 严重度：🟡（长期运行服务内存增长）
- 处置：**本轮未实现**（移除需处理 `get_session`/`cancel_session` 在途并发，风险高）。建议 v1.2 增加 session 生命周期清理（TTL / 显式 drop / LRU）。已记下。

### N2（新增，X1 的升级点）：超时后孤儿进程未回收
审计只提 cgroup 目录。实测 `tokio::time::timeout` 丢弃 `spawn_blocking` 的 JoinHandle 后，底层 OS 子进程继续存活（跨过超时）。本次 X1 修复已用 `SIGKILL` 一并回收。

### N3（轻微，非安全）：sandbox/Cargo.toml 声明了 `caps = "0.5"` 但未使用
`lib.rs` 全程用 `nix::libc` 原生 syscall，未引用 `caps` crate。仅产生 unused-crate 警告，无安全影响。建议后续清理或改用 `caps` 做能力收敛。

## 对 Gap 1–5 的回应

| Gap | 审计意见 | 核定 |
|-----|---------|------|
| Gap 1 每轮回归对比 | 合理 | 接受。本轮因直接在已验证快照上增量修，未做逐轮 diff；已补充 X1 针对性回归测试 |
| Gap 2 并发/压力测试 | 合理 | 接受。X2/X3/X7 的并发隐患确需压力测试覆盖，列入 v1.2 |
| Gap 3 资源泄露测试 | 合理且**正中要害** | 接受并已补：`test_linux_sandbox_timeout_reaps_cgroup` 直接验证 X1 |
| Gap 4 负面测试 | 合理 | 接受。现有测试多为正常路径，已建议在 v1.2 补"命令不存在/超大 pattern"等负面用例 |
| Gap 5 Cargo.toml 变更检查 | 合理 | **本轮无风险**：核对 `Cargo.toml` 与 `crates/sandbox/Cargo.toml`，v1.1 未新增任何外部依赖（sandbox 仅 `nix`/`caps`/`tokio` 等既有项，landlock/seccomp 走原生 syscall，无新第三方 crate） |

## 关于 `read_only_paths = ["/"]` 的保密性折中（需 owner 拍板）

这是 v1.1 已有的设计取舍（非本次新引入）：`SandboxConfig::default().read_only_paths = vec!["/"]` 给沙箱命令**整树只读+执行**权限（类比 `firejail --ro-root`）。
- 效果：写操作被 landlock 全禁（workspace cwd 除外），syscall 被 seccomp 过滤。
- 代价：**保密性不保证**——沙箱内的命令可读 `/etc/shadow`、`/root/.ssh` 等任意主机文件（只是不能改、不能执行新写出的文件）。

独立审计末尾也点名此折中需与 owner 确认。我的建议：若需保密隔离，应把 `read_only_paths` 收敛为 `{workspace, /usr, /lib, /bin, ... 白名单}`，但那样会重新引入"命令读不到路径而失败"的调参；当前 `"/"` 是"能跑起来 + 防篡改"的务实默认。请 owner 确认是否可接受。

## 闸门结论

| 项 | 状态 |
|----|------|
| 独立审计确凿问题（X1/X2/X4/X5/X7） | ✅ 已全部修复并加注释/测试 |
| X3 潜在隐患 | ✅ 已加防御性 warn |
| X6 误报 | 🔵 驳回，仅加注释澄清 |
| 新增 N1（session 表泄漏） | 🟡 已记录，建议 v1.2 修 |
| 新增 N2/N3 | N2 已随 X1 修；N3 轻微，下轮清理 |
| 测试盲区 Gap 1–5 | 接受，Gap 3 已补测试；其余排 v1.2 |
| 供应链（Gap 5） | ✅ 本轮无新增依赖 |

**v1.1 可保留，建议尽快发 v1.1.1 吸收 X1–X5 修复；N1 与保密性折中提交 owner 决策。**
