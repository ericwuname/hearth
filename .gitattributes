# 行尾归一：全仓文本一律 LF。
#
# 背景（P4 Node 13 发版实测，2026-09-01）：
#   Windows 侧 core.autocrlf=true 且无 .gitattributes → 任何 git checkout/reset
#   都会把文本文件改写为 CRLF；这些文件若以二进制方式同步回 Linux VM，
#   会造成"双树同内容不同字节"。后果不是排版问题，而是**以文件字节为输入
#   的测试锁必然失配**（project-xray 的 wiring-v13.toml FNV 锁即因此红：
#   本机 9713B/FNV 0x0444c6bfbbbb0ea2 = 锁值；VM 9953B/FNV 0x8426d940… = CRLF 版）。
#
# 处置：text=auto（二进制文件自动跳过）+ eol=lf（工作树强制 LF）。
* text=auto eol=lf
