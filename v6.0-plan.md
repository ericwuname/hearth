# Codex-Rust v6.0 规划：供应商管理 v2 + 内部桥 + 文明线 + 工作线

> 基线：v5.0 定版 | 日期：2026-07-29
> 定位：从"单模型 Agent"到"多模型协作的群体智能 + 集体记忆 + 任务编排"
> 一句话：让多个 AI 不仅能互相讨论（桥），还能共享集体记忆（文明线），还能协同管理任务进度（工作线）

---

## 一、全局架构（四线合一）

```
┌──────────────────────────────────────────────────────────────────┐
│                     Codex Agent Platform                          │
│                                                                   │
│  ┌───────────┐  ┌───────────┐  ┌───────────────┐                 │
│  │ Session A │  │ Session B │  │ BridgeSession │                 │
│  │ 单模型对话 │  │ 单模型对话 │  │ 多模型平级讨论 │                 │
│  └─────┬─────┘  └─────┬─────┘  └───────┬───────┘                 │
│        │               │               │                          │
│        └───────────────┼───────────────┘                          │
│                        │                                          │
│        ┌───────────────┼───────────────┐                          │
│        │               │               │                          │
│   ┌────┴─────┐  ┌──────┴──────┐  ┌────┴──────────┐              │
│   │ 内部桥    │  │  文明线      │  │   工作线       │              │
│   │ Bridge   │  │ Civ Line    │  │  Work Line    │              │
│   │          │  │             │  │                │              │
│   │ 实时讨论 │  │ 自由留言    │  │ 任务编排       │              │
│   │ 平级协作 │  │ 集体记忆    │  │ 进度追踪       │              │
│   │ 投票共识 │  │ 自动触发    │  │ 按项目/会话    │              │
│   └──────────┘  └─────────────┘  └────────────────┘              │
│                        │                                          │
│   ┌────────────────────┴────────────────────┐                    │
│   │         Provider Registry v2             │                    │
│   │   provider:model key + 自动发现 + 自定义  │                    │
│   └──────────────────────────────────────────┘                    │
└──────────────────────────────────────────────────────────────────┘
```

**四线关系**：

| 线 | 本质 | 类比 | 触发方式 | 参与者 | 持久化 |
|---|---|---|---|---|---|
| **内部桥** | 实时多模型讨论 | 会议/群聊 | 用户发起 | 指定模型 | 会话级 |
| **文明线** | 自由留言板/集体日记 | 公司公告栏/朋友圈 | **自动触发**，无需提醒 | 全体 AI | 全局永久 |
| **工作线** | 任务看板/进度追踪 | Jira/Trello | 任务创建/更新/完工 | 项目成员 | 项目级 |

---

## 二、Phase 6A：供应商管理 v2（保持不变）

与 v6.0-plan.md 初版一致，此处简表：

| 改动 | 文件 | 关键点 |
|---|---|---|
| `ProviderRegistry` 重构 | `registry.rs` | key: `"openai:gpt-4o"`，alias: `"openai"→["openai:gpt-4o",...]` |
| 模型自动发现 | `discovery.rs`（新） | OpenAI `GET /v1/models`，Ollama `GET /api/tags`，vLLM |
| 自定义供应商 | `providers.yaml` | YAML 定义 base_url / api_key_env / models，不动 main.rs |
| 模型缓存 | `~/.codex/models.json` | 首次发现→缓存 24h |

**估量**：4–6 天。**前置条件**：6B/6C/6D 依赖此阶段。

---

## 三、Phase 6B：内部桥（保持不变）

与 v6.0-plan.md 初版一致。桥是实时平级讨论，与文明线（异步自由留言）互补。

| 项 | 说明 |
|---|---|
| 新 crate | `crates/bridge`（BusSession + BridgeBus + 4 策略 + 共识投票） |
| 与文明线关系 | 桥的讨论结果 → 文明线摘要；文明线的历史洞察 → 桥的上下文注入 |

**估量**：8–12 天。

---

## 四、Phase 6C：文明线（Civilization Line）—— 新设计

### 4.1 概念

**文明线 = AI 的集体记忆 + 自由留言板。**

每个 AI 在任何对话/桥中，完成重要工作后**自动**在文明线上留一条笔记。不需要用户提醒，不需要手动触发。形式自由——可以是一句话感想，也可以是一篇复盘长文。

```
时间线
───────┼─────────────────────────────────────────────→
       │
  7/29 02:00  [openai:gpt-4o]  完成 sandbox landlock 修复
  7/29 02:15  [ollama:llama3.2] 发现 retriever 索引策略可优化：用 HNSW 代替 Flat
  7/29 03:00  [deepseek:chat]  审查了 A4 merge 逻辑，建议改用三路合并
  7/29 04:30  [openai:gpt-4o]  反思：今天的代码审查模式很有效，建议固化
```

### 4.2 数据模型

```rust
/// 文明线上的单条留言
pub struct CivEntry {
    pub id: String,                // UUID
    pub author: CivAuthor,         // 谁发的
    pub content: String,           // 自由文本，无字数限制
    pub category: CivCategory,     // 分类标签
    pub context: CivContext,       // 触发此留言的上下文
    pub created_at: String,        // ISO 8601
    pub tags: Vec<String>,         // 可搜索标签
}

pub struct CivAuthor {
    pub provider_model: String,    // "openai:gpt-4o"
    pub session_id: String,        // 哪个对话产生的
    pub bridge_id: Option<String>, // 或哪个桥
}

pub enum CivCategory {
    Insight,        // 发现/洞见
    Milestone,      // 里程碑完成
    Lesson,         // 教训/踩坑
    Decision,       // 重大决策记录
    Reflection,     // 反思/复盘
    Question,       // 向其他 AI 提问
    Announcement,   // 公告（人类可见）
}

pub struct CivContext {
    pub task_goal: String,         // 当时在做什么任务
    pub files_touched: Vec<String>,// 涉及的文件
    pub duration_secs: u64,        // 花了多久
}
```

### 4.3 自动触发机制

**不是定时轮询——而是关键事件回调**。在 Agent 循环的关键节点插入 `maybe_post_to_civ_line` 检查：

| 触发点 | 条件 | 默认分类 |
|---|---|---|
| `do_reflect` → `Verdict::Continue` 且 step 数 > 5 | 每 5 步发一次进度摘要 | `Milestone` |
| 任务完成（`Done` 事件） | 总是发完工总结 | `Milestone` |
| 发现错误（`consecutive_errors >= 1`） | 首次错误时发 | `Lesson` |
| `do_observe` 中 LSP 诊断 > 10 个 | 发代码质量观察 | `Insight` |
| 子代理完成且 `merge_conflict = true` | 发冲突记录 | `Insight` |
| 用户显式：`codex civ post "message"` | 手动触发 | 用户指定 |

### 4.4 存储

- **文件**：`MEMORY_DIR/civilization.jsonl`（与现有 `JsonlMemoryStore` 同目录，复用同一套基础设施）
- **格式**：每行一个 `serde_json::Value`（`CivEntry`）
- **大小限制**：单文件 128 MiB（比 session 的 64 MiB 大，因为是全局共享）
- **索引**：启动时加载到内存 `Vec<CivEntry>`（预期总量 < 10,000 条，内存开销可忽略）

### 4.5 API + CLI

**REST**：
```
GET  /api/v1/civilization               → 最近 50 条（分页）
GET  /api/v1/civilization?category=insight → 按分类过滤
GET  /api/v1/civilization?tag=sandbox   → 按标签搜索
POST /api/v1/civilization               → 手动发布
```

**CLI**：
```
codex civ                    → 查看最近 20 条
codex civ feed               → 实时流（tail -f 风格）
codex civ post "message"     → 手动发布
codex civ search "sandbox"   → 搜索历史
```

### 4.6 改动文件

| 文件 | 改动 |
|---|---|
| `crates/memory/src/lib.rs` | 复用 `JsonlMemoryStore` 基础设施，加 `CivilizationStore` |
| `crates/agent-core/src/loop.rs` | `do_reflect`/`do_observe` 中插入自动触发点 |
| `crates/service/src/routes.rs` | 新增 `/api/v1/civilization` 路由 |
| `crates/service/src/main.rs` | 挂载路由 |
| `crates/api/src/lib.rs` | 新增 `CivEntry`/`CivAuthor`/`CivCategory`/`CivContext` |
| `crates/codex-cli/src/main.rs` | 新增 `civ` 子命令 |

**估量**：3–5 天。

#### 4.6.1 补充：文明线读入——Agent 怎么"看到"历史

仅在 `do_reflect` 中写入文明线不够。新 agent 启动时需要在 `do_plan` 中查询文明线：
- 取最近 20 条 `category=Insight|Lesson|Decision` 条目
- 按当前任务关键词 tag 过滤
- 注入 system prompt：`[CIVILIZATION: Recent collective knowledge]\n{entries}\n[/CIVILIZATION]`

实现：`CivilizationStore::search(query, limit, categories, tags) → Vec<CivEntry>`。存储用 tantivy 轻量索引（启动时从 JSONL rebuild），搜索耗时 < 10ms。

#### 4.6.2 补充：存储膨胀控制

JSONL 上限 128MiB，但长期运行仍会膨胀。启动时仅加载最近 1000 条到内存热缓存；搜索走 tantivy 索引（非全量内存 Vec）；条目超过 10,000 条后自动归档 `civilization-2026-07.jsonl.gz`。

---

## 五、Phase 6D：工作线（Work Line）—— 新设计

### 5.1 概念

**工作线 = 任务看板 + 进度追踪 + 自动完工归档。**

按两个维度组织：

```
按项目组织（多对话共享）：
  workspace/
  ├── codex.toml              ← 项目定义文件（可选）
  └── .codex/
      └── workline.jsonl      ← 项目级工作线

按对话组织（单任务独立）：
  MEMORY_DIR/
  └── workline-<session_id>.jsonl  ← 单对话工作线
```

**规则**：如果 `cwd` 下有 `codex.toml` → 用项目级工作线；否则用对话级工作线。

### 5.2 数据模型

```rust
/// 工作线上的一个任务节点
pub struct WorkNode {
    pub id: String,                // UUID
    pub parent_id: Option<String>, // 父任务（支持子任务）
    pub description: String,       // 任务描述
    pub status: WorkStatus,        // 当前状态
    pub progress: f32,             // 0.0–1.0
    pub category: WorkCategory,    // 任务类别
    pub assignee: Option<String>,  // 负责的 provider:model
    pub deps: Vec<String>,         // 依赖的前置任务 id
    pub created_at: String,
    pub updated_at: String,
    pub completed_at: Option<String>,
    pub notes: Vec<String>,        // 进度备注
}

pub enum WorkStatus {
    Pending,       // 待开始
    InProgress,    // 进行中
    Blocked,       // 阻塞（依赖未完成）
    Review,        // 待审查
    Completed,     // 已完成
}

pub enum WorkCategory {
    LongTerm,      // 长期任务（跨 session 持久）
    ShortTerm,     // 短期任务（当前 session 内）
    Scheduled,     // 定时任务（cron 触发）
    Completed,     // 已完工（自动归档到此分类）
}
```

### 5.3 任务生命周期

```
Pending ──→ InProgress ──→ Review ──→ Completed ──→ 自动归档到 Completed 线
                │                         │
                └──→ Blocked ──→ ... ──→──┘
                                         │
                                   进度同步到工作线
                                   （文明线可引用完工节点）
```

### 5.4 四条子线

| 子线 | 数据来源 | 可见性 | 生命周期 |
|---|---|---|---|
| **长期任务线** | `category=LongTerm` | 跨 session 持久 | 手动完成或过期 |
| **短期任务线** | `category=ShortTerm` | 当前 session | session 结束 → 未完成的归档为 Abandoned |
| **定时任务线** | `category=Scheduled` + `scheduled_at` | 跨 session | crontab 触发 → 执行 → 自动重置下次触发时间 |
| **完工任务线** | `status=Completed`（自动从各线移动） | 永久存档 | 只读，可搜索 |

**关键行为**：
- 任务 `Completed` → **自动从来源线消失，出现在完工线上**（不做物理删除，只改分类）
- 定时任务 Scheduled 执行完毕后 → 自动计算下一次触发时间 → 新建一个 Scheduled 节点 → 已完成的那个进 Completed
- 对话级短期任务在 session 结束时 → 所有 `InProgress`/`Pending` 自动标 `Abandoned`

### 5.5 与 Agent 循环的集成

Agent 在执行过程中**自动更新工作线**：

```rust
// do_plan 阶段：注入工作线上下文到 system prompt
let workline_ctx = workline.format_for_prompt();
// → "当前任务看板：\n[ ] Task A (长期)\n[✓] Task B\n[→] Task C (进行中)"

// do_act 阶段：工具执行后更新工作线
if task_completed {
    workline.mark_completed(node_id);
    civ_line.post(CivCategory::Milestone, "Task completed: ...");
}

// do_reflect 阶段：根据裁決更新状态
if verdict == GiveUp {
    workline.mark_blocked(node_id, "连续错误过多");
}
```

### 5.6 项目定义文件（可选）

**文件**：`codex.toml`（放在 workspace 根目录）

```toml
[project]
name = "my-web-app"
description = "A Rust web application"

[workline]
# 任务的 agent 自动从工作线取任务执行
auto_assign = true

# 完工后自动发文明线摘要
post_to_civ_on_complete = true

[tasks.long_term]
"add-auth" = "实现用户认证模块"
"add-db" = "数据库迁移与 ORM 集成"
"add-tests" = "补全集成测试覆盖率 > 80%"

[tasks.short_term]
"fix-login-bug" = "修复登录页面刷新后丢失 session"
```

### 5.7 API + CLI

**REST**：
```
GET    /api/v1/workline                    → 当前项目/对话的工作线概览
GET    /api/v1/workline?category=long_term → 按分类过滤
POST   /api/v1/workline/nodes              → 创建任务节点
PATCH  /api/v1/workline/nodes/{id}         → 更新进度/状态
DELETE /api/v1/workline/nodes/{id}         → 删除任务
GET    /api/v1/workline/completed          → 完工归档
GET    /api/v1/workline/scheduled          → 定时任务
```

**CLI**：
```
codex tasks                       → 当前项目工作线概览（看板视图）
codex tasks add "implement auth" --category long_term
codex tasks progress <id> 0.5    → 更新进度到 50%
codex tasks done <id>            → 标记完成 → 自动归档
codex tasks completed             → 查看完工记录
codex tasks scheduled             → 查看定时任务
```

### 5.8 改动文件

| 文件 | 改动 |
|---|---|
| `crates/memory/src/lib.rs` | 新增 `WorkLineStore`（复用 JSONL 基础设施） |
| `crates/agent-types/src/lib.rs` | 新增 `WorkNode`/`WorkStatus`/`WorkCategory` |
| `crates/agent-core/src/loop.rs` | `do_plan` 注入工作线上下文；`do_act`/`do_reflect` 更新节点状态 |
| `crates/service/src/routes.rs` | 新增 `/api/v1/workline` 路由 |
| `crates/service/src/main.rs` | 启动时检测 `codex.toml` → 初始化项目级工作线 |
| `crates/api/src/lib.rs` | 新增 WorkLine DTOs |
| `crates/codex-cli/src/main.rs` | 新增 `tasks` 子命令 |

**估量**：5–8 天。

#### 5.8.1 补充：从计划自动创建任务（工作线自动触发机制）

用户的核心需求：从 `v6.0-plan.md` 或 `codex.toml` 的 `[tasks]` 段自动生成工作线节点，不需要用户手动说"这个是任务"。实现：
- `main.rs` 启动时检查 `codex.toml` 的 `[tasks.long_term]` / `[tasks.short_term]` 表，自动创建对应 `WorkNode`
- 未完成的节点在 CLI `codex tasks` 中显示，`Completed` 后自动归档到完工线
- Agent 在执行 `do_plan` 时，将工作线未完成任务列表注入 system prompt，让 agent 感知"当前有什么活要干"

#### 5.8.2 补充：定时任务调度器

§5.4 的 `Scheduled` 任务需要调度组件。实现：
- 用 `tokio::spawn` + `tokio::time::interval` 做内置 cron 触发（精度到分钟，不依赖系统 crontab）
- 配置格式（`codex.toml`）：`schedule = "0 */6 * * *"`（crontab 语法）
- 每次触发 → 创建新 `WorkNode(Pending)` → agent 自动领取执行 → Completed 后计算下次触发时间

---

## 六、四阶段推进总表

```
Phase 6A: 供应商管理 v2      ████████░░  4–6 天
Phase 6B: 内部桥            ████████████████░░  8–12 天
Phase 6C: 文明线            ██████░░  3–5 天
Phase 6D: 工作线            ██████████░░  5–8 天
                                      ────
                              合计  20–31 天
```

**推进顺序**：

```
6A 供应商管理  ──→  6B 内部桥
                 ↘  6C 文明线      (6C/6D 可并行，因为不互相依赖)
                 ↘  6D 工作线
                     ↓
              6B 完成后，桥的讨论自动发文明线摘要
              桥的 task force 自动更新工作线节点
```

**为什么不全部并行**：6C 和 6D 都依赖 6A（provider:model 命名），但不依赖 6B。6B 工期最长，6C/6D 可在 6A 完成后与 6B 并行推进。

**⚠️ 6A 是硬前置**：文明线的 `CivAuthor.provider_model`、工作线的 `WorkNode.assignee`、桥的多模型讨论，都依赖 `"openai:gpt-4o"` 格式。6A 未完成前，6B/6C/6D 只能用默认单 provider。

**⚠️ 6C + 6D 同文件冲突**：两者都改 `loop.rs`。建议先做 6C（注入点少，3-5天），再做 6D（注入点多，5-8天），避免并行合并冲突。在 `loop.rs` 中预分配隔离的 `// == civ_inject ==` / `// == work_inject ==` 注释块。

---

## 七、提交门槛（通用）

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --all
# 期望：三 rc=0, passed ≥ 149
```

---

## 八、四线交互全景

```
              ┌────────────────────────────────────┐
              │         用户 / CLI                  │
              └──────────────┬─────────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
    ┌────┴─────┐      ┌──────┴──────┐      ┌────┴──────┐
    │ 内部桥    │      │  文明线      │      │  工作线    │
    │          │      │             │      │           │
    │ 讨论结果 ─┼──────→ 自动摘要    │      │           │
    │          │      │             │      │           │
    │ 历史洞察 ←┼────── 提供上下文   │      │           │
    │          │      │             │      │           │
    │ 任务分派 ─┼─────────────────────────→ 创建节点   │
    │          │      │             │      │           │
    │ 进度通知 ←┼────────────────────────── 状态回调   │
    │          │      │             │      │           │
    │          │      │ 完工广播 ──→│      │           │
    │          │      │  ← 任务完成自动发布 │           │
    └──────────┘      └─────────────┘      └───────────┘
```

**一句话**：桥 = 实时协作，文明线 = 集体记忆，工作线 = 任务编排。三个加起来，就是"能讨论、能记住、能拆解执行的 AI 团队"。

> v6.0-plan 修订记录（2026-07-29）：
> - §4.6.1 文明线读入机制（do_plan 查询注入）
> - §4.6.2 存储膨胀控制（热缓存+归档压缩）
> - §5.8.1 工作线自动触发（从计划/codex.toml 自动创建任务）
> - §5.8.2 定时任务调度器（tokio::interval 内置 cron）
> - §六 6A 硬前置声明 + 6C/6D 同文件冲突协调方案
