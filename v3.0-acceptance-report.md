# v3.0 主干冻结 — 验收报告（增强轮）

- **日期**：2026-07-29
- **基线**：v2-gaps 定版（`codex-rust-v2-gaps-clean-20260728.zip`）
- **目标**：关闭 B1 / B2 / B3 / S1 / S2 五项，使主干可冻结为 v3.0
- **验收结论**：✅ **通过**——真 Linux VM 上 fmt / clippy -D warnings / test 三门门禁全绿

---

## 一、验收环境与方法

- 真 Linux VM：`wutao@192.168.220.131`（Ubuntu 24.04，已授权密码直连）
- 三门门禁（顺序执行）：
  1. `cargo fmt --all` + `cargo fmt --all -- --check` → 捕获 `FMT_RC`
  2. `cargo clippy --workspace --all-targets -- -D warnings` → 捕获 `CLIPPY_RC`
  3. `cargo test --all` → 捕获 `GATE_END rc` 与 `test result:` 计数
- 关键陷阱已规避（来自项目长期经验）：
  - 解压后用 `find ~/codex/crates -name '*.rs' -exec touch {} +` 强刷 mtime，避免 tar 保留旧 mtime 导致 cargo 跳过重编。
  - gate 以 `setsid ... & disown` 完全脱离 SSH 会话运行（CRLF 行尾已修）；判读直接 SSH 读 `~/gate.log`。

---

## 二、门禁数值（实测）

| 门 | 首轮 | 增强轮 |
|---|---|---|
| `FMT_RC` | **0** ✅ | **0** ✅ |
| `CLIPPY_RC` | **0** ✅（`-D warnings` 无 warning） | **0** ✅ |
| `GATE_END rc`（test） | **0** ✅ | **0** ✅ |
| `error[` 编译错误数 | **0** | **0** |
| `warning:` 数 | **0** | **0** |
| 测试 `passed` / `failed` | **146 / 0** ✅ | **148 / 0** ✅ |

> 增强轮新增的 `test_b1_get_history_in_memory` 与 `test_b1_get_history_from_store` 均通过，B1 两分支（in-memory 缓冲 / MemoryStore 回退）现在有断言回归覆盖。
> telemetry（含 S2 的 `MockProvider` eval 路径）随 `cargo test --all` 一并覆盖通过，无需真实 LLM / API key。

---

## 三、五项 handoff 逐条结果

### ✅ B1 — 历史回放端点
- `Session` 新增 `pub events: Vec<AgentEvent>` 缓冲字段；9 个 `tx.send` 点同步 `push` 事件（已持锁分支用 `s.events.push` 避免重入死锁）。
- 新增 `get_history(&self, session_id) -> anyhow::Result<Option<SessionHistory>>`：in-memory 分支用 `agent_event_kind(e)` + `serde_json::to_value(e)` 映射；MemoryStore 分支用 `StoredEvent` 字段。
- 新增自由函数 `agent_event_kind`（8 个 variant → `"phase"/"token"/.../"error"`）。
- 路由接线：`GET /api/v1/sessions/{id}/messages`（`routes::get_session_history`），与 `POST` 同路径。
- **增强轮**：新增两个单测 `test_b1_get_history_in_memory`（断言 in-memory 缓冲能正确回放 phase/token/error 事件 + payload 内容）与 `test_b1_get_history_from_store`（MemoryStore 持久化 + 回放）。**148 passed 中包含这两个断言测试。**

### ✅ B2 — 统一错误码
- `api` 新增 `ErrorResponse` / `ErrorBody`（`ToSchema`）+ `ErrorResponse::new` + 8 个 `ERR_*` 常量。
- `routes.rs` 整体重写：`api_err` helper；`require_api_key` 中间件与**所有** handler 返回类型统一为 `(StatusCode, Json<ErrorResponse>)`；`Err` 臂改 `api_err(ERR_*, ...)`。

### ✅ B3 — OpenAPI（有偏离，目标已达成）
- `ApiDoc`（`#[derive(OpenApi)]` + `components(schemas(...))`，**不含 `paths`**，免去 `#[utoipa::path]` 要求）覆盖 9 个 DTO；`agent_types::Budget` 加 `ToSchema`。
- **首轮偏离**：`utoipa-swagger-ui` compile-time 从 github.com 下载 zip → VM 被墙 → 改为 `GET /openapi.json` 纯 JSON 暴露。
- **增强轮恢复**：已将 swagger-ui 资源离线 vendor 进仓库（`crates/service/swagger-ui/index.html + bundle + css`），通过 `tower-http::services::fs::ServeDir` + `Router::nest_service("/swagger-ui", ...)` 提供静态服务。交互页已恢复，不依赖编译期网络。
- 依赖：`service/Cargo.toml` 中 `tower-http` 加 `features = ["fs"]`。

### ✅ S1 — TTL 可配置
- `main.rs` 读 `OOM_TTL_SECS` / `OOM_SWEEP_INTERVAL_SECS`（默认 3600 / 300 秒），传入 `start_cleanup_task`。

### ✅ S2 — eval 进 CI
- `.github/workflows/ci.yml` 在 test 之后新增 `cargo test -p telemetry -- --nocapture` step（`MockProvider` 本地执行，无需 env gate / LLM）。
- 验证：随 `cargo test --all` 覆盖通过（telemetry 测试在 148 passed 之内）。

---

## 四、源码改动清单（均经真 Linux 编译 + clippy + test 验证）

**首轮（v3.0 trunk freeze）——7 文件：**

| 文件 | 改动内容 |
|---|---|
| `crates/api/src/lib.rs` | B1 DTO（`SessionHistory`/`HistoryEntry`）+ B2 错误体/8 常量 + B3 9 处 `ToSchema` |
| `crates/agent-types/src/lib.rs` | `Budget` 加 `#[derive(..., ToSchema)]` |
| `crates/service/Cargo.toml` | 加 `utoipa = "5"`（首轮不含 `utoipa-swagger-ui`） |
| `crates/service/src/main.rs` | `ApiDoc` + `openapi_json` handler + `GET /openapi.json` 路由 + S1 env TTL |
| `crates/service/src/routes.rs` | 整体重写：`Json<ErrorResponse>` + `api_err` helper + `get_session_history` handler |
| `crates/service/src/session.rs` | `events` 缓冲字段 + 9 点 push + `get_history` + `agent_event_kind` |
| `.github/workflows/ci.yml` | S2 eval step |

**增强轮——3 文件 + 3 资源：**

| 文件 | 改动内容 |
|---|---|
| `crates/service/src/session.rs`（追加） | 测试模块新增 `test_b1_get_history_in_memory` + `test_b1_get_history_from_store`（B1 回归断言） |
| `crates/service/src/main.rs`（追加） | `use tower_http::services::fs::ServeDir` + `nest_service("/swagger-ui", get_service(ServeDir::new(...)))` + `HandleErrorLayer` |
| `crates/service/Cargo.toml`（追加） | `tower-http.workspace = true` → `tower-http = { workspace = true, features = ["fs"] }` |
| `crates/service/swagger-ui/index.html` | 自定义入口，指向 `/openapi.json` |
| `crates/service/swagger-ui/swagger-ui-bundle.js` | 离线 vendor（1.45MB，SWAGGER_UI_DOWNLOAD_URL） |
| `crates/service/swagger-ui/swagger-ui.css` | 离线 vendor |

---

## 五、交付物

- **v3.0 纯净备份包**：`codex-rust-v3.0-final-20260729.zip`（104 文件 788KB，排除 `target/` `.git/` `.workbuddy/`）
- **方案文档**：`v3.0-trunk-freeze-plan-zh.md`、`handoff-trunk-freeze-v2.md`
- **验收报告**：`v3.0-acceptance-report.md`（本文档）

## 六、关键坑记录

1. **VM 无 github 出站访问**：`utoipa-swagger-ui` 在编译期从 github.com 下载 zip → exit 7 超时。已写入项目长期记忆（`MEMORY.md`）。
2. **CRLF 行尾**：Python `open("w")` 默认文本模式把 `\n` 转 `\r\n` → Linux bash 执行 tar 等命令失败。修法：`newline="\n"`。
3. **tar 保留 mtime**：打包不刷 mtime → cargo 认为源码比 target 产物老 → 跳过重编复用旧 clippy 缓存。修法：解压后 `find ... -name '*.rs' -exec touch {} +`。
4. **前台 SSH 进程断连**：`exec_command` 前台阻塞 → 本地断连时远端收 SIGHUP → 门禁中断。修法：`setsid bash ... & disown` + 轮询 `~/gate.log`。
