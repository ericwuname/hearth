# Codex-Rust v4.0 后续路线图

> 基线：v3.1（全 12 缺口 + 11 To-Be = 100% 闭环，149 passed / 0 failed）
> 日期：2026-07-29
> 定位：v3.1 冻结后的下一阶段规划——从"功能完整"到"生产可用"

---

## 0. 现状盘点

### 已完成（不需再投入）

| 阶段 | 产出 | 关键指标 |
|---|---|---|
| v1.1 | 6 轮安全修复 + sandbox 闭环 | landlock/seccomp/cgroups 全部生效 |
| v1.2 | 40 项全局审计整改（24 修复 + 6 接受） | AUTH-0/OOM-1/MEM-1~5/STREAM-1~4/APPR-1 全部闭环 |
| v2-gaps | 6 项顶层缺口关闭 | G11/G9/G4/T6/T4/G5 + clippy 全清 |
| v3.0 | 5 项契约层补完（主干冻结） | 历史回放/错误码/OpenAPI/TTL env/eval CI |
| P1-P3 | 3 项 post-freeze 功能收尾 | A4 merge/prompt 注入/真实 LSP |

### 已知局限（4 项，见 `post-freeze-audit-handoff.md` §五）

| # | 局限 | 来源 | 当前影响 |
|---|---|---|---|
| L1 | P1 文件变更仅文件级（start_line/end_line = 0） | post-freeze §五.1 | 冲突检测粗粒度，子代理改同一文件不同区域仍报冲突 |
| L2 | P2 子代理产出不经 `format_injected_content` | post-freeze §五.2 | `RunReport.summary` 直接注入 prompt，无来源标记 |
| L3 | P3 rust-analyzer 初始化 10s 偏紧 | post-freeze §五.3 | 首次扫描超时即 fallback Noop，之后不再重试 |
| L4 | P3 未发 `shutdown`/`exit` 优雅退出 | post-freeze §五.4 | 不影响功能（SIGKILL 即时终止），但 ra 不写 checkpoint |

---

## 1. 三阶段路线图

```
v3.1 (now) ──→ v4.0 硬化     ──→ v4.1 生产就绪  ──→ v4.2+ 增强
           (局限关闭+       (部署/监控/       (新能力)
            测试补强)         E2E/限流)
```

### 1.1 v4.0 硬化（估 3–5 天）——关闭已知局限 + 质量补强

| # | 项 | 说明 | 估量级 | 阻塞 |
|---|---|---|---|---|
| **H1** | P1 行级文件变更追踪 | 工具层（edit/bash）执行后记录实际修改的行号区间，写入 `FileChange.start_line/end_line`。不依赖 `extract_files_from_tool_calls` 的 extension heuristic | 中 | — |
| **H2** | P2 子代理产出标记 | `collect_sub_agent_results` 中包 `format_injected_content`，`ContentSource::SubAgentOutput` | 小 | — |
| **H3** | P3 rust-analyzer 后台初始化 | `ensure_started` 改为 `tokio::spawn` 后台任务，10s→60s 超时；`diagnostics` 在初始化完成前返回空（不阻塞） | 小 | — |
| **H4** | 回归测试覆盖率扫描 | `cargo tarpaulin` 或 `cargo llvm-cov` 跑一次全仓覆盖率，标记 < 60% 的 crate 补测试 | 中 | — |

### 1.2 v4.1 生产就绪（估 5–8 天）——部署 & 运维 & E2E

| # | 项 | 说明 | 估量级 |
|---|---|---|---|
| **P1** | 速率限制（API-2，P3 延后） | `tower::limit::RateLimitLayer` 或 `governor` crate，每 IP/token 限制。AUTH-0 已挡未授权流量，限流防合法 token 滥用 | 小 |
| **P2** | Docker 部署方案 | `Dockerfile`（multi-stage：builder→runtime，`debian:bookworm-slim`） + `docker-compose.yml`。容器内 landlock/seccomp 需 `--privileged` 或 `--security-opt seccomp=unconfined` | 中 |
| **P3** | E2E 集成测试 | 用 `reqwest` 启动真实 service 进程，发 HTTP 请求，验证完整 SSE 流 + 工具执行 + 审批阻塞 + 历史回放。Mock LLM 用 `wiremock` 或本地 `axum` mock server | 中 |
| **P4** | 健康检查 + 就绪探针 | `GET /healthz` → 200（进程存活）；`GET /readyz` → 检查 provider registry 非空 + memory store 可写 | 小 |
| **P5** | 配置文档化 | 把 16+ 环境变量汇总成 `docs/configuration.md`，含默认值、说明、生产建议值 | 极小 |
| **P6** | 首次部署指南 | `docs/deployment.md`：最小生产部署步骤（API_KEY 强制、沙箱前提、端口/CORS、反向代理示例 nginx） | 极小 |

### 1.3 v4.2+ 增强（估 10+ 天，特性分支开发）

| # | 项 | 说明 | 估量级 |
|---|---|---|---|
| **E1** | retriever 默认开启 | 当前 `RETRIEVER_ENABLED` = off。v4.2 改为 on（需要 embed model），首次运行自动 index workspace | 中 |
| **E2** | 多 session 隔离 + 工作区隔离 | 当前所有 session 共享同一 cwd。每个 session 分配独立 temp dir → 互不干扰，清理更安全 | 中 |
| **E3** | 工具插件系统 | `Tool` trait 当前在 `tool-runtime` crate 内硬编码注册。加 `libloading` 或 WASM 插件接口，允许外部工具动态加载 | 大 |
| **E4** | 多 Agent 协作 | 当前子代理只有一层深度（`depth>=1` 禁止再派生）。多层协作 + agent 间通信通道 | 大 |
| **E5** | 前端参考实现 | 基于 OpenAPI (`/openapi.json`) 生成 TS 类型，写一个最小可用的 React/Vue 前端（会话创建、SSE 流、审批按钮、历史回放） | 大 |

---

## 2. 推荐推进顺序

"先硬化，再生产，后增强"——按风险递减排序：

### 立即做（v4.0，< 1 周）

```
H2(子代理标记) → H3(ra后台初始化) → H1(行级变更) → H4(覆盖率)
  先小后大：H2/H3 是几十行快速跟进，H1 需工具层打通，H4 可并行
```

### 然后做（v4.1，1–2 周）

```
P4(健康检查) → P5(配置文档) → P6(部署指南) → P1(限流) → P2(Docker) → P3(E2E)
  运维面先跑通（几分钟的事），再补工程面
```

### 特性分支（v4.2+，无固定排期）

```
E1(retriever默认) → E2(session隔离) → E3(插件) → E4(多Agent) → E5(前端)
  收益递减，按需启动
```

---

## 3. 与 top-level-design.md 的关系

v3.1 冻结后，`docs/top-level-design.md` 的 §13（12 缺口）+ §14（11 To-Be）全部闭环。建议：

- 将 v3.1 标记为"as-built 100%"版本号更新到文档头
- §13/§14 全部标注 ✅，说明已被后续阶段关闭
- 新增 §15 "v4.0+ 后续路线图"，引用本文档
- 将本文档的 H1-H6 / P1-P6 / E1-E5 登记为新的缺口跟踪清单

---

## 4. 分支策略（v4.0 起）

```
main (v3.1 trunk freeze)
│
├── v4.0/hardening (H1-H6)     ← 推荐下一轮集中冲刺
│   └── merge → tag v4.0
│
├── v4.1/production (P1-P6)
│   └── merge → tag v4.1
│
└── feat/* (E1-E5)             ← 按需开分支
```

v4.0 本身不需要再"冻结"——v3.1 已经是冻结点。v4.x 是正常迭代，每次合并后打 tag 即可。

---

## 5. 小结

| 阶段 | 定位 | 项数 | 估时 |
|---|---|---|---|
| v3.1 | 功能完整（当前） | — | ✅ 已完成 |
| v4.0 | 硬化（关闭局限+补测试） | 4 项 | 2–3 天 |
| v4.1 | 生产就绪 | 6 项 | 5–8 天 |
| v4.2+ | 特性增强 | 5 项 | 10+ 天 |

**从 7 月 28 日的 13,007 LOC / 12 🔴 缺口起步，到 7 月 29 日凌晨全 12 缺口 + 11 To-Be 闭环。两天的冲刺把"能跑"变成了"能交付"。接下来是"能生产"。**
